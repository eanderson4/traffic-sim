- **blocker** `contracts/asyncapi.yaml:152` — `stateSigReq` lacks its `{run}` parameter/messages, while those fields were accidentally placed under `sigReplyInbox`; that channel also has duplicate `description` keys. The sacred contract is malformed and AsyncAPI tooling will reject or misinterpret it.
- **blocker** `engine/cmd/demosrv/geojson.go:220` — every part fetch calls full `scenario.Load`—parsing, compiling, and validating the city network—then `c.path` at line 234 repeats it. A four-part city load performs eight full network compilations plus repeated reads/hashes, defeating ADR-0018’s city-scale serving path and risking multi-minute stalls/OOM.
- **should-fix** `engine/cmd/demosrv/supervisor.go:164` — `start` holds `mu` throughout the up-to-660-second identity wait; `status()` at line 259 blocks behind it. The menu’s three-second polling accumulates hanging requests and can exhaust the browser’s per-origin connections during large-world startup.
- **should-fix** `engine/cmd/demosrv/supervisor.go:231` — the global `stopRequested` flag is set before acquiring `mu`. A new start already queued on the mutex can acquire it before `stop`, observe the stale flag, and abort an unrelated spawn with a spurious 502. Use a per-run cancellation signal.

REVIEW-COMPLETE
