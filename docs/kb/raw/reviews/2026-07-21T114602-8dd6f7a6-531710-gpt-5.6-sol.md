- **should-fix** `engine/scenario/variant.go:146` — A network replacement cannot reuse the base network’s relative path because `baseRefs[v.Network]` rejects it. ADR-0012 defines `network` as a replacement, while collision rejection applies to added parts. A conventional variant containing its own `network.json` over a base `network.json` should be valid; only the effective network occupies that hash frame.
- **question** `engine/scenario/variant.go:182` — `Scenario.Dir` is the variant directory, but inherited `Manifest.Demand`, `Control`, and `Metrics` references remain relative to the base. Current consumers use eagerly loaded demand, but the returned `Scenario` is not fully interchangeable with a plain scenario for callers resolving `Manifest` paths through `Dir`.
- Full `engine` test suite passes. No NATS contract changes or determinism hazards found.

REVIEW-COMPLETE
