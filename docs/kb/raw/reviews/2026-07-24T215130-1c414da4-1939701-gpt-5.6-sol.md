- **blocker** `engine/cmd/demosrv/geojson.go:214` — Every part fetch calls `scenario.Load`, which reads, canonicalizes, compiles, and retains the entire city network; `c.path` at line 228 repeats that work. Thus each sequential LA part reparses/compiles the full network twice, likely turning the advertised ~150-second load into many minutes or OOM. Resolve the current net path/hash without full scenario materialization, and avoid repeating it per part.
- **should-fix** `engine/cmd/demosrv/geojson.go:95` — Manifest detection searches only the first 4096 bytes for `"parts"`. A valid long provenance/frame can place the field later, causing `main.go:431` to omit `no-store`; stale-manifest recovery can then repeatedly revalidate the cached manifest and fail on the same 404 parts.
- **should-fix** `engine/cmd/demosrv/main.go:412` — All part errors become 404, including scenario parse, filesystem, and regeneration failures. The client misclassifies operational failures as stale generations, retries once, and obscures the real server error.
- **nit** `viz/src/main.ts:129` — `cfg.theme in THEMES` accepts inherited names such as `constructor`; `getTheme` correctly selects navy while the toggle displays the bogus inherited name. Use the same own-property check as `getTheme`.

REVIEW-COMPLETE
