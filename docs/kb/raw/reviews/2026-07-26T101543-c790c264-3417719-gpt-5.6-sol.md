- **blocker** `engine/cmd/bake/bake.go:379` — The staging directory is renamed into the immutable content prefix before `index.json` is written. If the index write fails, the failed bake leaves a published partial prefix; successful bakes also expose a temporarily incomplete artifact. Write the index inside staging, then atomically rename the complete directory.
- **blocker** `engine/cmd/bake/index.go:83` — The bake content key omits output-shaping inputs including resolved network format, `BaseURL`, and tippecanoe version. `-net-format geojson` versus `pmtiles`, or different deployment origins/tool versions, can therefore produce different `index.json` and artifacts under the same supposedly immutable prefix.
- **blocker** `viz/scripts/bake-furniture.mjs:43` — The furniture step falls back to `network.geojson`, but PMTiles bakes do not place that file in the prefix. The advertised city-scale path therefore fails at line 65 and cannot produce signal heads, stop bars, or signs.
- **should-fix** `engine/natsio/demand/director.go:314` — A failed asynchronous `PublishRequest` is logged, but the sampler advances unconditionally at line 318. Any transient publish failure permanently deletes that scheduled vehicle, contradicting the fixed demand program and making demand loss silent in engine delivery metrics.
- **should-fix** `engine/director.go:116` — Interior-offset validation checks only `offset < lane.Length`; it does not ensure the selected vehicle fits behind its front bumper. An offset smaller than `Type.Length` places the vehicle partly before the lane and can overlap predecessor traffic outside `rearClear`’s same-lane footprint guard. The demand-file validation at `engine/scenario/scenario.go:667` has the same gap.

REVIEW-COMPLETE
