- **should-fix** — `engine/scenario/variant.go:199`: `Scenario.Dir` points to the variant directory, but `Manifest.Demand`, `Control`, and `Metrics` retain base-relative paths (`variant.go:139`). Those paths do not resolve beneath `Dir`, violating the manifest’s directory-relative contract and the claim that the result is indistinguishable from a hand-authored scenario. Current demand callers use preloaded objects, but future control/metrics consumers or generic tooling will read the wrong location.
- **should-fix** — `engine/scenario/variant_test.go:315`: acceptance verifies hash equality only, not the ADR-0012-required bit-identical execution/CRC. Hash equality does not exercise the materially different representation—patched demand exists only in memory and base parts may live outside `Scenario.Dir`. Add the established M11 CRC-equivalence test.
- **question** — Tests could not run in this read-only environment because Go could not create its temporary build directory.

REVIEW-COMPLETE
