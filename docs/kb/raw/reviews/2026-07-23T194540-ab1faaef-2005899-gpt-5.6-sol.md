- **should-fix** `viz/src/main.ts:590` — Recorded `dt` adoption is incomplete: `buffer.setSimDt(s.dt)` corrects speed derivation, but trailer integration still uses stale `cfg.dt`, and `SeekGate` remains configured from it at `viz/src/main.ts:192`. Direct or stale deep links therefore produce incorrect articulation and forward-seek detection.
- **should-fix** `engine/cmd/demosrv/registry.go:128` — Demo run IDs are inserted without checking `seenRuns`; only recordings detect duplicates. Two demos can publish different worlds under the same sacred NATS namespace, allowing stale clients to render a new scenario against the old network.
- **should-fix** `engine/cmd/serve/main.go:152` — `filepath.Abs` failure is silently ignored, despite the ADR migration note promising absolute `RunMeta.spec.net.path`. Fail startup instead; otherwise recordings can retain the cwd-dependent behavior this change claims to eliminate.

REVIEW-COMPLETE
