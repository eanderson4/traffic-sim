Review complete. This change is presentation-layer only — docs/show artifacts, quiz build scripts, and regenerated outputs. No engine code, no NATS subjects or payloads touched, so no contract/ADR exposure. The three surfaces that must agree (README table, `baselines.json`, the `D` constant inlined in `quiz.html`) all carry the same new bake hashes (`cbdbase/7406456b6031`, `kenbase/03c7bc162e73`), and I verified the checked-in SVGs byte-match what the staged `mkopenslide.py` would emit (viewBox 297 = 200 + 12 + 17×5 rows, two-row exits legend, "90s cycle" with green share correctly suppressed for mixed programs).

**Blockers** — none.

**Should-fix** — none.

**Nits**

1. `docs/show/README.md:11` and `docs/show/baselines.json` ("refuses 7% of demand") vs `scripts/chicago/mkquiz.py:86` CAVEATS ("93.6% of demand delivered in the baseline, 94.3% in the winning arm"). The precise numbers imply 6.4%/5.7% refused; "7%" overstates both. Reader who cross-checks the quiz caveat against the README sees a mismatch. Say "~6-7%" or match the baseline figure.

2. `scripts/show/mkopenslide.py:151` — the sub-pixel cull is per-edge, so a chain of consecutive sub-pixel edges that together form a visible road is culled entirely, opening gaps mid-road rather than only dropping isolated specks. Cosmetic at card scale and the committer presumably eyeballed both new SVGs; noting for the day an import with heavily fragmented geometry goes through.

3. `scripts/show/mkopenslide.py:252` — when an import's programs *do* all agree, the line still labels the first green phase "main-road green", a phrase that is meaningful for authored pods but a guess for an import (the first green of an OSM-imported program isn't necessarily the main road). Unreachable in the shipped pods (both imports fall into the cycle-only branch), so defer-by-default under the triage bar.

4. `scripts/show/mkopenslide.py:254` — a singleton `{0.0}` cycle set would print "0s cycle" via the `elif`. Requires a degenerate signal program you don't author; noting only.

**Determinism (checked, clean)** — the new set usage (`cycs`, `firsts`, `offs`) is reduced only via singleton `next(iter())` or equality, never iterated for output; the 2-dp rounding before set-membership correctly guards against float-order variance reading as "mixed"; sink iteration stays `sorted`; the cull predicate is a pure function of projected geometry. Checked-in SVG byte-reproducibility is preserved.

**Consistency (checked, clean)** — `--net` already exists in `mkopenslide.py` so the new `build-quiz.sh` calls are valid; `mkquiz.py`'s all-or-none rule (`have not in (0, len)`) permits setup-only scenarios as the comment claims; the `1-5` keys hint in `quiz.html` is generated (`navkeys`, mkquiz.py:613) not hand-edited, and the JS handler is already dynamic (`1..min(9,N)`); mkquiz's bake-existence check will fail the build rather than ship a dead link if the new hash dirs are absent.

REVIEW-COMPLETE
