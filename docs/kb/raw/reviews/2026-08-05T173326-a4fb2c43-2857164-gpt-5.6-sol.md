- **blocker** `.github/workflows/ci.yml:25` — Serializing packages masks a documented contract-plane startup race rather than fixing environmental contention. `RunLive` publishes registry metadata before `NewBus`/`NewContract` subscriptions are flushed (`engine/natsio/run.go:157`, `engine/natsio/run.go:173`), while tests treat registry visibility as readiness (`engine/natsio/contract_test.go:56`) and immediately send requests. The KB explicitly identifies this as the cause of `TestHoldLastThenDecay`-family flakes (`docs/kb/articles/cross-topic-concerns.md:51`). `-p 1` only reduces the race probability; it establishes no subscription-ready barrier and can let CI pass despite a normal-operation `no responders` failure. Flush subscriptions before advertising readiness, or add an equivalent explicit readiness barrier.

REVIEW-COMPLETE
