- **should-fix** — `engine/scenario/variant.go:212`: `Scenario.Dir` is the variant directory, but inherited `Manifest.Demand`, `Control`, and `Metrics` paths remain base-relative. The exported materialized scenario therefore violates ADR-0012’s “indistinguishable from hand-authored” directory-relative representation; generic consumers resolving those references through `Dir` will fail. Materialize resolvable paths or formally revise the ADR/API contract.
- Focused `go test ./scenario` passes. No determinism or NATS contract issues found.

REVIEW-COMPLETE
