- **blocker** `engine/natsio/driver/driver.go:380` — Budgeting defers `pickExit` until a later observation, but the candidate set depends on the vehicle’s then-current `LaneIdx` (`engine/natsio/driver/destinations.go:108`). Thus route selection depends on controller lag, claim timing, and replica sharding—not solely seed/vehicle ID. Identical runs can emit different route intents, violating ADR-0007/0008 failover determinism. Preserve the lane from first observation or otherwise make destination selection independent of budget timing.
- **should-fix** `engine/natsio/driver/driver.go:171` — `MetaWait` does not actually bound hello startup: each attempt can wait two seconds, followed by an unconditional 250 ms sleep, even when the deadline is nearly exhausted. Use the remaining deadline as the request timeout and do not sleep past it.
- **question** `engine/natsio/driver/driver.go:56` — The rationale says this work stalls the engine tick loop synchronously, but the driver runs in a NATS subscription callback and `Contract.publishObs` does not wait for it. Is there another synchronous embedded path? Otherwise document the actual failure mode—callback backlog and late intents—because this tradeoff changes routing behavior.

REVIEW-COMPLETE
