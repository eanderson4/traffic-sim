- **should-fix** — `contracts/asyncapi.yaml:667`: `ControlIntent` requires `SchemaHeaders.schema_version`, but the shipped v2 producer publishes headerless messages (`engine/natsio/driver/driver.go:421`) and the demux explicitly defines absent `intent_encoding` as v2. The source-of-truth schema rejects the project’s own wire traffic.
- **should-fix** — `contracts/asyncapi.yaml:15`: The global convention says every binary message carries both a `schema_version` header and payload version, but TSIB declares only `IntentEncodingHeaders` (`contracts/asyncapi.yaml:687`) and emits only `intent_encoding` (`engine/natsio/tsib.go:99`). Document the TSIB exception or amend the convention/ADR explicitly.
- **should-fix** — `engine/natsio/driver/driver.go:386`: Mixed route/non-route ticks do not preserve the v2 stream’s per-controller sequence assignment. Standalone route intents publish immediately, while earlier route-free egos wait until `flushBatch` at line 392; e.g. ego order `[batched A, routed B, batched C]` arrives as `[B,A,C]`. This contradicts the contract’s claim that per-controller sequencing and the record plane are identical (`contracts/asyncapi.yaml:225`). Preserve ordering or narrow the contract claim and test the mixed case.
- **nit** — `docs/kb/decisions/ADR-0026-batched-intents.md:90`: “880,024 B payload + header” double-counts the header; `24 + 44×20,000 = 880,024` already includes it.

REVIEW-COMPLETE
