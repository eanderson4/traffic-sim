- **blocker** `engine/natsio/driver_test.go:1127` — Warm-up “quiescence” exits on the first empty drain without synchronizing the driver connection or waiting through a quiet interval. A warm-up response can remain in flight and enter measurement, violating the FIFO’s clean-start assumption and corrupting every attributed source tick.
- **blocker** `engine/natsio/driver_test.go:1197` — The claimed fixed, identical boundary schedule is wall-clock/scheduler-dependent: each leg sleeps *after* variable work and runs sequentially. The exact percentile comparison and `v2Fast.splits > 0` assertion at `engine/natsio/driver_test.go:1245` can change with machine load, so this does not reliably establish ADR-0026 M3’s applied-lag acceptance claim.
- **should-fix** `engine/natsio/driver_test.go:889` — The passive tap subscription is never flushed before `h.finish`; early observations/intents may be missed, producing incomplete or incorrectly initialized lag samples.
- **nit** `engine/BENCHMARKS.md:259` — Production TSIB encoding does not perform an additional tick patch; `EncodeTSIB` writes the tick directly. Only this benchmark patches a reused payload.

REVIEW-COMPLETE
