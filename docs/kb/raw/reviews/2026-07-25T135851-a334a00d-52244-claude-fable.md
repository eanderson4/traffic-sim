No blockers. The diff is a clean viz-only split of the network layers into external/internal pairs; no NATS contract surface is touched (`internal` is a long-standing engine GeoJSON property, engine/geojson.go:32, always emitted as a bool so the `==`/`!=` filters partition cleanly, and absent-property stale caches degrade to the external pair — consistent with the comment). Layer ids in layertoggles.ts:58-59 match main.ts:535/543 exactly; the z13 gate matches the signal-head layers (main.ts:734/756/777); casing-below-lines ordering is correct and the round-cap rationale at main.ts:513-516 is sound; feature-state `ratio` is source-level, so the split doesn't affect it; no hover/query handlers reference the old single layer. Determinism: not applicable, pure rendering.

Findings:

- **nit** — viz/test/layertoggles.test.ts:72-77: the test named "names only real layers" only checks uniqueness, never that the ids exist in main.ts. A typo in a new id like `"network-internal-line"` would silently no-op the toggle (MapLibre fires an error event rather than throwing on unknown layers). This change is exactly the class it fails to catch. Pre-existing weakness, fine to defer.
- **nit** — viz/src/main.ts:479-485: the casing comment still explains the `edgeB` fallback in terms of "junction interiors … are all boundaries", but interiors now live in a layer where *every* feature takes the fallback branch — only "stale caches" still needs the sentence. Slightly stale wording, harmless.
- **question** — viz/src/main.ts:529: between z11 and z13 junction interiors render nothing at all (no casing either), so junction mouths rely on the external lanes' round caps to bridge visually. The comment says this is deliberate; confirming it was eyeballed at z11–12, where casing width (3.2px) is near its minimum.

REVIEW-COMPLETE
