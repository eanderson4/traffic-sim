• I've read the full diff. Now I'll verify the code's claims against the kernel and contract code it depends on.

• Kernel chain semantics confirmed. Now checking the contract-plane symbols and the hello/grant machinery the new client relies on.

• Symbols and semantics all line up so far. Now building and running the new package's tests against the staged tree.

• Two last cross-checks: `whatif.paired_t`'s return shape and serve's `-attach-timeout` default versus sigctl's worst-case attach time.

• One final check on what consumes the heartbeat (sigctl publishes it with a nil payload).

• Review done. I read the full 2,163-line diff, cross-checked every kernel-semantics claim against `engine/sigctl.go` / `engine/signal.go` / `engine/natsio` (contract, verb, sigframe, chunking), and verified the staged tree: `go build ./...`, `go vet` on the three touched packages, and `go test ./engine/natsio/sigctl` (ok, 1.1 s) are all clean.

  **What checks out**
  - Chain bookkeeping matches the kernel exactly: continue iff same phase and `last.until >= applied` (engine/sigctl.go:291), decline when `chainStart+bound <= applied` (engine/sigctl.go:311-338) — the controller's `canExtend` (sigctl.go:381-383) and the equality test are faithful.
  - `phaseElapsed` mirrors `phaseAtElapsed` (engine/signal.go:139) including offset wrap; the saturating onset fix is correct.
  - Seamless-walk claim holds kernel-side: a verb applied at the predecessor's `until` supersedes with no truncation and no lapse event (engine/sigctl.go:302-308, 360).
  - Determinism (ADR-0005): decision loop iterates sorted program ids; no map iteration reaches the wire otherwise; counts are integer; no RNG; wall clock touches only the client-side watchdog. Replay safety is unaffected (record plane re-enqueues by applied_tick).
  - Contract: no subject or payload changes — this is a client of the existing M1 channel; the proposed override echo is explicitly documented as not implemented. ADR + INDEX updated in the same commit. Rules satisfied.
  - Hello/heartbeat/attach-barrier shapes are byte-for-byte the demand director's established pattern.

  **Should-fix**
  - `engine/natsio/sigctl/sigctl.go:394-407` — the contested branch never consults min-green. When the on-call budget expires (`tick >= callSince+MaxGreenOnCallTicks`), `beginWalk` fires regardless of the enforced phase's age. With the program on fixed time (`myPhase < 0`), the schedule may have rotated into that green seconds ago — `callSince` measures the *call's* age, not the phase's — so the walk can cut a green far younger than `MinGreenTicks`, which only gates the gap-out branch (sigctl.go:409). If "max-green-on-call overrides min-green" is intended, say so in the package doc/ADR; otherwise gate the switch on `since+MinGreenTicks` too.

  **Nits**
  - `scripts/sigctl-bracket.py:48` — the fidelity gate keys on `verbs=` which is `Sent` (sigctl.go:603), incremented before the publish attempt; an arm where every verb was rejected still passes. Gating `accepted=(\d+)` > 0 (and ideally `rejected=0`) is the same regex effort and matches the gate's stated purpose.
  - Bracket docstring and the ADR addendum claim reuse of "whatif.py's own machinery (run_one, …)" — `run_one` is in fact re-implemented inline (`run()` at scripts/sigctl-bracket.py:86); only `load_metrics`/`paired_t`/`fidelity_problems`/`TRANSIENT` are imported. Doc accuracy only.
  - `engine/natsio/sigctl/sigctl.go:303-311` — if the arrival hold lapses before the walk registers arrival (`myPhase == target && tick >= holdUntil`, needs a dropped snapshot mid-walk), `walkStep` steps *past* the target and walks the whole table back. Self-correcting; worth a comment at most.
  - `engine/natsio/sigctl/sigctl.go:874` — on takeover commanding the phase the schedule already displays, `phaseSince = applied`, but the kernel's displayed-onset merging dates the onset earlier; the ADR's "controller and kernel agree on onset" holds only for renewals. Conservative direction, claim slightly overstated.
  - `engine/natsio/sigctl/sigctl_test.go:237-243` — the second half of TestChainBoundaryEquality argues with itself mid-comment ("…no:"); the assertion is right, the prose is confusing.

  **Questions**
  - The hello requests `Grants: ["director"]` while the contract defines a stub `GrantSignal` ("signal controllers (stub grant)", engine/intent.go:61). Director is what the verb handler checks today, but should the reference signal client be the one that defines/exercises the signal grant rather than normalizing director-for-everything?
  - 25 m detector radius reads only the first few queue positions; a queue held entirely upstream by downstream spillback reads as a gap and can gap-out its own phase. Physical stop-line loops share this property — intended for the reference client?

  REVIEW-COMPLETE

