- **blocker** `engine/cmd/default-driver/main.go:32` — TSIB is now default-on while the handshake still advertises contract version 2 and `contracts/asyncapi.yaml` still specifies only the v2 intent frame. A new driver therefore attaches successfully to a pre-TSIB v2 engine, then has its claimed vehicles’ batches dropped/misparsed. Ratify/document the contract and add capability/version negotiation, or keep batching opt-in until engine-first migration is enforceable.

REVIEW-COMPLETE
