Diff reviewed (viz-only: glitch clamp on trailer hitch angle + tests). Checked against `viz/src/artic.ts` in full, `TRAILER_M = 8` in theme.ts, and the existing test harness (`DT = 1/60`, `drive`).

**Blockers:** none.

The core math checks out: for any stable legit articulation, tractor sweep rate ω = (v/L)·sin(gap) ≤ v/L, so the `(v·dtS)/TRAILER_M` widening is exactly the tight upper bound on legitimate pre-integration gap growth — a steady gap g trips the widened limit only if g > 85°, which matches the stated design. The 170° cap correctly keeps the limit under wrapPi's π bound, and clamping `dtS` to `[0, MAX_TOTAL_DT_S]` is consistent with the world-jump re-anchor path. Clamp-before-hold ordering is right (a queued glitch would otherwise freeze). No contract surface touched (client-side rendering inference only — no ADR needed), no wall clock, no RNG, no map-iteration in an order-sensitive path (`prune` deletion is order-independent). Tests are deterministic and their assertions verify (85° + 57° = 142° < 150° flip → snaps; 91° < 121° → no snap; 175° > 170° cap → snaps).

**Should-fix:**
- None.

**Nits:**
- `viz/test/artic.test.ts:138` — the comment "the dtS-widened limit (85° + ~38°)" is wrong arithmetic: with `TRAILER_M = 8`, v = 10, dtS = 0.8 the widening is 1.0 rad ≈ 57°, not 38° (38° corresponds to L = 12). The assertion is unaffected (150° > 142°), but the number will mislead the next reader; the sibling test's "~36°" at :151 is computed correctly.

**Questions:**
- `viz/src/artic.ts:74-77` — for turn radius below ~8 m (r < TRAILER_M) there is no articulation equilibrium (sin gap = L/r > 1): the gap grows monotonically past 85° and the clamp will snap repeatedly through the turn, gluing the trailer to the tractor with periodic pops. Such geometry is physically untraversable by a real semi, so snap-to-aligned is arguably the least-bad rendering — but if OSM-derived internal lanes ever produce r < 8 m, this is the artifact you'll see. Expected/accepted?
- `viz/src/artic.ts:107` — at real-time DT the widening is ~1°, so a single-frame heading step > ~86° (an unsmoothed sharp polyline vertex, if the sampler ever hands one through) reads as a glitch and snaps rather than swings. If lane headings are interpolated per-frame this never fires; flagging only in case any geometry path delivers vertex-step headings.

REVIEW-COMPLETE
