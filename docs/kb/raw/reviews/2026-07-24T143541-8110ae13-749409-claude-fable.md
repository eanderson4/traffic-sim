**Verification done:** read the full patch; checked `boundaries()` equivalence against the old sweep (hop chaining, self-loop, higher-index successor, `S` invariance between append and processing); read `pickSuccessor`/`routeNextHop` (pure, memoized lookup — value-deterministic regardless of query order); confirmed the `past` map is only keyed/looked-up, never ranged; confirmed the eager `totalLaneKm` fill uses the same slice-order accumulation as the lazy path (bit-identical); cross-checked metview's hand-copied struct against the real sink structs in `engine/metricsjson.go`; grepped for post-construction `Lane.Length` writes.

## Findings

**Blockers:** none.

The `boundaries()` bucketing is genuinely state-identical: per-vehicle handling touches only the vehicle's own state, `Stats` increments commute, despawn compaction and `l.vehs` maintenance are outside the loop, and `routeNextHop` memoization is value-deterministic under any query order. The re-append guard `next.Index < i && v.S > next.Length` (engine/engine.go:487) exactly reproduces the old sweep's re-scan reach, including the self-loop (`next.Index == i` excluded — old sweep also didn't re-check same-tick) and already-visited-successor cases. The `Lane.Index` invariant check in `NewEngine` correctly protects the routing-by-index assumption. Map keyed by `*Lane` is never iterated — no map-order exposure.

**Should-fix**

1. `engine/cmd/metview/main.go:76` — the schema cross-check pointer says "engine/metrics.go §6", but the file-sink JSON structs live in `engine/metricsjson.go` (likewise `main_test.go:573`'s "metrics.go:119" for the veh/m claim). The comment's own instruction is "check against the sink when the schema changes" — a stale pointer defeats it. Same-file comment fix, no re-review needed if bundled as docs... it isn't (it's in `engine/`), so fix text before the gate stamp or defer with a note.

**Nits**

2. `engine/cmd/metview/main.go:79` — limitation comment says the sink emits interval `stops` as `*float64`; it's `*int` (`metricsjson.go:40`). Decode into `float64` works either way; comment is just inaccurate.
3. `engine/network.go:893` (Length immutability comment) — `engine/metrics_test.go:594` mutates `Length` after `NewEngine` (to provoke a `NewKernel` reject). Harmless today (that test never touches `TotalLaneKm`), but it's now a documented invariant violated by an in-repo test; consider "immutable in production paths after NewEngine".

**Questions**

4. `engine/cmd/metview/main.go:83` — totals mirror omits `dropped_crossings`/`denied_by_lane` while the summary table surfaces the denied-* family. Intentional scope cut, or worth a row given WQ-4 history? (Fine either way; extra JSON fields are ignored on decode.)

**Contract check:** no NATS subject/payload changes. metview is a read-only consumer of the ADR-0014 file-sink format over loopback HTTP with the ADR-0002 carve-out cited — no ADR required. KB entry (WQ-15) records the perf change and the determinism argument.

REVIEW-COMPLETE
