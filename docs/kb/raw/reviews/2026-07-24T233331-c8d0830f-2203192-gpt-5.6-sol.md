- **blocker** `engine/natsio/driver/driver.go:184` — retrying *every* request error is unsafe because hello is non-idempotent: a timed-out request may later create `ctl-N` at `engine/natsio/contract.go:489`, then the retry creates another controller. The ghost contributes drive capacity, can affect the pause gate, and persists until liveness detaches it. Retry only provably unhandled errors such as `nats.ErrNoResponders`, or add hello idempotency.
- **blocker** `engine/natsio/driver/driver.go:316` — the shared per-replica budget makes route-application timing depend on fleet partitioning and failover. Moving a vehicle between replicas changes which tick reaches `routeStep` with budget available (`engine/natsio/driver/driver.go:411`); it may pass a fork under default routing first, producing different world state. Freezing the lane stabilizes the selected exit, not when it takes effect, contradicting ADR-0008/package claims that replica choice and failover are behaviorally invisible.
- **should-fix** `engine/natsio/driver/driver.go:181` — deadline failure discards the last request error, collapsing no-responders, timeout, closed connection, and authorization failures into one message. Preserve and wrap the final error.

REVIEW-COMPLETE
