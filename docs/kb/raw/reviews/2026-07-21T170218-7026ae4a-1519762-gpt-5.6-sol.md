- **blocker** `engine/metrics.go:446` — An observation with `S > lane.Length` is treated as ordinary same-lane travel, booking all distance, time, occupancy, and time-loss to that lane. The later negative “refund” at `engine/metrics.go:927` corrects distance only, so lane VHT/occupancy/time-loss violate ADR-0014’s crossing split. If an interval/window/horizon falls between booking and refund, even distance remains permanently overcounted or appears negative in the next interval.
- **blocker** `engine/metrics.go:853` — Mid-run spawns stamp `EntryTick = tick`, although they enter before movement during interval `(tick-1, tick]`. A vehicle present for one tick can therefore have `EntryTick == ExitTick`, and all derived trip durations are short by one tick. Stamp the interval’s beginning (`tick-1`) or otherwise include an explicit correctly accumulated duration.
- **blocker** `engine/cmd/simrun/main.go:104` — `simrun -scenario … -metrics-out …` always constructs `DefaultKernelConfig`, silently ignoring the scenario’s parsed `metrics/*.yaml`. Thus measurement configuration changes the scenario hash but not the offline metrics that ADR-0014 designates as the analysis path. Preserve the loaded scenario and use `Scenario.MetricConfig`.
- **should-fix** `engine/director.go:95` — Enforcing live `RequestID` uniqueness changes authoritative engine/replay behavior and explicitly makes prior local recordings unreplayable, but the compatibility change exists only in a code comment. This consequence requires an ADR addendum/migration note under the repo rules, or metrics must identify queue entries without changing replay semantics.

REVIEW-COMPLETE
