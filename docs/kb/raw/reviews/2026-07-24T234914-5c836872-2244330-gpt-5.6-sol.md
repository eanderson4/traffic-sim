- **should-fix** — `engine/natsio/driver/driver_test.go:132`: the frozen-lane test can pass even if routing incorrectly uses the moved lane, because it never proves the two lanes produce different exits. Select a moved lane whose `pickExit` result differs and assert that precondition.
- **should-fix** — `docs/kb/decisions/ADR-0019-route-budget-determinism.md:30`: this decision weakens ADR-0008’s categorical behaviorally-invisible failover guarantee, but neither declares that it amends ADR-0008 nor updates ADR-0008. The KB therefore contains contradictory accepted decisions.
- **question** — `docs/kb/decisions/ADR-0019-route-budget-determinism.md:43`: a single reverse reachability pass can determine whether a lane reaches *any* exit, but not preserve the current weighted draw over the specific exits reachable from each lane. What concrete algorithm makes `pickExit` O(1) without changing routing semantics or requiring potentially quadratic reachability storage?
- **nit** — `engine/natsio/driver/driver_test.go:121`: “lower ID wins the sorted walk... verify order” is an unresolved/misleading note. `routeStep` processes frame slice order; ID sorting occurs upstream in the engine contract.

REVIEW-COMPLETE
