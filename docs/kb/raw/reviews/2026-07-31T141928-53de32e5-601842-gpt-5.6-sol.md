- **blocker** — `engine/sigctl.go:224`: every superseding command resets the hold horizon, so repeatedly commanding the same phase can starve traffic indefinitely despite the advertised 300-second starvation bound. `engine/sigctl.go:260` also checks only the newest entry, suppressing the required lapse event when renewal occurs exactly at the old hold’s bound.
- **should-fix** — `engine/sigctl.go:152`, `engine/keyframe.go:364`: `EnqueueSignal` accepts request IDs over 65,535 bytes; `MarshalState` then truncates the length to `u16` while writing the full string, producing an unreadable keyframe. NATS validation does not protect direct engine callers.
- **should-fix** — `contracts/asyncapi.yaml:1455`: JSON Schema `maxLength` measures string characters, while `handleVerb` enforces UTF-8 bytes. Non-ASCII IDs can satisfy the published schema but be rejected by the engine; express the byte constraint accurately or restrict the character set.

REVIEW-COMPLETE
