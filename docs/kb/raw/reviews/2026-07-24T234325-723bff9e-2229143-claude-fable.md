Review complete. Static review only — the sandbox declined `go vet`/`go test`, so compile/pass status is unverified; everything below is from reading the patch plus `destinations.go`, `obsframe.go`, and the full `driver.go`.

**Blockers** — none found.

- The budget logic is sound: only the ExitRouting draw is metered; explicit `Destination`, obs-echo adoption, and unconfirmed re-sends bypass the counter; a deferred ego's intent (accel/lane) still publishes; `wantLane` is cleaned up on confirm and on despawn (driver.go:356, :434).
- Determinism discipline holds: the draw stays a pure function of (seed, ID, frozen first-obs lane) via the domain-labeled stream; `wantLane`/`wantRoute` are lookup-only, never iterated; the only wall clock added is in connection setup.
- The hello-retry rework is correct — the request timeout is clamped to remaining budget, the sleep can't overshoot, first iteration always has `remaining > 0` so the error never wraps a nil `helloErr`, and a negative sleep is a no-op.

**Should-fix**

1. `driver_test.go:132–139` (TestRouteBudgetFrozenLane) — false-pass risk. The test never establishes that `pickExit` from the `moved` lane would give a *different* result than from the frozen lane. Both draws use the same (seed, ID) stream; they diverge only if the reachable-exit candidate sets differ between `originIdx` and `(originIdx+1) % len(net.Lanes)`. If those sets happen to coincide in the fixture, a buggy implementation that draws from the current lane produces the identical pick and the frozen-lane regression test passes vacuously. Assert `pickExit(net, newExitCache(), 42, 7, moved)` differs from `want` (and pick a different `moved` if it doesn't).

**Nit**

2. `driver.go:414–416` — the `if d.wantLane == nil` guard exists only because `TestRouteBudgetPerTick` constructs `Driver` without initializing the map (`TestRouteBudgetFrozenLane` does initialize it). Initialize it in the test literal and drop the production nil-check; `New` always sets it.
3. `driver_test.go:122` — the comment "(lower ID wins the sorted walk... verify order)" reads as an unresolved author note and is misleading: egos are processed in `obs.Egos` slice order, not sorted-ID order. Vehicle 6 is admitted because it's first in the slice.

**Question**

4. `driver.go:63–70` — the config comment honestly documents that the budget widens the ADR-0008 invisibility window: a vehicle can now sit observed-but-undrawn for up to `ceil(unrouted/budget)` observations, and `wantLane` is per-replica memory, so a failover in that window means the replacement draws from a later lane (previously the draw was same-tick as first observation, so the window was ~one obs). Inline docs may be enough at this stage, but given ground rule 4 ("KB current") and that this softens an ADR-0008 §6 claim, consider a one-line note in the ADR-0008 record or commit message. Related: `withDefaults` maps `<= 0` to 32, so there is no sentinel to disable metering — intentional?

REVIEW-COMPLETE
