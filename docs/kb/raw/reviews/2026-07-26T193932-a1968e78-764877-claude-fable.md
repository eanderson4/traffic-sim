Verification complete. The staged diff is contract+KB only (the M4 ratification of ADR-0026); I checked every wire-format, demux, and counter claim in the new AsyncAPI text against the shipped implementation (`engine/natsio/tsib.go`, `bus.go`, `tsib_test.go`).

## Findings

**Should-fix**

1. `docs/kb/decisions/ADR-0026-batched-intents.md:268-273` — the *Driver↔engine version skew* Risks bullet is now internally contradictory with the M4 text this diff rewrote. It still says "the **hello-version migration note** in M4 covers it" (that note was deleted — M4 now says the driver **cannot** detect a pre-TSIB engine) and "batches are dropped (**unknown** at best…)" (against a pre-TSIB engine there is no unknown-encoding counter; drops land on the generic malformed-v2 counter, exactly as the new M4/contract text says). M4 (diff line ~257) points to "the version-skew bullet in Risks below," which points back to a mechanism that no longer exists — circular and stale. Fix the bullet in this commit; it's the ADR's own record of the acknowledged limitation.

**Question**

2. ADR-0026 M4 scope includes the "`--gemini` review round" (the deeper `/external-review` milestone round per AGENTS.md), yet this same commit stamps the ADR Accepted and publishes the contract as 2.5.0. Has that round run, or is ratification landing ahead of it? If this two-model round is intended to satisfy it, say so in the ratified line.

**Nit**

3. `contracts/asyncapi.yaml` ctlIntent channel description ("never a fall-through to v2 parsing, so a future encoding fails loudly against a TSIB-AWARE engine") — the loud-failure guarantee holds only for the exact-case `intent_encoding` key. A case-typo producer (`Intent_Encoding: tsib`) falls into the v2 misparse path (generic drop counter, claim backstop) even on a TSIB-aware engine — same quiet failure mode the design otherwise reserves for pre-TSIB engines. Exact-case matching is a deliberate, documented choice, so at most one clarifying clause; fine to ignore.

**Verified accurate** (no action): header/record arithmetic (24 + 44·20,000 = 880,024 B; 68-B single-record pathological case with bytes 40–41 read as route_len 24 — pinned at `engine/natsio/tsib_test.go:206`); demux semantics incl. present-but-empty ⇒ unknown-loud and headers-without-key ⇒ v2 (`bus.go:290-341`, pinned at `tsib_test.go:253-289`); counter names match `Bus.IntentBatchStats` (`bus.go:556`); whole-batch vs per-record drop rules and route-field rule match `DecodeTSIB` (`tsib.go:110-139`); tick-informational and reserved-fields-ignored claims match the code; version bump 2.4.0→2.5.0 is additive with the required ADR. No blockers; no determinism or subject-name changes.

REVIEW-COMPLETE
