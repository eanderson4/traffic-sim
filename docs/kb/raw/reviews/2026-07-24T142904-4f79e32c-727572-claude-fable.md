Context verified beyond the diff: full `boundaries()` body (engine/engine.go:443-506), `pickSuccessor`/`routeNextHop` (routing.go — memoized per-destination reverse Dijkstra, deterministic tie-breaks, values independent of call order), `TotalLaneKm` callers (spawn.go:163, director.go:158 — both tick-path, single goroutine), and the despawn compaction path.

## Findings

**Blockers:** none.

Equivalence walk of the bucketed `boundaries()` (the determinism-critical piece), for the record:
- The initial bucket pass captures exactly the old sweep's candidate set — nothing in boundary handling ever *increases* `S`, so no vehicle becomes past-end mid-loop except crossers, which are re-appended under the same `S > Length` condition the old re-scan applied (engine/engine.go:483).
- Re-scan reach matches: append only when `next.Index < i` — self-successors (`==`) and upstream successors (`>`) were also unreachable in the old descending single-pass sweep.
- The `past` map is indexed and deleted, never iterated — no map-order nondeterminism.
- The within-lane order change (crossers at bucket tail vs. interleaved at `e.order` position) is absorbed only by order-independent handling: no RNG, integer-only stats, per-vehicle-local mutation, compaction after the loop preserving `e.order`. The comment at engine.go:432-442 states this contract for future editors; the CRC-identical 7200-tick stress run is consistent evidence. Sound.
- The `Lane.Index == slice position` construction check in `NewEngine` correctly turns a would-be silent crosser-deferral into a hard error.

**Should-fix**

1. `engine/cmd/metview/main.go:361` — `float64(tr.ExitTick-tr.EntryTick)` underflows uint64 if a corrupt trip has `ExitTick < EntryTick`, silently poisoning the bucket's mean time/speed with a ~5.8e17 s value. The intervals path treats corrupt windows as a real category and drops them (main.go:266, `EndTick < BeginTick`); the trips path deserves the same one-line guard for consistency.

**Nits**

2. `engine/cmd/metview/main.go:122` — hand-rolled `strings.LastIndex(name, "/")`; `filepath.Base` is the stdlib idiom. Also, two files with the same basename produce indistinguishable chips/rows.
3. `engine/network.go:89` — the lazy cache write is unsynchronized; safe with today's two tick-path callers as documented, but computing it eagerly at construction (where the new `Index` check already loops over `Lanes`) would delete the future-caller race class for free rather than by comment. Take or leave under the one-round bar.

**Questions**

4. engine/engine.go:436 — the order-independence contract is enforced only by comment; the CRC-pinned fixtures pin it empirically. Fine for now under the triage bar; if junction entry-capacity gating (the comment's own example) lands, that commit should carry a differential test against a reference sweep.

**Contract/ADR:** clean. No NATS subject or payload changes; metview's HTTP endpoints are a loopback-only dev tool under the ADR-0002 addendum carve-out with the guard tested; server-rendered page keeps ADR-0001 out of scope, verified by the no-`<script>` test; the metrics-schema hand-copy and its group-off/omitempty limitation are documented at the struct.

REVIEW-COMPLETE
