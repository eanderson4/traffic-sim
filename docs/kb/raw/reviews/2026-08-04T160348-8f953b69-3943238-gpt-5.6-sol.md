- **should-fix** `docs/architecture.md:92` — The diagram implies TSLB is published on `log.intent`. Under `-log-batch`, the subject changes to sacred contract subject `log.intents`; name both explicitly to avoid misleading implementers.
- **should-fix** `engine/natsio/tslb_migration_test.go:248` — The zero-value batching assertion only tests Go’s literal bool zero value. Unlike compression, it never records a tick and verifies `log.intent` rather than `log.intents`, so a future `withDefaults`/`LogTick` regression could silently re-enable the known nondeterministic configuration while this test remains green.
- **question** `contracts/asyncapi.yaml:33` — Is retaining contract version `2.6.0` intentional after changing the unflagged writer’s default subject back from `log.intents` to `log.intent`? The ADR migration note is present, but external consumers cannot identify the corrected default from the version.

REVIEW-COMPLETE
