You are reviewing a change on branch lit-lineage-splash (PR #2) in a
traffic-simulation repo (deterministic Go engine, NATS contract, ADR-driven;
project rules in AGENTS.md). The change adds: (1) analysis/lit-lineage/ —
a Python-stdlib pipeline that harvests OpenAlex data into a traffic-flow
literature citation graph (harvest/filter/anchors/analyze/export scripts +
research notes), and (2) a splash landing page (viz/splash.html,
viz/src/splash.ts, vite input, demosrv serves it at /).

The full diff (base main) is at /tmp/pr2-review-gNh4MS/diff.patch (stat: /tmp/pr2-review-gNh4MS/diffstat.txt).
Read the diff first, then whatever repo files you need for context (the
patch — not the working tree — is what ships). NOTE: engine/cmd/demosrv/main.go
was changed WITHOUT a Go toolchain available (unbuilt, untested) — eyeball
it carefully. The viz side passed pnpm check/build/test.

Report findings cited file:line, ranked: blocker / should-fix / nit /
question. Focus: correctness bugs; anything that breaks existing demosrv
routes or the map app; determinism of the analysis pipeline (iteration
order, RNG, float accumulation); design weaknesses relative to the repo's
own conventions. ALSO: the diff includes analysis/lit-lineage/research/
phase-3-plan.md — end your review with a short "phase-3 thoughts" section
answering its four open questions (SPC-weight vs top-k pruning; asserted
root layer vs cited_by backfill first; keeping the pruned dataset JSON out
of git; the 'index is biased' video framing). Review only — DO NOT modify
files. Be terse; padding wastes the committer's time. Finish with a final
line containing exactly: REVIEW-COMPLETE
