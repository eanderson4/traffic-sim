• I've read the full diff. Now verifying key context in the repo and running the test suite.

• Repo context verified so far. Now running the full test suite.

• Full suite passes. Checking whether the scripts the ADRs claim as shipped actually exist.

• One more contract surface to check — whether the baked replay contract pins the metrics trip/totals schema.

• Review of the staged diff (33 files, ~4.4k lines: ADR-0028–0034 docs, gridlock escape, permissive-green/yield kernel changes, routed-branch lookahead, warm start). Built the tree, `go vet` clean, full `go test ./...` passes (engine 8.7s, natsio 61.7s).

  **Blockers** — none found. The high-risk pieces checked out: v5 keyframe writer/reader gates are now on the same condition (keyframe.go:205 vs :358) with the short-read guarded (keyframe.go:369); `stuckTicks`/`stopDone` keyframed with a verified bit-exact round-trip; strand removal is mid-sweep safe (lane.vehs updated before later vehicles' head tests, gridlock.go:105); no Go map iteration in any sampled/hashed path (`LaneFingerprint` walks `n.Lanes` in index order, `gone` is sorted before use, serve's section print sorts); no clock/RNG in the escape; metrics strand path keeps `k.last` consistent (wholesale `k.last = cur`, metrics.go:681) and the `MeanTimeLossS` denominator fix is sound — per-tick loss lands in `k.timeLoss` (metrics.go:558), so stranded trips' loss is genuinely in the numerator.

  **Should-fix**

  1. `contracts/asyncapi.yaml` is stale against the payload it publishes. Keyframes on `ts.{run}.log.keyframe` are now TSKF v5 (v4 since ADR-0021), but the contract still says "keyframes are TSKF v2" (:61), "stay TSKF v2 except while the injection queue is non-empty (TSKF v3…); readers accepting v2|v3 are unaffected" (:86-87), "TSKF v2; v3 while director directives pending" (:842, also :851, :1196), and info version stays 2.5.0. ADR-0034's own migration note concedes the keyframe is a published payload, and per the ADR's correction, on a congested network almost every mid-run keyframe now lifts to v5 — so "readers accepting v2|v3 are unaffected" is materially false for new recordings. Update the TSKF references (v2–v5 and the lift conditions) and bump the info version, as ADR-0026 did for TSIB.
  2. ADR-0028 states shipped behavior that does not exist: "`mkscenario.sh` now passes `--horizon-s` … and `mkod.py` refuses a demand program the horizon cannot finish" (ADR-0028:79) — `scripts/chicago/mkscenario.sh` has no `--horizon-s`, and `mkod.py` has no `--profiles` (still `AM_PROFILE`, mkod.py:62,323). ADR-0030:5 likewise lists `scripts/runreport.py` and `scripts/chicago/mkzones.py` under "Adds:" — neither file exists. INDEX.md repeats both claims. Proposed ADRs may describe unbuilt tooling, but not in the present tense; either land the scripts or mark them as not yet built.
  3. `engine/gridlock.go:40` — file-header comment claims "In the ring fixture one removal drains all eighteen vehicles." The fixture places 32 (`fillFeeders`), and ADR-0034's own measurement says the ring "bleeds one vehicle per 300 s for 27 simulated minutes… 23 of 32 vehicles served, 9 stranded" — the opposite behavior. Stale comment from an earlier draft of the escape, in a file whose comments are the documentation.
  4. Unguarded path: an *external* `demand-director` (default `-start-tick 0`) can still attach to a warm-started run. `validateStateFlags` (engine/cmd/serve/main.go:891) only refuses `-state-in` with *embedded* demand parts, and the standalone binary refuses only nonzero `-start-tick` (cmd/demand-director/main.go:33). An external director with defaults would dump the whole 0..N+lead backlog as expired verbs and double-spawn the window already in the restored dirQueue — the exact defect ADR-0029 describes — because nothing in the run registry marks the run as warm-started for the director to refuse on. At minimum document; better, stamp warm-start in the registry and have `demand.Attach` refuse when `StartTick == 0`.

  **Nits**

  5. `engine/natsio/demand/director.go:279` — `skipped++` also counts the final failed draw (`ok == false`), so the logged skip count can exceed arrivals actually skipped by one per exhausted flow. Log-only.
  6. `engine/cmd/serve/main.go` `probeWritable` is inherently TOCTOU (probe passes, disk fills at hour 1); the abort-on-failure design already makes that loud, so acceptable — noting only because the flag help implies the probe is the safeguard.

  **Questions**

  7. `jammedAtJunction` (engine/gridlock.go:145) resolves "the junction it is routed into" via `pickSuccessor`, which for an unrouted vehicle at a diverge is blind `Successors[0]`. A vehicle that would have taken a free exit branch can therefore strand waiting on the default branch's blocked box. Rare and bounded, but is that the intended discriminator rather than "any successor's box is blocked"?

  REVIEW-COMPLETE

