- **blocker** `engine/cmd/metview/main.go:62` — `denied_pending` is declared `int`, but schema v1 emits a `float64` fractional vehicle backlog. `json.Unmarshal` rejects valid files containing values such as `1.5`, so `metview` cannot open legitimate overloaded-run output.
- **blocker** `engine/cmd/metview/main.go:165` — Lane aggregation discards `set_id` and optional-field presence. Valid overlapping measurement sets therefore double-count distance/time/stops/loss; records with disabled groups become indistinguishable zeros. Additionally, `engine/cmd/metview/main.go:177` divides density/occupancy by the absolute maximum `end_tick`, underreporting sets with nonzero `begin_s`. Aggregate by `(set_id, lane_id)` and actual covered duration.
- **should-fix** `engine/cmd/metview/main.go:185` — Rows originate from Go map iteration, while metric sorts at `engine/cmd/metview/main.go:193` provide no lane-ID tie-breaker. Equal-valued lanes—and which tied rows survive the top-100 cutoff—vary nondeterministically between processes.
- **should-fix** `engine/cmd/metview/main.go:28` — The loader ignores the top-level `schema_version`. Future or unsupported contract versions will be silently interpreted as v1 rather than rejected, risking plausible-looking but incorrect comparisons.

REVIEW-COMPLETE
