- **should-fix** `hooks/pre-commit:21` — The allowlist contradicts ADR-0013’s “every commit touching code, contracts, or tooling” guarantee. New top-level tooling/config such as `Dockerfile`, `Makefile`, `compose.yaml`, or a new code directory bypasses the gate. Prefer gating everything except an explicit docs/generated allowlist.
- **should-fix** `hooks/pre-commit:21` — Line-oriented `--name-only` parsing is unsafe for valid filenames containing newlines; Git quotes such names, so a gated path can fail the anchored `grep` and pass as docs-only. Use `-z` and NUL-safe matching.
- **should-fix** `scripts/external-review.sh:34` — This only checks whether `core.hooksPath` is set, not whether it points to `hooks/`. A global or stale alternate value suppresses the warning while leaving this gate unenforced.
- **should-fix** `scripts/external-review.sh:79` — Reviewer processes have no timeout. A wedged CLI/network call can hang the mandatory gate indefinitely rather than reaching ADR-0013’s defined fail-closed failure path.
- **nit** `scripts/external-review.sh:39` — The staged/unstaged overlap check is also line-oriented and can misreport unusual filenames; fix alongside the hook’s NUL-safe path handling.
- **question** `docs/kb/decisions/ADR-0013-external-review-gate.md:83` — Archived reviews are committed later through the docs-only bypass, so the audit artifacts can be altered without review and are not cryptographically bound to the stamp. Is that weaker audit-integrity guarantee intentional?

REVIEW-COMPLETE
