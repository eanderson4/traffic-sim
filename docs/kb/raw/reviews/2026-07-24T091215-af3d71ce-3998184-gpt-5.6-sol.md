- **blocker** `viz/src/artic.ts:108` — The `dtS > 0` gate can permanently miss a heading glitch. `SnapshotBuffer.sample()` exposes the newer tick before interpolation advances its heading (`viz/src/snapshots.ts:146`), so the tick-credit frame sees the old angle; later frames reveal the flip with `dtS === 0`. If the stream then pauses, ends, or starves, no subsequent positive-`dtS` frame exists and the perpendicular trailer remains stuck. The cadence test applies credit at the end of interpolation (`viz/test/artic.test.ts:237`), opposite the production pipeline, masking this case.

REVIEW-COMPLETE
