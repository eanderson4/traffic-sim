- **blocker** `engine/natsio/contract.go:320` — Warm restore recreates the contract with empty claims, hold-last intents, sequence state, and announcements. `engine/natsio/contract.go:914` publishes the first observation before announcing restored vehicles at line 945, so the default driver cannot claim them until after the first resumed step. With `-driver=true` by default (`engine/cmd/serve/main.go:78`), warm starts immediately diverge from the claimed bit-exact continuation. Refuse controllers or persist/reconstruct contract state and establish intents before stepping.
- **blocker** `engine/natsio/registry.go:73` — `Registry.Start` hard-codes `StartedTick: 0` for warm runs, contradicting the restored engine/hello tick and the nonzero initial keyframe. This publishes false metadata on the NATS contract plane. The initial keyframe also is not installed as the state pointer until a later cadence update (`engine/natsio/run.go:179`, `engine/natsio/run.go:246`).
- **should-fix** `engine/metrics.go:615` — The stranded branch emits and deletes the trip without booking the final `(tick-1, tick]` interval. Each stranded trip loses one tick of VHT, stopped time, and time loss, while its record still reports `ExitTick=tick`.
- **should-fix** `engine/gridlock.go:84` — Converting `StrandAfterS/Dt` by truncation strands early whenever the ratio is non-integral; e.g. 1 s at 0.3 s ticks fires after 0.9 s. Use a ceiling or validate exact tick-grid representability.

REVIEW-COMPLETE
