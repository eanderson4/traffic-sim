- **should-fix** `viz/src/legend.ts:20` hard-codes `dt = 0.1`, so the new clock is wrong for scenarios/replays using another configured timestep. ADR-0005 explicitly makes tick length configurable; pass `cfg.dt` into the legend.
- **should-fix** `engine/cmd/demosrv/supervisor.go:70` builds client URLs from the listen address. Default `127.0.0.1` sends remote browsers to their own machine, while `0.0.0.0` produces an unusable destination. Preserve browser-hostname resolution or separate bind and advertised addresses.
- **should-fix** `engine/cmd/demosrv/main.go:322` starts one demo, releases the supervisor lock, then reads the active run at line 330. A concurrent start can make this response combine demo A’s network with demo B’s run. Return the spawned run ID atomically from `start`.
- **question** `engine/cmd/demosrv/supervisor.go:153` changes live-run identity to a wall-clock-derived per-spawn value and `engine/cmd/demosrv/main.go:296` extends the orchestration status model, but no ADR records that consequential identity/API decision. Is ADR-0018 intended to cover this unrelated behavior?

REVIEW-COMPLETE
