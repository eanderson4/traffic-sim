## Review — glitch clamp for trailer articulation (viz-only, no contract surface)

Scope check: pure client-side viz math, no NATS subjects/payloads touched, no Go, no RNG/wall-clock/map-iteration determinism surface. All 14 tests pass. The clamp-before-hold ordering is right — it's what lets a glitched angle escape the queued freeze — and the dtS-widened limit correctly covers the sweep a legit turn accumulates on a fat replay frame.

**Blockers:** none.

**Should-fix:** none.

**Nits:**

1. `viz/src/artic.ts:71` — the widened limit can exceed π (v·dtS ≥ ~13.3 m, e.g. v=10, dtS≥1.33), and `|wrapPi(...)| ≤ π`, so in that band the clamp is untrippable and even a full heading flip integrates instead of snapping. Traced it through: the outcome is benign — a 150° flip at v=10/dtS=2 integrates down to ~34° gap within the same frame (only the final pose renders), and if it lands still-glitched and the truck queues, the clamp catches it on the next frame because it runs before the hold. Self-healing, so a transient at worst; `Math.min(limit, π − ε)` would close it if you ever care. Valid deferral.

2. Comment arithmetic is off in three places (assertions are unaffected): `viz/src/artic.ts:41` "sub-15 m-radius … ~80°" — with L=8 m, 80° steady articulation needs ~8.1 m radius (15 m holds ~32°); `viz/src/artic.ts:45` "ω ≈ 41°/s … reads ~80°" — at the tests' v=10 it's ω≈62°/s and the 0.5 s gap reads ~91°; `viz/test/artic.test.ts:184-186` "~20° sweep / ~80° gap / 85°+~24°" — actually ~31° / ~91° / 85°+~36° at v=10. The 41°/20°/24° numbers correspond to v≈6.6 m/s, not the v=10 used. Also `viz/test/artic.test.ts:154` says "75° clamp" — stale, threshold is 85°.

**Questions:** none.

REVIEW-COMPLETE
