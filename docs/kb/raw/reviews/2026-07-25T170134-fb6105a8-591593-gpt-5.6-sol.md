No findings. The change is consistent with ADR-0020; it does not alter simulation determinism or NATS subjects/payloads.

Validation limitation: `go test ./cmd/demosrv` could not run because the read-only sandbox prevented creation of `/tmp/go-build*`.

REVIEW-COMPLETE
