- **blocker** `engine/natsio/player.go:485` — backward seek publishes a lower tick, but `viz/src/snapshots.ts:62` drops every tick ≤ the previous high-water mark. Seeking 100→20 leaves the viz frozen until tick 101; seeking while paused never updates it. Add replay epoch/reset semantics or clear the consumer buffer on seek.
- **should-fix** `engine/cmd/replay/main.go:134` — the printed viz URL omits `dt`, while `viz/src/config.ts:18` defaults to 0.1. Replays recorded with another timestep display incorrect derived speeds and congestion; include `&dt=<spec.Params.Dt>` as `serve` does.

REVIEW-COMPLETE
