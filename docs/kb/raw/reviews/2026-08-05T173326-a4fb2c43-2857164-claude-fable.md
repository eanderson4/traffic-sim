Review of the staged diff (single hunk in `.github/workflows/ci.yml`: `go test ./... -count=1` → `-count=1 -p 1` plus rationale comment).

**Blockers:** none. The change is correct for its stated goal: `-p 1` limits the go tool to one program (build or test binary) at a time, no test in `engine/` uses `t.Parallel()`, so execution becomes fully serial, and all four tests the comment cites exist as named (`engine/natsio/contract_test.go:172,248,449`, `engine/natsio/driver_test.go:1071`). Only one CI invocation of `go test` exists, so nothing else needs the flag. CI-only change — no determinism or contract surface touched.

**Nits:**
- `.github/workflows/ci.yml:35` — `-p 1` serializes *compilation* as well as test execution, so the four package builds now run one at a time too. If CI wall-time grows noticeably, a `go build ./...` (default parallelism) step before the serial test run would repopulate the build cache and confine the slowdown to test execution. Not worth doing preemptively.
- `.github/workflows/ci.yml:25-34` — comment says "three other package binaries"; that count will silently rot as packages are added. Consider "the other package binaries."

**Question:**
- `-p 1` removes the *self-inflicted* contention, but GitHub-hosted runners are small shared VMs with noisy-neighbor CPU steal, so the real-time cadence assertions still run on hardware you don't control — this narrows the flake window rather than closing it. Commit c01c9e6 already re-measured one of these legs; if the family flakes again *after* this change, the durable fix is headroom in the assertions (or measuring cadence relative to observed scheduling), not further isolation. Fine to defer until it actually recurs, per the triage addendum.

REVIEW-COMPLETE
