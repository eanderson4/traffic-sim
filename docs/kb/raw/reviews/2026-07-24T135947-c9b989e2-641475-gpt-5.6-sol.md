- **blocker** `engine/cmd/metview/index.html:64` — The dashboard labels single runs as best/worst, contradicting ADR-0014 §7’s requirement that tooling not support single-seed conclusions. It needs paired-seed differences/CIs, or must remain a neutral viewer without ranking colors.
- **should-fix** `engine/cmd/metview/main.go:94` — Runs are compared without validating equal `ticks`/`dt`, and the UI does not display either. Different horizons or time steps can therefore receive authoritative-looking rankings despite being incomparable.
- **should-fix** `engine/cmd/metview/main.go:45` — Optional interval fields are decoded as scalar zeroes. When a metric group is disabled and its JSON field is omitted, the viewer fabricates `0` density/occupancy/stops/loss and ranks it. Preserve presence with pointers and suppress unavailable columns/sorts.
- **should-fix** `engine/cmd/metview/main.go:291` — Completed and horizon-censored trips are combined in the same mean time/distance/loss/stops. The resulting “trip” means are biased by partial journeys; report completed-only means separately and the censored fraction/partial statistics explicitly.

REVIEW-COMPLETE
