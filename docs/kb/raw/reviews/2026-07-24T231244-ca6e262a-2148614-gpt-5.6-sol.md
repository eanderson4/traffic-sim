- **should-fix** `engine/cmd/demosrv/supervisor.go:258` — `stop()` snapshots `startGen` before acquiring `mu`. A concurrent `start()` can increment the generation after that snapshot and then hold `mu` throughout the 660-second identity wait; the stale `abortGen` will not abort it, so API stop or SIGTERM can hang for the full timeout. Coordinate generation sampling with start or re-stamp while waiting.
- Tests could not run because the review sandbox forbids Go’s `/tmp/go-build*` directory; `git diff --cached --check` passed.

REVIEW-COMPLETE
