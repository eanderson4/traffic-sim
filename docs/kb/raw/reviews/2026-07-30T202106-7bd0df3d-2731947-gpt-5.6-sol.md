- **blocker** — `engine/routing.go:12` changes the public `route` intent semantics from shortest lane-length path to shortest free-flow-time path, while `contracts/asyncapi.yaml:1150` still guarantees “shortest path by lane length.” Message-contract changes require an ADR and migration note; update the contract/KB in this staged change or retain the existing behavior.

REVIEW-COMPLETE
