- **should-fix** — `engine/cmd/demosrv/main.go:438`: `Cache-Control: no-store` does not prevent `http.ServeFile` from honoring `If-Modified-Since`. A regenerated manifest with the same-second mtime can return `304`, reusing the stale manifest and violating ADR-0018’s generation-atomicity guarantee. Serve manifests without conditional handling or with a zero modtime.
- **should-fix** — `docs/kb/decisions/ADR-0006-nats-message-contract.md:295`: The ADR still calls replay “publish-only,” but `engine/natsio/bus.go:229` now queue-subscribes it to `state.sig.req`. Update the older addendum or explicitly scope “publish-only” to intents/control.
- **question** — `engine/natsio/bus.go:351`: Is rejecting every reply subject outside `_INBOX.*` intentional? NATS permits custom inbox prefixes, so otherwise-valid request clients will silently time out despite the contract describing a dynamic reply inbox.

REVIEW-COMPLETE
