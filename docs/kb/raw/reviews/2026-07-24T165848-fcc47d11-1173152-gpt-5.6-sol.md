- **blocker** — `contracts/asyncapi.yaml:125`, `contracts/asyncapi.yaml:138`, `docs/kb/decisions/ADR-0016-tssg-chunking.md:3`: the staged contract/accepted ADR promises TSSG chunking and `state.sig.req`, but their engine/viz implementation is unstaged. This commit ships a source-of-truth contract the runtime does not implement.
- **should-fix** — `scripts/overpass-lean.py:42`, `scripts/osm-stop-nodes.py:193`: filtering minor ways before stop resolution destroys original junction incidence. A stop at a retained arterial/removed side street becomes mid-block and may be walked to an unrelated downstream junction, fabricating stop control.
- **should-fix** — `scripts/osm-stop-nodes.py:219`: split continuation selects direction geometrically without validating the continuation way’s `oneway` flow. It can mark an against-traffic arm that `incident_arms()` excludes, potentially misclassifying the target as `allway_stop`.
- **should-fix** — `contracts/asyncapi.yaml:129`: clients “install only a complete generation,” but no generation identity exists. Complementary drops across two publish rounds can form an apparently complete mixed table without a gap/regression; compare the shared `tick` or add a generation header.
- **should-fix** — `contracts/asyncapi.yaml:441`: no operation references `stateSigReq`, so AsyncAPI tooling cannot discover the engine’s new request handler. The additive contract also remains version `2.2.0` at `contracts/asyncapi.yaml:23`.
- **should-fix** — `docs/kb/INDEX.md:101`: the registry still labels ADR-0016 proposed/pending and describes removed `sig_gen` plus publish-once behavior, contradicting the accepted ADR and staged contract.
- **should-fix** — `docs/kb/decisions/ADR-0016-tssg-chunking.md:49`: “schema_version stays 1” contradicts `contracts/asyncapi.yaml:754`, where TSSG headers require schema version 2. Clarify that the TSSG payload layout remains v1.

REVIEW-COMPLETE
