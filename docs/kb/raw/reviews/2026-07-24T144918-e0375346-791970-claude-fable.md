Test execution was denied by the permission gate — skipping runs; the KB entry and prior rounds record the CRC-pinned suite green. I have full context now.

---

**Context verified:** this staged diff is the previously reviewed round (`docs/kb/raw/reviews/2026-07-24T143541-*`) with that round's findings applied; the engine hunks are byte-identical to what was already verified in depth (`boundaries()` old/new sweep equivalence including hop chaining and the `next.Index < i` re-append reach; `pickSuccessor`/`routeNextHop` purity — memoized per-destination Dijkstra, value-deterministic under any query order, `routing.go:31-77`; `past` map keyed by `*Lane` but never ranged — no map-iteration exposure; eager `totalLaneKm` fill uses the same slice-order accumulation as the lazy path, bit-identical; `TotalLaneKm` callers are tick-path single-goroutine at `spawn.go:163`, `director.go:158`). Before/after CRC `280df03591679821` identical per the KB entry.

## Findings

**Blockers:** none.

Delta vs. the last reviewed round, all checked:

- Prior should-fix applied: schema pointers now correctly cite `engine/metricsjson.go` (main.go:75, main_test.go:583).
- Prior nits applied: interval `stops` comment now says `*int` (main.go:79); `Lane.Length` comment now scoped to "production paths after NewEngine" (network.go:10-14 of the patch).
- Prior question resolved: `dropped_crossings` now surfaced end-to-end — `metricsFile.Totals` (main.go:129), `summaryRow` (main.go:235), `computeSummary` (main.go:248), summary table column (page.tmpl:774-775). Field name matches the sink exactly (`metricsjson.go:76`). Extra sink fields (`v`, `denied_by_lane`, per-record `schema_version`) remain ignored on decode — harmless.
- New caveat line in the template ("single-seed; differences are hypotheses, not conclusions — ADR-0014", page.tmpl:769) — cosmetic, accurate.

**Nits**

1. `engine/cmd/metview/main_test.go:590-593` — no test asserts the new `dropped_crossings` passthrough (the fixture leaves it zero, so a dropped tag or field would go unnoticed). One-line addition to `TestSummaryDerivesSpeed`'s fixture if you touch this again; not worth a round.

**Determinism:** no wall clock, no RNG, no map iteration in sampled paths introduced; the one non-associative float accumulation (`laneLen`, engine.go:106-110 of the patch) is fixed-slice-order and bit-identical to the path it caches.

**Contract check:** no NATS subject/payload changes. metview is a read-only file consumer over loopback HTTP with the ADR-0002 carve-out cited in the package doc; no ADR required. WQ-15 KB entry records the perf change and determinism argument.

REVIEW-COMPLETE
