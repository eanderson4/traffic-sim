• Now let me check the surrounding context in the repo files the diff touches.

• Two more context checks: whether the PNG/sidecar in `docs/show/diag` are tracked, and how the Chicago quiz numbers are measured (partial-interval handling).

• I have enough to write the review. One quick line-number check in the new script.

• ## Review

  Scope check first: no Go, no NATS subjects, no engine paths touched — ADR-0005/0006 surface is nil. The `--kimi` gate change has its ADR-0013 addendum, AGENTS.md updated to match, archive naming (`kimi-k3.md`) matches script behavior. Pillow dependency is justified in the module docstring, confined to a presentation script, and the build degrades gracefully without it — consistent with AGENTS.md's dependency rule.

  ### Blockers

  None.

  ### Should-fix

  1. **`scripts/external-review.sh:110-111` — the read-only guardrail doesn't cover `$work`, which holds the other reviewer's output.** Kimi is given `--add-dir "$work"`, and `$work` is fully writable: it contains `sol.md` (Sol's in-flight review), `diff.patch`, and `brief.md`. The ADR addendum's own rationale is that kimi writes files unasked in `-p` mode. A stray write to `diff.patch` fails closed via the drift check (line 154 re-derives from git), but `sol.md` has no integrity check — the completion test (lines 139-143) is just size + terminal marker, so a clobbered/fabricated Sol review would pass and silently collapse the two-family rule the flag exists to preserve. Cheap fix: copy patch/stat/brief into the read-only tree (or a `chmod a-w` subdir) and `--add-dir` that instead of `$work`. The ADR's "guardrail, not sandbox" framing covers determined escape; this is the *granted* directory, not an escape.

  2. **`scripts/show/build-quiz.sh:82` — `CHI_SKIP_MAP=1` deletes two tracked files.** `docs/show/diag/chi-loop-urban__setup.png` and `.png.json` are both checked in (the sidecar is modified in this very commit). Running the canonical build with the documented opt-out `rm -f`s them, leaving a dirty checkout with a stageable deletion — one `git add -A` from committing a removal nobody intended. The opt-out should leave tracked state alone (mkquiz already ships text-only when the PNG is absent).

  3. **`scripts/show/mkcongestionmap.py:24-32` and `build-quiz.sh:66-71` — the "agrees exactly" / "cut the same way at both ends" claims are false for the shipped file.** The docstring says the map agrees with corridorspeed.py on "what every current run emits" (single set, no partials). The shipped sidecar contradicts it: `docs/show/diag/chi-loop-urban__setup.png.json` has `last_interval_end: 10000` vs `end_tick: 12000`, i.e. the current Chicago run *did* emit a partial final interval, which this script drops and corridorspeed.py (per the same docstring) includes. So the map measures 4,000–10,000 while the quiz table beside it measures 4,000–12,000. The on-page caption discloses the real window (mkquiz.py:447-455), so what ships is honest — but both comments assert a same-window property that doesn't hold. Correct the comments.

  ### Nits

  4. `scripts/external-review.sh:108` — `git archive | tar -x` under `set -eu` without `pipefail`; an archive failure hands kimi an empty tree and the review would read as a review of nothing. Low-probability (write-tree just succeeded), one-word fix.

  5. `scripts/show/mkcongestionmap.py:114-126` — a file mixing a named set with an unnamed one (`set_id` absent → `None`) forces `--set`, but `None` can't be expressed on the CLI, so the unnamed set is unselectable. Fine to leave until such a file exists.

  6. `scripts/show/mkcongestionmap.py` `colour_for` — the `limit_ms <= 0 → EMPTY` branch is dead given `--min-limit` default 0.1. Harmless.

  ### Questions

  7. The staged tree contains `docs/show/diag/chi-loop-urban__setup.png` (145 KB) but this commit updates only its sidecar. Confirm the PNG bytes are genuinely unchanged under the new window parameters (same metrics, deterministic Pillow output) — i.e. the checked-in image matches the new sidecar's `end_tick`/`last_interval_end` provenance. Otherwise the stale-image failure this commit exists to kill is shipping inside it.

  8. `viz/public/quiz.html` is a generated one-line artifact I can't meaningfully verify from the diff; assuming it's mkquiz output from this code. Gate-exempt path anyway.

  REVIEW-COMPLETE

