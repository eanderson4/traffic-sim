- **blocker** `engine/natsio/recorder.go:150` — Any stream-name collision triggers `UpdateStream` without verifying the stream is empty. `NewRecorder`/`RunLive` callers outside `serve` bypass `checkFreshRecording`, so an accidental rerun can mutate a preserved recording’s subjects, retention, `MaxAge`, and compression before OCC rejects the first append—potentially causing data expiry. Inspect state and reject non-empty streams before updating; preserve validated existing configuration.
- **should-fix** `engine/natsio/recorder.go:412` — `IntentBatchMax` is not actually a maximum: the first record bypasses the size check, although values as low as 1 are accepted at line 131. Additionally, `loggedIntentMax` assumes controller IDs are ≤256 bytes without an enforced invariant. Validate a usable minimum and/or size the encoded record before publishing.
- **should-fix** `engine/natsio/replay.go:64` — `MaterializeRunRecord` and `indexLogMsgs` do not verify the NATS `tick` header equals the TSLB payload tick. `logcursor` explicitly performs this check, so malformed recordings are rejected by bounded replay but silently interpreted differently by the other readers. Centralize and apply the cross-check to every reader.

REVIEW-COMPLETE
