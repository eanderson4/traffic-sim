Review of the staged diff (repeated `--add-lane` stacking + suffix-strip attribution in `whatif.py`).

I traced both passes of the widen-by-two path by hand (donor selection, id minting, offset accumulation, feeder wiring) and they are correct; the new tests pin exactly the failure modes that matter. No blockers. Determinism is clean: the new `edges` set is used for membership only, `by_edge` is built by ordered list iteration, and edge groups were already iterated via `sorted()`. No NATS subject or payload is touched, so no contract/ADR issue.

**Should-fix**

1. `scripts/chicago/mknetvariant.py:20-25` — the new `--drop-lane` doc text describes "a `--drop-lane` after an `--add-lane`", but `main()` applies operations in a fixed order: retime (line 143), speed (158), drop-lane (168), **then** add-lane (241). Within one invocation, add-then-drop cannot occur; the actual composition is drop-then-add, which reshuffles via a different mechanism (drop removes the outermost labelled lane first, then add clones the new outermost lane of the whole edge) and yields a different network than the documented order. This also deepens the pre-existing lie in line 12 ("applied in the order listed here" — the list says add-lane before drop-lane; the code does the reverse, and lists speed last but applies it second). The doc claim is true only across two separate invocations. Either fix the doc to say so, or reorder `main()` to match the list.

2. `scripts/whatif.py:240-243` interacting with `scripts/chicago/mknetvariant.py:259-262` — mknetvariant now explicitly embraces cloning an **unlabelled** outermost lane of a corridor edge ("you widen the edge, not the label"). But whatif's suffix-strip attribution only recovers the corridor when the stripped donor id is in the label map; a clone of an unlabelled donor strips to an id `corridors.get()` doesn't know, so its VMT is silently dropped — precisely the "no-op with a plausible label" bias both comments warn against. Latent today: I checked chi-loop-urban and all 562 labelled edges are fully labelled (0 partial edges), so every donor is currently in the map. Record and defer per the triage bar, but the two files now disagree about which lanes count as corridor capacity.

**Nit**

3. `scripts/chicago/test_mknetvariant.py:56` — `tempfile.mkdtemp()` directories are never removed; `tempfile.TemporaryDirectory()` would clean up.

**Question**

4. `scripts/whatif.py:241` — a base-network lane genuinely named `*_w1` whose stripped prefix happens to be a labelled lane would be misattributed. Pre-existing risk (the one-strip version had it too) and implausible given the `edgeid_index` naming scheme; flagging only because the loop now strips repeatedly. No action expected.

REVIEW-COMPLETE
