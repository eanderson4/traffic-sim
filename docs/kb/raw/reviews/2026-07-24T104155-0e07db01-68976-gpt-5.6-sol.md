- **blocker** — `engine/rightofway.go:150`, `engine/rightofway.go:255`: lookahead repeatedly calls `pickSuccessor` with the original `HeldTurn`, although `boundaries` consumes it after the first lane crossing (`engine/engine.go:435`). Across fragment/internal chains, gating and box-clearance can inspect a different branch than the vehicle actually takes, potentially permitting entry toward a blocked/red routed exit. Lookahead must virtually clear the turn after its first hop.

- **blocker** — `docs/kb/decisions/ADR-0006-nats-message-contract.md:330`: the patch deliberately changes externally visible `route` behavior while leaving contract version 2 untouched. Routed runs change trajectory, conflicting with the add-only rule that field semantics never change in place (`docs/kb/articles/concepts/vehicle-controller-interface.md:96`). Version/capability migration is required, or the governing versioning decision must be explicitly superseded.

- **should-fix** — `engine/natsio/driver/driver.go:315`: a replacement driver ignores `ego.Route` and derives a new exit using the vehicle’s current lane. Reachability and weights can differ after partial travel, so orphan re-claim can overwrite the persistent destination and violate ADR-0008’s behaviorally invisible failover requirement. Preserve an existing observed route.

- **should-fix** — `engine/routing.go:60`: every distinct destination permanently caches an `O(lanes)` table. On the documented ~55k-lane Chicago network, each table is roughly 220 KB; thousands of selected exits can consume hundreds of MB. Bound/evict the cache or constrain destination cardinality.

- **should-fix** — `docs/kb/decisions/ADR-0006-nats-message-contract.md:349`: the batch liveness exception remains inconsistent with `engine/natsio/contract.go:33`, `docs/kb/decisions/ADR-0008-controller-contract.md:71`, and `docs/kb/articles/concepts/vehicle-controller-interface.md:74`, which still promise timeout, orphan release, and failover unconditionally. Ground rule 4 requires updating the governing KB, not only AsyncAPI and ADR-0006.

- **should-fix** — `scripts/chicago/scorecard.py:33`: “largest set” is selected by interval-record count, which favors shorter periods as well as wider lane coverage. A small high-frequency set can therefore supply the reported stops and queue ranking instead of the network-wide set. Empty `intervals` also crashes at line 34.

- **nit** — `scripts/chicago/water.py:128`: a `GeometryCollection` can contain polygon members; absence of top-level `coordinates` does not mean “nothing polygonal.” This silently drops valid water polygons along with degenerate line/point pieces.

REVIEW-COMPLETE
