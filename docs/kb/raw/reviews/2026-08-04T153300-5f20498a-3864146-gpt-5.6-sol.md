- **blocker** `engine/sigctl.go:298` — Multiple `signal_set` verbs for the same program can apply at one tick boundary. A later verb truncates and drops the earlier override as an empty interval (`:275-280`), but the earlier `appliedSig`/`SigLog` entry retains a positive `hold_ticks`. The record therefore claims an effective hold that governed zero ticks, violating `LoggedVerbView`’s “exactly what the engine enforced” contract; its request ID also disappears from keyframed override history, allowing reapplication after warm restore.

REVIEW-COMPLETE
