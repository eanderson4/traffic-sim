- **blocker** — `engine/natsio/driver/driver.go:400`: Deferred vehicles continue under default routing, but `pickExit` later filters using the frozen first-observation lane. If the vehicle crosses a fork meanwhile, the selected exit may be unreachable from its current lane and silently degrade to default routing. The test never moves its egos, so it misses this failure.
- **blocker** — `engine/natsio/driver/driver.go:317`: The budget and `wantLane` state are replica-local. Replica count/sharding changes total routes resolved per tick, and failover before resolution captures a later lane. Thus trajectories depend on controller topology/timing, contradicting ADR-0008 and the package guarantee that replica identity is behaviorally irrelevant.
- **should-fix** — `engine/natsio/driver/driver_test.go:187`: The asynchronous extra-intent check exits immediately once count 11 is observed. A subsequently processed stray `RouteSet` still false-passes; flush and wait through a quiet interval before asserting.
- **nit** — `engine/natsio/driver/driver.go:188`: Fast “no responders” failures hot-spin during the final 250 ms because the loop skips sleeping while continuing requests.
- **question** — `engine/natsio/driver/driver.go:59`: The stated tick-loop stall premise appears incorrect: `onObs` is an external NATS subscription callback, not the synchronous `RunObserver.Observe` path in `run.go`. Is there measured evidence that this work blocks the authoritative engine loop?

REVIEW-COMPLETE
