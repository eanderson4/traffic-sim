- **blocker** `engine/BENCHMARKS.md:133` — The claimed “engine spends ~10.4 ms waiting” is false: production drains immediately and never waits for a full buffer. `engine/natsio/bench_intent_test.go:193` measures controller publish loops, broker routing, flushes, and polling before drain, so presenting the resulting ~20.5 ms as engine tick-budget consumption makes the M0 baseline misleading.
- **blocker** `engine/natsio/bench_intent_test.go:224` — ADR-0026 M0 explicitly requires `onIntent` CPU, but the benchmark only records publish-to-buffer wall time and aggregate `DrainIntents` time. `engine/BENCHMARKS.md:155` therefore incorrectly declares the M0 gap filled; there is no engine-side callback CPU measurement.
- **should-fix** `engine/natsio/bench_intent_test.go:139` — With exactly 50 samples, the nearest-rank calculation at line 265 makes “p99” the single maximum sample. This contradicts the comment that 50 samples prevent one load spike from defining p99 and gives M3 an unstable comparison baseline.

REVIEW-COMPLETE
