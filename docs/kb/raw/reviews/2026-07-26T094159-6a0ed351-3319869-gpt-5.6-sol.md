- **blocker** `engine/engine.go:531` — Safety math assumes the leader brakes at `SafetyDecel`, but valid intents allow `-9 m/s²` (`engine/intent.go:224`) while serve defaults the gate to `6`. A harder-braking leader can therefore still be rear-ended, contradicting ADR-0025’s collision-prevention guarantee. Either constrain all braking to the assumed bound or calculate against the physical leader deceleration.
- **blocker** `engine/natsio/bake.go:158` — The first message of each later tick is hashed before being stored in `pending` (`engine/natsio/bake.go:163`), then hashed again when processed next iteration. The resulting record digest violates ADR-0023/`contracts/baked-replay-v1.md`’s “every message once, in stream order” algorithm and produces incompatible content keys.
- **should-fix** `engine/engine.go:835` — Pending-directive CRC serialization omits the newly trajectory-affecting `Destination` and `OffsetM` fields. Distinct queued states can share CRCs, and corruption may remain undetected indefinitely when a directive expires without injection.
- **should-fix** `engine/metrics.go:1269` — Despawn attribution assumes a set route identifies the actual final chain, but explicit held turns override routing (`engine/engine.go:723`). A routed vehicle turning toward another exit can have distance, lane metrics, and destination booked to the route it did not take.

REVIEW-COMPLETE
