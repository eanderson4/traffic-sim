Review of the staged diff (ADR-0029 phase 1 warm start, ADR-0031/0032/0033 kernel fixes, ADR-0034 gridlock escape, keyframe v5). I read the full patch and verified assumptions against the repo (`sigState`/`phaseAt`, `pickSuccessor`, `foeApproachBlocks`, `computeCRC`, keyframe writer/reader gates, `Kernel.Observe`, director `Attach` ordering, scenario param defaults). Sandbox blocked `go build`/`go test`, so this is static review only — the pre-commit suite is the compile/test authority.

## Findings

**Blocker** — none found.

**Should-fix**

1. `engine/cmd/demand-director/main.go:28` — the standalone `-start-tick` ships a known silent double-spawn (fastForward skips arrivals ≤ start, but the restored engine already holds accepted directives for `(start, start+Lead]`; the contract's request-id dedup is per-process, so that window spawns twice). serve refuses the combination; the standalone binary's only guard is flag *help text* — there is no runtime warning when `-start-tick > 0` is actually used (`director.go:198-201` logs success, not the hazard). The commit's own standard elsewhere is "a printed note is not enough" (seed splice, `-state-reseed`); here there isn't even a note. One WARNING line at attach, or refuse-without-`-force`, closes it.

**Nit**

2. `engine/gridlock.go` (`strandStuck`) — `limit := uint64(e.Params.StrandAfterS / e.Params.Dt)` truncates, while the repo's other seconds→ticks conversions round (`+0.5`, e.g. `demand/director.go:309`). Deterministic either way, but for a dt whose quotient lands just under an integer the threshold is one tick short of the stated 300 s.
3. `engine/metrics.go:617-623` — the stranded branch has no counterpart to the completion branch's defensive nil-tstate reconstruction (`metrics.go:662-671`): a stranded id with a missing tripState counts in `strandedTrips` but silently emits no trip record. Defensive path only, but the two branches now disagree on the fallback.
4. `engine/natsio/server.go:83` — SchemaVersion comment still says "TSKF keyframe v2"; stale since v3, more visibly wrong at v5. Pre-existing, but this is the commit moving the version.

**Question**

5. serve warm start re-attaches a *fresh* embedded default driver; bit-exactness is pinned at engine level and at RunLive-without-controllers (`natsio/warmstart_test.go`), not through the driver. `driver/driver.go:10-13` (per-vehicle keyed policy RNG, re-claim "behaviorally invisible") suggests it holds, but nothing pins "warm serve run continues the cold serve run". Fine for the stated unrecorded-debugging scope — just confirm that's the understood boundary.

## Checked and clean

- **Determinism (ADR-0005):** no map iteration in sampled paths — `LaneFingerprint`/`TypeFingerprint` hash in index order with length prefixes; `resetStuckBehind`'s map is membership-only over a deterministic BFS worklist; serve's `StrandedBySection` print sorts keys; `strandStuck` sweeps `e.order`; no wall clock or RNG in any new kernel path; ID tie-break deterministic. `sigPermissive` is pure integer phase arithmetic.
- **Keyframe v5 contract:** correctly treated as a published-payload change with a migration note (ADR-0034), writer emits the lowest representable version so unchanged states stay byte-identical; the writer/reader gate mismatch (v5 + empty director queue) is fixed on both sides and the `r.err` check before trusting the directive count closes the short-read hole; `keyframeDestVersion` renames are consistent across writer, reader, and `destination_test.go`.
- **ADR-0031/0033 scope containment:** unsignalised junctions unchanged (`foe.Signal == nil → foePriority`, `RowNone` early return kept for non-permissive); ego-major merge behavior only changes for signalised foes, per the ADR table. Single-successor networks bit-identical under ADR-0032 (`pickSuccessor` only invoked when `len(Successors) > 1`; held-turn zeroing matches `boundaries()`).
- **Escape accounting:** strands excluded from `Despawned`/`Arrived`/`CompletedTrips`, in the `MeanTimeLossS` denominator, `k.last` replaced wholesale (no leak), `Observe` runs every tick so per-tick `StrandedIDs` is safe; in-sweep removal from `lane.vehs` before later head tests is handled.
- **Warm-start interlock:** digest/tick/length cross-checks make sidecar-swap and tamper loud; lane fingerprint covers count, ids, order, length; type-ordinal binding guarded; atomic write ordering leaves only refusable partial states; `validateStateFlags` rules and tests are coherent and the seed-splice refusal is the right default.

REVIEW-COMPLETE
