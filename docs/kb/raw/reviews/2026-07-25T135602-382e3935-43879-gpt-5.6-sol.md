- **should-fix** `viz/src/main.ts:520` — Layer order changed from “all casings below all congestion lines” to external casing → external line → internal casing → internal line. At junction joins, the wide internal casing can paint over external congestion lines. Add both casing layers first, then both line layers.
- **should-fix** `viz/src/main.ts:476` — The staged code declares WQ-3 implemented, but the corresponding KB update is unstaged; the shipping snapshot still describes it as an open candidate in `docs/kb/articles/podcast-demo-workqueue.md:193`. Stage the KB update to keep the implementation and registry consistent.
- Typecheck and all 19 visualization test files pass. No determinism or NATS-contract changes found.

REVIEW-COMPLETE
