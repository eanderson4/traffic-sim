- **should-fix** `viz/src/signals.ts:162` — The running-mean gate does not guarantee a cluster stays within 45°. Bearings ordered `0° → 40° → 60°` all merge: after the first two, the mean is ~20°, so 60° passes. This can still combine distinct skewed arms—the bug this change targets—and produces a blended stop bar. Test a three-bearing bridge and compare against a fixed representative or all cluster members.
- Tests and TypeScript check pass. No contract change or ADR issue found.

REVIEW-COMPLETE
