• Now reading the substantive code changes (lines 4983–6165).

• Now let me verify key context files in the staged tree.

• Context check: engine metrics schema and ADR-0014 claims.

• Two more spot checks: mkquiz's all-or-none enforcement vs `--omit-diagram`, and how `--report` reloads saved results in whatif.py.

• One consistency check: the diff repeatedly cites "ADR-0014 §5" for "comparison tooling drops partials" — verifying ADR-0014's actual section numbering.

• I have enough for findings. Getting exact staged-file line numbers for the mis-cited ADR section.

• Review of the staged diff (6165-line patch) against the staged tree. Prior-round findings in the embedded archives were all verified against the current staged state — the round-3 blockers (whatif multi-set summing, warn-only window mismatch, None-sort crash, coverage numerator, `CHI_WARMUP`/`CHI_END` pairing, `CHI_SKIP_MAP` deleting tracked files, `$work` writability, `git archive` pipefail) are genuinely fixed in what ships now. Scope: no Go, no NATS subjects, no payload schemas — ADR-0005/0006 surface is nil; determinism discipline holds (file-order float accumulation, `sort_keys` sidecar, `date` is archival naming only).

  **Blockers** — none.

  **Should-fix**

  1. Wrong ADR section cited in six new spots. "Comparison tooling drops partials" is pinned in ADR-0014 **§3** (docs/kb/decisions/ADR-0014-observability-metrics.md:95, inside §3's v1 pins, lines 68–104). Everywhere else in the repo "ADR-0014 §5" means the scenario binding grammar — so these new citations collide with the established meaning: `scripts/chicago/corridorspeed.py:71` and `:118`, `scripts/whatif.py:149` and `:509`, `scripts/show/mkcongestionmap.py:26`, `docs/show/chi-loop-options.md:10`. One-character fix per site; in a repo where ADR citations are load-bearing, pointing at the wrong section is worse than no citation.

  2. `scripts/whatif.py:170-175` and `scripts/chicago/corridorspeed.py:57-66` — the multi-set refusal has no escape hatch. `mkcongestionmap.py` offers `--set` for the same situation; the A/B harness and corridor tool just exit, so a metrics file legitimately carrying two sets (ADR-0014 §5 permits overlapping sets) is unmeasurable by either without hand-splitting JSON. Add a `--set` selector or state the asymmetry as deliberate.

  **Nits**

  3. `scripts/whatif.py:170` / `scripts/chicago/corridorspeed.py:56` — the set guard inspects ALL intervals, including pre-warmup ones: a file whose second set ends before the warmup cut still exits, though nothing post-warmup would be double-counted. Harmless over-strictness.

  4. `scripts/chicago/mkquiz.py:490-524` — sidecar presence and `lanes_with_traffic` fail closed, but a sidecar lacking `bands`/`empty_rgb` (older generator, hand edit) yields a legend-less colour map with no complaint — the same "undecodable picture" the missing-sidecar exit exists to prevent. Validate the two keys you render.

  5. `scripts/chicago/mkquiz.py:505-510` — a typo'd `--omit-diagram` value silently no-ops; cheap to warn on unmatched entries.

  **Questions**

  6. `docs/show/diag/chi-loop-urban__setup.png` bytes are unchanged in this commit while its sidecar gains `end_tick: 12000`/`last_interval_end: 10000` provenance (raised as a question in the archived 16:10 round, no visible resolution in the diff). Confirm the checked-in PNG is byte-identical to a regeneration under the new flags — plausible if the metrics file has no intervals past 12000, but this is exactly the stale-artifact class the commit exists to kill, so it deserves a verified answer rather than an assumption.

  REVIEW-COMPLETE

