- **should-fix** `engine/natsio/recorder.go:383` — `loggedIntentMax` assumes controller IDs are ≤256 bytes, but legacy publishers retain the raw, length-unbounded subject token (`engine/natsio/contract.go:830`). `logIntentBatch` uses this false bound for splitting, so `IntentBatchMax` can be exceeded; IDs beyond `u16` also produce undecodable framing. Validate/cap controller length or size using the actual encoded record.
- **should-fix** `engine/natsio/tslb.go:102` — `DecodeTSLB` accepts `count == 0`, although the contract says `log.intents` messages are non-empty. Such a message can extend replay’s `lastTick` without authoritative intents/CRC and defeats the decoder’s “fail exactly” guarantee. Reject zero-record batches.
- **nit** `contracts/asyncapi.yaml:456` — S2 is stream-level, but only `logIntent`/`logIntents` declare it; `logKeyframe`, `logCRC`, `logEvent`, and `logVerb` reference the same `TS_LOG_{run}` stream without matching configuration (`contracts/asyncapi.yaml:514`). Keep the duplicated stream metadata consistent.
- **question** `docs/kb/decisions/ADR-0035-batched-intent-log.md:3` — The implementation makes this the default and increments the public contract, but the ADR remains `PROPOSED`. Is acceptance intentionally deferred until this review?

REVIEW-COMPLETE
