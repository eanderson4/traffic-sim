- **should-fix** `engine/cmd/demosrv/supervisor.go:161` — `start` holds `s.mu` throughout the potentially 660-second identity wait (`:191`, `:331`). Consequently `stop()` cannot acquire the lock, and the signal handler calls `sup.stop()` before `hs.Close()` (`engine/cmd/demosrv/main.go:99`). SIGINT/SIGTERM can therefore leave demosrv and its child running until startup completes or times out. Make readiness cancellable or avoid holding the supervisor lock during the wait.
- **nit** `viz/src/stopsign.ts:13` — documentation says clustering has “no distance gate,” but `sameCone` enforces a 75 m centroid gate at `viz/src/stopsign.ts:63`.

REVIEW-COMPLETE
