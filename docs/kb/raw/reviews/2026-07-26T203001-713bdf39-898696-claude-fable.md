Review complete. Engine delta is small (a 5-line demux case) with the bulk in contract/ADR docs; the pinned-ordering test checks out against the driver's actual publish path.

## Findings

**Blockers** — none. The explicit-`v2` case routes through the identical `bufferV2Intent` path as absent (`engine/natsio/bus.go:325-330`), decode-failure symmetry is preserved (generic `dropped` counter, same as headerless), the ADR amendment and asyncapi text agree with the code, and both engine-side (`tsib_test.go:340-349`) and contract-doc coverage exist. `TestBatchMixedRouteTickArrivalOrder`'s [B, A, C] claim matches the driver (`driver.go:379-392` — route-bearing intents publish mid-loop, batch flushes after); same-connection NATS publish order makes the assertion deterministic, and `settle()` guards against a third message.

**Should-fix**

1. `engine/natsio/bus.go:290-296` — the `onIntent` doc comment still states the old demux rule: "ABSENT = per-vehicle v2 … anything else — including a present-but-EMPTY value — = drop + count." Explicit `v2` is now a third accepted form, so the function's own header contradicts its body. Same drift at `bus.go:357-358`: `bufferV2Intent` says "the two demux exits that mean 'v2'" — there are now three. This diff's whole theme is "schema contradicted shipped reality"; don't ship the same contradiction into the primary in-code statement of the demux rule.

2. `contracts/asyncapi.yaml:31` (unchanged by the patch) — the diff semantically changes the contract ratified at HEAD under the same `2.5.0` version: `intent_encoding: v2` flips from dropped-and-counted to accepted, and `ControlIntent` headers go from required `SchemaHeaders` to `SchemaHeadersOptional`. The file's own convention is add-only/versioned, and the migration text keys external consumers on "asyncapi ≥ 2.5.0". If this is a deliberate pre-merge amend of a version that has never left the branch, that's defensible — but the ADR amendment should say so explicitly; otherwise bump (2.5.1/2.6.0). Note the bump ripples into the two "2.5.0 aligns schema to reality" strings added by this very diff.

**Nits**

- `contracts/asyncapi.yaml:1081-1122` (`TSIBHeaders.intent_encoding`) — the schema keeps `const: tsib` (correct for a TSIB message) but its description now carries the full v2-synonym demux narrative, duplicating the channel description a third time. Three copies of the demux rule in one file is future drift surface; consider pointing two of them at the canonical one.
- `engine/natsio/driver_test.go:1441-1442` — comment-only fix (10 ms → 100 ms) matches the actual `scalePace` constant; fine.

**Questions**

- ADR-0026 amendment justifies the synonym as protecting "a producer sending a literal `v2`" — does any such producer exist, or is this hardening against a hypothetical? The repo's own triage doctrine calls that reject-by-default; the counterargument (it's 5 lines closing a genuine engine-upgrade asymmetry, amended into the ADR) is reasonable, but worth one sentence of "no known producer; accepted for interop symmetry" honesty in the ADR.

Determinism: no map iteration, wall clock, or float accumulation in any touched path; the one wire-order divergence (mixed route ticks) is documented, deterministic, and pinned by test.

REVIEW-COMPLETE
