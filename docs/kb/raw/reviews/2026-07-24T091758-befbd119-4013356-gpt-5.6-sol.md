- **should-fix** `viz/src/replaypanel.ts:44` — `verbErrors` is mischaracterized as pause/seek/speed control failures. The producer increments it when recorded director/spawn verbs are rejected during replay, indicating record/spec divergence (`engine/natsio/player.go:422`). The UI should describe it as replay verb divergence, not an operator control-plane failure.
- **should-fix** `viz/src/replaypanel.ts:340` — The guard prevents overlapping polls but not a poll racing a control POST. A GET started before pause/speed/seek can resolve after the POST and overwrite its newer status, reverting the button, speed, or seek slider until another poll. Serialize all status-writing requests or discard responses older than the latest operation.
- Focused viz tests and TypeScript checks pass.

REVIEW-COMPLETE
