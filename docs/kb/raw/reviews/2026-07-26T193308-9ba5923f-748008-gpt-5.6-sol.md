- **blocker** — `contracts/asyncapi.yaml:675`: TSIB headers omit `schema_version`, violating the document’s own binary-message rule (`contracts/asyncapi.yaml:14`) and ADR-0006’s “schema_version everywhere” requirement. Either emit/require it or explicitly amend the invariant in ADR-0026.
- **blocker** — `docs/kb/decisions/ADR-0026-batched-intents.md:251`: the accepted ADR claims old engines avoid TSIB through the hello-advertised version, but `contract_version` remains `2` and capability advertisement is explicitly deferred (`contracts/asyncapi.yaml:231`). A default-on TSIB driver cannot detect an old engine; reconcile the migration mechanism before ratification.
- **should-fix** — `contracts/asyncapi.yaml:219`: “future encoding against an old engine fails loudly” is false. A pre-demux engine treats TSIB as malformed v2 and only increments the generic drop counter; malformed v2 decoding does not log. Describe this as counted/silent unless observability is added.
- **nit** — `contracts/asyncapi.yaml:663`: “one TSIB per cadence tick” contradicts both cap splitting and route-only ticks, which can produce multiple or zero batches. Say “up to one per 20,000 route-free intents.”

REVIEW-COMPLETE
