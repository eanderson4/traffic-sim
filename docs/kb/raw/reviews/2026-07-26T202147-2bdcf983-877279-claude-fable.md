Review complete. Findings:

**Blocker**

1. **Contract and ADR still specify the demux rule the code no longer implements.** `engine/natsio/bus.go:325-330` (staged) accepts explicit `intent_encoding: v2` as an exact synonym for absent, but the normative demux spec everywhere still says absent ⇒ v2, `tsib` ⇒ TSIB, **any other value ⇒ drop + count**: `contracts/asyncapi.yaml:105` (info block), `contracts/asyncapi.yaml:219-223` (the ctlIntent channel demux spec — the load-bearing one), `contracts/asyncapi.yaml:1072` (`IntentEncodingHeaders` description, "any other value … dropped"), and `docs/kb/decisions/ADR-0026-batched-intents.md:53-58`. So the staged contract tells a producer that setting `intent_encoding: v2` gets its message dropped, while the staged engine accepts it — and the whole point of the code change is that HelloReply advertises `"v2"` as an encoding name (`asyncapi.yaml:1270`), making that header value a natural thing to set. This commit already edits both documents; "message contracts are sacred" means the v2-synonym carve-out must land in the same commit. Doc-only fix: amend the three asyncapi spots and the ADR demux bullet list.

**Should-fix**

2. **`onIntent` doc comment is stale in the same hunk.** `engine/natsio/bus.go:290-296` still says "anything else — including a present-but-EMPTY value — = drop + count" with no mention of the explicit-`v2` case added directly below it. Update alongside the switch.

**Question**

3. `contracts/asyncapi.yaml:679-682` / `:911` — `SchemaHeadersOptional` says "engine-published messages stamp schema_version," but `ControlIntent` is referenced only by the receive-side `ctlIntent` channel (the record plane uses the separate `LoggedIntent` message). Which engine-published use of this message is the clause describing? If none, the wording invites a reader to think the engine publishes on `ctlIntent`.

Clean otherwise: the explicit-v2 path is byte-identical to the absent path (same `bufferV2Intent`, no counter asymmetry, no map iteration/wall-clock/RNG in changed code); the new `TestBatchMixedRouteTickArrivalOrder` helpers all exist and the test pins the documented [B, A, C] shape; the arrival-order exception is consistently worded across ADR, channel description, and batch message; `driver_test.go` changes are comment-accuracy only; the case-sensitive `msg.Header[...]` lookup matches nats.go's verbatim (non-canonicalizing) `Header.Set`, so the new `tsib_test.go` case exercises the real path.

REVIEW-COMPLETE
