- **should-fix** `engine/cmd/demosrv/supervisor.go:132` — `time.Now().UnixNano()` is not guaranteed unique across processes, yet readiness relies on the suffixed run ID being an exact ownership nonce. A collision with another demosrv starting the same base run can pass `ProbeRunMeta` because `CreatedUnix` has only second resolution and five-second tolerance. Use a cryptographically random nonce.
- **should-fix** `contracts/asyncapi.yaml:23` — the public contract remains version `2.2.0` despite adding `sig_chunk` semantics and the new `state.sig.req` channel at `contracts/asyncapi.yaml:126`. Bump the contract minor version and document this contract revision alongside the existing M9/M10 version notes.

REVIEW-COMPLETE
