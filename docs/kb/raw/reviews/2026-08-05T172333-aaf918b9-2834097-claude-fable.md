Review of the staged diff (test-only change: retry policy for schedule-disturbed legs in `TestBatchAppliedLagBoundary`). No production, contract, or determinism-sensitive code is touched; no ADR needed. The acceptance criterion itself (two zero-overrun legs, exact ≤ comparison) is unchanged — the change only re-samples until it gets a valid measurement, which is a sound flake fix rather than a tolerance loosening. Findings:

**should-fix** — `engine/natsio/driver_test.go:1448` (via `engine/natsio/server.go:46`): each retry attempt leaks a live embedded JetStream server until the whole test ends. `runLeg` creates `natsio.NewTestServer(t)` per call, and its shutdown is registered with `tb.Cleanup`, which fires at test end — not leg end (same for `srv.Connect`/`JetStream` conns and their streams). Before this change that was a bounded 6 servers; now worst case is ~38, including up to 12 at 5k-vehicle scale with ~2 MB obs frames in their streams. The accumulation happens exactly in the scenario the retry is designed for (loaded runner), so each disturbed attempt adds background load that biases later attempts toward disturbance — the mechanism partially defeats itself. Shut the server down explicitly at leg end (or scope each attempt in a subtest so Cleanup fires per attempt).

**should-fix** — `.github/workflows/ci.yml:25` interaction: CI runs `go test ./... -count=1` with the default 10-minute per-package timeout. A fully disturbed run adds several minutes of retries + settles (prod: up to 12×2 legs; scale: up to 6×2 legs at ~10 s each) on top of the rest of the `natsio` package, precisely when the runner is slow. If the package deadline hits first, the intended clean "regression signal" `Fatalf` is replaced by a goroutine-dump panic across all tests. Consider `-timeout` headroom in CI or a tighter attempt budget so worst case stays comfortably inside 10 m.

**question** — `driver_test.go:1446-1466`: the comment justifies per-leg (vs paired) retry, but the residual risk is different: a leg can be marginally loaded yet zero-overrun (driver response slowed past a tick boundary without a schedule overrun), inflating its tick lags. Independent retry lets v2 be accepted from a quiet window and tsib from such a marginal one → false `accept` failure with no re-measure. This exposure pre-exists the change and retry-on-assertion-failure would mask real regressions, so accepting it is defensible — worth a sentence in the comment acknowledging it.

**nit** — `driver_test.go:1447,1455`: `maxAttempts < 1` never hits the `attempt == maxAttempts` exit and loops forever. Only literals 12/6 exist today; a `>=` would make it self-defending.

Determinism check: clean — fixed seed untouched, lag measured in ticks not wall time, no map-iteration or float-accumulation changes, `time.Sleep`/wall clock confined to test scheduling. Contract check: clean — no subjects or payloads touched. Doc discipline: header comment at lines 1057–1070 updated consistently with the new policy.

REVIEW-COMPLETE
