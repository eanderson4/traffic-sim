- **blocker** `scripts/serve-baked.py:107` — Generated replay links pass a root-relative `?bake=/baked/...`. `resolveBakedUrl` uses that value as `new URL()`’s base, so playback fails with “Invalid base URL.” Print an absolute manifest URL as the script previously required.
- **should-fix** `scripts/chicago/mkquiz.py:232` — The quiz generator still emits `/?bake=...`; regenerating `viz/public/quiz.html` reverts the staged `/app.html` fix. `scripts/demos/bottleneck-town.sh:65` and two show docs retain the same now-broken route.
- **should-fix** `scripts/show/mksite.sh:65` — This moves baked artifacts onto the Pages site, contradicting ADR-0023’s R2/`data.phantomjam.com` service boundary (`docs/kb/decisions/ADR-0023-baked-replay-pipeline.md:14`) without an ADR addendum.
- **should-fix** `scripts/show/mksite.sh:25` — The documented default invocation depends on one developer’s absolute home path, so it fails in every other clone unless `--baked` is supplied.

REVIEW-COMPLETE
