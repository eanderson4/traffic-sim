**No blockers.** The budget mechanics, frozen-lane determinism fix, and hello retry narrowing are all sound; no contract (subject/payload) changes; admission order follows the obs ego slice, not map iteration; `pickExit` remains a pure function of (seed, id, lane) with sorted candidates. Findings:

**Should-fix**

1. `engine/natsio/driver/driver.go:426-430` — Warm-cache picks consume budget. The stall being fixed is the cold O(network) reachability scan, but the counter decrements for memo-hit draws too, hard-capping route resolution at 32/obs unconditionally. The ADR's "~0.3 sim-s" bound assumes demo spawn rates; the motivating case is city-scale, where a burst of N cold origins is warm after a handful of scans yet still waits ⌈N/32⌉ observations on default routing (3 200 spawns → ~10 s of forks taken by successor default). The routeStep comment justifies metering cold scans ("a burst of first-touch scans delays every intent behind it") but never says why warm hits are metered. Either meter only cache misses, or record the deliberate choice (predictable admission independent of process-local cache state) in the comment/ADR — right now it reads as an oversight. Acceptable to defer per the triage bar, but record it.

2. `engine/natsio/driver/driver.go:357-363` — Routing state is created for every obs ego but reaped only for `d.fleet` members. A vehicle the engine considers claimed but whose `ClaimReply` was lost (`tryClaim` timeout, driver.go:516-523) is driven every tick, accumulates `routed`/`wantRoute`/`wantLane` entries, and is never cleaned on despawn; `streams` is never reaped at all. Pre-existing pattern — `wantLane` just adds a third map to it — so defer-and-record is fine.

**Nit**

3. `engine/natsio/driver/driver_test.go:470-471` — Stale editing note: "(lower ID wins the sorted walk... verify order)". There is no sorted walk — admission is obs-slice order, and the test passes because ego 6 precedes ego 7 in the slice. Misleading comment plus a leftover "verify order" marker.

4. `engine/natsio/driver/driver.go:200` — The retry-only-`ErrNoResponders` fix silently depends on no-responders support (server + headers enabled). Against a server without it, the startup race resurfaces as one 2 s timeout → immediate hard failure instead of a retry. True today with the official server defaults; worth one sentence in the comment so a future broker-config change doesn't mysteriously break driver boot.

**Question**

5. `docs/kb/decisions/ADR-0019-route-budget-determinism.md:55-57` — "router.go … was deleted" shipped in commit 868c16b, not this change. Fine as a record of the round; just confirm it's intentional that ADR-0019's consequences describe a prior commit's cleanup.

Determinism notes checked and clean: no wall clock in decision paths (`time.Now` only in bounded startup retry loops), no map iteration feeding draws (`exitCache` maps are lookup-only, candidates sorted by lane ID), budget admission order deterministic per frame, `wantLane` freeze correctly decouples the draw from controller lag. The tests exercise the real `onObs` path including the echo-confirm flip and use a steady-state check instead of a single racy read.

REVIEW-COMPLETE
