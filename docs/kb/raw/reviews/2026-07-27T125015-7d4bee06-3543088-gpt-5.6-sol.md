- **blocker** `scripts/demos/bottleneck-town.sh:47` — The bake path invokes `serve` and `bake` directly. Since `serve` exits 0 after fidelity warnings, this can bake under-delivered or uncontrolled runs, bypassing the new mandatory checks in `scripts/chicago/record-hero.sh`.

- **blocker** `scripts/whatif.py:386` — Nonzero/metrics-missing runs are merely skipped and never added to `voided`; `report_pod` can still emit an unbalanced, quietly thinner paired comparison. The new refusal only covers zero-exit fidelity warnings, not execution failures, despite the same statistical bias.

- **should-fix** `engine/natsio/contract.go:404` — `ObsWorstOutage` records which controller’s frames failed only implicitly, then compares the global maximum against the fastest controller’s bridge. With mixed cadences, three failures on a cadence-5 controller are falsely voided because an unrelated cadence-1 controller sets the threshold to two ticks. Track bridge breaches per controller rather than comparing unrelated global extrema.

- **should-fix** `scripts/demos/bottleneck_town.py:285` — Lane geometry contradicts `edgeIndex` semantics. Increasing `i` moves farther right, making index 0 leftmost despite the documented/rightmost contract; consequently the extra “left-turn bay” at index `main_lanes` is generated kerbside and Left/Right controller context is mirrored.

- **should-fix** `scripts/demos/meter-sweep.sh:43` — The output pod is not cleaned. On rerun, `cp -r source "$POD/base"` nests the new scenario inside an existing destination while leaving the stale top-level scenario active; variants have the same problem.

REVIEW-COMPLETE
