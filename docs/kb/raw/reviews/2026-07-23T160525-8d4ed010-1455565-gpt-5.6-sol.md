- **blocker** `engine/cmd/serve/main.go:156` — The unpaced guard considers only `-driver`, but scenario demand also uses an asynchronous NATS director started after `RunLive` at `engine/cmd/serve/main.go:296`. With `-pace 0 -driver=false`, the engine can finish before demand attaches, producing an empty or scheduler-dependent recording. Attach demand before starting the loop or reject unpaced mode whenever demand parts exist.
- **should-fix** `engine/cmd/serve/main.go:256` — Any sufficiently high finite pace can similarly outrun the default driver/demand director because the run goroutine starts before either client attaches. The warning in flag help does not prevent nondeterministic, under-controlled recordings; establish attachment readiness before advancing ticks or enforce a safe bound.
- **should-fix** `viz/src/config.ts:18` — `dt` remains a user-controlled URL value with a silent `0.1` default, although ADR-0005 makes it scenario-configurable and authoritative `RunMeta.Spec.Params.Dt` already exists. Saved/manual viz URLs can therefore silently show incorrect speeds and congestion; derive it from run metadata or validate the override against it.
- **should-fix** `engine/cmd/serve/main.go:358` — `paceFloor` does not reject positive values that truncate below 1 ns, and its upper check accepts the rounded `float64(math.MaxInt64)` boundary, which can convert to a negative `time.Duration`. Validate `1 <= v < 2^63` for every nonzero pace.

REVIEW-COMPLETE
