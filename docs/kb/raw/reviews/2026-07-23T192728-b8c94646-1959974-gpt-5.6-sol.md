- **blocker** — `engine/cmd/serve/main.go:145`: Persisting `spec.Net.Path` as an absolute host path silently changes the public `RunMeta` payload semantics without an ADR/migration note. New recordings become checkout/machine-bound; moving the store or repository makes `NewPlayer` unable to load the network even when the registered scenario hash matches.
- **should-fix** — `engine/cmd/demosrv/replay.go:145`, `viz/src/replaypanel.ts:180`, `viz/src/replaypanel.ts:222`: The unbound status poll defeats stale-tab protection. An old tab polls the new replay’s status, replaces its stored `replayRun`, then sends correctly bound commands to that new replay. Status must be bound to the page’s expected run and reject mismatches without adopting them.
- **should-fix** — `viz/src/snapshots.ts:165`, `engine/scenario/scenario.go:506`: `SeekGate` hard-codes a 240-tick maximum, but valid scenarios have no lower `dt` bound. At `dt=0.001` and 8× replay, normal decimated snapshots advance roughly 800 ticks, so every frame is misclassified as a seek and repeatedly clears interpolation/overlays.
- **should-fix** — `viz/src/main.ts:582`, `viz/src/main.ts:599`: Trailer articulation integrates simulation velocity using wall-frame time. At 8× replay, the tractor advances eight simulation seconds per wall second while the trailer integrates only one, producing systematically incorrect articulation. Use elapsed simulation time derived from replay ticks.

REVIEW-COMPLETE
