- **blocker** `engine/natsio/demand/director.go:274`, `engine/natsio/contract.go:299` — Warm start restores already-accepted future directives in `dirQueue`, but `fastForward` skips only arrivals `<= startTick` and the new contract’s request-ID dedup map starts empty. The director therefore republishes lead-window arrivals already queued in the state, they are accepted again, and demand/CRCs diverge. Rebuild dedup from restored directives or advance past the restored queue.
- **blocker** `engine/natsio/run.go:179` — `RunLive` unconditionally records a warm-start keyframe at tick N, despite the recorder/player contract requiring a tick-0 anchor (`engine/natsio/player.go:200`). The CLI’s `-store` check does not protect direct `RunLive` callers; the added test itself creates a completed but unopenable recording. Reject this combination in `RunLive` or formally support nonzero record floors.
- **should-fix** `engine/natsio/run.go:150`, `engine/natsio/registry.go:79` — Warm-start registry metadata still publishes `started_tick: 0`, contradicting the actual first engine tick/keyframe. This makes the public KV contract internally inconsistent; pass the restored tick into registry creation.
- **should-fix** `docs/kb/decisions/ADR-0029-warm-start-from-keyframe.md:3` — Consequential warm-start behavior is shipping while its governing ADR remains **Proposed**. Accept the implemented phase or split it into an accepted phase-1 ADR before landing.

REVIEW-COMPLETE
