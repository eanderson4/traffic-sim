- **blocker** `scripts/external-review.sh:41`, `hooks/pre-commit:24` — The stamp binds only the resulting index tree, not the reviewed base `HEAD`. After review, `git reset --soft`, branch movement, or an amend can produce a different commit diff with the same index tree and reuse the stamp. Bind the reviewed base commit plus tree, and explicitly handle or reject amend semantics.
- **should-fix** `scripts/external-review.sh:102` — `keep` remains `1` after success, so every successful run leaks the complete patch and reviews under `/tmp`; the cleanup trap even reports retained artifacts. Reset it after successful archive/stamp.
- **should-fix** `scripts/external-review.sh:42` — Second-resolution timestamps plus deterministic `$dh` allow concurrent reviews of the same patch to overwrite/interleave audit files. Use a collision-proof run identifier and no-clobber writes.
- **should-fix** `scripts/external-review.sh:47` — `git diff --cached` omits binary payloads, so reviewers do not inspect the exact staged content despite the documented guarantee. Capture with `--binary --full-index`.
- **should-fix** `scripts/external-review.sh:91` — Completion checking accepts the marker anywhere, despite requiring an exact terminal line. Use an exact-line check, ideally verifying the final nonblank line.
- **should-fix** `docs/kb/INDEX.md:84` — The staged change adds ADR-0013 but does not register it in the KB index. The existing local edit is unstaged and therefore does not ship.

REVIEW-COMPLETE
