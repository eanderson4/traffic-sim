- **blocker** `engine/natsio/sigctl/sigctl.go:762` — Each vehicle marks every detector within the radius, without selecting/deduplicating an approach or link. Signal links commonly share or closely cluster stop-line points but belong to different phases, so one vehicle fabricates multiple queues—including conflicting calls—and can satisfy `SwitchMinQueue > 1` by itself. The reference controller therefore makes incorrect switching decisions in normal imported networks.
- **blocker** `engine/natsio/sigctl/sigctl.go:882` — Same-phase renewal bookkeeping stores `applied + hold`, but the kernel clamps the effective end to `chainStart + MaxChainTicks`. The final normal renewal before the 300-second bound therefore leaves `estimated()` believing the override remains active for up to another hold interval after it actually lapses, producing decisions against the wrong enforced phase.
- **blocker** `engine/natsio/sigctl/sigctl.go:306` — Seamless clearance depends on every verb applying at the predicted `snapshot tick + 1` (`engine/natsio/sigctl/sigctl.go:863`), although pace-0 explicitly provides no such guarantee. With variable drain lag, the next walk command can supersede yellow/all-red before its natural duration—or share a drain and reduce it to zero—so the claimed clearance guarantee and bracket validity do not hold.
- **should-fix** `scripts/sigctl-bracket.py:48` — The fidelity gate tests `verbs>0`, which is merely the number sent. An arm with every publish failed or every verb rejected passes as actuated despite applying no signal command. Check accepted/applied evidence rather than `Sent`.
- **should-fix** `engine/natsio/sigctl/sigctl.go:629` — The chunk accumulator neither remembers/verifies the generation’s `n` nor requests resynchronization after a gap/count change, contrary to ADR-0016. For example, `1/3` followed by `2/2` installs an incomplete table; a dropped initial chunk can only recover accidentally through later broadcasts.

REVIEW-COMPLETE
