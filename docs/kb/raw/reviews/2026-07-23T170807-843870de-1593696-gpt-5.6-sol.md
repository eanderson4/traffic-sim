- **blocker** `engine/natsio/player.go:217` — `endTick = min(spec.Ticks, idx.lastTick)` truncates valid recordings when CRCs/keyframes are sparse and no intent/verb lands near the horizon. The log’s last message tick is not proof that simulation stopped there.
- **blocker** `engine/natsio/server.go:186` — reserving the `-replay` suffix changes the public run-id/subject contract, and `engine/natsio/recorder.go:76` now rejects IDs that AsyncAPI still permits. The staged change has no required ADR or migration note.
- **should-fix** `engine/natsio/player.go:297` — after stepping onto `endTick`, playback still sleeps at `engine/natsio/player.go:309`; `done`/paused are set only on the next loop. At slow speeds, status and `/resume` incorrectly treat a completed replay as active for seconds.
- **should-fix** `engine/natsio/player.go:565` — snapshot decimation assumes `dt=0.1`. ADR-0005 makes tick length configurable, so `round(speed)` produces 5 Hz at `dt=0.2` and 20 Hz at `dt=0.05`, contradicting the advertised ~10 Hz wall cadence.
- **question** `engine/natsio/player.go:473` — is the direct HTTP engine-control API an intentional ADR-0002 exception? Pause/seek/speed are service controls outside NATS, but no ADR documents the boundary or exception.

REVIEW-COMPLETE
