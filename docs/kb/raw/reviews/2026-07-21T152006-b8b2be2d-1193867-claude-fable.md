Reviewed the staged diff (new metric kernel `engine/metrics.go` + tests, comment tweak in `engine/engine.go`) against the engine internals it observes (`spawn.go`, `director.go`, `boundaries()`/Step phase order, `mobil.go` S-preservation, `vehicle.go v0eff`). Sandbox denied `go test`/`go vet`, so this is a static review; the gate's own test run should confirm.

Verified clean: read-only observation (CRC invariance holds structurally — no engine state touched); every map iteration on an emission/accumulation path is sorted or slice-ordered (`gone`, `servedIDs`, `Totals` ids, `tstates` at Finalize, sets/lanes in config order; `exitResolve`'s bare `for exit = range exits` is guarded by `len(exits)==1`); float accumulation follows `e.Vehicles()` append order; drain sorts have unique keys so unstable `sort.Slice` is deterministic; distance conservation across wrap/hop/multi-crossing/despawn is exact given `boundaries()` reverse-index reprocessing and MOBIL's preserved `S`; spawner served-detection is sound because `step()` assigns a fresh `pend` (new monotone ID) in the same tick it injects; `v0effOf` mirrors `Vehicle.v0eff` exactly. No NATS subject/payload change — kernel is transport-agnostic per ADR-0014 §6, ADR ratified in `1ef8c2a`.

**Findings**

**should-fix**
1. `engine/metrics.go:617` — `Finalize` has no pairing guard. `Observe` panics loudly on skipped/doubled ticks, but `Finalize` trusts `e.Tick` blindly: Steps taken between the last `Observe` and `Finalize` silently stretch the final partial interval's duration (`dur = (end−begin)·dt` covers unobserved ticks, deflating q/k/occupancy) and stamp a wrong `ExitTick` on every horizon-partial trip — exactly the silent mis-booking the Observe guard exists to prevent. One-line fix in the same style: panic unless `(!k.observed && e.Tick == 0) || e.Tick == k.lastTick`.

**nit**
2. `engine/metrics.go:591` — `dueNow` is keyed by `RequestID`, but the kernel-side queue doesn't dedup (dedup lives in the contract layer per `director.go`); two queued directives sharing an ID collapse to one map entry and undercount `DeniedServed`. Fine to defer given the contract-layer invariant.
3. `engine/metrics.go:536-540` — the defensive despawn-reconstruct branch books exit-tick `stops`/`stoppedS` from `prev.stopped`, while the normal despawn path books none for the exit tick. Inconsistent semantics, though only in a should-never-happen branch.
4. `engine/metrics.go:294` — a set whose `BeginTick` is at/past the run horizon validates fine and then emits zero records silently; the strict fence catches every other dead config. (Horizon isn't known at construction, so arguably unfixable there — noting it.)

**question**
5. `engine/metrics.go:296` — `NewKernel` requires tick 0 and `Observe` requires starting at tick 1, so a keyframe-resumed engine can never carry a kernel. Intended scope pin for ADR-0014 (metrics only over full runs), or a gap to note in the ADR's implementation notes?

No blockers. Determinism discipline and conservation are solid; the test suite is unusually thorough (single-clamp discrimination case, ambiguous-exit, wrap+boundary, backlog integral replay).

REVIEW-COMPLETE
