Review complete. Verified along the way: tests green (`go test ./...` in the engine module), `go vet` clean, worktree matches the staged diff.

**Blockers** — none.

**Should-fix**

1. **The static-baseline arms were documented, not stamped.** Every what-if arm in `data/scenarios/` except `chi-loop-urban-half-adaptive` has no `adaptive_routing` key, so after this commit they silently inherit adaptive-on while every published number in `docs/show/README.md` was measured on static routing — and their scenario content hash is unchanged, so ADR-0012 §6's `(content-hash, seed)` run key now points at a different trajectory than the published runs. The warning at `docs/show/README.md:280` documents the footgun; stamping `adaptive_routing: false` into those manifests (as the addendum at `docs/kb/decisions/ADR-0036-congestion-adaptive-routing.md:56-60` says a reproducer must do by hand) removes it and makes the baseline self-describing. Cheap, and it closes the only gap between the addendum's reproducibility claim and what's actually checked in.

**Nits**

2. `data/scenarios/chi-loop-urban-half-adaptive/scenario.yaml:14` — `adaptive_routing: true` is now redundant with the default. Harmless; optionally drop or keep as explicit intent.
3. `engine/engine.go:409` — the `routeEpochTicks == 0` lazy-init sentinel would clobber restored `ttEMA`/`ttSnap` with free-flow if any restore path ever bypassed `NewEngine`. Today none does (`keyframe.go:305` always constructs via `NewEngine`, which sets the sentinel nonzero), and the branch is genuinely needed (`persistence_test.go:109`'s struct-literal engine would otherwise mod-by-zero at `engine.go:420`), so this is fine as-is — just an invariant worth a clause in the comment.
4. `docs/kb/INDEX.md:10` — drain completions changed from the previously committed 831 to 786; internally consistent with the addendum (`drain-chi-adaptive-final` rerun), flagged only so the supersession is deliberate.

**Checked and clean**

- **Replay/determinism of old recordings:** `RunMeta` embeds the full spec and `engine.Params` has untagged fields (no `omitempty`), so old metas carry `AdaptiveRouting: false` explicitly — and pre-field metas unmarshal to the zero value `false`. The default flip cannot retroactively change any recorded run's replay (`engine/natsio/registry.go:27`, `engine/replay.go:51`).
- **Keyframe versioning both directions:** v6 written only flag-on (`keyframe.go:153`); v6-into-flag-off refuses loudly (`keyframe.go:312`); pre-v6 into flag-on seeds the dwell clock at the restore tick. No viz/TS consumer parses keyframes, so v6-on-the-wire-by-default has no client impact.
- **CRC fixtures:** no pinned CRC literals anywhere in tests — all comparisons are live-vs-replay self-consistent, so the flag flip (which folds adaptive state into the CRC at `engine.go:1054`) breaks nothing pinned.
- **`*bool` manifest change:** mapping and all three cases (true / false / absent-inherits-on) are tested (`scenario_test.go:134-141`); golden scenario-hash canaries pass, so canonicalization is unaffected. Local-file contract, no NATS subject/payload change beyond what ADR-0036 already recorded.
- **`initAdaptiveRouting` extraction:** `e.Net` is assigned before the call site (`engine.go:261` vs `engine.go:302`); behavior identical to the inlined original.

REVIEW-COMPLETE
