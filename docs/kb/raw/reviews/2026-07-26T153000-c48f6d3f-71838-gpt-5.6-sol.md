- **blocker** — `engine/natsio/bus.go:53`: This adds a public `intent_encoding` header and TSIB payload, but `contracts/asyncapi.yaml:607` still permits only the single-vehicle v2 message. ADR-0006 §9 makes AsyncAPI the source of truth; the staged commit ships an undocumented message contract.
- **blocker** — `engine/natsio/bus.go:309`: An explicitly present but empty `intent_encoding` is treated as absent and parsed as v2. ADR-0026 requires only an absent header to mean v2; every other value must drop and increment `intentEncodingUnknown`.
- **should-fix** — `engine/natsio/tsib.go:51`: `EncodeTSIB` accepts more than `TSIBMaxRecords` and returns a payload that `DecodeTSIB` always rejects wholesale. The exported codec should reject over-cap input or otherwise prevent producing an invalid batch.

REVIEW-COMPLETE
