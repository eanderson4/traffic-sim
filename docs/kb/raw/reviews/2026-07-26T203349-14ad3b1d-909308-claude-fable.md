All claims in the diff verified against the shipped code (`DecodeTSIB` accepts count 0 at tsib.go:110-118; the driver's mid-loop route publish at driver.go:378-392 matches the pinned [B, A, C] shape; demux at bus.go:299-356 matches every contract statement).

## Findings

**Blockers:** none.

**Should-fix:** none. The `v2`-synonym demux (bus.go:327-332) is correct: existence-gated lookup, first-value-wins on duplicates, empty-value still lands in `default` → drop+count — all consistent with the asyncapi text and pinned by the new test (tsib_test.go:370-376). The mixed-route arrival-order exception is deterministic (single publisher connection, single subject — NATS preserves order), per-vehicle-semantics-neutral as the ADR argues, and pinned by `TestBatchMixedRouteTickArrivalOrder`.

**Nits:**
- engine/natsio/bus.go:297-298 — the rewrapped `onIntent` doc comment leaves a dangling fragment (`// against an old engine` / `// fails loudly instead of misparsing.` split mid-clause). Cosmetic rewrap.
- contracts/asyncapi.yaml:914-916 — `SchemaHeaders` (required `schema_version`) is now referenced only by non-intent messages while `ControlIntent` moved to `SchemaHeadersOptional`; fine, but `SchemaHeadersOptional`'s name suggests general reuse while its descriptions are ctlIntent-specific. A ctlIntent-scoped name (e.g. `IntentHeaders`) would age better.

**Questions:**
- Version discipline: commit 0f8d41d already published asyncapi `2.5.0`, and this diff changes 2.5.0's semantics in place — explicit `intent_encoding: v2` goes from "dropped as unknown" to "accepted synonym", and ctlIntent headers from required to optional. Both are acceptance relaxations aligning schema to shipped wire reality, and the monorepo has no bound external consumers yet, so folding into 2.5.0 is defensible — but it's a judgment call the committer should own explicitly (2.5.1 would be the conservative alternative). The ADR-0026 amendment note is present, satisfying the contract-change rule either way.

Contract/ADR consistency is otherwise clean: asyncapi channel text, `SchemaHeadersOptional`, `ControlIntentBatchHeaders`, ADR-0026 amendment, and the bus comment all state the same demux rule; the KB cross-topic note honestly records (not fixes) the silent-publish-failure pattern in `publishIntent`/`flushBatch`, matching the repo's blockers-only triage bar. No determinism risks introduced: no map iteration in ordered paths (the driver's `fleet` map iteration at driver.go:401 is delete-only), no wall clock or RNG in changed code, and the one wire-order divergence is documented and test-pinned.

REVIEW-COMPLETE
