- **blocker** `scripts/whatif.py:553` — Window validation silently skips saved reports whose per-seed records lack `interval_window`; all existing checked-in reports have this older schema. Thus `--report` continues ranking precisely the legacy measurements this change declares invalid and can rewrite them with an empty window. Fail closed or migrate/derive the window.
- **blocker** `viz/public/quiz.html:122` — The shipped Chicago quiz still declares a winner and labels options `UPGRADE`/`WORSE` from results now explicitly marked superseded due to a truncated interval and fidelity-void uncontrolled coasting. A warning does not make those rankings valid; remove the scenario until re-measured or stop presenting an answer.
- **should-fix** `scripts/chicago/mkquiz.py:430` — Provenance validation permits missing/empty `bands` and `empty_rgb`, producing the undecodable legend-less color map that the fail-closed sidecar requirement claims to prevent.
- **question** `docs/show/diag/chi-loop-urban__setup.png.json:49` — Only the sidecar changed while the PNG did not. Confirm the image was regenerated byte-identically under the new `--end-tick 12000` behavior rather than retroactively annotating a stale artifact.

REVIEW-COMPLETE
