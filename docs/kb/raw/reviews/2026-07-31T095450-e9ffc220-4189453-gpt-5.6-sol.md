- **blocker** — `engine/keyframe.go:320`, `engine/natsio/run.go:103`: pre-v6 `-state-in` restores set `RestoreNotice`, but the primary `RunLive` warm-start path never logs it. Worse, `serve -netfile` exposes no adaptive-routing opt-out, so an old static state silently resumes under adaptive routing despite the warm-start continuation promise. Surface the notice and provide/preserve an explicit static setting.
- **question** — Tests could not run because the review checkout makes `/tmp` read-only.

REVIEW-COMPLETE
