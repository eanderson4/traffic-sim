Review complete. Verified the kernel's assumptions against the engine (`Step` phase order, `boundaries()` reverse-order multi-crossing, `mobil.go` S-preserving adjacent hops, `e.order` filter-preserved append order, spawner scheduling) and against ADR-0014.

**Blocker**

1. `engine/metrics.go:892` — `spawnerBacklog` has a unit error: `float64(tick-st.tick) * st.rate` multiplies a lag in **ticks** by a rate in **veh/s** (`spawn.go:76` sets `rate: r / 3600` veh/s; the spawner's own mean interval is `1/(rate·dt)` ticks, `spawn.go:89`). The implied requested-vehicle count over a lag of L ticks is `L·dt·rate`, so the backlog — and everything downstream: per-tick `DeniedWaitS` accrual (metrics.go:505), horizon `DeniedPending` (metrics.go:558), `Totals` — is inflated by 1/dt = 10× at the validated dt = 0.1. ADR-0014 §3's stated purpose for denied-entry is "so oversaturated comparisons don't lie"; this makes them lie by an order of magnitude. `TestKernelDeniedBacklog` (metrics_test.go:935–962) bakes the same tick-for-second confusion into its expectation ("pending P = H − t0" with r in veh/s), so it validates the bug rather than the physics — fix both together (`b := float64(tick-st.tick) * st.rate * dt`, clamp unchanged; the kernel doc comments at metrics.go:169 and 514 repeat the dt-less formula and need the same correction).

**Should-fix**

— none beyond the blocker.

**Nit**

2. `engine/metrics.go:379-382` — the pure-lateral-hop branch clamps `d = v.S − prev.s` to 0, which silently drops a lap's distance if a ring self-successor wrap and a lateral hop land in the same tick (wrap makes d negative for a reason other than "no movement"). Unreachable today — the only ring net is single-lane — so fine to defer, but worth a comment distinguishing it from the same-lane wrap branch that does handle this.
3. `engine/metrics.go:472-481` — the defensive despawn reconstruction sets `stoppedS = t` when the last observation was stopped but never increments `stops`, so that record can show stopped time with zero episodes. Dead path by construction; consistency-only.
4. `engine/metrics.go:217` — `f` field comment says "desired-speed factor (v0eff on the exit lane)"; `f` is the per-driver factor `Vehicle.F`, an *input* to v0eff, not v0eff itself.

**Question**

5. `Observe` relies on the exactly-once-per-Step convention with no guard — a skipped or doubled call mis-books despawns/crossings silently. `NewKernel` fences attach-time strictly; a one-line `lastTick` check would extend the same loud-failure posture to the per-tick pairing. Plausibly "too early to need this" under the triage bar — committer's call.

**Checked and clean:** determinism (all map emissions sorted — `gone` IDs, Finalize trip IDs, `Totals` lane keys, drain sorts on unique keys; float folds ride fixed `e.Vehicles()`/origin/queue/`Successors` orders; BFS parent maps are membership-only); CRC non-interference (kernel never writes engine state; `TestKernelCRCInvariance` covers it); distance conservation across crossings matches `boundaries()` semantics (`S -= lane.Length`, reverse-order reprocessing → multi-boundary chains are real and handled); no NATS subject/payload changes in this diff (kernel is transport-agnostic per ADR-0014 §6 — the JSON/wire layer is not staged here); the `Vehicles()` doc correction is accurate (pending vehicles hold pre-assigned IDs, so append order ≠ ID order; despawn filtering preserves order); interval bounds/partial semantics match ADR-0014 §3 including the LastTick streaming close.

REVIEW-COMPLETE
