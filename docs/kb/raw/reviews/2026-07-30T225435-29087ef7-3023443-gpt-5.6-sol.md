- **blocker** `docs/kb/decisions/ADR-0036-congestion-adaptive-routing.md:6` misclassifies TSKF v6 as “no payload-schema change.” `engine/keyframe.go:106` changes the public `ts.{run}.log.keyframe` binary payload, while the contract source of truth still specifies only v2/v3 at `contracts/asyncapi.yaml:914` and `contracts/asyncapi.yaml:1284`. Update the contract and provide the actual keyframe-reader migration note.
- **should-fix** `engine/routing.go:319` tests hysteresis using the raw Dijkstra cost, but `engine/routing.go:323` serves a mixed static/adaptive chain. If a downstream adaptive hop was rejected, the installed route can be substantially costlier than both `dist[i]` and the static path, violating the stated “clearly better” rule. Compare the cost of the candidate’s actual mixed chain before accepting it.
- **should-fix** `engine/network.go:64` and `engine/vehicle.go:110` add trajectory-determining state that remains absent from `computeCRC` at `engine/engine.go:991`. A replay can diverge in EMA, frozen weights, or dwell clocks while reporting matching CRCs until—or unless—a later route decision exposes it, weakening ADR-0005’s replay verification claim.

REVIEW-COMPLETE
