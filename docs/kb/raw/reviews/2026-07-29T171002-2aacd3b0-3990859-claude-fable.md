Reviewed the full staged diff (4,574 insertions across warm start ADR-0029 ph1, gridlock escape ADR-0034, permissive green ADR-0031/0033, routed lookahead ADR-0032, keyframe v5) plus surrounding repo context. Determinism sweep (map iteration, float accumulation, wall clock, RNG) came up clean: `strandStuck` sweeps `e.order`, `resetStuckBehind` iterates slices with a map used only for membership, fingerprints hash in index order over IEEE-754 bits, serve's gridlock print sorts before slicing, and `gone` is sorted after map collection. Contract changes (keyframe v5, `RunMeta.StartedTick`, additive `stranded_trips` metrics fields) are all covered by ADR-0029/0034 including a record-plane migration note; `SchemaVersion` correctly untouched. `Registry.Start`'s two callers are both updated. `k.last` is rebuilt from `cur` each Observe, so stranded vehicles leave the denominator correctly; per-tick time loss is accumulated at metrics.go:558, so the stranded-in-denominator change is consistent. The v5 writer/reader queue-count gate is symmetric and pinned by a round-trip test. I could not run build/vet (command approval unavailable in this session); review is static.

**Findings**

**Should-fix**

1. `engine/gridlock.go:160-172` — the "a red light is deliberately NOT a trigger" claim (repeated in ADR-0034) is not what `jammedAtJunction` enforces. It never consults the ego's own signal; it only requires `next.Internal && boxBlocked`. A head held at red whose box is blocked by spillback (no room on the far side — an ordinary oversaturated urban condition, common on exactly the Chicago network this targets) strands after 300 s even if its light was red the whole window. `TestEscapeIgnoresOrdinarySignalQueue` only covers red with a clear box, so the gap is untested. Either add a `sigState == SigRed → not jammed` condition or weaken the comment/ADR claim to "a red light alone is never a trigger."

2. `docs/kb/INDEX.md:113` — the ADR-0029 entry is stale relative to this same commit: it says PROPOSED and describes only the unimplemented lane-id-format design (`format v4`, `initial_state:` scenario key), while the ADR is "Accepted for phase 1" and phase 1 shipped here as a sidecar-guarded state file plus `-state-in/-state-out` flags. The footer at line 137 repeats "ADR-0029 … PROPOSED". Per rule 2/4 the index should reflect what shipped.

**Nit**

3. `engine/natsio/demand/director.go:277` — `fastForward` doesn't apply onSnapshot's `at > d.endS` exhaustion cut, so a StartTick past a flow's end draws arrivals the cold run never drew (sampler ordinal and skip-count diverge after end-of-window). No verbs differ — every such flow is exhausted before sending either way — and the path is unreachable in production (both entry points refuse warm start with demand), but the "bit-identical to the cold program" claim is slightly overstated at that edge.

4. `engine/gridlock.go:176,195` — the strand lane itself consumes the first BFS level, so the upstream reach is `maxLaneHops-1` lanes of Prevs, not the "up to maxLaneHops" the comment states. Off-by-one in documentation only.

5. `engine/natsio/run.go:120-131` — a library caller setting `StateDumpPath` and leaving `StateDumpTick` at its zero value gets a silent tick-0 dump (serve distinguishes "unset" via `flag.Visit`; the ContractConfig API cannot). Harmless today since serve is the only production caller, but worth an explicit doc line on `StateDumpTick`.

**Question**

6. `engine/cmd/serve/main.go:245` — the `-driver` refusal guard uses `*withDriver && *drivers > 0` while the attach path (main.go:505-508) exits on `-drivers < 1` independently, so `-state-in -driver -drivers 0` is rejected by the later check with an unrelated message rather than the warm-start explanation. Both refuse, so no hole — just confirming the double-path is intended.

No blockers. The two headline properties are well pinned: warm start is CRC-verified bit-exact end-to-end (engine and RunLive levels), and every refusal in `validateStateFlags` traces to a real divergence mechanism with a test naming it. The keyframe v5 empty-queue writer/reader fix and the stopDone/stuckTicks keyframing are correct and each carries a regression test.

REVIEW-COMPLETE
