- **blocker** `docs/kb/decisions/ADR-0036-congestion-adaptive-routing.md:224` — The same `(content-hash, seed)` now identifies both static and adaptive trajectories, directly violating ADR-0012 §6. This breaks existing hash-only bindings: `engine/cmd/demosrv/replay.go:62` will accept an old static recording against the now-adaptive scenario, and offline metrics use the hash as their only identity. Acknowledging the collision in an ADR does not fix consumers; hash resolved routing mode or version the identity contract.
- **should-fix** `engine/cmd/simrun/main.go:19` — Unlike `serve`, the canonical bring-up harness has no adaptive-routing opt-out. After the default flip, builtin and `-netfile` static baselines cannot be reproduced through `simrun`; add a symmetric flag while keeping scenario-owned values protected.

REVIEW-COMPLETE
