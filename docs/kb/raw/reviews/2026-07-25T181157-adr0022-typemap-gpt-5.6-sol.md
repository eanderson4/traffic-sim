- **blocker** — `docs/kb/decisions/ADR-0022-us-urban-speed-typemap.md:62`, `docs/kb/decisions/ADR-0022-us-urban-speed-typemap.md:111`, `scripts/osm-urban-us.typ.xml:36`: 13.41 m/s crosses netconvert’s default 13.6111 m/s `right-before-left` threshold, so this *does* change right-of-way. The A/B has 1,218 external junctions changing `priority` → `right_before_left`, 781 connection-state changes, and lane-level connectivity changes; see one concrete transition at `data/networks/chi-loop/loop.net.xml:140298` versus `data/networks/chi-loop-urban/loop-urban.net.xml:140089`. The “fewer waiting positions” explanation is incomplete/post-hoc, and the claims that topology/right-of-way are untouched must not ship. SUMO documents this speed-dependent junction threshold. 
- **should-fix** — `scripts/import-city.sh:90`: `TYPEMAP=` hashes no typemap at all, although netconvert’s implicit stock typemap remains an output-shaping input. Replacing/repackaging that file under the same reported netconvert version leaves `REPO_REV` unchanged. Hash the resolved stock file too, or explicitly pin and verify its digest.
- **should-fix** — `docs/kb/decisions/ADR-0022-us-urban-speed-typemap.md:80`, `scripts/import-city.sh:31`: “region-scoped and opt-in” contradicts the implementation, which silently defaults every invocation to the US map. This is especially weak while eight listed California networks knowingly receive the wrong 30 mph value. Require an explicit region/typemap choice or add the California map now. The four-state 30 mph claim itself is defensible; California’s applicable default is 25 mph. 
- **nit** — `docs/kb/INDEX.md:53`: the ADR table now includes ADR-0022 but still says `ADR-0001..0021`.
- **verified** — `scripts/osm-urban-us.typ.xml:33`: all 38 stock types are present and every non-`speed` attribute matches both installed stock copies exactly. `TYPEMAP` unset/empty semantics, quoting, `set -u`, and empty-array expansion are correct.
- **verified** — `scripts/import-city.sh:57`: omitting `--type-files` from pass two is correct; SUMO reimport preserves existing network types/connections by default, and the resulting urban file retains the pass-one speeds. 

REVIEW-COMPLETE
