- **blocker** — `docs/show/baselines.json:35`: The new CBD replay is a 7,000-tick fidelity-clean run, while the quiz results are measured over 12,000 ticks (`docs/show/quiz/chi-loop-cbd.json:6`), where demand loss begins and differs by arm. This violates the file’s explicit “SAME baseline” invariant (`docs/show/baselines.json:9`) and shows guests a materially different operating regime from the scored experiment. The build only verifies file existence (`scripts/chicago/mkquiz.py:554`), so it ships silently.

REVIEW-COMPLETE
