Review of the staged diff (glitch clamp in `viz/src/artic.ts` + 4 tests). Viz-only, no NATS surface touched, no engine determinism exposure — the findings below are all about the clamp's own semantics.

**Blockers:** none.

**Should-fix**

- `viz/src/artic.ts:80` — The pre-integration check compares the *new* `thetaFront` against a trailer angle that is `dtS` old, so for a legitimately large sim delta the measured "hitch angle" includes the heading swept during that delta, not just the articulation. Concrete misfire: tight turn at steady ~60° articulation (which your own test at `viz/test/artic.test.ts:152` declares legitimate), 8× replay on a slow-rendering frame handing `dtS ≈ 0.5–2 s`. With sin gap = ωL/v ⇒ ω ≈ 41°/s at v=10, a 0.5 s frame gives 60° + ~20° swept ≈ 80° > 75° → the trailer snaps aligned mid-turn, and can re-trip every frame while the turn lasts — trailer visually glued to the tractor exactly in the fast-replay case the substep machinery was built for. The window is real but narrow (needs tight turn + dt ≳ 0.4 s; at 2 s the old re-anchor fires anyway), and test `artic.test.ts:170` deliberately bakes in this tradeoff (80° + 0.8 s ⇒ snap), so if the committer judges the artifact acceptable at this stage, defer with a note — but the test's framing ("post-integration clamping would land near 50° and KEEP the wrong pose") is only half true: a kept 50° pose decays to legitimate articulation within ~1 s of sim time, whereas this false-positive snap discards a *correct* pose. An option that keeps both tests honest: widen the threshold by the heading the tractor could legitimately sweep in `dtS` (e.g. `MAX_HITCH_RAD + (v/TRAILER_M)·dtS` capped), or apply the strict 75° check only when `dtS ≤ MAX_DT_S·k`.

**Question**

- `viz/src/artic.ts:80` vs the hold path: a truck creeping through a turn below `HOLD_V` (0.05 m/s stop-and-go on a curve) holds `tr` while `thetaFront` rotates; once the frozen gap crosses 75° the trailer now pops to aligned mid-creep. Previously it just kept the stale angle. The snap is arguably the better artifact (that's the stated point of clamping the hold path), but it converts a static wrongness into a visible discontinuity — intended?

**Nits**

- `viz/src/artic.ts:44` — the `// °` unit comment on a constant that holds radians (name says `RAD`, initializer converts from degrees) reads wrong at a glance.
- `viz/test/artic.test.ts:157` — `omega` here depends on `TRAILER_M`; if the theme dimension ever changes, the test still self-consistently targets 60° (good), worth nothing further — no action.

**Contract/ADR:** no subject or payload changes; no ADR needed. **Determinism:** client-side viz only; the `Map` usage is order-independent and there's no wall-clock or RNG in the diff.

REVIEW-COMPLETE
