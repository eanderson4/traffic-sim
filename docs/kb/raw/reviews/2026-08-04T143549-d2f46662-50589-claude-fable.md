Diff is viz-only (`viz/src/splash.ts`): adds a baked-first CTA path behind a hard-coded `BAKED_FEATURED_URL` constant, currently empty, so the shipped behavior is unchanged until the URL is filled in. No NATS subject/payload or engine changes — no contract or determinism surface touched. ADR-0023 exists and the `?bake=<index.json URL>` shape in the comment matches it.

**Blockers:** none.

**Should-fix**

- `viz/src/splash.ts:126-148` — baked mode doesn't gate `main()`'s live-state logic. With `BAKED_FEATURED_URL` set, the registry/status fetches still run and can set the `unregistered` notice ("The featured demo isn't in this deployment's registry — browse the demos page instead," lines 138-141), which directly contradicts the CTA now reading "watch the replay →" — the replay works regardless of the registry. Likewise the `running` class/badge (lines 72-74) can light up "live" next to a replay CTA. Guard the notice (and arguably the whole fetch block) on `BAKED_FEATURED_URL === ""` so the baked card is fully static, as the header comment claims.

**Nits**

- `viz/src/splash.ts:75-83` / `92-95` — with `const BAKED_FEATURED_URL = ""`, TS narrows the type to the literal `""`, so both `!== ""` branches are provably dead code today; lint rules like `no-unnecessary-condition` will flag it. Annotating `: string` sidesteps that.
- Baked mode still issues two dead fetches per page load (`/api/demos`, `/api/status`); harmless but subsumed by the should-fix.

**Questions**

- Publishing a bake requires editing this constant, rebuilding, and a fresh review round of a viz source. Was a data-driven source (registry field, build-time env, or the existing `?bake=` query mechanism from ADR-0023) considered, or is one-constant-per-publish the intended cadence for now? "Too early" is a fine answer per the triage bar — just confirming it's a choice.

REVIEW-COMPLETE
