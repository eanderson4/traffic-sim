- **blocker** — `engine/BENCHMARKS.md:211`, `engine/natsio/driver_test.go:1002`: “batch completes no later at any boundary schedule” is not structural. Production batch responses may contain standalone v2 route updates and may split at `TSIBMaxRecords`; even a single TSIB is encoded and published after the loop, not simultaneously with the v2 tail. The 400-vehicle, post-routing test exercises neither exception, so it cannot establish ADR-0026’s general applied-lag target. Narrow the claim or measure route-update/cap-split workloads.
- **should-fix** — `engine/natsio/driver_test.go:1013`, `engine/natsio/driver_test.go:1230`: the loop is not fixed-cadence or identical across modes. Sleeping `pace` after variable work makes each boundary interval `work duration + pace`; batch and v2 therefore receive different schedules. Exact cross-leg percentile acceptance can reflect processing/scheduler differences. Anchor boundaries to absolute deadlines if identical cadence is load-bearing.

REVIEW-COMPLETE
