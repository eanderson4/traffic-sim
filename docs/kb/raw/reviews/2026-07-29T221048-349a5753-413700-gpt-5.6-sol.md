- **blocker** `engine/natsio/tslb.go:96` — `count` is trusted as the slice capacity before payload-length validation. A corrupt 18-byte frame with `count=0xffffffff` attempts a multi-hundred-GB allocation and can terminate replay via OOM instead of returning an error. Bound count by the remaining bytes/minimum record size before allocation.
- **should-fix** `engine/natsio/logcursor.go:81` — the bounded-memory player groups by the NATS `tick` header but `DecodeTSLB` only verifies records against the TSLB payload tick. A mismatched outer header applies the entire batch at the wrong tick. Compare the decoded tick with `r.tick` and fail the record.
- **should-fix** `contracts/asyncapi.yaml:51` — the contract repeatedly promises one `log.intents` message per tick, but `engine/natsio/recorder.go:373` emits no message for an empty-intent tick. Document this as one message per non-empty tick, or emit valid zero-count batches.
- **nit** `engine/natsio/recorder.go:128` — `IntentBatchMax` is not validated like `KeyframeChunkMax`; zero defaults, but negative or undersized explicit values silently produce messages exceeding the configured “bound.”

REVIEW-COMPLETE
