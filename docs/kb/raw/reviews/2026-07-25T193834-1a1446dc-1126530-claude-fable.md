All checks done. Findings below.

---

## Review — staged diff (5 substantive files + review archives)

Substantive content: `README.md`, `deploy/README.md`, `deploy/k8s/cronjob.yaml`, `docs/kb/INDEX.md`, new `docs/kb/decisions/ADR-0023-baked-replay-pipeline.md` (603 lines, PROPOSED). The remaining ~60k lines are review-round archives under `docs/kb/raw/reviews/` (generated content, not reviewed line-by-line).

**Verified against the repo** (all check out): TSSF 24 B header/24 B-vehicle at `engine/natsio/frame.go:39-41`; `SnapshotBuffer.bufferMs` readonly + duplicate/regressive-tick drop in `push` at `viz/src/snapshots.ts:47,73-76`; `Engine.Vehicles()` returns `e.order` (deterministic slice, not a map) at `engine/engine.go:234`; 0–1.5 ratio clamp at `viz/src/congestion.ts:108`; `-pace` default 1 at `engine/cmd/serve/main.go:58`; ADR-0020's two go-live preconditions (ws auth + `/healthz`, ADR-0020:115-121) match the README claim. ADR-0023's arithmetic is internally consistent (20 B TSRB/TSRL headers, 1801 frames at stride 5 over 9000 ticks, 429,000 km u32 span at 0.1 m, size-budget tables, 12 TSRL frames/60 s chunk, 625 ms buffer at 1×).

### Should-fix

1. **`docs/kb/INDEX.md:105-106` — staged registry skips ADR-0021 and ADR-0022.** Both exist in `docs/kb/decisions/` (ratified per the review archives this same diff adds), but the staged INDEX jumps from ADR-0020 to ADR-0023. The fix already exists as an *unstaged* hunk in your working tree — stage it; INDEX.md is docs-exempt from the gate, so this costs no re-review.

### Nit

2. **ADR-0023 item 2 (~line 199) — broken sentence splice.** "…in ticks it is `120 × bakeEveryTicks`). — the top of the agreed 1–2 Hz band, because…" — the cadence-derivation insertion severed the original "Bake rate 2 Hz … — the top of the agreed band" sentence. It's a normative design doc; the fragment doesn't parse.

3. **ADR-0023 item 4 — `lanes.json` build order unspecified.** Scan order and pair emission are pinned ("never a Go map", sorted by `lane_idx`), but the deduped lane-id table that *defines* `lane_idx` has no stated ordering. If it's built from a map, same inputs → different bytes → different hash12 per rebake. The output-digest rule (item 8b) keeps URLs immutable, but bake reproducibility silently dies. One clause ("first-occurrence order in the deterministic scan" or "sorted") closes it.

4. **ADR-0023 items 1/5 — region path naming inconsistent.** Item 1's layout template says `frames/{region}/…` with region key `z11/{x}/{y}` (item 3), but the item 5 example uses flattened `frames/z11-352-819/…`. Pick one (the flattened form is presumably intended — slashes in the key would nest R2 prefixes).

### Question

5. **ADR-0023 item 2 — fixed `laneEveryFrames = 10` vs the "0.2 Hz" claim.** At a dt whose derived bake rate is 1 Hz (e.g. dt 0.5 → stride 1), 10 baked frames = 10 s → 0.1 Hz aggregates, not 0.2 Hz. The index carries the actual value so nothing breaks; just confirm the constant-frames (vs constant-seconds) choice is intended.

**No blockers.** No NATS subject/payload changes (`contracts/asyncapi.yaml` untouched by this diff; TSRB/TSRL/index.json are declared engine↔viz artifacts under ADR-0006's co-migration rule with a contracts doc promised at implementation — consistent with the repo's contract discipline). The cronjob change is comment-only and its `-pace 1` / 360k-tick arithmetic is correct. The `deploy/README.md` tag recipe (`head -c2 /dev/urandom | od -An -tx1 | tr -d ' '`) produces a valid 4-hex-char suffix; command substitution strips the trailing newline.

REVIEW-COMPLETE
