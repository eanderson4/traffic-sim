- **should-fix** `contracts/asyncapi.yaml:465` — `answerSignalTableRequest` is titled as an engine→client reply but declares `action: receive` on a channel containing both request and reply messages. The contract therefore does not accurately model which message the engine receives versus sends. Define the receive operation for `SignalTableRequest` and explicitly document/model the dynamic-inbox send reply.

REVIEW-COMPLETE
