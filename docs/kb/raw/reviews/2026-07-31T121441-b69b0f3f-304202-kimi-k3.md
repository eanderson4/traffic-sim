• I've read the full diff. Now checking the repo context I need: `signal.go` internals, references to the deleted `docs/architecture.md`, and the clearance semantics.

• One more context check — `compileTicks` zero-cycle handling and the keyframe restore path in the tree:

• Two more targeted checks: remaining direct `phaseAt(` callers that bypass the new derivation point, and the baked/viz signal-state encoders.

• Review complete. Findings below, ranked.

  ## Blockers

  None. The determinism discipline holds throughout: `sigOv` is lookup-only with every per-tick use walking `Net.Signals` in network order (`engine/sigctl.go:2029`, `engine/engine.go:1099-1110`, `engine/keyframe.go:965-976`); the CRC fold and TSKF v7 gating share the same non-empty-table condition so live/replay/restore agree; no wall clock or RNG enters the kernel; the lapse is a pure function of keyframed `until`. Contract shape is properly additive (same subject, omitted-when-spawn discriminator, schema_version stays 2, asyncapi 2.7.0) with the ADR-0006 addendum and ADR-0037 status flip landing in the same commit per the "message contracts are sacred" rule. The v7 flags-word design (presence of the v6 lane section can no longer ride the version number) is correctly mirrored between writer and reader (`keyframe.go:925-930` vs `hasLaneSection`, `keyframe.go:992-993`), and the clearance-retention math (`stepSignalOverrides`, `sigctl.go:2050`) exactly covers `sigInClearance`'s onset−1 lookback — boundary verified, not just asserted.

  ## Should-fix

  - `contracts/asyncapi.yaml:1470-1472` — `LoggedVerbView` now contains a **duplicate `tick` key** (the applied_tick property at :1457 and a second `tick` "at attach" at :1471), plus stray `run:` and `reason:` properties that belong to a reply schema. These are pre-existing context lines, not added by this diff — but this commit rewrites the schema's `required` list and adds six fields to it, and a strict YAML parser rejects duplicate mapping keys (lenient ones silently shadow the applied_tick description). The contract doc is the source of truth for the record plane; fix while the schema is open.

  ## Questions

  - `docs/architecture.md` (deleted, 243 lines) — the deleted blob's header already reads "Rendered from the code as of 2026-07-31 … `contracts/asyncapi.yaml` v2.7.0", i.e., it was regenerated for *this* milestone and deleted in the same commit. Grep finds no dangling references to it, so it's link-clean — but is the deletion intentional, and if so, why regenerate-then-delete?
  - Commanded green→red snaps insert no amber interphase: `sigPhaseAt` returns the commanded phase from `since`, so `sigInClearance` is false (prev fixed-time phase wasn't amber) and `sigGate` treats the commanded red as *stale red* from the first tick — approaching vehicles in the comfort band get `emergencyDecel` walls instead of the amber-commitment path (`engine/signal.go:328-331`). Deterministic, enforcement-untouched, and the fixture pins zero comfort-criterion violations, so this may be exactly the intended milestone-1 semantic — but the ADR-0037 addendum doesn't state it, and real controllers insert clearance on a phase snap. Worth one sentence in the ADR either way.

  ## Nits

  - `engine/natsio/logcursor.go:96-108` and `engine/natsio/replay.go` (three call sites): the spawn branch unmarshals the payload twice — `decodeLoggedVerbKind` then `decodeLoggedVerb` on the same bytes. `lv` already carries every spawn field; build the `TickedSpawn` from it.
  - `engine/sigctl.go:1964-1970` — `hold` can't be 0 (compileTicks rejects sub-tick phases, so `p.cycle ≥ 1`), which means the keyframe reader's `until <= since` guard (`keyframe.go:1073-1075`) is unreachable for writer-produced payloads. Fine as defense-in-depth; just noting the invariant is implicit, not documented at the writer.

  REVIEW-COMPLETE

