- **blocker** — `scripts/show/build-quiz.sh:146`: This publishes the CBD and Kennedy A/B tables, but their page caveats (`scripts/chicago/mkquiz.py:85` and `scripts/chicago/mkquiz.py:90`) omit the known controller-fidelity failure. The repo states all three Chicago tables exceed the fidelity bar and their absolute speeds are not valid measurements (`docs/show/README.md:350`). Only the existing full-network card discloses this. Normal presentation therefore shows the two new result sets as trustworthy without the project’s required warning.

REVIEW-COMPLETE
