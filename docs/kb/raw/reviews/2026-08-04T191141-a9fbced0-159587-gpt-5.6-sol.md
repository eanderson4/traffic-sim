- **blocker** — `engine/natsio/sigctl/sigctl.go:348`: Candidate demand includes links already green in the enforced phase. Common overlapping phases therefore turn already-served traffic into a “conflicting call,” forcing unnecessary clearance walks after the max-green timer. Score only movements not served by the current phase.
- **blocker** — `engine/natsio/sigctl/sigctl.go:772`: Each vehicle increments every overlapping detector circle. Stop-line detectors are frequently colocated; in the shipped 4-way fixture, one position matches 4–7 links, sometimes across phase groups. This creates phantom calls and invalidates gap-out decisions. TSSF position alone needs exclusive approach/link assignment rather than mark-all.
- **blocker** — `engine/natsio/sigctl/sigctl.go:306`: Seamless transition walking assumes every verb applies at snapshot tick + 1. The contract explicitly does not guarantee that, and the bracket runs unpaced. Backlogged snapshots can issue multiple walk steps before one control drain; the kernel then applies them at one boundary and zeroes the intermediate transition, recreating the free-clearance bug this change claims to fix.
- **blocker** — `engine/cmd/serve/main.go:567`: `-sigctl` is allowed with `-state-in` when `-driver=false`, but the controller’s `myPhase`, call age, walk target, and command history are neither keyframed nor recoverable from the live plane. A warm continuation therefore diverges from the uninterrupted run, violating ADR-0029’s bit-exact promise. Refuse this combination or persist/restore controller state.
- **should-fix** — `engine/natsio/sigctl/sigctl.go:663`: `Ready` succeeds after any complete table, even when a nonempty table installs zero controllable programs because IDs or geometry do not match. This opens the start gate for a silent fixed-time run despite the documented “attached means controlling” guarantee.
- **should-fix** — `scripts/sigctl-bracket.py:54`: The fidelity gate treats an accepted reply as proof of applied actuation. Acceptance precedes enforcement; same-boundary supersedes and rail clamps can produce zero effective holds. The bracket can therefore report a nominal actuated arm without proving any commanded phase governed a tick.
- **should-fix** — `docs/kb/decisions/ADR-0037-runtime-signal-control.md:3`: The ADR status still says the reference controller “remains milestone 2,” while the appended addendum and KB index say milestone 2 is implemented. Update the status/title to keep the decision record internally consistent.

REVIEW-COMPLETE
