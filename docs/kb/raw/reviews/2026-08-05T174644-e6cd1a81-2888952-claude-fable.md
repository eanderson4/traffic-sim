Review of the staged diff: flush-on-construct in `NewBus`/`NewPublishBus`/`NewContract`, `RunLive` reordered so `reg.Start` comes last in construction (now after `LogStart`, signal-table publish, and `dumpState`), the KB "(b) FIXED" update, and four archived review rounds (docs-plane).

**Blockers: none.** The fix is correct and this revision answers both prior rounds' blockers. `Flush()` PING/PONGs after the queued SUBs, so constructor return implies server-side SUB registration — the right primitive on a single embedded server (ADR-0004 local-first; clustered interest-propagation caveats don't apply). Moving `reg.Start` (a puback-awaited JetStream KV `Put`, registry.go:112) after the contract flush closes the registry-first window (round-3 sol blocker), and moving it after `LogStart`/`PublishSignals`/`dumpState` closes the StatusRunning-before-keyframe window (round-4 sol blocker — that round reviewed an earlier revision of this change; the staged diff supersedes it). After `reg.Start`, the first `ProcessControl` is immediate (start-gate loop at run.go:220 or main loop at run.go:233), so a hello that queues during construction is serviced right away — responders exist and the enqueue-then-reply model (contract.go:354–366, 481) means no `no responders` is reachable from registry visibility. Error paths unwind: `Bus.Close`/`Contract.Close` are nil-safe (bus.go:600–607, contract.go:402–406), the only non-test caller guards `lr != nil` (cmd/serve/main.go:508, 520), and every test call site either fatals on error or discards `lr`.

**should-fix** — `docs/kb/articles/cross-topic-concerns.md` (diff line 10): the KB line records the fix but not the deliberate abort-path change riding along: construction failures (recorder/bus/contract/LogStart) now return before `reg.Start`, so a failed run leaves **no registry trace at all**, where it previously appeared as `StatusAborted` with the error string — registry watchers/tooling that listed aborted runs will no longer see these. run.go:74–76 documents it, but the prior round (archived in this same diff) asked for a commit-message-or-KB note and the KB line doesn't carry it. Also, the line claims only "AFTER bus and contract are constructed" while the code now guarantees more (keyframe + signal table out too — exactly the delta that closed round 4's blocker); make the reference line exact. Both are docs-plane edits — ungated, no stamp invalidation.

**question** — "registry visibility implies the run is addressable" has been promoted to a readiness guarantee that the test harness (contract_test.go:56) and, soon, viz depend on. Reverting the ordering later would be a breaking change to metadata-plane semantics without any wire change to flag it. A one-line addendum on the ADR owning the registry lifecycle would make it a promise per rule 5's spirit. Deferable under the triage addendum; raised and archived in round 4 already — recording that it remains open.

**nit** — bus.go:243 + bus.go:295: live-run construction flushes twice (`NewPublishBus`, then `NewBus` after the intent sub) — two sync RTTs where one covers both. Harmless on an embedded server; already triaged in the archived round.

**nit** — contract.go:373–387: bind failures return without `c.Close()`, leaking already-bound subs, while the new flush path does close — asymmetry the new code makes visible. Pre-existing, unreachable in normal operation (subscribe on a live conn), connection teardown cleans up.

**nit** — run.go:205: if `reg.Start` itself fails, the recorder's stream, tick-0 keyframe, and a possible state dump are orphaned with no registry entry pointing at them. Operational footnote; archived in round 4.

**Clean checks.** Determinism: all changes are construction-time, outside the tick loop; no map iteration in sampled paths, no float accumulation, no RNG; `time.Now()` in `Registry.Start` is pre-existing metadata-plane wall clock, never sim state. Contract: no subjects or payloads change; `nats.go` stays confined to `engine/natsio/` (ADR-0006). Archives: four rounds committed as brief/diffstat/model outputs, no `*-reviewed.patch`, per the ADR-0013 2026-08-04 addendum; the KB caution about client-side vs engine-side per-test flushes is accurate and worth keeping.

REVIEW-COMPLETE
