All context gathered. Findings:

---

**Review of staged diff** — flush-on-construct in `NewBus`/`NewPublishBus`/`NewContract`, `reg.Start` moved after construction in `RunLive`, KB update, plus three archived review rounds (docs-plane).

**Blockers:** none. The fix is correct: `Flush()` PING/PONGs after the queued SUBs, so constructor return implies server-side registration — the right primitive on a single embedded server (ADR-0004 local-first; clustered interest-propagation caveats don't apply). `reg.Start` is a JetStream KV `Put` (ack-awaited) issued *after* the contract flush, so the KB's new claim — registry visibility implies responders exist — actually holds now; the prior round's blocker (registry-first window) is closed by this diff. Error paths unwind correctly: `Bus.Close`/`Contract.Close` are nil-safe and unsubscribe everything bound so far (bus.go:600–607, contract.go:402–406), and `cmd/serve/main.go:508` guards `lr != nil`, so the new `nil, err` returns are safe for the only non-test caller.

**should-fix** — `engine/natsio/run.go:67-71`: the function's own doc comment now contradicts the code. "registry → recorder → bus → contract → loop" is the old order, and "On recorder failure the run aborts loudly: the error is returned and the registry is marked aborted" is no longer true — recorder/bus/contract failures now return *before* `reg.Start`, leaving no registry trace at all. That abort-path change is deliberate (and defensible: never advertise a run whose responders don't exist), but the diff changed the behavior without updating the sentence documenting it two lines above. Fix the comment; note the abort-path consequence (failed constructions are now invisible to registry watchers — only the caller sees the error) in the commit message or KB.

**question** — "registry visibility implies responders exist" has just been promoted from incidental ordering to a readiness guarantee that clients demonstrably rely on (the test harness treats meta visibility as run-up; viz will too). The registry is the metadata plane other services read — once consumers depend on this ordering, reverting it is a breaking change. The KB records it; per rule 5's spirit, a one-line addendum on ADR-0006/0008 (whichever owns the registry lifecycle) would make it a promise rather than an implementation detail. Defensible to defer.

**nit** — `engine/natsio/bus.go:243` + `bus.go:295`: live-run construction flushes twice (once in `NewPublishBus`, again in `NewBus` after the intent sub). Two sync RTTs where a restructure could pay one; harmless on an embedded server, not worth the layering churn (flagged and triaged in the archived round — noting for completeness).

**nit** — `engine/natsio/run.go:183`: if `reg.Start` itself fails, the recorder's just-created JetStream stream is orphaned with no registry entry pointing at it (previously `Start` ran before stream creation, so this pairing couldn't occur). Operational footnote only.

**Clean checks:** determinism — flushes are construction-time, outside the tick loop; no map iteration in sampled paths, no float accumulation, no RNG or wall-clock touching simulated state. Contract — no subjects or payloads change; wire untouched; `nats.go` stays confined to `engine/natsio/` (ADR-0006). KB — the "(b) FIXED 2026-08-05" claim is accurate *with* the `RunLive` reorder in this same commit, and the caution about client-side vs engine-side per-test flushes is correct. Archives — reviews committed without `*-reviewed.patch`, consistent with the ADR-0013 2026-08-04 addendum.

REVIEW-COMPLETE
