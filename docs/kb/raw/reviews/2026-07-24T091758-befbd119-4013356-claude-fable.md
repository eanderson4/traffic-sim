Review of the staged diff (viz-only: replay panel in-flight poll guard + surfacing `verbErrors` in the warn line). No blockers.

**Should-fix**

- `viz/src/replaypanel.ts:438-444` — The warn-line composition (`divergence N · verb errors M`, and the show/hide decision) now lives in `ReplayPanel.update()`, the DOM shell, contradicting the file's own split convention (`replaypanel.ts:29-31`: "the DOM-free half … carries every state transition so node --test can drive it"). The new test (`viz/test/replaypanel.test.ts:96,108`) only covers the raw field passthrough, not the composed text or the both-zero → hidden case. Move it into `viewModel` as e.g. `warnLine: string | null` and assert on that.

**Nit**

- `viz/src/replaypanel.ts:335-356` — The in-flight guard itself is untested. The "skipped tick doesn't count toward the FailGate" claim is the load-bearing part; it's cheap to test with a stubbed `refresh` that resolves on demand.
- `viz/src/replaypanel.ts:340` — Trade-off worth noting in the commit message: against a backend that *hangs* (3 s abort) rather than fast-fails, each failed poll now consumes ~3 ticks, so the 3-streak hide takes ~9 s instead of ~3 s. Consistent with the "keep the panel on transient trouble" philosophy, so fine — just intended, not accidental.

**Question**

- `viz/src/replaypanel.ts:343` vs `:195-209` — The guard serializes poll-vs-poll, but a poll response can still land after a control-verb POST's fresher status (`refresh()` and `ctl()` both assign `this.status` unconditionally), briefly reverting the toggle/speed UI until the next poll. Pre-existing and self-correcting within 1 s — flagging only because the new comment claims stale-overwrite is prevented; it's prevented for polls only. Deliberate scope?

Correctness of the guard itself checks out: `finally` releases the flag on every path (including an `onStatus` callback throw), skipped ticks touch neither `fail()` nor `ok()`, and the warn element is hidden whenever both counters are zero (the stray `⚠ ` text is invisible). `verbErrors` was already parsed defensively (`replaypanel.ts:86`, defaulting to 0), so no payload/contract change — no ADR needed. No determinism exposure (viz-only).

REVIEW-COMPLETE
