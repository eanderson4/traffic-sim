- **blocker** `engine/metrics.go:473` — A vehicle spawned and despawned within one tick never appears in either observation map, so its trip, VMT/VHT, time-loss, and even `DroppedCrossings` are silently omitted. Short imported origin/exit lanes or larger ADR-0005-permitted ticks make this reachable; lifecycle events or an explicit fail-loud check are required.
- **blocker** `engine/metrics.go:563` — Spawner backlog charges wait before detecting and subtracting an injection. When an overdue vehicle injects but the replacement request is already due, the served vehicle incorrectly accrues another `dt` of wait; subtract the changed pending ID before accruing the current tick’s backlog.
- **blocker** `engine/metrics.go:1026` — The staged code pins metric records to `SchemaVersion: 1`, while accepted ADR-0006 pins envelopes to version 2. The independent metrics-version decision and other consequential semantics (whole-run-only attachment and exit/backlog approximations) are absent from the staged ADR diff. Stage the ADR/contract note with this code.
- **should-fix** `engine/engine.go:174` — Append order is not determined by the seed alone: director inputs and their recorded application order also affect spawning. Say deterministic from the complete run inputs/log, matching ADR-0005 replay semantics.

REVIEW-COMPLETE
