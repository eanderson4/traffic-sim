- **should-fix** `engine/cmd/demosrv/main.go:427` — The stable manifest URL is served with `http.ServeFile`/`Last-Modified`, while `viz/src/netload.ts:61` merely requests `no-cache`. A changed generation with an equal/older second-resolution mtime can return 304 and reuse the stale manifest; both part attempts then 404. This breaks ADR-0018’s stale-generation recovery. Use `no-store` or a content-hash ETag.
- **should-fix** `viz/src/nats-client.ts:41` — Reconnect only updates status; it does not issue another signal-table request. The sole pull is at `viz/src/main.ts:863`, while paused replay deliberately never rebroadcasts TSSG (`engine/natsio/player.go:399`). If initial retries expire during a disconnect, reconnecting can stream snapshots forever without signals. Reset the retry budget and pull on reconnect.
- **should-fix** `engine/natsio/bus.go:270` — Snapshot publishes, signal broadcasts, and signal replies share one `pubErrs` counter for their “first three” logs (`engine/natsio/bus.go:312`, `engine/natsio/bus.go:342`). Three failed chunks suppress all later snapshot failure logs, contradicting ADR-0016’s loud logging on both frame types. Keep aggregate stats but use separate rate-limit counters.
- **should-fix** `engine/natsio/bus.go:347` — The reply path ignores `Flush` failure. `PublishMsg` can enqueue successfully while the flush proving delivery fails, leaving the entire resync response silently lost and uncounted despite the ADR’s loud-failure requirement.

REVIEW-COMPLETE
