- **should-fix** `engine/cmd/demosrv/supervisor.go:62` — `wsClientURL()` reuses the server-side dial normalization, converting wildcard listeners to `127.0.0.1`. With `-ws 0.0.0.0:8443`, remote browsers receive `ws://127.0.0.1:8443` and connect to themselves. Separate probe and browser-public URLs.
- **should-fix** `engine/cmd/demosrv/geojson.go:185` — stale part requests call `c.path()` before validating `wantSchema`/`wantHash`. After a scenario edit, the first old-part request can synchronously generate the entire multi-GB replacement before returning the promised 404. Validate generation first, then generate only for a current URL.
- **should-fix** `engine/natsio/bus.go:181` — if the intent subscription fails after `NewPublishBus` installed `sigReq`, `NewBus` returns without unsubscribing it. This leaves a phantom signal-table responder on the connection; call `b.Close()` on the error path.
- **question** `engine/cmd/demosrv/supervisor.go:319` — readiness succeeds when registry metadata appears, before the start gate opens and before any snapshot is published (`engine/natsio/run.go:147`). Is navigation intentionally allowed while the embedded driver may still spend minutes attaching?

REVIEW-COMPLETE
