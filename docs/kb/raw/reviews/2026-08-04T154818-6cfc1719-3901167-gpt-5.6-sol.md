- **blocker** `engine/natsio/recorder.go:48` — The exported `RecorderConfig{}` still enables batching and S2 because both inverse booleans default false. Non-CLI `RunLive` callers therefore retain the exact live-run nondeterminism this change claims to disable; `docs/kb/decisions/ADR-0035-batched-intent-log.md:95` incorrectly says the zero value is the new reproducible behavior.
- **should-fix** `contracts/asyncapi.yaml:447` — The sacred contract still says `logIntents` is the default and associates `logIntent` with `-log-batch=false`, contradicting the newly flipped CLI default. The same `x-nats` block still declares `compression: s2` at `contracts/asyncapi.yaml:466` despite the default stream now being uncompressed.
- **should-fix** `docs/kb/decisions/ADR-0035-batched-intent-log.md:208` — The migration note says recordings written “after this change” carry `log.intents`; unflagged recordings now carry `log.intent`. State the behavior by flag/configuration rather than chronology.

REVIEW-COMPLETE
