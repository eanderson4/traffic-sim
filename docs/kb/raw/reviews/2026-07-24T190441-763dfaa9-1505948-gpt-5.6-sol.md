- **blocker** `engine/natsio/probe.go:54` — The 30-second timestamp tolerance can accept a foreign broker running the same static run ID. `engine/cmd/demosrv/supervisor.go:297` then performs only a non-atomic “child not dead yet” check, so a child losing the bind race can exit immediately after readiness succeeds. The test at `engine/cmd/demosrv/identity_test.go:82` actually demonstrates this false positive by accepting pre-existing metadata from a broker the spawned child does not own. Use a per-spawn nonce or another atomic ownership proof.
- **should-fix** `viz/src/stopsign.ts:79` — Clustering uses junction plus bearing only, with no spatial gate. Complex junctions containing parallel, same-direction approaches—such as frontage or divided-road movements—collapse into one cluster, placing a single sign at their combined centroid, potentially between roads. Apply a distance constraint like signal-head clustering.
- **should-fix** `docs/kb/decisions/ADR-0016-tssg-chunking.md:106` — The consequences claim old clients decode new streams unchanged, contradicting the migration note at line 53: legacy readers install each chunk as a whole table and retain only the last chunk. Correct this contract documentation so compatibility expectations are unambiguous.

REVIEW-COMPLETE
