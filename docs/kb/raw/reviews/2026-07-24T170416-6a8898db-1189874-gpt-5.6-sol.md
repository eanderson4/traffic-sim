- **blocker** `engine/cmd/demosrv/geojson.go:164` introduces chunk manifests, but the staged browser remains the old single-document loader at `viz/src/main.ts:125`. It reads the manifest’s intentionally empty `features` array and renders no roads. The required `viz/src/netload.ts` and `viz/src/main.ts` integration are unstaged, so they will not ship.

- **blocker** `engine/cmd/demosrv/supervisor.go:286` treats any pre-existing `{run}/meta` as proof that the spawned child owns the broker. A foreign demosrv running the same registered demo already has that key, so readiness succeeds immediately while the new child later fails to bind. `engine/cmd/demosrv/identity_test.go:84` explicitly pre-populates the key before spawning, codifying this false positive. Readiness needs a per-launch identity/nonce, not the stable run ID alone.

- **should-fix** `engine/cmd/demosrv/geojson.go:151` publishes part URLs containing only the registry ID, while `engine/cmd/demosrv/geojson.go:186` recomputes the scenario’s current hash for every part request. Editing a scenario between manifest and part fetches can mix generations or return 404s. Include schema/hash identity in part URLs and validate it in the handler.

- **should-fix** `engine/cmd/demosrv/registry.go:66` adds `ws` specifically for menu deep links, but the staged client’s `viz/src/demos-core.ts:60` still builds already-running demo/replay links without it. With `demosrv -ws 127.0.0.1:8444`, fresh starts work, but clicking an already-running card silently reconnects to the default port 8443.

- **should-fix** `engine/cmd/demosrv/supervisor.go:25` adds a direct `nats.go` dependency outside `engine/natsio/`, contrary to the repository’s stated dependency boundary. Put the WebSocket registry probe behind an `engine/natsio` helper, or document the boundary change through the required ADR/KB workflow.

REVIEW-COMPLETE
