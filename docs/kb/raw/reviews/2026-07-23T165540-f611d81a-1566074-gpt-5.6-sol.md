- **blocker** `engine/natsio/player.go:150` — `{run}-replay` creates a new public run-namespace convention without an ADR/contract migration. It can equal a legitimate recorded run ID, yet the synthetic namespace gets no matching KV metadata/state pointer; clients either find no registry entry or read the unrelated real run’s metadata. The suffix check rejects the wrong side of the collision.
- **should-fix** `engine/natsio/player.go:216` — `endTick` is inferred from the last logged message, not the completed run horizon. A valid run with sparse `CRCEvery`/`KeyframeEvery` and no late intents ends early in playback (potentially tick 0), falsely reported as truncated.
- **should-fix** `engine/natsio/player.go:394` — seeking to `endTick` clears `done`, so the returned status incorrectly says `Done:false`; `/resume` can briefly return 200 before the loop re-enters the end hold.
- **should-fix** `engine/natsio/player.go:308` — pacing uses an uninterruptible `time.Sleep`. Since arbitrarily tiny positive speeds are accepted, pause, speed changes, and shutdown can stall for seconds through centuries.
- **should-fix** `engine/natsio/player.go:193` — startup materializes the entire recording and retains per-tick maps. Memory and startup cost scale with total run length, conflicting with the repo’s week-scale replay goal and ADR-0006’s keyframe-plus-stream-cursor seek design.

REVIEW-COMPLETE
