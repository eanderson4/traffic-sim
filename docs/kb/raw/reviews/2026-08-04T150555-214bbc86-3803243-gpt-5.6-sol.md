- **blocker** `engine/sigctl.go:267` — The starvation rail resets on every different phase index. Alternating phases that both show red for one approach can starve that movement indefinitely; `engine/sigctl_test.go:153` explicitly codifies this bypass and suppresses lapse events. This contradicts ADR-0037’s bounded-starvation guarantee.
- **blocker** `engine/sigctl.go:282` — Chain clamping shortens `until` but never updates `d.HoldTicks` before appending to `appliedSig`/`SigLog`. Consequently `engine/natsio/verb.go:142` records the requested per-command duration, not the effective duration promised by `LoggedVerbView`; a renewal at the chain bound records a positive hold despite enforcing zero ticks.
- **blocker** `contracts/asyncapi.yaml:169` — `state.sig` still promises that light state is derivable solely from the program table and tick. During an override this produces incorrect client-visible lights. ADR-0037 acknowledges the gap, but the public contract continues asserting a now-false invariant.
- **blocker** `engine/sigctl.go:198` — Signal request-id dedup survives restore only while the override remains in clearance retention. After it is swept, a warm-restored run accepts and reapplies the same ID, contradicting the run-lifetime idempotency promised at `contracts/asyncapi.yaml:1455`.

REVIEW-COMPLETE
