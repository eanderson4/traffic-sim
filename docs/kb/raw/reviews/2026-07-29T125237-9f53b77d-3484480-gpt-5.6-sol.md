- **blocker** `engine/natsio/run.go:195` — After the attach gate opens, the loop steps without publishing the restored tick’s snapshot or announcing its existing vehicles. The default driver cannot claim the warm-start fleet until `AfterStep` at `engine/natsio/contract.go:945`, so restored vehicles coast uncontrolled for at least the first tick, breaking warm-start continuation.
- **blocker** `engine/cmd/demand-director/main.go:28` — `-start-tick` exposes a mode explicitly known to resend the restored lead-window directives and spawn them twice. A help-text warning is insufficient for silent demand corruption; reject nonzero use until queue reconciliation exists.
- **should-fix** `engine/spawn.go:137` — Injection lookahead still calls `leaderAt(..., nil)`, so routed director probes inspect `Successors[0]`, not the vehicle’s destination branch. This preserves the wrong-branch queue/collision defect ADR-0032 fixes for ordinary vehicles.
- **should-fix** `engine/rightofway.go:405` — `junctionHold` does not mirror the new permissive-green logic. For `'g'` with `RowNone`, it returns false unless the box is already occupied, ignoring approaching foes and allowing injection at a speed that may be unable to stop for the conflict.
- **should-fix** `engine/engine.go:887` — `stuckTicks` affects future vehicle existence but is absent from the rolling CRC. Two authoritative states with different strand deadlines verify identically until removal, weakening ADR-0005’s replay oracle and potentially never surfacing before the horizon.

REVIEW-COMPLETE
