- **should-fix** `engine/scenario/variant.go:48` — Explicit `network: ""` is indistinguishable from omission and silently inherits the base at `engine/scenario/variant.go:154`. This violates fail-loud named-field replacement; reject an explicitly empty override or represent presence with `*string`.
- **should-fix** `engine/scenario/variant.go:189` — Base network and parts are reread after `Load(baseDir)` at lines 214 and 265. A concurrent edit can make the hash cover bytes different from those compiled/validated into `base.origins`, breaking content-hash integrity. Reuse captured base bytes or recompile/revalidate the reread snapshot.
- **question** Tests could not run because the review sandbox prohibits Go’s `/tmp/go-build*` directory creation.

REVIEW-COMPLETE
