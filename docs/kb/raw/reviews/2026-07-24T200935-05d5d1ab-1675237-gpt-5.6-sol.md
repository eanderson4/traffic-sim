- **should-fix** `engine/cmd/demosrv/geojson.go:85` — The cache key is computed by `scenario.Load`, but the network is reread separately at line 103. An edit between those reads can cache different network bytes under the old content hash, defeating the stated edit/revert and generation-pinning guarantees. Revalidate the scenario hash after generation or export from the exact bytes that were hashed.
- **should-fix** `viz/src/nats-client.ts:86` — Reply completeness counts messages before confirming the callback successfully decoded/accumulated them. A malformed unchunked reply sets `have=want=1`, returns `true`, and `viz/src/main.ts:368` performs no retry because the accumulator is idle. Count only successfully accepted chunks or have the callback return acceptance/completion.
- **should-fix** `engine/natsio/sigchunk_test.go:235` — Staged Go is not `gofmt`-clean (`5*time.Second`); repo conventions require clean formatting.
- **nit** `contracts/asyncapi.yaml:117` — The contract still says 20-tick republication is the “entire” late-joiner catch-up, contradicting the newly accepted request/reply recovery contract documented immediately below.

REVIEW-COMPLETE
