• Now let me examine the key code paths in context.

• Many fixtures use `DefaultParams()` — I need to check whether flipping the default breaks pinned CRC fixtures. Let me run the test suite and check remaining surfaces in parallel.

• Build is clean. Now the critical check: do the pinned CRC fixtures survive the default flip?

• Test suite is green. Let me check how the what-if arms' manifests are produced — whether the pin actually covers re-runs.

• Review complete. Verified by reading the staged diff plus context, and by running `go build ./...` and the full `go test ./...` from `engine/` — everything is green on the staged tree, so the "M1–M3 CRC fixtures stay valid" claim holds empirically.

  **Blockers**

  None.

  **Should-fix**

  - `scripts/chicago/fwsweep.sh:51` — its scenario.yaml heredoc was not given the `adaptive_routing: false` pin. The addendum's invariant is "every published docs/show arm … emitted by the scenario generators … so regeneration keeps it," but this generator was missed: the freeway-scale brackets are part of the measured static-routing corpus (docs/show/README.md:334 cites `--freeway-scale 2.0`). Re-running fwsweep today silently measures the adaptive baseline. All other manifest generators check out: `mkscenario.sh`, `mkscenario-portal.sh`, `merge-pod.py`, `bottleneck_town.py` are pinned, and the `sed`-copy pod builders (`pod-chi-loop.sh:47`, `pod-portal.sh:44`) inherit the pin from base; `meter-sweep.sh` builds via merge-pod.py.

  **Nits**

  - `engine/keyframe.go:321` — the RestoreNotice text only advises "pin `adaptive_routing: false`", but on the `-netfile`/`-state-in` path the actionable knob is `-adaptive-routing=false` (main.go:94-97), and in the replay/bake/player paths the notice is purely informational (spec comes from RunMeta). One more clause would save a confused user.
  - `engine/natsio/player.go:502-504` — the notice prints on every `seek` landing on a pre-v6 keyframe; a scrubbing session repeats it. Once per Player would do. Rare path (only mixed-regime recordings), so genuinely minor.
  - `engine/engine.go:418-428` — the lazy-init comment documents the struct-literal `routeNextHop`-before-first-`Step` divide-by-zero, but the same test-only engine would nil-index `e.ttSnap` in `MarshalKeyframe` (`keyframe.go:238`) if keyframed before its first `Step`. Same accepted class; worth one line in the comment.

  **Questions**

  - `scripts/import-city.sh:129` writes an unpinned smoke manifest. I read this as deliberate (fresh city imports should get the new realism default), but it is a third silent behavior change alongside the flip — confirm it's intended rather than overlooked.
  - The addendum's hash-safety argument (b) rests on "the only two users set `true`." No repo manifest outside tests sets `adaptive_routing` at all, so those two users live in gitignored `data/` — unverifiable from the tree; flagging only so the claim is on record as externally unchecked.

  Notes on the things I specifically hunted and cleared: no NATS subject/payload changes ship (RestoreNotice is never serialized or CRC'd, engine.go:230-239; RunMeta.Spec already carried the field, registry.go:29); replay of pre-flip recordings stays bit-exact because restore uses `meta.Spec` (replay.go:110, player.go:206, bake.go); v6-into-flag-off refuses loudly (keyframe.go:312); the `routeEpochTicks == 0` lazy-init sentinel is sound for all NewEngine-built engines (`max(1, …)`, engine.go:336); scenario bool→*bool moves no existing content hash since hashing is over canonicalized file bytes (scenario.go:434-450); no new map iteration, float-accumulation, or wall-clock in the sim core (edge `log.Print` only).

  REVIEW-COMPLETE

