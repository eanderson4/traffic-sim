Reviewed the staged diff (splash→`index.html`, map app→`app.html`, Pages bundle script, `_headers`) plus `viz/src/splash.ts` in full and `engine/cmd/demosrv/main.go` for `/baked` handling. No NATS subject/payload changes, no determinism-relevant paths — this is all site/viz plumbing. Findings:

**Should-fix**

1. `viz/src/splash.ts:21-22` — `BAKED_FEATURED_URL` is now hardcoded non-empty, so **every** deployment takes the baked branch, including demosrv-served ones (GKE per `deploy/README.md`, local dev). demosrv serves only the built viz + `/net` + `/overlay` + `/api` — it has no `/baked/*` route (grep confirms) — so on those deploys the featured CTA links to `<origin>/baked/chishow/f941d8888b18/index.json`, which 404s: the landing page's primary CTA dead-ends where it previously launched the live LA run. The entire live-fallback machinery in this file (`activate()`, gating, registry probe) is now dead code on every deploy. This diff simultaneously updates `deploy/README.md` as if the demosrv routing were still current. Make the bake URL a build-time env (`import.meta.env`) or probe the manifest and fall back, rather than one constant serving two mutually exclusive deploy targets.

2. `viz/public/_headers:5,8,20` — mid-path splats (`/baked/*/network.geojson`, `/baked/*/index.json`). Cloudflare's documented `_headers`/`_redirects` matcher supports a single splat and its examples are all trailing; whether a mid-path splat matches (and matches across `/`, which `/baked/*/index.json` needs for `<run>/<hash>`) is exactly the kind of thing that fails silently as "no header applied". If `network.geojson` misses `Content-Encoding: br` the replay renders no roads; if `index.json` keeps `immutable` a stale pinned manifest survives in caches — the failure mode the comment itself calls the worst outcome. Verify with `curl -sI` against a preview deploy before go-live; downgrade to nothing if already verified — say so in the commit message.

**Nit**

3. `viz/src/splash.ts:21` vs `scripts/show/mksite.sh:168` vs `viz/public/quiz.html` — the featured hash `chishow/f941d8888b18` (and the other two runs) is now maintained in three places. mksite's fail-loud check only guards its own `RUNS` list; re-baking chishow and updating splash.ts but not mksite (or vice versa) ships a working check and a broken CTA. Consider deriving `RUNS` by grepping the sources.

4. `viz/src/splash.ts:13-22` — the name `BAKED_FEATURED_URL` and half its comment still describe a bake-manifest URL; the value is now a full `/app.html?bake=` page link. Rename (`FEATURED_REPLAY_HREF` or similar) when touched next.

5. `scripts/show/mksite.sh:152` — default `BAKED=/home/eric/grove/traffic-sim/data/baked` is machine-specific; fine for a personal show script, but a `$TRAFFIC_SIM_MAIN`-style env fallback would travel better. Line 147: the `.pages-staging-new` default looks like a leftover “-new” suffix from iteration.

6. URL scheme change `/?bake=` → `/app.html?bake=`: any previously shared deep links (docs are updated in-diff; external shares/recordings aren't) now land on the splash, which silently ignores `?bake=`. Pre-go-live this is presumably fine; if any `/?bake=` links are already circulating, a tiny redirect shim in `index.html` would preserve them.

**Question**

7. `scripts/show/mksite.sh --skip-build` trusts whatever is in `dist/` — a stale pre-rename build would pass the `index.html`/`app.html` existence checks only if rebuilt, so this is probably self-guarding; confirm `pnpm build` empties `dist/` (vite default `emptyOutDir`) so a stale `splash.html` can't ride along.

Blockers: none under the repo's triage definition (no sim/determinism/record-plane/contract impact) — but #1 is a real regression on the demosrv deploy path and I'd fix it before this commit if that target is still live.

REVIEW-COMPLETE
