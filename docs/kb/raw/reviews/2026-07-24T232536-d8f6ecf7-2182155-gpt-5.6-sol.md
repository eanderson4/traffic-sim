- **blocker** `engine/natsio/driver/driver.go:401` — Deferred vehicles keep following default routing, but their eventual exit is filtered for reachability from the frozen first-observation lane. If a vehicle passes a fork before budget admission, that exit may be unreachable from its current lane; the engine then silently degrades to default routing. The test keeps every ego on one lane and misses this failure.
- **blocker** `engine/natsio/driver/driver.go:396` — `wantLane` is replica-local. If a controller fails before a deferred route is assigned, the replacement captures a later lane and can draw a different destination. This directly violates ADR-0008’s behaviorally-invisible failover requirement and the code’s own “replicas must draw the SAME exit” claim.
- **should-fix** `engine/natsio/driver/router.go:23` — This exported router is unused and resurrects logic explicitly removed when routing moved engine-side. It duplicates `engine/routing.go` and already diverges from its determinism discipline: `routeHeap.Less` at line 76 has no lane-index tie-break.
- **should-fix** `engine/natsio/driver/driver_test.go:168` — The echo-tick assertion reads the asynchronous subscriber counter immediately after publishing. A bad extra `RouteSet` can arrive after line 175, letting the regression test false-pass.
- **nit** `engine/natsio/driver/driver.go:372` — “Nothing stored” is false: over-budget vehicles receive a `wantLane` entry at line 402.

REVIEW-COMPLETE
