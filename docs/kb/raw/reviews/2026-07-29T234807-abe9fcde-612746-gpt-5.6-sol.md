- **blocker** `scripts/chicago/mknetvariant.py:264` — A second pass creates IDs such as `lane_w1_w1`, but `scripts/whatif.py:230` strips only one `_w1` before corridor lookup. The second added lane is therefore silently excluded from corridor metrics, biasing exactly the repeated-widening comparison this patch introduces. Update the shared clone-attribution convention/consumer with this change.
- **should-fix** `scripts/chicago/mknetvariant.py:236` — No test covers repeated `--add-lane`: unique IDs, increasing `edgeIndex`, cumulative offset, and second-clone feeder wiring. This behavior is subtle and directly affects scenario validity; add a focused regression test.

REVIEW-COMPLETE
