**No blockers.** The budget mechanism is sound: `pickExit` stays a pure function of (seed, vehicle ID, first-obs lane), the lane freeze happens before the budget gate so deferral can't shift the draw input, over-budget egos leave no `wantRoute`/`routed` state, and cleanup covers `wantLane`. No subject or payload changes, so no ADR trigger (ADR-0006). No map iteration on decision paths, no wall clock in routing (the hello loop's `time.Now` is attach plumbing, not sim state). Test logic and cumulative intent counts (2/6/11) check out; I could not execute the test (command approval unavailable in this session), so run `go test ./engine/natsio/driver/` before committing if the review script didn't already.

**Should-fix**

1. `driver.go:181` — the deadline-exhausted hello error drops the underlying request error. The old path wrapped it (`%w`); now "no responders" (engine absent), request timeout, and a permanently failing connection (e.g. closed/auth error, which this loop will now retry pointlessly for the full MetaWait) all produce the same message. Keep the last `err` and include it: `fmt.Errorf("driver: hello: deadline exhausted (%s): %w", cfg.MetaWait, lastErr)`.

**Nits**

2. `driver.go:188-190` — in the final <250 ms before the deadline the sleep is skipped, and a fast-fail "no responders" error makes the loop hot-spin requests until `remaining <= 0`. Bounded to 250 ms, so harmless; a `time.Sleep(min(250ms, remaining))` would remove it.
3. `driver.go:406-408` — the `d.wantLane == nil` guard exists only because the test builds `Driver` by literal; the test already initializes `wantRoute` etc., so initializing `wantLane` there instead keeps the production path free of a test-shaped branch.
4. `driver.go:346-349` (pre-existing shape, now one map wider) — the fleet sweep only clears state for vehicles in `d.fleet`. An ego observed while its claim reply was lost (in obs, never in `d.fleet`) that despawns before the route echo leaks its `wantRoute`/`wantLane` entries forever. The snapshot backstop usually re-claims first, so deferring is a valid triage.

**Questions**

5. Default ratio: `Capacity` 1000 vs `RouteBudgetPerTick` 32 means a full-capacity claim burst defers draws up to ~31 ticks. The frozen first-obs lane can by then be behind the vehicle, so the draw lands unreachable, the kernel degrades to default routing, and the vehicle is *permanently* marked routed — ExitRouting silently not honored for the tail of a burst. The routeStep comment accepts this as deterministic degradation, which is defensible; confirm 32 was sized against the measured first-touch scan cost on the city network rather than picked round, since a too-small default converts a latency problem into a routing-coverage one.

REVIEW-COMPLETE
