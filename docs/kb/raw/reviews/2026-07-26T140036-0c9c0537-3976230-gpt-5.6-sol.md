- **blocker** — `docs/kb/decisions/ADR-0026-batched-intents.md:55`: The specified 44-byte v2 fixed section does not contain `seq`; sequencing is assigned per controller during `DrainIntents`. Likewise, `onIntent` buffers `ArrivedIntent`, not `KeyedIntent`, and the driver has no per-vehicle sequence counters (`:73`, `:90`). Correct the wire and ingest specification before implementation.
- **blocker** — `docs/kb/decisions/ADR-0026-batched-intents.md:48`: Magic-byte demux is not backward-compatible because legacy v2 begins with arbitrary `vehicle_id u64`; valid IDs can begin with `"TSIB"` and be misclassified. Use an unambiguous discriminator, such as a NATS header, rather than consuming part of the existing payload namespace.
- **blocker** — `docs/kb/decisions/ADR-0026-batched-intents.md:81`: Byte-identical on/off runs are not guaranteed under the explicitly barrier-free model. Existing publishing exposes early vehicle intents while later ones are computed; batching delays all until computation completes, so a concurrent drain can assign different applied ticks, trigger hold-last differently, and change logs/CRC. Either define synchronization semantics or limit the guarantee to replay determinism within each captured run.
- **should-fix** — `docs/kb/decisions/ADR-0026-batched-intents.md:59`: Specify that a route-bearing intent is sent exactly once as the complete standalone v2 intent and omitted from TSIB. Sending fixed axes in TSIB plus route in v2 creates competing same-vehicle intents where first-wins arbitration can discard the route.
- **should-fix** — `docs/kb/decisions/ADR-0026-batched-intents.md:53`: The batch `tick` field has no defined meaning or validation behavior. State whether it is the source observation tick and explicitly non-authoritative, or remove/reserve it; using it for acceptance would change current arrival-time semantics.
- **should-fix** — `docs/kb/decisions/ADR-0026-batched-intents.md:109`: The complexity formula is inconsistent with its example. Total messages are `Σ ceil(records_per_controller/cap)`; `controllers × ceil(total_vehicles/cap)` gives eight messages for the stated 40k/four-controller case, not four.

REVIEW-COMPLETE
