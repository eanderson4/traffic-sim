Change is narrow: `--add-lane` now groups the whole edge (by edge id) instead of only label-map lanes, so a repeated `--add-lane` clones the previous pass's clone instead of colliding on the same `_w1` id. I verified the mechanics against the full script.

**Verified correct:**
- Second pass: group includes `X_w1` (edgeIndex+1), `max` picks it, new id `X_w1_w1`, offset steps one further lane out — matches the documented "widen by two" semantics.
- Wiring composes: pass 1 appends `X_w1` to every feeder of `X`, so pass 2's feeder scan (`X_w1 → X_w1_w1`) reaches those same upstream lanes. Clone's own successors come from the deep copy. Correct.
- Guard logic is sound: internal lanes have no `edge` key → `L.get("edge")` is `None` → never matches after `edges.discard(None)`; the `by_edge[L["edge"]]` access is only reached when the key exists.
- Determinism: `sorted(by_edge.items())` (pre-existing) keeps clone order and output stable; sets are used only for membership. No wall clock, no RNG, no float accumulation introduced.
- No NATS subject/payload touched; offline generator only, doc text updated in place. No ADR needed.

**Findings:**

**nit** — `mknetvariant.py:245`: the set comprehension uses `L["edge"]` and will KeyError if the corridor label map ever names a lane that lacks the `edge` key (e.g. a junction-internal lane in the lut), while the comment at :246-250 only covers the `"edge": null` shape. `L.get("edge")` in the comprehension would make the `discard(None)` cover both. (Same exposure existed pre-diff, so not a blocker.)

**question** — `mknetvariant.py:255` vs `:170`: `--add-lane` now sees whole edges but `--drop-lane` still groups labelled lanes only, so clones are invisible to a later `--drop-lane` on the same corridor: it would remove the outermost *labelled* lane while leaving the synthetic clone stranded further out (successor pruning keeps it loadable, but the "narrow by one" semantics quietly become "reshuffle"). If add+drop on one corridor is out of scope, fine — worth a line in the docstring at most.

**nit** — `mknetvariant.py:260`: with edge grouping, the donor can now be a lane the corridor label map never mentioned (an unlabelled outermost lane, or one labelled for a different corridor sharing the edge), so the clone inherits *that* lane's speed/width rather than a corridor lane's. Physically defensible — you widen the edge, not the label — but it is a behavior change beyond the stated widen-by-two motivation; a one-line note in the comment block would keep it deliberate.

No blockers, no should-fixes. The `_w1_w1` id scheme, feeder composition, and determinism all check out.

REVIEW-COMPLETE
