No blockers. This is a test-only + docs change (no contract or engine-core surface touched); I verified the benchmark's assumptions against `bus.go`, `contract.go`, and `server.go` — the drain/hold-last/claim-filter mechanics, the nil-recorder safety claim (only `emitPause` dereferences `rec`, reachable only via the pause gate set in `AfterStep`, which never runs here), and connection cleanup (`Connect` registers `tb.Cleanup`) all check out. The BENCHMARKS.md table is internally consistent (rates recompute from the spans). Findings:

**Should-fix**

1. `engine/natsio/bench_intent_test.go:139` + `:265` — **with 50 samples, the reported "p99" is exactly the maximum.** `int(math.Ceil(0.99*50))-1 = 49`, the largest element, so the comment "enough samples that p99 isn't one load spike" is precisely wrong — p99 here *is* the single worst tick. This matters beyond labeling: ADR-0026 M3's acceptance gate is "tick p99 ≤ baseline," and comparing per-run maxima of 50 samples makes that gate hostage to one scheduler spike (BENCHMARKS.md itself reports a 45 ms outlier tick swinging p99 between runs). Either raise `measTicks` to ≥100 (p99 then excludes the max) or record the baseline column honestly as "max of 50." Cheap fix, and it hardens the very comparison this baseline exists for.

2. `engine/BENCHMARKS.md:132-136` — **"the engine spends ~10.4 ms waiting on publish→buffer delivery … ≈ 20 % of the 100 ms tick" overstates run-loop cost.** In production the run loop never waits on delivery: controllers publish asynchronously during the tick and only `DrainIntents` (~10.2 ms at 30k) sits on the run loop's critical path; the delivery goroutine's CPU competes with the kernel but doesn't serialize ahead of it. The serial publish→wait→drain shape is a benchmark artifact (a fine, like-for-like M0↔M3 latency baseline — keep the measurement), but the "tick-budget terms" framing will be read off into ADR-0026's context table as engine budget. Reword to "controller-publish→drained latency" or similar.

**Nit**

3. `engine/natsio/bench_intent_test.go:234` — the `len(keyed) != n` failure message says "claim filter dropped some," but the count can also be *high* (hold-last re-issues — caught only by the subsequent `Held` check — or duplicate delivery). Message slightly misdiagnoses one failure direction.

**Question**

4. ADR-0026 M0 (`docs/kb/decisions/ADR-0026-batched-intents.md:211`) names "`onIntent` CPU" as a deliverable; §(d) reports the publish→buffer wall span as a single-goroutine proxy (reasonable, since delivery is one goroutine) but never says so explicitly, and no profile was captured. Intentional descope? If so, one clause in §(d) noting the proxy would close the loop against the ADR's own checklist.

Determinism review: clean — wall clock and `time.Since` are confined to the test harness; the one map iteration (`c.hold`) is pre-existing code and sorted before use; payload construction follows `Vehicles()` slice order. No subject or payload changes, so no ADR note needed beyond the ADR-0026 references already present.

REVIEW-COMPLETE
