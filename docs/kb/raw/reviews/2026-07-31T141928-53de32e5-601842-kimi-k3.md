• I've read the full diff. Now let me verify key context in the repo files the diff touches.

• Now let me check `handleVerb`'s dedup store and a few other context points.

• Let me verify the version gating in `keyframe.go` — especially whether the v5 section condition also needed the `adaptiveState` treatment.

• Let me verify a few remaining context points — test helpers, event materialization, and the restore-reader's v5 gating.

• Context checks pass. Let me verify the change actually builds and the new tests pass.

• One more check — whether duplicate signal program IDs are rejected at network load (the override table is keyed by ID).

• Let me run the full natsio suite to be thorough.

• Full suites green (`engine` 8.4s, `engine/natsio` 59.5s, `go vet` clean). Review below. Line numbers refer to the staged tree.

  ## Blockers

  None found. The hard parts check out: the retention window (`sigctl.go:259-267`, keep while `e.Tick <= ov.until+clearance`) lines up exactly with the deepest lookback `sigDisplayedOnset`/`sigPhaseAt` can make (`engine/signal.go:352-357`), including across supersession chains; writer/reader keyframe conditions are shared (`keyframe.go:222-226` vs `:955-956` in diff, staged `keyframe.go:308,352`); the no-override path reduces to `phaseAtElapsed` bit-exactly (`signal.go:318-322`); CRC fold and lapse detection iterate `Net.Signals` in network order, map never iterated (`engine.go` diff hunk, `sigctl.go:240`); `signal_lapse` event ordering vs `LogTick` is irrelevant since consumers split by subject.

  ## Should-fix

  None that clear the repo's own triage bar ("hardening against hypotheticals is reject-by-default").

  ## Nits

  - `engine/sigctl.go:174-178` — the duplicate-id backstop checks only `sigNew`; the cited precedent (`director.go:150-163`) also checks the already-queued directives. A kernel-only caller re-enqueueing an applied request id double-applies. Unreachable via the contract layer (lifetime dedup at `contract.go:789`), so parity is cosmetic.
  - `engine/keyframe.go:364-365` (and pre-existing `:333-334`) — `w.u16(uint16(len(...)))` silently truncates an over-long id instead of failing marshal, producing an unreadable keyframe. Unreachable post-`handleVerb` bound (`verb.go`, `maxRequestIDBytes`), but a loud `MarshalState` error would match the file's fail-loud idiom.
  - `engine/sigctl.go:241-248` — a hold superseded at exactly its bound tick (new verb applied at `T == old.until`) records no `signal_lapse`; supersession silently wins the tie. Deterministic and the superseding verb is on the record, but it's an undocumented corner of "lapses are never silent."
  - `engine/natsio/run.go:243` — `data, _ := json.Marshal(...)`: harmless (the struct cannot fail), consistent with `contract.go:750`.

  ## Questions

  - `contracts/asyncapi.yaml` `VerbRequestView.required` changed from `[verb, request_id, origin, vtype, earliest_tick]` to `[verb, request_id]` — a schema-level weakening of the spawn shape, papered over by per-kind prose. Fine for additive-wire purposes; confirm the schema-loosening (vs. `oneOf` per kind) was a deliberate choice over keeping spawn's required list exact.
  - State-plane gap is owned in the ADR addendum, but worth stating in the review: during milestone 1 every viz/analytics client deriving light states from `ts.{run}.state.sig` + tick is not merely stale but silently *wrong* for the whole hold (up to 300 s). The record plane and CRCs are exact; only derived displays lie. Confirm milestone-2 prioritization stands.

  Determinism: no map iteration, no float accumulation, no wall clock/RNG in the new paths; `signalHoldMaxTicks` (`sigctl.go:165-171`) is a pure dt-compiled constant per the `routeEpochTicks` idiom. Contract: subject unchanged, payload additive with hard-error discriminator, ADR-0006 addendum + asyncapi 2.7.0 + ADR-0037 status flip all land in the same diff — the "message contracts are sacred" ritual is satisfied. Tests cover accept/reject/dedup/grant, boundary-length ids, phase presence, lapse/supersede clearance semantics both directions, same-state merge, keyframe round-trips both flag regimes, and replay/bake/player CRC-exactness.

  REVIEW-COMPLETE

