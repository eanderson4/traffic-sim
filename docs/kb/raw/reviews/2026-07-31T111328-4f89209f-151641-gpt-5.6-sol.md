- **should-fix** — `viz/src/main.ts:1402`: every startup error without `?bake=` is diagnosed as a missing live engine. Failures loading/parsing the network, overlays, furniture, or initializing MapLibre receive the same misleading suffix; explicit `?ws=` targets are also incorrectly described as “this host.” Only add this guidance for a confirmed WebSocket connection failure.
- **should-fix** — `viz/src/main.ts:1403`: `/quiz` is not the repo’s documented/generated local route; `scripts/chicago/mkquiz.py:14` and `scripts/show/build-quiz.sh:144` expose `/quiz.html`. Under local-first serving, this guidance can send users back into the app fallback instead of the quiz. Use `/quiz.html` unless a guaranteed rewrite is part of the deployment contract.

REVIEW-COMPLETE
