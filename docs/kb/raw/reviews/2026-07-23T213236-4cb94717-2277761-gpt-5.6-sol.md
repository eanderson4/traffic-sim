- **blocker** — `viz/src/signals.ts:88`: `shape` remains possibly `undefined` under strict TypeScript. `pnpm check` fails with TS2345, so the staged viz does not build.
- **blocker** — `viz/src/signals.ts:78`: grouping solely by phase-state column conflates synchronized movements from physically distinct/opposing approaches. Lines 85–101 then centroid those lanes—often into the intersection—and average/cancel their bearings. The test at `viz/test/signals.test.ts:68` explicitly codifies merging spatially unrelated lanes. Grouping needs an approach/spatial component, not just light-state equivalence.
- **blocker** — `viz/src/signals.ts:41`: literal NUL bytes make Git classify this TypeScript source as binary (`Bin 2994 -> 5183 bytes`), omitting its changes from the exact staged patch supplied for mandatory external review. Use an escaped sentinel such as `"\0"` so source diffs remain reviewable.

REVIEW-COMPLETE
