- **should-fix** `contracts/asyncapi.yaml:694` — `SchemaHeadersOptional` leaves `intent_encoding` unconstrained. Since additional properties are allowed, `tsib`, empty, and unknown encodings validate as `ControlIntent`, contradicting runtime demux and overlapping `ControlIntentBatch`. Add an optional `intent_encoding` property constrained to `v2`, preferably via dedicated v2 intent headers.
- **nit** `engine/natsio/bus.go:290` — The demux comment still says anything except absent/`tsib` is dropped, contradicting the new explicit-`v2` branch. The constant comment at `engine/natsio/bus.go:53` is stale similarly.

REVIEW-COMPLETE
