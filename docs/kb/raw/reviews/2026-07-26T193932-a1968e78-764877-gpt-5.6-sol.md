- **blocker** `contracts/asyncapi.yaml:228` — Keeping `contract_version` at 2 makes the attach handshake falsely report compatibility: a pre-TSIB engine accepts a default-on TSIB driver, then silently drops its control stream. Deploy ordering cannot enforce or detect compatibility for external controllers. Bump/negotiate the capability, or keep TSIB opt-in until detectable.
- **should-fix** `contracts/asyncapi.yaml:1031` — Header behavior is underspecified for duplicate `intent_encoding` values. NATS permits multiple values; implementation accepts based solely on the first. Specify first-value-wins or require exactly one value and reject the rest.
- **should-fix** `contracts/asyncapi.yaml:1045` — The layout says reserved fields are `0`, while `contracts/asyncapi.yaml:1066` says receivers ignore them. Clarify that encoders write zero but decoders accept any value, otherwise independent implementations may disagree on structural validity.

REVIEW-COMPLETE
