Findings — the flip itself is wired correctly (`main.go:499-500` negation verified; unflagged serve now writes per-intent, uncompressed), and the `network.go` CRC comment is accurate against `engine.go:1063-1077`. No blockers. The problem is the patch fixes a doc/behavior contradiction while leaving three places still asserting the old defaults — the exact drift class this commit exists to correct.

**should-fix** — `contracts/asyncapi.yaml:447-450`: the `logIntent` channel description still says "SUPERSEDED AS THE DEFAULT by logIntents" and "Still written when the recorder runs with UnbatchedIntentLog (serve -log-batch=false)". Both claims are inverted by this patch, and they now contradict the patch's own edits 25 lines below (`logIntents`: "logIntent is the per-message default"). The contract file is internally self-contradictory as staged.

**should-fix** — `docs/kb/decisions/ADR-0006-nats-message-contract.md:389-391`: the ADR-0035 addendum to the message-contract ADR still says `log.intents` "is the default" and that `-log-batch=false` is the opt-out. Message contracts are sacred per AGENTS.md; the authoritative contract ADR now disagrees with the staged contract file and serve.

**should-fix** — `docs/kb/decisions/ADR-0035-batched-intent-log.md:95-97` (staged text) and `engine/natsio/recorder.go:48-66` (untouched): the revised §5 retains "named for the non-default so the zero value is the new behaviour" verbatim. After the flip that sentence is false — `UnbatchedIntentLog`/`UncompressedStore` now name the serve *default*, and the zero value selects the *blocked* batched/compressed behavior. `recorder.go`'s field comments say the same inverted thing ("compression is on", "zero value is the batched form"). The patch's own §5 is self-contradictory.

**question** — `engine/natsio/recorder.go:54,66`: intentional that a zero-value `RecorderConfig` (every `RunLive`/`NewRecorder` call outside serve — currently all tests, e.g. `contract_test.go:177`) still gets the blocked batched+compressed configuration? ADR-0035's new claim "an unflagged run gets the measured-reproducible configuration" holds only through serve's flag negation; the library default was not flipped. Probably fine for in-process tests (the blocker is async-controller timing), but the asymmetry should be stated somewhere, since the field-naming convention no longer signals it.

**nit** — `contracts/asyncapi.yaml:466,499`: `compression: s2` is still declared unconditionally as the binding value while the comment says default off; a machine reader of the binding sees s2 as invariant. (Carried over from the 2026-07-29 review round.)

REVIEW-COMPLETE
