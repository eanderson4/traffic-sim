- **blocker** `engine/natsio/player.go:486`, `viz/src/snapshots.ts:64` — Backward seek publishes a lower tick on the same subject, but the production viz drops every tick not exceeding its previous high-water mark. A paused seek never renders; after resume, visualization remains frozen until playback passes the pre-seek tick. Add explicit reset/epoch semantics and document the contract change.
- **should-fix** `engine/natsio/player.go:201`, `engine/natsio/replay.go:152`, `engine/natsio/replay.go:224` — Startup fetches the entire stream and retains per-tick intents, verbs, and CRC maps; every seek additionally fetches every keyframe payload. Memory, transfer, and seek cost scale with total recording size and keyframe state, conflicting with the week-scale goal and ADR-0005’s keyframe-plus-stream-cursor design.

REVIEW-COMPLETE
