- **should-fix** `engine/metrics.go:1108` — `exitResolve` caps enumeration at three returned chains. If those three reach exit A but a truncated fourth reaches exit B, it incorrectly books a route to A and sets that trip destination instead of conservatively booking only the last-seen-lane remainder as required for multiple reachable exits. `DroppedCrossings` signals uncertainty but does not prevent corrupted VMT/per-lane totals.
- **should-fix** `engine/metrics.go:568` — A pure overshoot-refund despawn with `dPos == 0` and `prev.v == 0` retains `t = dt`. `accountShares` then assigns a full tick of VHT, occupancy, and time-loss to a negative-distance correction, contradicting the documented distance-only/zero-time refund semantics. Set `t = 0` whenever `dPos == 0`.
- **should-fix** `engine/engine.go:177` — `Vehicles` exposes the engine’s authoritative backing slice and mutable vehicle pointers publicly. A caller can reorder the slice or mutate vehicle state despite the “read-only” comment, changing subsequent sweep order, floating-point accumulation, physics, and CRCs. The metrics kernel is in-package and does not require this determinism-breaking public surface.

REVIEW-COMPLETE
