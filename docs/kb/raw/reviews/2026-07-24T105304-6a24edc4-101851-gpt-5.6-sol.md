- **blocker** — `docs/kb/decisions/ADR-0006-nats-message-contract.md:328`: the patch explicitly changes `route` semantics and routed trajectories while retaining contract version 2. This contradicts the repo’s “field semantics never change in place” rule at `docs/kb/articles/concepts/vehicle-controller-interface.md:96`. Bump/negotiate the contract version or formally supersede that versioning decision.

- **should-fix** — `engine/natsio/driver/driver.go:315`: `routed` is set before the one-time route-bearing Core NATS intent is published/observed. A dropped message or failed publish permanently leaves the vehicle on default routing because later intents omit `RouteSet`. Retry until `ego.Route` echoes the desired destination.

- **should-fix** — `engine/routing.go:56`: each distinct destination permanently retains an `O(number of lanes)` next-hop table. At the documented ~56k-lane zone size this is ~224 KB per destination; per-vehicle exit selection can accumulate hundreds or thousands of tables. Bound/evict the cache or constrain destination cardinality.

- **should-fix** — `scripts/chicago/scorecard.py:35`: the widest measurement set is selected without requiring `time_loss_s` or `stops`. ADR-0014 permits either group to be disabled, so a lexicographically winning set can silently report zero queues/stops despite another set containing them. Select sets by required metric availability.

- **nit** — `scripts/chicago/water.py:129`: GeometryCollections may contain valid polygon members; the code knowingly discards all of them. Reporting the count avoids silence but still loses legitimate water geometry.

REVIEW-COMPLETE
