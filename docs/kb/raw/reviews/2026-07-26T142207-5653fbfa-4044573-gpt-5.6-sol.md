- **blocker** `engine/natsio/bench_intent_test.go:208` — ADR-0026 requires throughput “to drain-complete,” `onIntent` CPU, and tick p50/p99. This records only aggregate publish→buffer and average drain time; `delivered/s` excludes the ~equally expensive drain. Consequently `engine/BENCHMARKS.md:114` calls 2.9M msgs/s the ingest ceiling when the measured complete path is roughly 1.5M intents/s, and no p50/p99 baseline exists for M3 comparison.
- **should-fix** `engine/natsio/bench_intent_test.go:216` — `len(keyed)==n` does not prove zero claim violations after the first tick because hold-last can replace rejected fresh intents. `Stats()` is only logged at line 237, while `engine/BENCHMARKS.md:103` says violations are asserted. Fail if the cumulative violation count is nonzero.
- **should-fix** `engine/BENCHMARKS.md:106` — Results specify `b.N=1`, but neither the benchmark nor documentation records the required invocation (for example, `-benchtime=1x`). The standard harness may choose another `b.N`, making the published procedure non-reproducible.

REVIEW-COMPLETE
