Checked the diff against the surrounding harness (`bench_intent_test.go` loop structure, `NewTSIBMsg`/`DecodeTSIB`, `Bus.IntentBatchStats`, header constants in `bus.go:49-54`, ack/obs publish order in `run.go:184-185`, driver `IntentBatchOff`/`ID()`). Report:

## Findings

**Blockers:** none.

Production-code surface is tiny and safe: `server.go:108` adds a subscribe-side wildcard helper (no new published subject or payload — no ADR/migration note needed); `tsib.go:38-42` is a pure constant refactor (`tsibTickOff=8` matches the layout: magic 4 + ver 2 + flags 2, count still at 16). Everything else is bench/test/docs.

**Should-fix:** none rising to that bar.

**Nits**

- `bench_intent_test.go:288-294` — `lagSamples` can only ever contain zeros (any nonzero is `Fatalf`'d two lines earlier), so `lag_proxy_p50/p99_ticks` are percentiles of an asserted constant. The comment and BENCHMARKS.md both say this is deliberate ("pins the measurement path"), so fine, but reporting one `lag_proxy_ticks=0` metric would say the same thing with less machinery.
- `driver_test.go:891-901` — first-sample skew in v2 mode: the tap attaches after the driver on an already-pacing run, so if it catches an intent before its first obs, `lastSrc` pairs with `lastObs=0` and one early sample is inflated. With ~396 samples and nearest-rank p99 (3 samples above the rank), a single outlier can't move either percentile, and the TSIB leg is immune (`lastSrc: -1` guard). Absorbed; noting only in case sample counts ever shrink.

**Questions**

- `driver_test.go:413-458` — `TestBatchAppliedLag` is two sequential wall-paced legs (2×400 ticks × 3 ms pace) with a cross-leg percentile comparison; on a loaded CI box a descheduled driver missing the 3 ms window could push real lag samples to 2 in either leg. The lag metric is source-tick-relative (not wall-time-relative) and the +1 p99 tolerance plus the p50-floor check make it fairly robust, and the file documents the sensitivity honestly — but is this in the default `go test` path or expected to run only on the bench box? If default, watch it for flakes before adding any guard (per the repo's own "one round, no hypothetical hardening" rule).

**Verified claims** (things the diff asserts that I cross-checked): stats formula `controllers × (warmup + b.N×measTicks)` matches the actual loop shape (warmup outside `b.N`, `bench_intent_test.go:308-316`, fresh `Bus` per invocation so counters can't leak across b.N ramp-ups); the pinned literals `intent_encoding`/`tsib`/offset-8 match `bus.go:53-54` and the TSIB layout, and pinning them independently in the external-consumer test is the right move; the `lagTap`'s claimed ack-before-obs ordering matches `run.go:184-185` (`PublishAcks` then `AfterStep`); reusing one `*nats.Msg` with in-place tick patching per tick is safe because nats.go copies the payload into the write buffer synchronously under the connection lock; `benchBufferedIntents < n` still works in batch mode since the bus buffers expanded records; `-bench IntentIngest` in the BENCHMARKS.md run line matches both benchmark names.

REVIEW-COMPLETE
