- **blocker** `engine/natsio/driver_test.go:1277` — Acceptance checks only one **complete-response** lag sample per observation. It never checks per-vehicle lag, so it misses the exact regression at issue: v2 can publish early vehicles before a boundary while TSIB holds every vehicle until computation completes. The live tap at `engine/natsio/driver_test.go:902` is likewise controller-level. Therefore the “PER-VEHICLE no worse” conclusion in `engine/BENCHMARKS.md:211` is unsupported and may be false.
- **should-fix** `engine/natsio/driver_test.go:1243` — The supposedly identical fixed schedules can diverge when either leg overruns; overruns are merely reported, yet the exact comparison remains load-bearing. Require zero overruns for the acceptance run or reject/retry the measurement, otherwise host load can change boundary placement and produce misleading pass/fail results.

REVIEW-COMPLETE
