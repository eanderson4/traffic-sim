**Review of staged diff** — docs/KB article (mechanism 8), new presentation script `mkcongestionmap.py`, PNG fallback in `mkquiz.py`, regenerated show artifacts. No engine, no NATS subjects/payloads touched — no ADR trigger. No determinism risk in sampled paths (presentation scripts only; no wall clock, no RNG, dict orders deterministic). Schema usage verified against the repo: `set_id` is a string (`engine/metricsjson.go:29`), so the `--set` string compare is sound; `intervals`/`lane_id`/`begin_tick`/`sum_dist_m`/`sum_time_s` all match; warmup cut (`begin_tick < warmup`) matches `corridorspeed.py:57`; `speedLimit`/`shape`/`lanes` match the network JSON convention. The KB table is internally consistent (back-solved origin-lane speeds of ~6–22 km/h from the Edie/interior/share triples all cohere), the regen command matches `record-hero.sh`'s actual signature and `-metrics-out` path, and the multi-set guard in `lane_speeds` correctly refuses the ADR-0014 double-count.

**Blockers** — none.

**Should-fix**

1. `docs/show/diag/chi-loop-urban__setup.png` has no recorded provenance and no regeneration path. `build-quiz.sh` generates the two pod SVGs but not this PNG; the KB article's regen command produces `docs/show/img/chi-congestion-fw35.png`, not the diag one. The script's own banner is "DRAWN FROM THE RUN, NEVER HAND-COLOURED" (mkcongestionmap.py:14), yet nothing states which run/warmup/width produced the checked-in setup slide, so it will silently drift from the article's map on the next re-bake. One comment line in `build-quiz.sh` (or the exact command in the article) fixes it.

**Nits**

2. `mkquiz.py:5628-5630` (patch) — the PNG fallback's hardcoded alt text ("every lane coloured by its measured speed…") describes a congestion map, but `diagram()` is also the fallback for option cards (`mkquiz.py:459`). A future non-congestion option PNG gets a lying alt. Fine until that exists.
3. `mkcongestionmap.py:5725` (patch) — empty `intervals` gives `next(iter(sets))` → raw `StopIteration` traceback instead of the script's otherwise-clean `sys.exit` diagnostics.
4. `mkcongestionmap.py:5722` — the multi-set error counts `None` in `{len(sets)}` but filters it from the listed ids; a file with one named set plus unset-id intervals reports "2 sets ['x']".

**Questions**

5. `bottleneck-town__setup.svg` / `diag/index.html` — sink circles reorder and two cy values shift with no generator change staged. This looks like the expected one-time re-canonicalization after the `sorted(named)` fix in `mkopenslide.py:101` (committed in 762f832 alongside a pre-fix SVG). Confirm regeneration is now byte-stable (run twice, identical output) — if not, there's residual PYTHONHASHSEED sensitivity left in the pipeline.
6. KB article: mechanism 8's numbers come from `corridorspeed.py`, which (pre-existing, not this diff) sums across measurement sets with no `set_id` guard — the exact double-count `mkcongestionmap.py` refuses. Presumably the fw35 file is single-set (the map script would have errored), but worth one line in the article or a guard in `corridorspeed.py` later.

REVIEW-COMPLETE
