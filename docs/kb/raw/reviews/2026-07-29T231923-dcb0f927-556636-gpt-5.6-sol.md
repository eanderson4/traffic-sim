- **should-fix** `scripts/chicago/mkod.py:1274` — `total_veh()` is nominal rate integration, not the number Poisson sampling will emit. Sampling variance, forced first arrivals, and gaps crossing slice boundaries can all create differences; therefore `completed + stranded + active` gaps are not necessarily “delivery” failures.
- **should-fix** `scripts/chicago/mkod.py:827` — district/through reporting still weights destinations using each flow’s scaled base rate, ignoring its temporal shape. With commute, freight, and reverse-commute profiles having different integrals, the printed district shares and “veh/h” totals do not describe either peak demand or whole-program vehicle counts.
- **should-fix** `scripts/chicago/mkod.py:1006` — `tally()` claims to record what `emit_flow()` writes, but accumulates unrounded rates while `emit_flow()` serializes one decimal at `scripts/chicago/mkod.py:462`. Small positive rates may even serialize as `0.0` while remaining counted. Aggregate/header values can therefore disagree with the shipped YAML.
- Tests: `python3 scripts/chicago/test_mkod.py` passes all 69 tests, but no integration test compares reported totals against serialized slices.

REVIEW-COMPLETE
