• I'll verify the key claims in the diff against the repo: the restore path construction, the lazy-init sentinel, and struct-literal engine usage in tests.

• Restore path checks out. Now verifying the edge surfaces: serve's `-state-in` path, RunMeta's Params serialization, player.go's `lg`, and existing manifest users of `adaptive_routing`.

• Static analysis done. Now running the test suite to verify the flip doesn't break existing fixtures.

• While tests run, checking whether CRC-fixture tests beyond the patched ones rely on the old default.

• Review complete. Verified against the tree: `go build`, `go vet`, and full `go test ./...` all green with the flip in place; restore path confirmed to construct via `NewEngine` (keyframe.go:305); `Params` has no JSON tags so `AdaptiveRouting` serializes explicitly in RunMeta (engine.go:60, natsio/registry.go:29) — the ADR-0012 addendum's "pre-flip recordings show `AdaptiveRouting: false` explicitly" holds.

  **Blockers**

  None.

  **Should-fix**

  - `engine/cmd/serve/main.go:246-261` — the `-state-in` warm-start path never surfaces `RestoreNotice`. `restoreState` sets it (keyframe.go:320-322) and every natsio edge caller logs it (bake.go:72, player.go:210/502, replay.go:114, run.go:108), but serve — the primary `-state-in` CLI, and the one whose new `-adaptive-routing` help text (main.go:94-97) explicitly tells users how to warm-start pre-v6 states bit-exactly — never reads the field. A user who warm-starts a pre-v6 static state with default flags gets exactly the silent regime switch the notice was built to prevent. One `if e.RestoreNotice != ""` after the restore in serve closes it. (Grep confirms `RestoreNotice` is consumed only in `engine/natsio/`.)

  **Nits**

  - `docs/kb/decisions/ADR-0036-congestion-adaptive-routing.md` addendum names the pin-emitting generators as "mkscenario.sh, merge-pod.py, bottleneck_town.py" but the diff also pins `scripts/chicago/mkscenario-portal.sh:54` — list is incomplete.
  - serve flag help (main.go:97) advises `-adaptive-routing=false` for pre-v6 warm starts, but with `-scenario` that flag is rejected and `-state-in` is still allowed for demand-less scenarios (only refused with demand parts, main.go:972) — there the manifest pin is the only opt-out. Help text could say so in one clause.
  - `engine/engine.go:418-428` — the lazy-init comment documents the restore invariant but not the other clobber direction: a struct-literal engine that pre-seeds `ttEMA`/`routeEpochs` before its first `Step` gets silently re-initialized. No such test exists today (only persistence_test.go:109 builds a literal, and it touches no adaptive state), so this is documentation, not a bug.

  **Questions**

  - None on determinism: sampled paths iterate `Net.Lanes`/vehicle order slices only, `routeEpochs` is keyed-access never iterated, no wall clock, RNG untouched. No subject or payload changes; RunMeta schema unchanged, so no ADR-0006 contract note required beyond what the two addenda already record. Suite green confirms no engine test pinned absolute CRCs under the old default (fixtures compare run-vs-run, not golden values) — so the "every M1–M3 CRC fixture stays valid" claim rests on those fixtures being flag-off-pinned only where patched (destination_test.go:31, gridlock_test.go:578, adaptive_test.go:157); the rest now run adaptive-on by inheritance and pass because they pin nothing absolute. Acceptable, but worth knowing the suite no longer guards the static baseline's CRCs anywhere except those three files.

  REVIEW-COMPLETE

