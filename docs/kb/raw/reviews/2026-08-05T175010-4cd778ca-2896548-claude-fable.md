Review of the staged diff: flush-on-construct in `NewBus`/`NewPublishBus`/`NewContract`, `RunLive` reordered so `reg.Start` comes last in construction, the KB "(b) FIXED" line, and five archived review rounds (docs-plane). The engine hunks are byte-identical to what the archived round 5 (`174644-e6cd1a81`) reviewed; the delta this round is the KB line rewrite (which addresses round 5's should-fix) plus that round's archives.

**Blockers: none.** Verified against the tree, not just re-asserted from the archives:

- `Flush()` after the queued SUBs is the right primitive on a single embedded server (ADR-0004 local-first); constructor return now implies server-side SUB registration. Error paths unwind: `Bus.Close` is nil-safe on both subs (bus.go:600–607), `Contract.Close` unsubscribes everything bound so far (contract.go:402–406), and `NewBus`'s flush-failure path closes the `NewPublishBus`-installed responder too (bus.go:243–246).
- The reorder closes both prior sol blockers: `reg.Start` (puback-awaited KV `Put`, registry.go:79–89) now follows the contract flush, `LogStart`, `PublishSignals`, and `dumpState` (run.go:182–207). After `reg.Start`, the first `ProcessControl` is immediate (gate loop run.go:220–229 or main loop run.go:231–233), so hellos enqueued during construction are serviced at once — no `no responders` reachable from registry visibility.
- Abort-path change is safe for existing consumers: the only non-test caller guards `lr != nil` (engine/cmd/serve/main.go:508, 520); `checkFreshRecording` still runs "before RunLive touches the registry" as its comment claims (main.go:417–424); the one test asserting `StatusAborted` (natsio_test.go:264) aborts mid-loop — after `reg.Start` — so it still sees the aborted trace. The demosrv supervisor's registry-visibility readiness probe (flagged in the 2026-07-24 archived reviews) is genuinely strengthened by this ordering.
- KB line accuracy: the "FIXED" claim, the abort-path rider ("NO registry trace"), the per-test-flush caution, and the still-open `reg.Start`-orphans-recording note all match the code. Round 5's should-fix is addressed.

**question** — the readiness promise ("registry visibility implies the run is addressable") still lives only in the KB and run.go:70–76; rounds 4 and 5 both suggested a one-line addendum on the ADR owning the registry lifecycle (rule 5's spirit — clients will build on this ordering, and reverting it later is a breaking metadata-plane change with no wire signal). Open for the third round; deferable, but the committer should consciously end this one — a docs-only ADR addendum wouldn't even invalidate the stamp.

**nit** — `docs/kb/articles/cross-topic-concerns.md` (diff line 10): "the static record plane is out (… signal table published)" slightly overstates — `bus.PublishSignals` (run.go:188) is best-effort (failures counted in `sigErrs`, not fatal), so the guaranteed part is the keyframe plus the *flushed sig.req catch-up responder*, not a delivered broadcast. Docs-plane, ungated.

**nit** — bus.go:243 + bus.go:295: live-run construction flushes twice (once in `NewPublishBus`, again after the intent sub); two sync RTTs where one covers both. Harmless on an embedded server; triaged in the archived rounds.

**nit** — contract.go:385–387 (pre-existing, made visible by the new symmetric flush path): `bind` failures return without `c.Close()`, leaking already-bound subs until connection teardown. Unreachable in normal operation.

**Clean checks.** Determinism: all changes construction-time, outside the tick loop; no map iteration in sampled paths, no float accumulation, no RNG; `time.Now()` in `Registry.Start` is pre-existing metadata-plane wall clock. Contract: no subjects or payloads change; `nats.go` stays confined to `engine/natsio/` (ADR-0006). Archives: five rounds as brief/diffstat/model outputs, no `*-reviewed.patch`, per the ADR-0013 2026-08-04 addendum.

REVIEW-COMPLETE
