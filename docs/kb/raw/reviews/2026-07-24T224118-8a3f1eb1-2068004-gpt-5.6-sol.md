- **should-fix** `engine/natsio/player.go:263` — `NewPublishBus` now installs the `state.sig.req` queue subscription, but `Player.Run` never calls `p.bus.Close()`. A stopped player therefore remains a responder while its NATS connection is reused, potentially serving stale signal tables or sharing requests with a replacement player. Defer `p.bus.Close()` from `Run` or add an explicit player lifecycle close.

REVIEW-COMPLETE
