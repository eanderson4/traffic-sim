- **should-fix** `engine/cmd/metview/main.go:288` — `IntervalRecord.K` is defined as veh/m, but `MeanK` is labeled and rendered as veh/km without multiplying by 1000 (`engine/cmd/metview/page.tmpl:49`). Every displayed density is 1000× too small; the tests mask this by using fixture values already shaped like veh/km.
- **should-fix** `engine/cmd/metview/main.go:58` — Optional schema-v1 metric fields are decoded as scalar zeroes. Valid files with disabled groups therefore show unavailable density/occupancy/stops/time-loss as measured zeroes and allow false rankings. Preserve field presence or reject unsupported group-off files.
- **nit** `engine/cmd/metview/page.tmpl:50` — The bar is always rendered beside the time-loss value, but `engine/cmd/metview/main.go:435` makes it encode density, occupancy, or stops for other sorts. Move it to the selected metric’s column or label it explicitly.

REVIEW-COMPLETE
