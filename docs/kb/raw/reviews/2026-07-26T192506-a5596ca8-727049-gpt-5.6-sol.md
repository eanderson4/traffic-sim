- **blocker** — `contracts/asyncapi.yaml:649` publishes a new public wire contract while ADR-0026 remains Proposed (`docs/kb/decisions/ADR-0026-batched-intents.md:3`, `docs/kb/INDEX.md:110`). M4 explicitly requires accepting the ADR alongside contract ratification (`docs/kb/decisions/ADR-0026-batched-intents.md:247`).
- **should-fix** — `contracts/asyncapi.yaml:23` leaves `info.version` at `2.4.0`. Prior additive contract changes increment this minor version; TSIB needs a new version and matching change note in `info.description`.
- **should-fix** — `contracts/asyncapi.yaml:651` and `docs/kb/INDEX.md:110` promise one message per controller per tick, but the declared 20,000-record cap requires multiple batches for larger controllers (`contracts/asyncapi.yaml:1034`). Describe this as one-or-more bounded batches.
- **should-fix** — `contracts/asyncapi.yaml:1059` reuses unrestricted `IntentView`, whose optional `route` is valid (`contracts/asyncapi.yaml:1008`). Thus the machine-readable TSIB view accepts route-bearing records despite the wire contract forbidding them; define a route-free record view and at least cap `records` with `maxItems: 20000`.

REVIEW-COMPLETE
