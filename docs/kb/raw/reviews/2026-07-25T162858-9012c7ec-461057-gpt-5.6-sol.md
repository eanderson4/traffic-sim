- **should-fix** `engine/cmd/demosrv/supervisor.go:349` — `stop()` stamps only `startGen+1`. If two or more start requests acquire `mu` before the stopping goroutine, generations after the first queued start exceed the stamp and can enter the full 660-second readiness wait. Thus `POST /api/demo/stop` can hang for ~11 minutes under concurrent starts. Use a stop epoch/latch that remains effective until the stopper acquires `mu`, rather than predicting one future generation.
- **should-fix** `engine/cmd/demosrv/deploy.go:115` — Bearer auth schemes are case-insensitive, but `CutPrefix(..., "Bearer ")` rejects conforming headers such as `Authorization: bearer <token>`. Parse the scheme case-insensitively while retaining the constant-time credential comparison.

REVIEW-COMPLETE
