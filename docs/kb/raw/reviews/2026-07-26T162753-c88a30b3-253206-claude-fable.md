**No blockers.** The diff is tests + docs + a byte-identical constant refactor in `tsib.go` — no wire/contract change, so no ADR note needed. Verified before signing off: run.go:184-185 confirms the ack-before-obs ordering `lagTap` cites; patching `m.Data` before `PublishMsg` is safe (nats.go copies into the write buffer synchronously, so tick-patch reuse across ticks doesn't race); the `IntentBatchStats` accounting is correct for `b.N > 1` (fresh bus per subtest invocation, warmup runs once per invocation).

**Should-fix**

1. `engine/natsio/driver_test.go:846-848, 884-909` — `lagTap`'s premise "Delivery order on one connection is broker order" is false at the level the code relies on. nats.go dispatches each async subscription on its **own goroutine**; ordering is guaranteed only *within* one subscription. With obs, intent, and ack on three separate subscriptions, the ack handler can run before the intent handler for an earlier-arriving message (and in v2 mode the intent handler before the obs handler), mispairing samples by ±1 tick. Fix that keeps the design: one wildcard subscription `ts.<run>.ctl.*.<ctlID>` dispatching on the subject token — single-subscription delivery is ordered, which restores exactly the guarantee the comment claims.

2. `engine/natsio/driver_test.go:966-969` — zero-tolerance p99 comparison between two *independent* timed live runs on a shared box. BENCHMARKS.md itself records v2 p99 as 1–2, i.e. the v2 leg sometimes lands at the floor of 1; one late drain boundary in the TSIB leg (legitimately produces a lag-2 sample under load, per your own undercount note) then fails `p99TSIB > p99V2`. Combined with finding 1 this is a flake generator. Keep p50 strict, give p99 a +1-tick allowance (or assert `p99TSIB ≤ 2` absolutely) — or triage as accepted flake risk, but record it.

**Nit**

3. `engine/natsio/driver_test.go:889` — `binary.LittleEndian.Uint64(m.Data[lagTickOff:])` has no length guard; a short TSIB-headered message panics the handler goroutine and kills the test binary. The driver never sends one; one `if` buys the safety.

4. `engine/natsio/bench_intent_test.go:216` — error text says nil means "over-cap or route-bearing", but `NewTSIBMsg` skips route-bearing intents rather than returning nil; only over-cap nils. Message misstates the contract it's pinning.

5. `engine/natsio/bench_intent_test.go:364-365` — `lag_p50_ticks`/`lag_p99_ticks` are constant 0 by construction yet named like a measured lag; raw bench output will mislead anyone who hasn't read BENCHMARKS.md. `lag_proxy_ticks` (or similar) self-documents.

**Question** — none; the open design points (v2-mode "newest obs" pairing rule, encode-excluded caveat, 0-by-construction proxy) are all disclosed accurately in the docs.

REVIEW-COMPLETE
