- **should-fix** `engine/cmd/demosrv/deploy.go:141` — `filepath.Join(".", "serve")` becomes `"serve"`, so `exec.Command` at `engine/cmd/demosrv/main.go:199` searches `$PATH` instead of executing the validated local file. `-nobuild .` can fail late or run an unrelated binary. Return absolute paths or preserve `./`.
- **should-fix** `engine/cmd/demosrv/deploy.go:152` — checking whether *any* execute bit is set does not prove executability by the current process. For example, an owner without owner-execute cannot use a file merely because group/other execute is set. The promised startup validation can pass and still fail on first spawn; use an effective-access check.
- **question** `docs/kb/decisions/ADR-0020-demosrv-public-deployment.md:71` — what prevents deployment from exposing `-wspublic` before broker permissions land? `engine/cmd/serve/main.go:213` still exposes an unauthenticated publish-capable NATS/JetStream endpoint. The ADR documents this as a go-live blocker, but nothing in this change enforces that precondition.

REVIEW-COMPLETE
