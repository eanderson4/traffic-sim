- **blocker** `engine/netimport/consolidate.go:107` — Rewiring an internal predecessor directly to the far internal lane bypasses that lane’s yield/permissive-green entry control. `rowGate` treats vehicles already on an internal lane specially and only calls `exitBlocked` (`engine/rightofway.go:82`), whose downstream-internal check considers occupied foes and holding stop lines but not approaching foes (`engine/rightofway.go:332`). A vehicle can therefore enter a minor or permissive movement while its foe is simultaneously approaching, causing normal-operation right-of-way violations/overlaps. The claimed merged-box behavior needs cluster-wide conflict control or an entry gate on internal→internal transitions.
- **should-fix** `engine/netimport/consolidate.go:81` — The “demand-facing lanes are never deleted” claim only protects topology origins/exits/end walls. ADR-0021 permits any non-wall lane as a route destination, so another scenario can legitimately name a qualifying sliver and receive an unknown-destination failure after reimport. Either preserve known demand destinations, explicitly constrain the importer contract, or document/validate this migration generically rather than only for Chicago’s current flows.

REVIEW-COMPLETE
