Read the diff and both full files. Findings:

**Should-fix**

1. `scripts/chicago/mknetvariant.py:262-264` vs `scripts/whatif.py:240-243` — the two halves of this diff disagree. The grouping change deliberately allows the clone donor to be an **unlabelled** outermost lane of a corridor edge (the comment says so explicitly). But whatif's attribution recovers the corridor only by stripping `_w1` and looking the *donor id* up in the label map: for a clone `unlabelled_outer_w1`, the strip yields an id that is not in `corridors`, `key` stays `None`, and the interval is silently dropped from the corridor metric — recreating exactly the bias the whatif hunk exists to fix, but only on the edges whose outermost lane is unlabelled (so the bias is also non-uniform across the corridor). Either attribute by edge membership, pick the donor from the labelled-lane chain when one exists, or at minimum have whatif count/log unattributed `*_w1` intervals so the gap is visible.

**Question**

2. `scripts/chicago/mknetvariant.py:269-271` — donor selection moved from "outermost *labelled* lane" to "outermost lane of the edge", and the clone is a full deep copy. That makes it newly possible to clone a portal lane (`origin`/`exit` flags copy over). A cloned origin lane is inert (demand names origins by id), but is a duplicated `exit` portal on the offset shape acceptable to the loader/engine semantics? If yes, fine; if not, the drop-lane path already has the portal-exclusion logic to borrow.

3. Doc/behavior: the fixed operation ordering in the docstring now matches the code (retime → speed → drop → add), and the claim that a later-invocation `--drop-lane` sees only labelled lanes is accurate (`mknetvariant.py:176` still filters on `ids`). No issue — confirming the claims were checked.

**Nit**

4. `scripts/chicago/test_mknetvariant.py:119` — `tempfile.mkdtemp()` per `run()` call, never removed; three tests leak three dirs per run. `tempfile.TemporaryDirectory()` or `addCleanup` is one line.

5. `scripts/whatif.py:241` — the strip loop can mis-attribute a *genuine* base-network lane whose id happens to end in `_w1` and is absent from the label map. This risk pre-existed with the single strip; the loop widens it only marginally (stacked strips). Not worth hardening now, just noting it's known.

**Determinism / contract** — clean. `sorted(by_edge.items())` keeps clone order deterministic; the `edges` set is membership-only; no wall clock/RNG; no NATS subject or payload touched; network-variant output changes are covered by ADR-0012's whole-network-replacement stance and the tests pin the new naming (`_w1_w1`), which whatif now understands.

Test logic itself checks out: I traced both passes by hand (feeder wiring `up_0 → e1_1_w1_w1`, `e1_* → e2_1_w1_w1`, cumulative offsets 2·W and 3·W) and the assertions match what the code produces.

REVIEW-COMPLETE
