- **blocker** — `engine/cmd/demosrv/geojson.go:224`: every part fetch runs heavyweight `scenario.Load`, then `c.path` at line 238 repeats it before checking the warm cache. Since `scenario.Load` compiles the entire network, an N-part city load performs 2N full compilations plus repeated multi-GB reads, defeating ADR-0018’s city-scale path.
- **should-fix** — `engine/cmd/demosrv/main.go:438`: `Cache-Control: no-store` does not stop `http.ServeFile` honoring `If-Modified-Since`. A same-second manifest regeneration can return 304 and resurrect stale part URLs, violating the documented generation-atomicity guarantee.
- **should-fix** — `contracts/asyncapi.yaml:165`: `stateSigReq` declares `signalTableReply` as a message on `ts.{run}.state.sig.req`, although replies are explicitly sent only to `sigReplyInbox`. The sacred contract currently models the reply on two subjects.
- **should-fix** — `docs/kb/decisions/ADR-0006-nats-message-contract.md:295`: the ADR still calls replay “publish-only,” but `NewPublishBus` now subscribes replay players to `state.sig.req`. Amend this to mean publish-only with respect to intents/control.

REVIEW-COMPLETE
