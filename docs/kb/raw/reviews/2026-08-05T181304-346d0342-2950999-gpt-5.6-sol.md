- **blocker** `viz/src/splash.ts:21` — `BAKED_FEATURED_URL` is always set to same-origin `/baked/...`, but demosrv has no baked route (`engine/cmd/demosrv/main.go:397`). The primary landing-page CTA therefore 404s during normal demosrv operation, as `deploy/README.md:11` explicitly acknowledges. Probe and fall back, or serve the bake.
- **should-fix** `scripts/show/mksite.sh:76` — Post-bake Brotli compression of `network.geojson`, paired with `viz/public/_headers:8`, contradicts ADR-0023’s explicit deployment contract that `Content-Encoding: br` applies only to chunk objects (`docs/kb/decisions/ADR-0023-baked-replay-pipeline.md:533`). This consequential artifact/deployment-contract change needs an ADR amendment.
- **should-fix** `viz/index.html:191` — The static Pages landing page prominently links to `/demos.html`, although the bundle knowingly ships that page without a backend (`scripts/show/mksite.sh:4`); it immediately requests `/api/demos` (`viz/src/demos.ts:145`) and fails. Hide or retarget this CTA on the static deployment.
- **should-fix** `scripts/show/mksite.sh:24` — The documented no-argument workflow defaults to a developer-specific absolute path, so it fails in every other clone. Derive from `$ROOT` or require `--baked`.
- **nit** `scripts/show/mksite.sh:66` — `--out` feeds an unguarded `rm -rf`; reject empty/root/repository paths before deletion.

REVIEW-COMPLETE
