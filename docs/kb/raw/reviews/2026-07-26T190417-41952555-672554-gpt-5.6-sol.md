- **blocker** `engine/natsio/driver_test.go:1199` — Pre-sentinel quiescence is still timing-based, not “provable.” `LastObsTick` is stored before response computation/publication (`engine/natsio/driver/driver.go:342`), so a driver descheduled or computing for >50 ms can pass `quietWindow()` with an unfinished warm-up response. Its post-`sentMark` intents can then satisfy the sentinel watermark at `engine/natsio/driver_test.go:1265`, recreating the backlog false-pass this patch targets. Track a completed-observation watermark after `onObs` finishes, or otherwise synchronize actual response completion.

REVIEW-COMPLETE
