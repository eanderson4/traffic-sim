Review of the staged M3 diff (benches + acceptance tests, small production surface: `Bus.BufferedIntents`, `SubjectCtlAll`, test-server `MaxPayload`, `tsibTickOff` refactor). I verified the pinned wire strings against production (`bus.go:49-54`), the ack-before-obs ordering the tap relies on (`run.go:184-185`), the bench harness structure (warm-up once per invocation, `e.Step()` after the lag check — the batch-stats math and lag-proxy-0 assertion are correct), and the driver's flush semantics (`driver/driver.go:414-423`).

**Blockers:** none.

**Should-fix**

1. `engine/natsio/driver_test.go:1278,1304` — the measurement and tail-flush loops of `TestBatchAppliedLagBoundary` discard the drained intents: unlike the warm-up/quiescence phases (lines 1151, 1187, 1215) and the production loop (`run.go:177`), they never call `e.EnqueueIntent`, so nothing is ever actually applied. The reported "application tick" (`e.Tick+1`) is what *would* happen had they been enqueued, and the cross-leg timing comparison stays valid (arguably tighter — the engine trajectory becomes intent-independent and identical across legs/modes). But the test and BENCHMARKS.md repeatedly say "COMPLETE-application," which overstates what occurs, and this design choice is the one load-bearing mechanic the otherwise exhaustive comments never mention. Either enqueue (matching production semantics) or add one sentence documenting the deliberate omission and its identical-trajectory benefit.

2. `engine/BENCHMARKS.md:352` vs the detail section (line ~97 of the same file): the KB consequence flag says v2 splits "~5–9%" at straddling pace; the detail section says 16–35/300 ≈ "~5–12%". Same measurement, two numbers — reconcile.

**Nits**

3. `engine/natsio/driver_test.go:1385` — comment says "the 10 ms pace"; `scalePace` is 100 ms (line ~1394). Stale.

4. `engine/natsio/driver_test.go` (~1415) — `v2Fast.splits == 0` is a hard `t.Fatal`. Straddle probability ≈ stream-duration/pace (~11%/tick on this box, 16–35 hits/300); on a substantially faster box it shrinks toward occasional zero over 300 ticks — machine-dependent flake latent in the required-positive assertion. Acceptable under the repo's no-hypothetical-hardening bar; noting for the record.

5. `engine/natsio/driver_test.go:920-928` (`newLagTap`) — the tap is created *after* `driver.New` in `TestBatchAppliedLag`, so it can miss the first tick(s); the first v2 intent then pairs against `lastObs = 0`, producing one garbage-large sample per leg. Nearest-rank p99 over ~400 samples absorbs it, but the "must not miss early ticks" comment overstates what the `Flush` achieves — only the SUB-before-traffic race is closed, not the driver's head start.

6. `bench_intent_test.go` `BenchmarkIntentEncode` — TSIB `SetBytes` counts one header even at 30k where production emits 2 batches (2 headers). Off by 24 B; negligible.

**Question**

7. `engine/natsio/server.go:36` — raising every test server to 4 MiB mirrors production (`bus.go:374`, `cmd/serve/main.go:271`, `cmd/replay/main.go:80`), so fidelity improves; confirm no existing test relied on the old 1 MiB default rejecting oversized frames (the `TSIBMaxRecords` comment still describes a "1 MiB per-message discipline" that no broker in the test suite now enforces).

**Determinism/contract:** clean. No subject or payload changes (`SubjectCtlAll` names an existing wildcard; `BufferedIntents` is read-only Go API; the `tsibTickOff` change is byte-identical refactor, round-trip test updated). No engine-core touches. The `touched` map in `attribute` is pointer-keyed membership only, never iterated — no map-order risk. Bench tick-patching of the reused `nats.Msg` is safe (data copied into the connection buffer synchronously; `wg.Wait` orders re-patches).

REVIEW-COMPLETE
