- **blocker** — `viz/src/nats-client.ts:93`: zero replies/timeouts are swallowed as success, while `viz/src/main.ts:361` retries only partial/gapped responses. A dropped entire reply set therefore never retries. Since paused replay intentionally stops signal broadcasts (`engine/natsio/player.go:399`), a client attaching during pause can remain permanently without signals. Return completion/failure status and retry boundedly when no complete table was installed.
- **should-fix** — `engine/natsio/probe.go:60`: the five-second timestamp tolerance allows a foreign same-run broker to pass identity. `engine/cmd/demosrv/supervisor.go:297` can then return success before the newly spawned child’s bind failure closes `done`. This preserves the wrong-broker race for rapid concurrent starts; use a per-spawn nonce or wait for positive child-owned identity.
- TypeScript checks and tests pass. Go tests could not run because the review sandbox cannot create Go’s temporary build directory.

REVIEW-COMPLETE
