should-fix — `engine/natsio/driver_test.go:1446`: Retrying up to 12 times until one zero-overrun sample passes masks intermittent production regressions. A change that makes the 10 ms cadence fail frequently—but occasionally succeed—now passes, despite overruns being normal-operation performance failures. Retry only explicitly identified infrastructure failures, or cap/record the aggregate failure rate rather than accepting the first clean attempt.

REVIEW-COMPLETE
