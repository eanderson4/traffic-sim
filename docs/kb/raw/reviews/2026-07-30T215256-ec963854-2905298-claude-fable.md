Context read: `pickSuccessor` (engine.go:888), `leaderAt`'s held-turn handling (engine.go:531–556), the `rowGate` in-box caller (rightofway.go:89), and the Car type constants (Length 5, S0 2 — the fixture geometries check out: the 6.75 m spacing is below the 7 m jam spacing, so the sealed queue cannot compact, and the tail at s=0.5 leaves −4.5 m of room). Verdict below.

---

## Review

Traced the new arm end-to-end: `strandStuck` → `jammedAtJunction` in-box arm → `exitWalk(v, lane, false, false)`, plus all four fixtures against the IDM constants (Car: Length 5, S0 2 ⇒ `need` = 7). The fixtures' arithmetic is sound: the sealed queue (spacing 6.75 < 7 m jam spacing) cannot compact, the in-box vehicle creeps to gap-S0 and stalls at s=13.5 still inside the box, and the discriminator cases produce free = 25 / −4.5 / 2-capped-by-hold respectively. The wrapper split (`exitBlocked`/`boxBlocked` delegating to `exitWalk`/`boxWalk`) is behavior-preserving for all pre-existing callers since `turnSpent=true` reproduces the old unconditional `probe.HeldTurn = 0`, and `holdSeal` can only be set on the `downstreamHold` break, so `(true, holdSeal)` at `free < need` is the only path that reports it — correct.

**Blockers:** none found.

**Should-fix**

1. `engine/rightofway.go:89` — the in-box motion gate (`rowGate`, internal-lane branch) still calls `exitBlocked(v, lane, true)` ⇒ `turnSpent=true`, but its first hop is the *same* turn-consuming crossing the escape's in-box arm now honors with `turnSpent=false`. With a controller-set `HeldTurn` on a multi-successor internal lane, the wall/mergeThreat walk classifies against the route/default branch — the exact wrong-branch class this diff removes for the escape, and which `leaderAt` (engine/engine.go:547–556) already spends on the first hop. Consequence: a held-turn vehicle can be walled inside the box against a threat on a branch it won't take, or not walled against one it will. Same "moot on netimport, internal lanes are single-successor" mitigation applies, and it's pre-existing rather than a regression — record and defer per the triage bar, but the diff's own doctrine ("must not classify against a branch the vehicle will not take") now applies to a sibling caller it doesn't cover. Relatedly, the `exitWalk` doc (engine/rightofway.go:281–282, "Every entry-gate caller passes true") is inaccurate: rightofway.go:89 passes true and is an in-box caller, not an entry-gate one.

**Nits**

2. `engine/gridlock.go:196` / ADR amendment — the amendment's opening paragraph cites the condition as `exitBlocked(v, lane, false)`, but what ships is `exitWalk(v, lane, false, false)`; the refinement paragraphs later correct this. Fine as a historical narrative, mildly confusing as a spec — the "Amendment" header text is what a future reader will quote.

**Questions**

3. `engine/rightofway.go:232–241` (`boxWalk` foes-inside → `(true, false)`) — the red-is-never-a-trigger doctrine is enforced only along v's *own* walk. A foe vehicle pinned inside the conflict box by *its* downstream red for 300 s strands **v** (foe-inside reads as a capacity block), while the foe itself is holdSeal-protected. Indirect signal-caused strand; likely acceptable (a foe parked in a conflict zone for 300 s is a pathology regardless of cause) and plausibly "too early to need this" — flagging for the doctrine's record, not asking for code.

**Checked and clean**

- Determinism (ADR-0005): the new decision path iterates only s-sorted `lane.vehs`, ordered `Successors`/`Foes*` slices and `e.order`; no map iteration on a sampled path (`mergeThreat`/`resetStuckBehind` maps are membership-only, work order slice-driven, pre-existing), no wall clock, no RNG, no new float-accumulation reordering (`free`/`dist` order unchanged).
- Contract (ADR-0006): no NATS subject/payload/keyframe format change; `Stats.Stranded` semantics widen (in-box strands now counted, red-capped entry strands now excluded) and the ADR amendment in this diff documents both, including the acknowledged pre-existing entry-arm behavior change.
- Entry arm now discarding `holdSeal` is a deliberate, tested doctrine symmetrization (`TestInBoxDiscriminatorReadsTheSeal` fourth case pins it).
- Coverage composes: a seal lying beyond the *next* box is invisible to the in-box arm (walk stops at the first internal lane), but the vehicle creeps to that box's approach where the entry arm's `boxWalk` sees it — no dead zone.

REVIEW-COMPLETE
