- **blocker** `engine/keyframe.go:322` — `log.Printf` uses the standard logger’s wall-clock timestamp, introducing a time syscall and nondeterministic output inside the simulation core. This directly violates ADR-0005’s “no wall clock or time syscalls inside the sim core.” Surface the migration warning through the caller/edge layer or a deterministic warning mechanism instead.

REVIEW-COMPLETE
