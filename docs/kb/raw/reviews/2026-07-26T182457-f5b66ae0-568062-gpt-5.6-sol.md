- **blocker** — `engine/natsio/driver_test.go:1278`: the authoritative applied-lag test drains intents but never calls `e.EnqueueIntent(k)` before `e.Step()`; the tail flush repeats this at `engine/natsio/driver_test.go:1304`. It measures arrival/drain timing against a world where controller intents are never applied, not the production `DrainIntents → EnqueueIntent → Step → applied_tick` path. Therefore the M3 applied-lag acceptance claim in `engine/BENCHMARKS.md:211` is not established.
- **should-fix** — `engine/natsio/driver_test.go:843`: `lagTap` assumes a wildcard subscription creates a total “broker order” across observations/acks published by the engine connection and intents published by the driver connection. NATS ordering is per publisher; one subscriber callback cannot recover causal ordering across those publishers. Pairing at `engine/natsio/driver_test.go:896` can therefore associate an ack with the wrong response, beyond the documented ±1 guarantee.
- **should-fix** — `engine/BENCHMARKS.md:344`: “per-vehicle applied lag is never worse” overstates aggregate p50/p99 comparisons and directly conflicts with `engine/BENCHMARKS.md:264`, which acknowledges early v2 vehicles apply before the uniform batch. The FIFO test records distributions, not paired vehicle outcomes, so “no vehicle anywhere applied later” at `engine/BENCHMARKS.md:274` is also unsupported. State only the ADR-required p50/p99 result.

REVIEW-COMPLETE
