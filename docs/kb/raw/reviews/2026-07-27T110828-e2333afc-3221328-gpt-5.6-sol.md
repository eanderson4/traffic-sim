- **blocker** `scripts/chicago/fwsweep.sh:61` — Failed runs are ignored with `|| true`, while prior metrics are not removed. Reusing an output directory can therefore report stale metrics as the result of a failed run and label it “done.” Delete outputs first and fail the sweep on nonzero exit.
- **blocker** `scripts/demos/merge-rerun.sh:34`, `scripts/demos/town-rerun.sh:40` — These durable A/B pipelines accept every zero-exit `whatif.py` run, but `scripts/whatif.py:224` does not reject the new run-invalidating observation-loss or coasting warnings. The generated reports/quiz can therefore rank simulations the patch itself declares void.
- **should-fix** `engine/natsio/contract.go:976` — `ObsErrs` equates each failed frame with uncontrolled coasting, contradicting ADR-0008’s two-tick hold-last bridge. An isolated failure remains controlled; the invalidating condition is a sufficiently long consecutive outage. The counter/log and `record-hero.sh:64` currently produce false invalidations.
- **should-fix** `engine/natsio/intentload_test.go:187` — `TestObsFrameSizeCliff` has no assertions and never tests `ObsErrs`; it passes if every frame succeeds, every frame fails, or the new counter regresses. Add a boundary assertion and a contract-level counter test.
- **should-fix** `scripts/demos/meter-sweep.sh:42` — The output pod is never cleaned. On rerun, `cp -r ... "$POD/base"` copies into the existing directory, leaving the stale top-level scenario active and nesting the newly generated one beneath it.
- **should-fix** `scripts/chicago/mkquiz.py:316` — The page still claims “One of them measurably helps” and `scripts/chicago/mkquiz.py:346` says exactly one winner, while the newly supported merge/town payloads explicitly contain multiple significant upgrades and ask which helps most.
- **nit** `engine/cmd/serve/main.go:599` — Iterating the `SubDrops` map makes diagnostic ordering nondeterministic. Sort subjects before printing for reproducible run logs.

REVIEW-COMPLETE
