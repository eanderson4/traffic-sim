• The diff is 12.5k lines. Let me page through it, then check context files.

• I have the full substantive diff. Let me verify a few load-bearing details in the actual files.

• The latest archived round's findings appear addressed in the final diff. Verifying ADR-0014 citations and mkquiz's all-or-none enforcement against the staged tree.

• Two quick line-number checks in the staged files for accurate citations.

• Review of the staged diff (12,551-line patch; ~1,285 substantive lines — the rest is `docs/kb/raw/reviews/` archives of the four rounds that gated this commit, which I used to check triage). Verified against the staged tree: `scripts/external-review.sh`, `engine/metricsjson.go:53` (`time_loss_s` is a non-omitempty float, so `tr["time_loss_s"]` in whatif.py can't KeyError), ADR-0014 §3 (`docs/kb/decisions/ADR-0014-observability-metrics.md:95` does pin "comparison tooling drops partials" — the §5 mis-citation from round 4 is fixed), and mkquiz's all-or-none check (`mkquiz.py:568-576`, which correctly still fires if `--omit-diagram` parts a fully-illustrated scenario).

  Prior rounds' blockers are genuinely fixed in what ships: whatif multi-set summing now refuses with a `--set` escape, `mean_time_loss_s` is unchanged with the censored-inclusive figure added as a separate bound (matching the KB decision it previously contradicted), `CHI_SKIP_MAP` no longer deletes tracked files, `$work/in` is split off and frozen before reviewers start, window disagreement fails closed, and the None-sort crash is handled.

  **Blockers** — none.

  **Should-fix**

  1. `scripts/chicago/corridorspeed.py:85` — no `--end-tick`. mkcongestionmap.py:86 gained `--end-tick` for exactly the reason in its help text ("a metrics file from a longer run otherwise draws a different window than the table beside it claims"); corridorspeed only drops partials, so the same longer run still silently widens its window past the published 12,000. The new `measured over ticks X–Y` line (:125) discloses rather than enforces — against this commit's own fail-closed standard. Add `--end-tick` or state the asymmetry as deliberate.

  2. `scripts/chicago/mkquiz.py:431,442,445` — sidecar `bands`/`empty_rgb` unvalidated: `prov.get("bands", [])` renders a legend-less colour map with no complaint (the "undecodable picture" the missing-sidecar `sys.exit` at :416-421 exists to prevent), and `rgb[0]`–`rgb[2]` indexing dies with a bare `IndexError` on a hand-edited sidecar while every other sidecar failure exits legibly. Carried over from round-4 nit 4; still present.

  **Nits**

  3. `scripts/chicago/mkquiz.py:506-513` — a typo'd `--omit-diagram` value silently no-ops; warn on unmatched entries. (Round-4 nit 5, still present.)
  4. `scripts/whatif.py:162-176` / `corridorspeed.py:60-66` — the multi-set guard inspects pre-warmup intervals too: a second set that ends before the warmup cut still exits though nothing post-warmup could double-count. Over-strict, harmless. (Round-4 nit 3, still present.)
  5. `scripts/show/mkcongestionmap.py:120-121` (`colour_for`) — the `limit_ms <= 0 → EMPTY` branch is unreachable; the draw loop filters `lim < --min-limit` (default 0.1) first.
  6. `scripts/show/mkcongestionmap.py:62-63` — `iv["sum_dist_m"]`/`iv["sum_time_s"]` KeyError on a malformed interval, while corridorspeed.py:96-97 uses `.get` defaults; the two tools that "share two rules" fail differently on the same bad input.
  7. `scripts/show/mkcongestionmap.py:160` — coverage numerator `resident` counts measured lanes regardless of drawability in this network (below `--min-limit`, degenerate shape) against a drawable-only denominator; slightly overstates coverage. Heuristic and documented as such — fine to leave, noting it.

  **Questions**

  8. `docs/show/diag/chi-loop-urban__setup.png.json` — the PNG bytes are unchanged in this commit while the sidecar gains `end_tick`/`last_interval_end` provenance. Round 4 asked whether a regeneration under the new flags is byte-identical (plausible: the metrics file has no intervals past 12,000, so `--end-tick 12000` changes nothing); no resolution is visible in the diff. Worth a verified answer since stale-artifact is the failure class this commit exists to kill.
  9. `scripts/external-review.sh:125-126` — `$work` itself stays writable and its path is derivable from the `$work/in/...` paths in the brief, so `sol.md` still has no integrity backstop beyond not being the granted directory. The ADR addendum's "guardrail, not sandbox" framing accepts determined escape; confirm that is the intended resting point for the *other* reviewer's file specifically.

  Determinism/contract scope: no Go, no NATS subjects or payload schemas touched — ADR-0005/0006 surface is nil. Accumulation is file-order; the sidecar is `sort_keys`; `date` in external-review.sh is archival naming only. The whatif report JSON's new keys (`interval_window`, `time_loss_bound_s`, `censored_frac`) are a harness artifact, not a message contract; the ADR-0014 interpretation question for the censored-inclusive bound is answered in the docstring with the completed-only mean left untouched.

  REVIEW-COMPLETE

