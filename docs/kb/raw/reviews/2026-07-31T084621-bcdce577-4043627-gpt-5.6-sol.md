- **blocker** `engine/engine.go:95`, `data/scenarios/pod-chi35/base/scenario.yaml:1`, `data/scenarios/merge-pod/base/scenario.yaml:3` — Flipping the default silently changes all published show scenarios because their manifests omit `adaptive_routing`. The documented reproduction command now produces adaptive—not published static—results under unchanged scenario hashes. Stamp `adaptive_routing: false` into every static-baseline show arm.
- **should-fix** `docs/kb/decisions/ADR-0036-congestion-adaptive-routing.md:231` — “Every run artifact self-describes the regime” is false: offline metrics omit routing parameters (`engine/metricsjson.go:18`), and what-if reports record neither resolved params nor engine build (`scripts/whatif.py:615`). Persist the resolved routing mode in these artifacts or narrow the identity claim.
- Tests could not run because the read-only sandbox prevents Go from creating its `/tmp/go-build*` directory.

REVIEW-COMPLETE
