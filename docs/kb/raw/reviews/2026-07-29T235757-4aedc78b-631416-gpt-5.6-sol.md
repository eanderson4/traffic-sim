- **should-fix** `scripts/whatif.py:240` — Corridor attribution only succeeds when suffix stripping reaches a labelled donor. `scripts/chicago/mknetvariant.py:261` explicitly allows cloning an unlabelled outermost lane, so those added lanes remain silently excluded from corridor metrics. Either derive corridor identity from the widened edge or reject partially labelled corridor edges.
- **nit** `scripts/chicago/test_mknetvariant.py:53` — `tempfile.mkdtemp()` directories are never removed; use `TemporaryDirectory()` to avoid leaking three directories per test run.

REVIEW-COMPLETE
