- **blocker** `engine/cmd/demosrv/geojson.go:151` — Part URLs omit the scenario hash. Each request resolves the *current* scenario independently, so an edit between part fetches silently combines different network generations. `viz/src/netload.ts:57` also has no stale-generation retry. Hash-pin URLs and refetch the manifest on a stale-part 404.
- **blocker** `engine/cmd/demosrv/supervisor.go:316` — Readiness accepts any existing `{run}/meta`, including `done`, `aborted`, malformed, or stale entries. `engine/cmd/demosrv/identity_test.go:75` explicitly proves metadata without `status:"running"` passes, defeating the foreign/stale-broker protection.
- **blocker** `engine/cmd/demosrv/supervisor.go:44` — The identity timeout is 120 seconds, while `engine/cmd/demosrv/main.go:155` documents roughly 400 seconds for the largest advertised network’s startup path. A valid large demo can be killed before registration/readiness completes.
- **blocker** `engine/cmd/demosrv/supervisor.go:25` — This imports `nats.go` directly outside `engine/natsio/`, violating the repo’s explicitly confined ADR-0006 dependency exception. The new NATS probe belongs behind `engine/natsio`.
- **blocker** `engine/cmd/demosrv/geojson.go:22` — This introduces a consequential engine↔viz manifest/part contract, but the exact staged patch contains no ADR or KB update. The existing `ADR-0018-chunked-geojson.md` is untracked and therefore does not ship.

REVIEW-COMPLETE
