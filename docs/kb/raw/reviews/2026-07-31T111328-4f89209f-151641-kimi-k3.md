• Let me verify the viz test actually passes as written.

• Tests pass (12/12, including the new ws-scheme test). Findings:

  **Blockers** — none. No engine, NATS subject, or payload changes; determinism and contract concerns don't apply. No ADR required.

  **Should-fix**

  1. `viz/src/main.ts:1400-1403` — the "this host has none" hint fires on *any* `main()` rejection when `?bake=` is absent, not just connection failures. On a host that does have a live engine, a network-fetch, parse, or config error now gets a false "this host has none; go to /quiz" appended. Either gate on the error being a ws/connect failure or soften the wording ("if this host has no live engine, …").

  **Nits**

  2. `viz/src/main.ts:1403` — points to `/quiz`, but every other reference in the repo uses `/quiz.html` (`scripts/serve-baked.py:108`, `docs/show/README.md:52`, `scripts/show/build-quiz.sh:144`). Extensionless resolution works on GitHub Pages but 404s on plainer static hosts; `/quiz.html` works everywhere.
  3. `viz/src/main.ts:1397` / `viz/src/nats-client.ts:28` — the hint only appears after nats.ws's `connect()` burns its default retry budget (~10 attempts × ~2 s backoff) before rejecting, so a Pages visitor stares at "connecting…" for tens of seconds first. Acceptable for now, but a fail-fast path for the known-static deploy would be the real fix.
  4. `viz/src/config.ts:102` — default param `protocol = "http:"` silently makes any non-https scheme (`file:`, extension contexts) choose `ws:`. Fine as-is; just noting the fallback is deliberate, not exhaustive.

  **Questions** — none.

  Verified: `favicon.svg` / `favicon-32.png` exist in `viz/public/` (vite static root, so they deploy with the site); `quiz.html` is generated by `scripts/chicago/mkquiz.py` and both copies were edited identically; added template lines contain no `%` so the `%(css)s` formatting is safe.

  REVIEW-COMPLETE

