- **blocker** `viz/src/demos.ts:130` — The menu reads only `reg.demos`; recordings are never rendered. Even if rendered, activation is hardcoded to `/api/demo/{id}/start` at `viz/src/demos.ts:102`, leaving the new replay start route unreachable from the advertised menu.
- **blocker** `engine/cmd/demosrv/supervisor.go:177` — Replay readiness checks only the WebSocket port. That port opens before `NewPlayer` finishes indexing the recording and before the HTTP control listener starts. Navigation can therefore occur early, and the one-shot probe at `viz/src/replaypanel.ts:207` permanently hides the controls on failure.
- **should-fix** `viz/src/snapshots.ts:156` — `SeekGate` detects only backward seeks. A forward seek is accepted as normal progression, so `viz/src/main.ts:465` retains pre-seek interpolation and congestion state, briefly interpolating across unrelated states and deriving bogus speeds.
- **should-fix** `engine/cmd/demosrv/replay.go:56` — The visualization’s `dt` and network come from the current mutable `scenarioDir`, while replay execution uses recorded `RunMeta`. Editing the scenario after recording can produce a deterministic replay displayed against the wrong network and timestep; the authoritative recorded metadata should drive both.

REVIEW-COMPLETE
