• I've read the full diff. Now checking repo context: the kernel's signal chain semantics, the sig table publication path, and contract helpers the new code relies on.

• Kernel chain semantics confirmed. Now checking the hello/verb contract handling and the sig-table responder.

• One last check — the heartbeat handler and whether a nil-payload heartbeat at snapshot rate is safe for a director-grant controller.

• Two final checks: serve's cleanup path (the bracket's fidelity gate depends on the "sigctl: done" line) and `spec.Net.Path` resolution under `-scenario`.

• One verification pass: build and run the new package's tests against the staged tree.

• Review done. I read the full diff, verified the controller's claims against the kernel and contract code, and ran `go vet` + `go test ./engine/natsio/sigctl/` on the staged tree (vet clean, all tests pass incl. `TestControllerLive`, 1.1 s).

  **Verified against the repo (no findings):**
  - Chain-contiguity boundary matches the kernel exactly: controller's `applied > holdUntil` new-chain rule ≡ `engine/sigctl.go:291` (`last.until >= e.Tick`); the equality case is pinned by `TestChainBoundaryEquality`.
  - `MaxChainTicks` math matches `engine/sigctl.go:80-85`; `phaseElapsed`/`PhaseAt` mirror `engine/signal.go:139-148` including offset-wrap semantics.
  - No contract change: consumes `state.sig`/`state.snap`/`state.sig.req`, publishes verbs on the existing `ts.{run}.ctl.verb.{id}` and heartbeats on the existing heartbeat subject. `signal_set` requires the director grant (`contract.go:772`) and the hello requests it; the proposed VerbReply echo is documented as *not* implemented in the ADR addendum — correct discipline.
  - Chunk accumulator matches the publisher's `sig_chunk` format (`bus.go:490`); reply confinement requires a default `_INBOX.` reply subject and the controller uses `nats.NewInbox()` — compatible (`bus.go:470`).
  - Signal-free runs don't wedge `Ready()`: the bus always answers `state.sig.req` and emits an explicit empty frame (`sigframe.go:124-128`), and `installTable(nil)` releases the wait.
  - Determinism: decision loop iterates sorted program ids; `presenceCounts` output is order-independent integer adds; wall clock touches only the standalone watchdog/liveness, never sim-affecting state; no RNG. Sim-affecting nondeterminism is limited to the documented pace-0 drain-lag skew, which the record plane captures for replay.
  - Bracket gate: the `sigctl: done — verbs=…accepted=…rejected=…` regex matches `Close()`'s format string, and `defer out.cleanup()` (`cmd/serve/main.go:579`) runs on normal exit, so the fidelity gate's evidence line exists.

  **Blockers:** none.

  **Should-fix:** none new — the substantive issues I went looking for (walk-past-target, unclamped optimistic `holdUntil`, next-boundary assumption at pace 0, standalone `Ready()` gap, detector multi-marking) are all already recorded in the ADR's round-1/2/3 deferral lists with accurate characterizations.

  **Nits (new, all defer-by-default under the triage bar):**
  - `live_run_test.go` divergence loop checks each `SigLog` directive's full requested span, but on supersede the kernel truncates the prior hold (`engine/sigctl.go:302`) without retro-truncating the record — a superseded directive's stale tail overlaps the next directive's enforcement and could in principle score a false "diverged". Clamping each span at the next directive's tick for the same program would make the M2 acceptance assertion exact. Test-strength only.
  - `sigctl.go` `LoadGeom`: a zero-length first shape segment leaves `Dx,Dy = 0,0`, degenerating the approach-side test into a full circle (that link never gaps out — the round-1 failure mode, one link wide). A one-line `n == 0 → skip link` would match the no-shape treatment. No evidence netimport emits such shapes.
  - `cmd/sigctl/main.go`: the "hold must exceed cadence + renew-below" constraint is doc-only; a violating flag set silently lapses every hold. Cheap to validate at flag parse.

  **Questions:** none beyond the two already recorded for the contract pass (signal grant vs director grant; VerbReply applied-tick echo).

  REVIEW-COMPLETE

