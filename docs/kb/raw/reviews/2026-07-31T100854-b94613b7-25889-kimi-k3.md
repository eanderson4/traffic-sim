• Now let me examine the relevant repo files for context.

• Let me verify the test suite actually passes under the flipped default — that's the load-bearing claim of this change.

• Tests are green. Now checking whether the generator-pin claim covers all published docs/show arms.

• Review complete. Findings below.

  **Verification done:** read the full patch; traced the restore/replay paths, map-iteration risk, and hash claims in the tree; ran `go vet ./...` and `go test ./...` in `engine/` — all green under the flipped default.

  ## Blockers

  None.

  ## Should-fix

  - **Generator pin coverage is narrower than the ADR claims.** The addendum (ADR-0036, new lines ~77–79) says the static pin is "emitted by the scenario generators … so regeneration keeps it," but only three of six scenario.yaml writers were pinned. Unpinned: `scripts/chicago/mkscenario-portal.sh:37`, `scripts/chicago/fwsweep.sh:51`, `scripts/import-city.sh:129`. I confirmed the *currently published* docs/show arms are covered (chi-pod variants are `sed` copies of the pinned `mkscenario.sh` base — `pod-chi-loop.sh:47`; merge-pod and bottleneck-town pinned in the diff), so nothing published silently flips today. But any future arm built via the portal pod or freeway sweeps regenerates without the pin and silently changes regime — exactly the ambiguity the pin mechanism exists to prevent. Pin them too, or scope the addendum sentence.

  ## Nits

  - `engine/engine.go:421-427` — the lazy-init comment documents the pre-first-Step `routeNextHop` divide-by-zero hole for struct-literal engines, but not the symmetric `MarshalState` hole: a struct-literal flag-on engine marshaled before its first `Step()` panics indexing nil `ttSnap` at `engine/keyframe.go:238`. Test-only surface either way; one sentence would close it.
  - Log routing is inconsistent: `engine/natsio/replay.go:114`, `bake.go:73`, `run.go:109` use the global `log` while `player.go:210` and `player.go:502` use the injected logger. Same message, two sinks.

  ## Questions

  - `RestoreNotice` is surfaced on every production restore path (run/bake/player/replay — all call sites checked), and replay/bake restore from the recorded `meta.Spec` (`natsio/replay.go:110`, `player.go:194`), so pre-flip recordings carry `AdaptiveRouting: false` and never trigger the regime switch or the notice. Correct as designed — confirming that was the intent for `-state-in` of old static keyframes (notice fires only there).
  - The bool→*bool canonical-hash claim (addendum: "no such manifest existed before this commit … so no existing scenario hash moves") is not verifiable from the tree — `data/` is gitignored. The yaml.v3 byte-equivalence reasoning (nil≡omitted, `*true`≡`true`) checks out; the existential premise rests on the committer's say-so.

  Determinism audit clean: no map iteration feeds any sampled path (`routeEpochs`/`routeTabs` are keyed-access only), the lazy init and EMA relax iterate fixed lane order, no wall clock in the kernel (`RestoreNotice` is edge-logged only, never serialized or CRC'd — confirmed against the fold at `engine/engine.go:1069`), and the restore invariant the Step comment relies on (all restore paths go through `NewEngine`, `keyframe.go:305`) holds.

  REVIEW-COMPLETE

