All spot-checks against the repo pass (config.ts buffer default, SnapshotBuffer `push` dedup, main.ts `edgeB`/`net.frame`, TSSF layout, player.go end-hold/terminal-frame/TSSG notes, ADR-0020's two preconditions, the §9 size arithmetic). Findings:

---

## Review

**Scope note:** 61k inserted lines, but everything except ~690 is generated review archives under `docs/kb/raw/reviews/`. Substantive content: ADR-0023 (new, PROPOSED), README/deploy-doc clarifications, a cronjob comment, KB index entry. No code, no NATS contract change ships (`contracts/asyncapi.yaml` is modified but unstaged). The commit is gated only because it touches `deploy/`.

### Should-fix

1. **ADR-0023 §2 dt-validity bracket contradicts itself** — `docs/kb/decisions/ADR-0023-baked-replay-pipeline.md:187-190` (patch lines 282-285): "the bake REJECTS any dt whose derived rate falls outside the 1–2 Hz band (**dt ∉ [0.25, 1.0] s is unsupported** for now — every current scenario uses 0.1)". dt = 0.1 is outside [0.25, 1.0], so the bracket as written rejects the only dt in use — the parenthetical contradicts the sentence it's in. The intended constraint is on the derived frame **interval**: `bakeEveryTicks × dt ∈ [0.5, 1.0] s` (which 0.1 satisfies: 5 × 0.1 = 0.5 s). This is the exact sentence an implementer will turn into a validation check; fix the bracket before implementation.

### Nits

2. **ADR-0023 §2 orphaned clause** — `ADR-0023-baked-replay-pipeline.md:191-193`: "…in ticks it is `120 × bakeEveryTicks`). — the top of the agreed 1–2 Hz band, because the viz's lerp machinery…". The "— the top of…" fragment originally attached to "Bake rate **2 Hz**" and got severed by the inserted cadence-derivation text; the paragraph doesn't parse as written.

### Questions

3. **ADR-0021/0022 sequencing** — this commit ships the review-round archives for ADR-0021 (`…b359fb90-adr0021-*`) and ADR-0022 (`…adr0022-typemap-*`), and the staged INDEX.md jumps 0020 → 0023, while `ADR-0021-od-demand-buildings.md` / `ADR-0022-us-urban-speed-typemap.md` sit untracked with their index entries unstaged. Intentional commit ordering (numbering is reserved by the untracked files), but confirm those ADRs aren't orphaned — the archives referencing them land first.

No blockers. Determinism discipline in the ADR itself is sound (CRC-abort bake, floor-not-round angle quantization avoiding the 256 overflow, content-addressed keys covering seed + record digest + tool version, guarded u32/u8 narrowing). The deploy/README tag-entropy fix and the cronjob comment (`activeDeadlineSeconds: 300` job merely restarts the deployment; the 10-wall-hour claim depends on `-pace 1`/dt as stated) are correct.

REVIEW-COMPLETE
