- **blocker** `engine/rightofway.go:103` — The seam gate checks only the immediate internal successor. In a consolidated chain `internal → uncontrolled internal → controlled internal`, it enters the uncontrolled junction before discovering the downstream red/stop. Before consolidation, the road-lane path’s `gateTarget` walked through uncontrolled internals and held upstream. This can stop vehicles inside the intermediate box and recreate capacity seals. Add equivalent lookahead plus a regression with a `RowNone` intermediate internal and controlled downstream internal.

REVIEW-COMPLETE
