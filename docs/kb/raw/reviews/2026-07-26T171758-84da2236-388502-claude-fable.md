Vet blocked by sandbox permissions — proceeding on read-review alone (imports check out; the harness helpers, header keys, and run.go ack-before-obs ordering the tests rely on were verified against the tree).

## Findings

**Blockers** — none.

**Should-fix**

1. `engine/natsio/driver_test.go:1273` (patch line 813–815) — `if v2Fast.splits == 0 { t.Fatal(...) }` asserts a *machine property*, not a code property: at 500 µs pace, a box fast enough to always land the 400-intent v2 stream inside one window produces 0 splits and a hard failure in the default `go test ./natsio/` path. Your own numbers (47–59/300, ~16–20%) show it isn't near the edge on this box, but the failure mode on faster hardware is fail-closed with no code defect. Same class: the prod-pace **exact** `p99TSIB ≤ p99V2` at `driver_test.go:1248` can trip on a >3 ms scheduler/GC hiccup on the TSIB leg alone — arrival delay (not a split) yields complete-lag 2. Both are wall-paced tests in the default path. Per the repo's own triage rule you may defer with a note; if you keep them exact, record the flake-response plan (loosen vs `-short`-gate) in the commit message.

**Nits**

2. `driver_test.go:1180–1196` — the measurement loop drains but never `e.EnqueueIntent`s (warm-up and quiescence do). Fine for drain-boundary attribution, but the asymmetry is uncommented and invites a future "fix" that would change the fleet dynamics mid-test. One line saying "intents deliberately not applied; attribution only" would pin it.
3. `driver_test.go:897` — v2 pairing: an intent arriving before the tap's first obs gives `lastSrc = 0`, an inflated v2-only lag sample. Bias is in the lenient direction (makes v2 look worse, i.e. the asserted inequality easier), and nearest-rank p99 over ~400 samples absorbs a singleton — no action, just noting the bias direction is the safe one.
4. `bench_intent_test.go:346` — `IntentBatchStats`' fifth value (`encodingUnknown`) is discarded; asserting it 0 is free and matches the "zero drops" acceptance framing.
5. `driver_test.go:850` — the comment's "response to obs(u) lands between obs(u) and ack(u+1)" is a cross-publisher ordering assumption NATS doesn't guarantee (driver and engine are separate connections); a slow response pairs with the newer obs and undercounts v2 lag. The ±1 tolerance already absorbs this and the comment's "residual limit" paragraph is honest — consider naming this specific mechanism there.

**Questions** — none open.

**Contract/determinism check (clean):** no wire change — `SubjectCtlAll` (`server.go:831`) is a helper over the existing `ts.{run}.ctl.>` tree, the `tsibTickOff` refactor is byte-identical, `BufferedIntents` is a lock-correct read-only Go API, and the ADR-0026 milestone covers all of it. The bench's in-place tick patch on a reused `*nats.Msg` is safe (nats.go copies payload into the outbound buffer synchronously during `PublishMsg`; one publisher per msg, `wg.Wait` between ticks). The `allTicks = warmup + b.N×measTicks` batch-count assertion matches the actual loop structure (warm-up runs once per trial, fresh Bus per trial). The `touched` map in `attribute` is membership-only, never iterated — no map-order hazard. No wall-clock/RNG in shipped (non-test) paths.

REVIEW-COMPLETE
