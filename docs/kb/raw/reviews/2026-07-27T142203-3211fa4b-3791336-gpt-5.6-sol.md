- **should-fix** `scripts/show/build-quiz.sh:48` — The canonical build still declares Chicago text-only and neither generates nor copies `chi-loop-urban__setup.png`. `mkquiz.py` now silently consumes any correctly named PNG already present (`scripts/chicago/mkquiz.py:381`), so a clean build omits the map while an incremental build can ship a stale/wrong-run map. Generate the PNG in this pipeline and fail if the expected metrics-derived asset is unavailable.
- **should-fix** `scripts/show/mkcongestionmap.py:188` — The generator says the legend belongs beside the image and prints it only to stdout, while the quiz inserts only the bare image (`viz/public/quiz.html:159`). Viewers cannot decode the colors, and the required warmup tick is also absent despite `scripts/show/mkcongestionmap.py:33` saying it must be quoted alongside the picture.
- **should-fix** `scripts/chicago/mkquiz.py:381` — Diagram selection validates only the filename. Unlike baked replays, it records/verifies no metrics path, run identity, warmup, set ID, or content hash. Renaming any PNG to `chi-loop-urban__setup.png` silently binds it to the staged results, weakening the repo’s “same run” measurement discipline.
- **nit** `scripts/chicago/mkquiz.py:405` — `--diagrams` help still documents SVG-only naming and text-only fallback, omitting the newly supported PNG setup behavior.

REVIEW-COMPLETE
