- **should-fix** `contracts/asyncapi.yaml:471` — `sendSignalTableReply` references `stateSigReq`, so AsyncAPI tooling models replies as sent to `ts.{run}.state.sig.req`; implementation sends to a dynamic inbox. Define a reply channel representing `_INBOX.>` instead of contradicting the wire contract.
- **should-fix** `engine/natsio/bus.go:344` — The documented request payload is empty, but the responder accepts and ignores arbitrary bodies. With the broker cap raised to 4 MiB and observers granted publish access, malformed large requests can trigger multi-megabyte response sets. Reject non-empty payloads at the contract boundary.

REVIEW-COMPLETE
