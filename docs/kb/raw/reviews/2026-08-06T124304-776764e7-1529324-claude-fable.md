Review complete. I read the full non-generated portion of the patch, the post-image of `mkopenslide.py`, `build-quiz.sh`, the generated SVG legends, and cross-checked the bake hashes across all three places they appear.

## Findings

**Blockers** — none.

**Should-fix**

1. **Generated-payload bloat: the two new setup SVGs are ~1.4 MB combined and get inlined into the guest-facing page.** `chi-kennedy__setup.svg` is 988 KB with 10,035 `<path>` elements, of which 2,057 are fully degenerate zero-length segments (e.g. `<path d="M159.0,111.2 L159.0,111.2" ...>` — diff.patch line 86) plus thousands more sub-pixel fragments; `chi-loop-cbd__setup.svg` is 436 KB. Because mkquiz.py inlines setup slides into the `D` blob, `viz/public/quiz.html` grows from 271 KB to 1.8 MB in this diff. The per-edge merge loop at `scripts/show/mkopenslide.py:138-146` never drops paths whose projected extent is below ~0.5 px in the 400×297 viewBox; a one-line skip there would cut both artifacts (and the page) by a large factor. Everything still renders, so this is weight, not correctness — but it lands on every page load and as ~1.4 MB of checked-in churn per regeneration, in a repo that just spent an ADR addendum (2026-08-04) shedding archive weight.

**Nits**

2. `scripts/show/mkopenslide.py:233-237` — `cycs`/`firsts` are sets of raw float sums; two programs with identical cycle length whose phase durations sum in a different order can compare unequal and demote the legend to "mixed programs". The `offs` set two lines down already rounds to defend against exactly this. Cosmetic only.
3. `scripts/show/mkopenslide.py:241` — `100 * first / cyc` divides by zero if every signal has an empty phase list (cyc = 0 with `len(cycs)==1`). Requires a config you don't run; defer-by-default per the triage bar, recording it only.

**Questions**

4. `docs/show/README.md:9-12` — scenarios 2 and 3 keep "—" in the "showable window" column while `baselines.json:55,63` now carry window prose for the same runs ("whole run reads" / "last 8000 ticks"). Intentional split between show runbook and quiz metadata, or an update missed?

## Verified clean

- **Consistency:** the two new bake hashes agree everywhere they appear — README table, `baselines.json`, and the regenerated `quiz.html` blob all say `cbdbase/7406456b6031` and `kenbase/03c7bc162e73`; centers, zooms, and tick counts match too.
- **Generated vs. template:** the "keys: 1-5" hint in quiz.html comes from mkquiz.py's `%(navkeys)s` template (mkquiz.py:359), not a hand edit, and the keydown handler is dynamic over `keys.length` — a rebuild reproduces this file.
- **Determinism:** the new legend logic touches sets only via `len()` and `next(iter())` on singletons; `sinks()` already sorts to defeat PYTHONHASHSEED; `spacings` is sorted; `by_edge`/`pts` iterate in input order. Regeneration is byte-stable — confirmed by `bottleneck-town__setup.svg` changing only the reworded coordination line with identical geometry and y-coordinates. Legend row/height math (`exits_rows`, `sig_y`, `legend_h`) is consistent in both branches and matches the emitted SVGs (viewBox 297 = 200 + 12 + 17·5).
- **Contract:** no NATS subject or payload touched; no ADR required. The prose fix scoping the digest issue to the three un-rebaked runs is accurate to the hash pointers in the same diff.

REVIEW-COMPLETE
