- **blocker** — `scripts/show/mkcongestionmap.py:139`: the map drops horizon-partial intervals, but the published quiz calculations include them (`scripts/whatif.py:155`). The staged sidecar confirms the map measures ticks 4,000–10,000 (`docs/show/diag/chi-loop-urban__setup.png.json:53`), while the option results claim a 12,000-tick run. The picture and numbers therefore use different windows. Fix the comparison tooling and regenerate results, or otherwise align the measurements.
- **blocker** — `scripts/show/mkcongestionmap.py:218`: the claimed network-binding guard cannot detect the explicitly documented CBD→urban mismatch. Because CBD lane IDs are a subset of the urban network, `foreign` is empty and the wrong metrics pass, painting the remaining city grey. Bind metrics to a network digest/identity or validate expected coverage.
- **should-fix** — `scripts/show/build-quiz.sh:104`: comments require overriding `CHI_WARMUP` and `CHI_END` together, but the script accepts either independently, allowing an accidental non-published window. Enforce both-or-neither.

REVIEW-COMPLETE
