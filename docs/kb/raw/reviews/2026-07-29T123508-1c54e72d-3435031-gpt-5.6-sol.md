- **blocker** `docs/kb/decisions/ADR-0034-gridlock-escape.md:10` incorrectly claims “no NATS payload changes.” `engine/keyframe.go:153` adds `stuckTicks` to TSKF v5, which changes the JetStream `log.keyframe` payload. Document the wire migration and older-reader rejection in the message-contract ADR.
- **blocker** `engine/gridlock.go:89` increments `stuckTicks` whenever a vehicle is slow, regardless of whether it is a blocked lane head. A vehicle can spend 300 seconds in an ordinary queue, reach the head, encounter one transient `boxBlocked` tick, and be removed immediately. Track continuous head-and-blocked time, or reset the timer whenever `jammedAtJunction` is false.
- **should-fix** `engine/metrics.go:615` closes stranded trips without accounting for their final simulated tick. Because removal occurs after integration but before observation, this omits that tick’s distance, VHT, time loss, occupancy, and stopped time from trip and aggregate metrics.

REVIEW-COMPLETE
