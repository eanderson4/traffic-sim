No blockers. Viz-only change, no NATS/contract surface touched (`verbErrors` was already in the demosrv status payload and `parseStatus`; this only surfaces it). Determinism: n/a — UI polling path. The in-flight guard, warn-line composition, and view-model tests are all correct as written.

**Nits / questions:**

- **nit** — `viz/src/replaypanel.ts:341` The comment claims the guard prevents "an older response landing LAST overwriting newer state," but it only serializes poll-vs-poll. A `refresh()` started before a pause/speed/seek POST can still resolve after the POST's response and overwrite `ctl.status` (`replaypanel.ts:209`) with pre-action state; the panel then renders stale UI until the next poll corrects it (~1 s, self-healing). Pre-existing race, fine to leave — but the comment overstates the guarantee.
- **nit** — `viz/src/replaypanel.ts:345` The new skip-doesn't-count-toward-FailGate semantics live in `ReplayPanel.poll()`, the untested DOM shell (repo convention: the DOM-free half carries testable transitions). No test pins the guard or the changed hide latency. Consistent with the stated convention, so a nit, not a should-fix.
- **question** — `viz/src/replaypanel.ts:352` Side effect of serializing: during an outage each poll now burns the full 3 s timeout before its failure counts, so hiding takes ~3×(3 s+1 s) ≈ 12 s instead of ~3–5 s when failures could stack. Looks intended ("a skipped poll is not a failure"); confirm the slower hide is acceptable.
- **nit** — `viz/src/replaypanel.ts:203` The guard's safety depends on `refresh()` always settling (the 3 s `AbortSignal.timeout`). A test `FetchLike` that never resolves would wedge polling permanently; real `fetch` is covered. No action needed.

Tests updated appropriately for the view-model additions (`viz/test/replaypanel.test.ts:110-118`), including the each-counter-alone and both-zero cases.

REVIEW-COMPLETE
