Reviewed the full staged diff (engine, viz, scripts; skimmed generated docs/SVGs/JSON for consistency). Verified independently: `go vet` clean on `natsio` + `cmd/serve`; new Go tests pass; all 186 viz tests pass; default driver confirmed `MaxNeighbors: 0` (driver.go:182); engine signal offset is SUMO "phase 0 begins at offsetTicks" (signal.go:121-131), which matches both the green-wave author and the timing-strip renderer. No NATS subject/payload/header changes — the contract edits are error accounting plus exported accessors, so no ADR trigger; ADR-0027 covers the baked-trip-cards side.

## Blockers

None.

## Should-fix

1. **Fidelity-gate strings now live in three unpinned copies.** `engine/cmd/serve/main.go:567` ("observation frames FAILED to publish"), `scripts/chicago/record-hero.sh:64`, and `scripts/whatif.py:227` (which also duplicates "the driver could not keep up" and "of demand never entered the network"). A future rewording of serve's output silently disarms both downstream gates — the exact failure mode this diff exists to close. Nothing pins them; a small Go test (or a shared constant emitted into a file the scripts source) would. The comment at main.go:560 shows the coupling is known; it's still unguarded.

2. **Mirrored lane indexing in the town generator** — `scripts/demos/bottleneck_town.py:281` (KNOWN DEFECT block). Index 0 sits at the centreline, inverting netfile.go's "0 = rightmost" contract, so right turns issue from the inside lane and opposed protected lefts cross unarbitrated inside their green. The in-file triage (measured 0-1 collisions, symmetric across arms, fix requires redesigning the bay) is credible and the deltas stand; but this is baked into checked-in reports/SVGs, so per AGENTS.md record it as a deferred should-fix in the commit message or KB, not only in the docstring.

## Nits

3. `engine/cmd/serve/main.go:574` — the knee guidance claims to be "derived from the actual cap and the actual layout" but hardcodes `ObsFeaturePolicyCtx`, a 24 B route, and zero neighbors rather than reading `ctl.window`. Correct for the default driver (PolicyCtx on, `MaxNeighbors: 0`); optimistic for any controller that attaches with neighbors or wrong for one without PolicyCtx (~50k knee).
4. `scripts/show/mkoptiondiag.py:221` — `junction_xy` only recognizes internal lanes whose *edge id* starts with `i…`; the ramp meter's `rmpj_0` has `edge: ""`, so `merge-pod__ramp-meter.svg` ships with no amber signal head (confirmed: zero `<circle>` in the checked-in SVG), contradicting the comment at render() that the retiming is "visible on the plan too". The timing strip still carries it.
5. `scripts/demos/bottleneck_town.py:735` — `Leg(..., left_lane=main_lanes)` assumes the flare materialized; `build_piece` silently drops it for pieces ≤ FLARE_LEN+20 (line 376), which would IndexError in `build_junction`. Safe at current junction spacing (≥450 m); latent trap for re-spaced variants.
6. `scripts/demos/merge-rerun.sh:24`, `town-rerun.sh:31` — `pgrep -f "meter-sweep.sh"` matches any cmdline containing the string (an editor or `less` on that file), so the wait loop can hang forever on a machine where someone has the script open.
7. `scripts/demos/town_quiz.py:12684` (patch) — `carries_traffic` filters pairs with `if a and b`, so a seed with vmt_km exactly 0.0 is treated as missing rather than as a catastrophic throughput loss.

## Notes (no action)

- Determinism discipline is good throughout: serve's drop report sorts map keys before printing (main.go, new loop); `mkopenslide.py` sorts the sink set before cell-grouping with an explicit PYTHONHASHSEED rationale; demand/metrics YAML emitters sort. `noteObsErr`'s CAS max-loop is correct and monotonic; the run-loop/atomic split is documented and honored.
- `ObsBridgeTicks` returning `HoldLastTicks` (not `(cadence−1)+HoldLastTicks`) is the right conservative bound, and `TestObsOutageStreak` pins exactly the streak-vs-total distinction the gates depend on.
- `whatif.py`'s refuse-by-default on voided runs, with the bias direction explained and the void list carried through `--report` re-scores, is the right shape.
- `TestObsFrameSizeCliff` runs ungated (~seconds, fine); `TestIntentPlaneLoad` is env-gated as documented.

REVIEW-COMPLETE
