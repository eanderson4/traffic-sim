- **Blocker — `viz/src/splash.ts:70`:** The featured CTA POSTs to an admin-gated endpoint without credentials. ADR-0020 requires `-admintoken` for public deployment, so “launch live demo” returns 401 whenever LA is not already active. Either make the public card watch-only/autostart-aware or provide an intentional authenticated flow.

- **Should-fix — `analysis/lit-lineage/scripts/harvest.py:305`:** A failed seed query is logged and silently omitted, after which incomplete `works.jsonl` and research summary overwrite prior results. Fail the harvest or preserve/mark partial output; otherwise transient API failure silently changes the claimed corpus.

- **Should-fix — `analysis/lit-lineage/scripts/filter.py:133`:** Deduplication removes duplicate work IDs without redirecting citations to the selected canonical ID. References to discarded OpenAlex IDs therefore stop resolving, changing in-degree, SPC weights, and the main path.

- **Should-fix — `analysis/lit-lineage/scripts/analyze.py:300`:** Hub selection is nondeterministic at the cutoff. `in_deg` was populated by iterating a set, and sorting only by degree preserves that randomized insertion order for ties. Add the work ID as a tie-breaker.

- **Should-fix — `viz/src/splash.ts:10`:** The featured demo is hard-coded rather than resolved from `/api/demos`. If the deployment registry lacks ID `la`, changes its run metadata, or renames it, the primary CTA POSTs an unknown demo and its running state is never recognized. At minimum validate/find the registry entry and disable the card clearly when absent.

- **Should-fix — `analysis/lit-lineage/scripts/harvest.py:318`:** `works.jsonl` is emitted in API/dict insertion order. Fresh OpenAlex rankings can change, and tie behavior later depends on input order (`filter.py` replacement ties, author-name overwrite). Sort by stable ID and add explicit tie-breakers throughout.

- **Should-fix — `analysis/lit-lineage/research/phase-2.md:1`:** Substantial new traffic-domain knowledge is kept under `analysis/` but not registered/distilled into `docs/kb/`, contrary to AGENTS.md’s “new domain knowledge belongs in the KB” rule.

- **Nit — `analysis/lit-lineage/scripts/analyze.py:188`:** The comment says `head` is built oldest→newest and calls `a` the older endpoint, but citation edge `a -> b` makes `a` newer. The returned ordering appears correct; the explanation is reversed.

### Phase-3 thoughts

1. Use SPC-weight pruning for the defensible default spine, but cap per-node degree for usability; a raw global SPC slider can still let one dense school dominate.
2. Do `cited_by` backfill first and record the attempt/results. Use asserted edges only for remaining gaps, with explicit `asserted` provenance and receipts—not as ordinary citation edges.
3. Keeping the pruned JSON out of git is acceptable only if builds fetch a versioned, checksum-pinned artifact and the immutable source snapshot/provenance is retained somewhere durable. “Regenerate from current OpenAlex” is not reproducible enough.
4. The bias framing is strong if stated narrowly: this graph reflects OpenAlex coverage, metadata quality, query/filter choices, and citation practices—not an objective ranking of the field. Show concrete missing/overweighted examples and avoid treating absence as proof of neglect.

REVIEW-COMPLETE
