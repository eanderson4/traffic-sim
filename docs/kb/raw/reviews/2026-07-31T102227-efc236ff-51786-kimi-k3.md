• Now let me verify key context in the repo: the restore construction path, struct-literal engines, and how serve builds specs.

• One more check: whether `routeNextHop` can run inside `NewEngine` before `initAdaptiveRouting` (spawner init / initial population placement order).

• Now the key test-integrity question: which tests use `DefaultParams()` without pinning the flag — especially CRC fixture tests.

• Two last checks: what `scripts/whatif.py` drives, and whether `simrun` honors manifest params.

• All verification done (full `go test ./...` green, go1.26.5). Findings:

  ## Blockers

  None. The flip is mechanically sound: new loops iterate `net.Lanes` in fixed network order, `routeEpochs` is lookup-only, the CRC stays explicit-field and flag-gated (`engine.go:1069`), `RestoreNotice` never enters the CRC, and no wall clock enters the sim core (logging is confined to `natsio`). Replay/bake/player all restore under the recorded `meta.Spec` (`replay.go:110`, `bake.go:59,68`, `player.go:194,206`), so pre-flip recordings replay bit-exact. ADR note (b) checks out: the content hash re-encodes the typed manifest (`scenario.go:209`), so `omitempty` on nil `*bool` keeps absent-field manifests byte-identical, and the only affected case (explicit `false`) indeed couldn't exist pre-commit.

  ## Should-fix

  1. **Unpinned scenario generators.** `scripts/chicago/fwsweep.sh:51`, `scripts/chicago/mkscenario-portal.sh:37`, `scripts/import-city.sh:129` still emit manifests with no `adaptive_routing` pin. The addendum's reproducibility argument ("regeneration keeps it") covers only the three pinned generators; if the freeway-scale sweep or portal scenarios back any published/bracket numbers, regenerating them silently flips their baseline — the exact failure mode this commit pins against. Pin them or state in the ADR why their outputs carry no published numbers.

  2. **Warm-start pin unreachable under `-scenario`.** `serve` rejects `-adaptive-routing` with `-scenario` (`main.go:148`), so continuing a pre-v6 `-state-in` state under a scenario requires editing the manifest — which changes the content hash and thus the (hash, seed) identity of the continuation relative to the run that wrote the state. The flag help (`main.go:94-97`) advertises the pin without noting it's netfile-only for this use case. Acceptable if deliberate, but the help/`RestoreNotice` text (`keyframe.go:321`) should say what a scenario user is actually supposed to do.

  ## Nits

  3. `engine/natsio/bake.go:72`, `replay.go:114`, `run.go:108` log via package-global `log` while `player.go:211,503` uses the injected logger — inconsistent edge logging within one package.

  4. The `RestoreNotice` surfacing on the replay/bake/player paths is effectively dead code: flag-off recordings restore under a flag-off spec, and flag-on recordings always wrote v6 keyframes. Only `run.go:108` (live warm-start) is reachable. Harmless defense, but `player.go:503` would additionally re-log on every seek.

  5. `engine.go:63-64` says "see engine.go's Params doc" — a self-pointer, and the field doc (`engine.go:52-59`) never mentions the default flipped ON; it still reads as flag-gated-off design text.

  6. `bench_test.go:103` inherits the new default, so benchmarks now measure the adaptive path while `BENCHMARKS.md` numbers were measured static. Pin the bench spec or note the baseline shift.

  ## Questions

  7. `docs/show/README.md:283` warns re-running `whatif.py` "measures a different baseline unless the manifests set `adaptive_routing: false`" — but this commit stamps exactly that pin on disk and into the generators, so re-runs do reproduce. Intentionally conservative wording?

  8. The acknowledged residual hole — struct-literal engines calling `routeNextHop` before their first `Step` divide by zero (`engine.go:421-424`) — rests on `persistence_test.go:109` being the only struct literal and restore always going through `NewEngine` (verified, `keyframe.go:305`). Fine today; is a comment the desired long-term guard?

  REVIEW-COMPLETE

