- **should-fix** `engine/cmd/demosrv/geojson.go:217` — Every part request hashes the entire network, then `c.path()` rereads and rehashes it at line 234. A four-part 900 MB network causes roughly 8 GB of redundant reads per page load, directly undermining ADR-0018’s city-scale path. Reuse the already-computed generation or memoize network identity.
- **should-fix** `contracts/asyncapi.yaml:479` — AsyncAPI 3 operation messages should reference messages declared by their channel, not the component directly. Use `#/channels/stateSigReq/messages/signalTableRequest` and, at line 485, `#/channels/sigReplyInbox/messages/signalTableReply`, or omit these redundant subsets.
- **nit** `engine/cmd/demosrv/geojson.go:237` — A valid-generation but nonexistent part returns a generic error, mapped to HTTP 500 at `engine/cmd/demosrv/main.go:417`. Missing resources should return 404; reserve 500 for generation or I/O failures.
- **question** `engine/cmd/demosrv/supervisor.go:213` — Status is mirrored as active before the potentially 660-second identity/readiness phase completes. This lets another menu tab deep-link to an engine still building. Is “active” intentionally “starting or running,” despite `/api/status` exposing no state distinction?

REVIEW-COMPLETE
