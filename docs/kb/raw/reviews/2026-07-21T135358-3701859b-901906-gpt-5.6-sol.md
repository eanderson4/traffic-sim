- **blocker** `engine/metrics.go:413` — A pre-existing vehicle that exits during the first `Step` has no `tripState`; dereferencing `ts.dist` panics. A vehicle spawned and despawned within one tick is absent from both `last` and `cur`, so it is silently omitted entirely.
- **blocker** `engine/metrics.go:644` — Boundary-crossing time-loss clamps each lane share independently. ADR-0014 requires clamping once per vehicle-tick and splitting that tick’s time-loss proportionally; when desired speeds differ across the boundary, this inflates totals and violates the normative definition.
- **blocker** `engine/metrics.go:759` — `isCrossingPlusHop` treats global `Lane.Index` values as lateral indices. File-network lateral neighbors are defined by `Left`/`Right` and need not be consecutive in file order; missed crossings fall through to the negative-distance clamp and lose the tick’s travel.
- **should-fix** `engine/metrics.go:403` — Exit presence time uses the previous observation’s speed, although ballistic integration may accelerate or decelerate during the exit tick. This biases VHT, occupancy, and time-loss; the approximation is not ratified in ADR-0014 or the metric contract.
- **should-fix** `engine/metrics.go:691` — A partial interval truncated by `EndTick` remains buffered until run-wide `Finalize`, rather than closing when `EndTick` is observed. Long runs therefore cannot stream completed finite-window results promptly.

REVIEW-COMPLETE
