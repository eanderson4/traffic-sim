- **blocker:** None.
- **should-fix** — `docs/kb/decisions/ADR-0019-route-budget-determinism.md:30`: ADR-0019 explicitly accepts failover-dependent behavior but does not declare that it amends ADR-0008; `docs/kb/decisions/ADR-0008-controller-contract.md:100` and `engine/natsio/driver/driver.go:12` still categorically promise behaviorally invisible failover.
- **should-fix** — `engine/natsio/driver/driver.go:426`: the budget meters every draw, including cache hits, although only first-touch O(network) scans motivate the throttle. Large bursts sharing one origin incur avoidable multi-second default-routing windows. Meter cache misses or document why cache-independent admission is required.
- **should-fix** — `engine/natsio/driver/driver_test.go:131`: the frozen-lane test never proves the arbitrary moved lane produces a different destination. It can pass if routing incorrectly uses the current lane. Select a lane with a demonstrably different `pickExit` result and assert that precondition.
- **should-fix** — `docs/kb/raw/reviews/2026-07-24T165400-03b171ee-1158662-reviewed.patch:1`: the staged change includes 225 review artifacts totaling 234,040 inserted lines, with reviewed patches recursively containing earlier diffs. This dominates the commit and contradicts the repo’s one-review-round rule; retain only the intended final review record.
- No NATS subject or payload changes detected.

REVIEW-COMPLETE
