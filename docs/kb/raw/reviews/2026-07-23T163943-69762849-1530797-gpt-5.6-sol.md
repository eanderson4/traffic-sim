- **blocker** `engine/cmd/replay/main.go:41` — The browser-facing HTTP control API introduces direct service-to-service RPC, contradicting ADR-0002’s NATS-only backbone. This new service boundary/control contract requires an ADR or must use NATS subjects.
- **blocker** `engine/natsio/player.go:166` — `endTick = min(spec.Ticks, idx.lastTick)` silently truncates valid sparse recordings. With `CRCEvery > 1`, no intents/verbs, and a final tick off both CRC/keyframe cadences, the last log message precedes the completed run.
- **should-fix** `engine/natsio/player.go:211` — An unpaused seek acknowledges before the loop is prevented from stepping again; `/seek` can therefore return status beyond the requested landing tick. Serialize seek completion/status with playback continuation.
- **should-fix** `engine/natsio/player.go:346` — `/pause` returns success immediately after changing a flag, although a tick may still execute. This contradicts the documented guarantee that successful POST status is returned after the change takes effect.
- **should-fix** `engine/cmd/replay/main.go:107` — HTTP bind failure is only logged; playback continues and prints a usable-looking control endpoint. Startup should fail loudly when the required control plane cannot listen.

REVIEW-COMPLETE
