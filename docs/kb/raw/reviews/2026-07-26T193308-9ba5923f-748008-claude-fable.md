Review of the staged diff (contract ratification for ADR-0026: AsyncAPI 2.5.0 + KB/ADR status). Verified the contract text against the landed M0–M3 implementation (`engine/natsio/tsib.go`, `bus.go`, `driver/driver.go`).

**Verified correct** (no findings): TSIB layout math (24 + 44·20,000 = 880,024 B; record field order matches `putIntentFixed`, bus.go:80–89); demux semantics (key-existence check, present-but-empty → `intentEncodingUnknown`, exact-case verbatim key — bus.go:303–340 matches the channel text exactly); counter names and `Bus.IntentBatchStats` (bus.go:556); whole-batch route rejection, per-record NaN/Inf drop, tick-informational, ignored header flags/reserved (tsib.go:110–139); the migration claims — multi-record TSIB read as v2 always fails the v2 length check (≥112 B vs max 44+48=92, bus.go:160), and the single-record 68 B case passes only when message bytes 40–41 (low bytes of the first record's accel) read 24, then hits claim filtering with vehicle_id decoded from the "TSIB" magic — arithmetic checks out; `EncodeTSIB` preserves slice order, no map iteration, no determinism exposure. Version bump 2.4.0→2.5.0 is additive and ADR-backed, subject unchanged — contract discipline satisfied.

**Findings:**

- **should-fix** — `contracts/asyncapi.yaml:655` vs the new demux text (asyncapi.yaml:~211–227): `ControlIntent` still declares `headers: SchemaHeaders` (**required** `schema_version: 2`), but the new channel text defines v2 as "intent_encoding ABSENT ⇒ v2 (every pre-ADR-0026 producer; other headers are ignored)" — and the actual v2 producer (`driver.go:418`, bare `nc.Publish`) sends **no headers at all**, which the engine never checks. The contract is now self-inconsistent about what a v2 intent message carries in the very section this diff rewrites. Pre-existing mismatch, but this diff makes it visible; one-line fix in the file already staged (drop or soften the ControlIntent headers requirement, or note it's unenforced).
- **nit** — `contracts/asyncapi.yaml:22–24` (info §), `:663`, `:665–667`: "ONE TSIB batch per cadence tick … (splitting into ⌈n/20,000⌉ bounded batches above the cap)" is self-contradicting phrasing repeated in three places; "at most ⌈n/20,000⌉ TSIB batches per tick" says it cleanly.
- **nit (out-of-diff)** — `docs/kb/decisions/ADR-0026-batched-intents.md:90`: "880,024 B payload + header" — 880,024 already *includes* the 24 B header (code comment tsib.go:43 and the new contract text both say "with the header"). ADR body isn't in this diff; fix whenever the ADR is next touched.
- **question** — `ControlIntentBatch` deliberately carries no `schema_version` header (version rides in the TSIB magic/version field; matches `NewTSIBMsg`, tsib.go:99). Intentional and self-consistent — but if the should-fix above is resolved by keeping SchemaHeaders on ControlIntent, state the asymmetry explicitly so a validator generation doesn't "fix" it later.

No blockers. Docs/KB portions (INDEX.md entry, cross-topic-concerns known-issues note, ADR status flip) are accurate against the code and benchmarks (§(d) exists, references check out).

REVIEW-COMPLETE
