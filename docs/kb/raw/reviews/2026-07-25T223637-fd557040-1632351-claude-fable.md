All claims in the diff verified against HEAD implementation. Findings:

## Findings

**Blockers** — none. Every normative claim the staged docs add is true of the shipped code: `bakeStride` (bake.go:53–63) matches the contract's max(1, round(0.5/dt)) + [1,2] Hz band and the new unit-test cases (float spot-checks on dt 0.05/0.3/1.0 all round correctly); the record-digest framing, fixed header order, first-value and empty-treated-as-absent rules match `recordHasher` (engine/natsio/resim.go:35–75) and the actual header constants `schema_version`/`tick`/`kf_chunk`/`sig_chunk` (bus.go:49–52, recorder.go:94); the exact-tick aggregate grid matches bake.go:229–234, 267; the region-set-union claim matches `manifestRegions` (bake.go:503–515); consumers do read `bakeEveryTicks` from index.json (viz/src/baked.ts:162), never recompute. The cadence change itself is already ratified in ADR-0023 (lines 186–193) and implemented at HEAD — this diff is documentation catching up plus tests, so no new ADR is required; the added ADR-0023 deferral note satisfies the contract-change discipline for the hash-coverage gap.

**Should-fix**

- `viz/test/baked.test.ts:404` — the hold-last assertion cannot fail for the reason its comment claims. The fixture (baked.test.ts:256–257) emits **identical** pairs (ratioQ 170 → 1.0) at every aggregate tick, so `laneRatiosAt(19).get("lane-a") === 1` passes whether the shim held tick 10's aggregate, snapped to tick 0's, or wrongly read tick 20's. Vary ratioQ per aggregate tick so the assertion actually pins the "latest aggregate ≤ tick" selection. (Test-only; defer if you prefer, but the comment overstates coverage as written.)

**Nits**

- `docs/kb/decisions/ADR-0023-baked-replay-pipeline.md:506` — hard line-number refs "TODO at bake.go:357, bake.go:545" are correct today but will rot on the next edit to bake.go; the greppable `TODO(MVP-deferred)` markers would be a stabler pointer.
- `engine/cmd/bake/bake_test.go:247–250` — the rate re-check recomputes the same `1/(stride×dt)` formula production uses on the same inputs; it's a tautology, not an independent oracle. The table's expected strides are the real test. Harmless.
- TODO/ADR annotations are dated 2026-07-26 while the review ran 2026-07-25 local (-04:00); pick one clock convention for review stamps.

**Questions** — none; the three in-diff TODO deferrals (later-lane-chunk TSRL frames undecoded in the e2e, no off-stride-terminal fixture in the viz test, geometry:null throw in `parseFurniture`) are each self-documented with a producer-side justification and sit within the stated triage bar.

REVIEW-COMPLETE
