blocker — `engine/netimport/consolidate.go:302`: Merge foes are assigned when two multi-successor internals share *any possible* successor, but runtime foe checks are lane-level and never check each vehicle’s selected successor. Thus A→[X,Y] and B→[Z,Y] serialize even when vehicles actually choose X and Z; `boxBlocked` also treats either occupied lane as conflicting. The new fan-out fixture itself creates this case, so normal traffic gets false conflicts and throughput is distorted. Foe evaluation must be route/successor-specific, or fan-out must not live on one internal lane.

should-fix — `engine/netimport/consolidate.go:152`: The parallel-extension guard loses chain ancestry. For A→B→X plus independent C→X, deleting B then C changes `extBy[X].sliver` to C; deleting A no longer recognizes B as its chain ancestor and computes `max(A, C)` rather than `max(A+B, C)`. `SharedExtensions` only audits the damage; it does not prevent emission of incorrect lane length/geometry.

REVIEW-COMPLETE
