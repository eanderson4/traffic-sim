- **blocker** `engine/gridlock.go:108` — `stuckTicks` accrues for every stopped vehicle, not only while it is the lane head at a blocked junction as ADR-0034 specifies. A vehicle can spend five minutes deep in an ordinary queue, reach the head, encounter a transiently blocked box, and strand immediately instead of after `StrandAfterS` at that junction. Reset the timer whenever `jammedAtJunction` is false, or track qualifying blocked-head time separately.
- **blocker** `engine/warmstart.go:54` — the sidecar does not bind `engine.Params`, while `engine/cmd/serve/main.go:212` applies parameters independently. A state saved with different `SafetyDecel`, density, or other physics settings resumes a different deterministic program without refusal or explicit override. ADR-0029 itself acknowledges this default trap; shipping “bit-exact continuation” while relying on operator memory is unsafe.
- **should-fix** `engine/cmd/demand-director/main.go:31` — warm-start protection only triggers when users explicitly pass nonzero `-start-tick`; the default remains `0`. `Attach` already reads `RunMeta.StartedTick` at `engine/natsio/demand/director.go:141` but ignores it. Attaching normally to a warm-started run therefore submits the historical backlog and can duplicate restored queued directives. Derive/refuse from registry metadata rather than an optional flag.
- Tests could not run because the review sandbox forbids Go’s `/tmp/go-build*` directory creation.

REVIEW-COMPLETE
