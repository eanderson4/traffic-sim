- **blocker** `contracts/asyncapi.yaml:152` — Inserting `sigReplyInbox` split the existing `stateSigReq` definition. The request description, `{run}` parameter, and request message now belong to `_INBOX.>`, while `stateSigReq` declares none; `_INBOX.>` also has duplicate `description` keys. The sacred contract no longer accurately models the new subject and may fail strict YAML validation.
- **blocker** `engine/cmd/demosrv/supervisor.go:172` — Generation allocation occurs before acquiring `mu`, while `stop()` cancels only `startGen.Load()` at `engine/cmd/demosrv/supervisor.go:251`. A queued second start can make stop cancel the queued generation rather than the currently waiting one, leaving shutdown blocked for up to 660 seconds. Conversely, stop can clear the cancellation before a queued start acquires the lock, allowing that start to launch after the stop response.

REVIEW-COMPLETE
