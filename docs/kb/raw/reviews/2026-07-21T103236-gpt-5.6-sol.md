- **blocker** `scripts/external-review.sh:78` — The script hashes the *current* index after reviewers finish, not the saved patch they reviewed. Any `git add` during review causes an unreviewed diff to receive a valid stamp. Hash the snapshot before launching and abort if the index changes.
- **blocker** `hooks/pre-commit:14` — The gate excludes `hooks/` and `scripts/`, so a later commit can modify or disable the review mechanism without review. It also excludes executable code under `viz/scripts/` and `analysis/`, contradicting “Every code commit” in `AGENTS.md:40`.
- **should-fix** `scripts/external-review.sh:63` — `--gemini` never checks that `gemini.md` is non-empty. A successful CLI invocation producing no review is archived and stamped as a three-reviewer run.
- **should-fix** `hooks/pre-commit:5` — Documentation says every diff was reviewed by three models and that `scripts/review-stamp.sh` writes the stamp; implementation requires two models and that script does not exist. This conflicts with `AGENTS.md:43` and can misdirect operators.
- **question** `scripts/external-review.sh:72` — Review archives are created after the staged snapshot and remain unstaged when the script says to commit. Is a separate docs-only provenance commit intentionally required?
- **nit** `scripts/external-review.sh:70` — `slug` is calculated but never used.
