- **blocker** `engine/sigctl.go:289` — Chain ancestry is selected before an empty same-boundary override is removed at `engine/sigctl.go:305`. With phase A already held, submitting B then A at one boundary makes B govern zero ticks but causes the final A to receive a fresh `chainStart`. Repeating this bypasses the cumulative 300-second rail indefinitely and suppresses the expected lapse. Zero-duration intermediates must not break the existing same-phase chain.
- **should-fix** `engine/sigctl.go:85` — `math.Round(300/dt)` can exceed the advertised 300-s maximum for valid tick lengths; e.g. `dt=0.7` produces 429 ticks = 300.3 s. A safety maximum should round down or the contract must explicitly permit one-grid-step overshoot.
- **should-fix** `engine/engine.go:1108` — `requestID` is excluded from the CRC as “audit trail,” but `EnqueueSignal` uses retained request IDs to accept/reject future commands at `engine/sigctl.go:209`. It is therefore behavior-deciding keyframed state; CRC verification currently cannot detect divergence in warm-restore idempotency state.

REVIEW-COMPLETE
