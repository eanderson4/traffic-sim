- **blocker** — `engine/natsio/bench_intent_test.go:288`: lag is calculated as `e.Tick - hTick` before `e.Step()`, so it is unconditionally zero and cannot detect the collect-before-publish regression ADR-0026 M3 explicitly targets. `engine/BENCHMARKS.md:209` marks applied lag as satisfied, while `engine/BENCHMARKS.md:258` admits the required source-observation→`applied_tick` percentiles remain uncollected. M3 and the “validated end-to-end” claim at `engine/BENCHMARKS.md:251` are therefore incomplete.
- **should-fix** — `engine/BENCHMARKS.md:233`: saying excluded encoding “favors neither mode” is unsupported. The harness pre-encodes all records once (`engine/natsio/bench_intent_test.go:206`) and only patches the TSIB tick, excluding production’s materially different per-tick allocation/encoding paths. Describe these as ingest-only results rather than end-to-end, or benchmark the actual driver path.
- **nit** — `engine/BENCHMARKS.md:208`: “delivered records/s ~6–7×” does not match the table: approximately 5.4–6.0× at 5k, 7.7–11.7× at 15k, and 6.5–7.0× at 30k.

REVIEW-COMPLETE
