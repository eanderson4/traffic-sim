• Now let me read the kernel code for context — `rightofway.go` and the helpers the seam gate calls.

• Now the helpers: `pickSuccessor`, `sigGate`, `sigPermissive`, and the stopDone reset in boundaries.

• Checking the contract doc hunk and netimport.go context.

• Tests pass. One last check on the test harness helper semantics.

• Review done — patch read in full, kernel/importer context verified, `go build ./...`, `go test ./netimport/ .` (incl. `clusterseam_test.go`), and `test_mknetvariant.py` all green on the staged tree.

  **Blockers** — none.

  **Should-fix** (defer-by-default per the triage bar; record and move on)

  - `engine/netimport/consolidate.go:152` — the parallel-extension guard tests chain-ancestry only against the *last* extender (`succOf(origSucc[s.ID], prev.sliver)`). Mixed topology — sliver P chain-feeds internal X through Q, but an independent longer sliver R has already replaced Q's extension on X — misclassifies P as parallel: P's length is then silently dropped (if shorter) or replaces R's (losing R's geometry), breaking the length-preservation invariant `TestConsolidateChainRecursive` pins. Only reachable where a chain and a parallel pair share one far internal; `SharedExtensions` fires when it happens and fires 0 on the real chi import, so this is audit-covered, not reachable in normal operation. Record it; no fix needed now.

  **Nits**

  - `engine/rightofway.go:506` — `foeApproachConflict` duplicates `rowConflict`'s foe half (rightofway.go:244-254). The header comment says "mirror rowConflict exactly"; if the yield classes ever change there, this copy drifts. A one-line cross-reference in `rowConflict` would make the coupling bidirectional.
  - `scripts/chicago/test_mknetvariant.py` (`network_with_sliver`) — `sliver["length"] = 0.2` is assigned twice (before and after the `setdefault` loop). Harmless, but the second write reads like it guards something it doesn't.
  - `engine/netimport/consolidate.go:104` (`isSliver`) — `nf.Lanes[idx[s]]` silently resolves to lane 0 on a dangling successor id. Corrupt-input class per your own bar; the "never dangle" comment is the right call, just noting the failure mode is silent rather than loud.

  **Questions**

  - `engine/rightofway.go:90-130` — at ROW (non-signalized) seams the gate deliberately omits `boxWalk`, so a minor/major/stop movement can now enter the far box without clear-through room and stop inside it; pre-consolidation the same movement approached on a road lane and the entry gate required full clearance (`rowConflict` → `boxBlocked`). The ADR addendum argues re-running the chain rule would re-serialize discharge, and drain3 measurably improved — but this is a physics change beyond "stop-line duties apply exactly as on a road approach," and box-standing is the failure mode ADR-0010's entry rule exists to prevent. Was the in-box occupancy rate at seams checked in drain3, or only strand/completion aggregates?
  - `contracts/network-format-v1.md` still states foe lists are "ordered by lane index" — I verified `computeConflictFoes`' file-order pair loop keeps that true under map iteration (each junction's lanes are only touched by its own pair loop), so no doc drift; flagging only because the map iteration in `consolidate.go:249` looks like an ADR-0005 hazard at first glance and a one-word comment would save the next reviewer the proof.

  Contract/determinism sweep, clean: no NATS subject or payload changes (Report JSON fields are import-side); network-format doc change is wording-only and covered by ADR-0038; importer-identity hash correctly extended to `consolidate.go` (`scripts/import-city.sh:117`); no RNG, wall clock, or order-dependent map iteration in the new kernel/importer paths.

  REVIEW-COMPLETE

