• Now let me examine the key repo files for context.

• The code paths check out so far. Now the real verification: build and run the test suites.

• Tests all green. Now checking two remaining surfaces: variant manifests and demosrv's hash binding.

• One more check: variant `params` replace semantics vs the new pin, and whether any what-if variants set `params:`.

• All verification done — full test suite run, golden hash test green, key paths read. Report:

  **Verified, not findings**

  - `go build ./...`, `go vet ./...`, `go test ./...` (engine module) all green on the staged tree — including `TestAdaptiveRoutingPinMovesHash` (golden `e2e90e…` confirms `*bool` true canonicalizes byte-identically to pre-flip `bool` true) and the CRC/restore fixtures (`TestAdaptiveRestoreIsCRCExact` et al.). The default flip breaks no pinned trajectory in the suite.
  - Determinism: no new map iteration in sampled paths (`routeEpochs` is keyed access only), no wall clock/RNG changes. `RestoreNotice` is deterministic derived state, never serialized/CRC'd (engine/engine.go:231).
  - Restore-mismatch gating intact both directions (engine/keyframe.go:312, 320); every non-test `RestoreState`/`RestoreStateChecked` caller logs the notice (natsio replay/bake/player×2/run; serve's `-state-in` rides through `RunLive`).
  - Lazy `initAdaptiveRouting` invariant holds: all restore paths construct via `NewEngine` (engine/keyframe.go:305), so the `routeEpochTicks == 0` sentinel (engine/engine.go:418) can't clobber restored ttEMA/ttSnap.
  - Contracts: no NATS subject/payload changes; keyframe format untouched; scenario-hash/v1 stands per the golden test. ADR-0036/ADR-0012 addenda document the (hash, seed) two-trajectory consequence — the ADR hygiene this repo requires is present.

  **Should-fix**

  1. **Pinning re-keys the exact scenarios whose recordings demosrv binds.** `checkRecordingHash` hard-fails (409) when scenario hash ≠ recorded hash (engine/cmd/demosrv/replay.go:98-100). Stamping `adaptive_routing: false` onto the docs/show manifests shifts their content hashes (ADR-0036 addendum: "their content hashes shift"), so every pre-pin recording of those scenarios now fails the display binding — while the ADR-0012 addendum (diff lines 33-35) cites that same binding as what this design protects. If the intent is "re-record/retire pre-pin recordings," say so in the addendum; right now the two addenda read as contradictory on this point.

  2. **Variant `params:` wholesale replace silently drops the pin.** engine/scenario/variant.go:121-123 does `eff.Params = *v.Params`: a variant that overrides `params:` for `dt` alone zeroes `AdaptiveRouting` to nil → engine default ON, discarding a base's `adaptive_routing: false` pin with no error. Pre-flip this was harmless (zero = off = default); the flip makes it a silent regime swap — exactly what this commit exists to prevent. No current variant sets `params:` (pod scripts sed-copy the base manifest whole, scripts/chicago/pod-portal.sh:44), so it's latent; either field-merge `Params` or add a validation note before someone writes `params: {dt: …}` in a variant.

  **Nits**

  3. Logger inconsistency: replay.go:114, bake.go:72, run.go:108 use bare `log.Print` while player.go uses its injected `lg`/`p.log`. Same edge plane, two conventions.
  4. simrun `-adaptive-routing` defaults `true` but is explicit-only (applied only under `flag.Visit`, engine/cmd/simrun/main.go:100-112) — the default value is dead and will lie if `DefaultParams` ever flips back. Same in serve.
  5. engine/engine.go:418-429: the acknowledged divide-by-zero for struct-literal engines calling `routeNextHop` before first `Step` (engine/routing.go:209) remains a panic-in-waiting; comment-owned, test-only — fine, but a one-line guard in `routeTable` would outlive the comment.

  **Questions**

  6. INDEX.md now says 786 vs 766 completions where the PROPOSED text said 831 vs 766 — re-measured between rounds? Internally consistent with the ADR addendum, just confirming the swap is deliberate.

  No blockers. Findings 1 and 2 are record-and-defer candidates per the repo's triage bar, though #1 deserves at least a sentence in the addendum since it contradicts the stated rationale.

  REVIEW-COMPLETE

