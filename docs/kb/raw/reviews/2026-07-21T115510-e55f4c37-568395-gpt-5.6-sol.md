- **blocker** — `engine/scenario/variant.go:337`: patch targets are resolved incrementally, while `set` may modify `id`. An earlier patch can rename flow A to flow B’s anchor, causing a later B patch to silently modify A instead. Resolve all anchors against the untouched base before merging, or forbid patching `id`.
- **should-fix** — `engine/scenario/variant.go:185`: the materialized `Scenario` has one `Dir`, but inherited control/metrics files are read from the base (`engine/scenario/variant.go:241`) while added files come from the variant (`engine/scenario/variant.go:252`). Thus `Manifest.Control`/`Metrics` cannot later be resolved consistently relative to `Scenario.Dir`, contradicting the “indistinguishable from hand-authored” invariant.

REVIEW-COMPLETE
