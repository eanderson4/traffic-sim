- **blocker** `engine/natsio/driver_test.go:843` — The lag measurement assumes callback order across three separate async NATS subscriptions. Each subscription has its own dispatcher, so obs/intent/ack callbacks can execute out of broker order despite sharing a connection. Worse, the passive tap cannot know whether an intent reached the engine before its drain boundary. Consequently `lastSrc` is not reliably the source tick of the acknowledged intent, invalidating the M3 acceptance result and the claims in `engine/BENCHMARKS.md:211`.
- **should-fix** `engine/natsio/driver_test.go:968` — Comparing two sequential wall-clock-paced runs with strict `TSIB <= v2` percentiles makes the test scheduler/load-sensitive. A few CI stalls can fail an otherwise correct change; this belongs in a controlled benchmark or needs repeated/interleaved sampling and tolerance.
- **should-fix** `engine/BENCHMARKS.md:258` — Summary says both modes have p99 1 tick, contradicting the recorded result at `engine/BENCHMARKS.md:218`, where v2 p99 is 1–2 ticks.

REVIEW-COMPLETE
