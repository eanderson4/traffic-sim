- **blocker** `engine/cmd/metview/main.go:62` declares `denied_pending` as `int`, but schema v1 defines it as `float64`. Valid files with fractional latent demand fail during `json.Unmarshal`, so the viewer cannot open legitimate metric output.
- **should-fix** `engine/cmd/metview/main.go:32` discards `set_id`, then `engine/cmd/metview/main.go:168` aggregates every interval solely by lane. Scenarios with multiple measurement sets silently double-count or mix different windows.
- **should-fix** `engine/cmd/metview/main.go:32` also discards `partial`; `engine/cmd/metview/main.go:178` includes partial intervals despite ADR-0014 explicitly requiring comparison tooling to drop them.
- **should-fix** `engine/cmd/metview/main.go:181` divides each lane’s weighted values by the absolute maximum `end_tick`, rather than that lane/set’s summed observation duration. Any nonzero `begin_s` underreports density and occupancy.
- **should-fix** `engine/cmd/metview/main.go:28` omits and never validates top-level `schema_version`. Future/invalid schemas will be accepted and displayed with plausible zero values instead of failing closed.
- **should-fix** `engine/cmd/metview/main.go:186` builds rows via Go map iteration, while `engine/cmd/metview/main.go:194` sorts without a lane-ID tie-breaker before truncating to 100. Equal-valued lanes therefore produce nondeterministic API/dashboard membership.
- **nit** `engine/engine.go:462` says “at most one extra hop,” but decreasing-index successor chains can be re-bucketed repeatedly in the same sweep, matching the old multi-hop behavior.

REVIEW-COMPLETE
