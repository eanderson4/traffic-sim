- **blocker** `engine/metrics.go:892` — `st.rate` is vehicles/second, but backlog multiplies it by a tick count without `k.dt`. At `dt=0.1`, denied pending/wait can be overstated roughly 10×; demand-rate changes also retroactively revalue the entire backlog.
- **blocker** `engine/metrics.go:74` — Default period is hardcoded to 9000 ticks, although ADR-0005 permits configurable tick lengths. The promised 900-second default becomes 1800 seconds at `dt=0.2`; configuration must derive ticks from engine `dt`.
- **blocker** `engine/metrics.go:426` — Vehicles spawned and despawned within one tick are knowingly omitted from completed trips, VMT/VHT, time-loss, and intervals. Short imported origin/exit lanes make this reachable, violating ADR-0014’s “every vehicle” trip primitive and decision-grade totals.
- **blocker** `engine/metrics.go:453` — A crossing followed by despawn reconstructs the route using the shortest exit path rather than the consumed `HeldTurn`; branched networks can attribute distance/time to the wrong lanes. Additionally, `engine/metrics.go:482` never updates `ts.dest`, so even an unambiguous crossing-despawn reports the previous feeder as `DestLaneID`.
- **should-fix** `engine/metrics.go:166` — ADR-0014 requires denied-entry count and wait, but totals expose only wait and backlog pending at the horizon. Demand delayed and later injected/expired leaves no denied count, so the required run total cannot be produced.

REVIEW-COMPLETE
