Review complete. I read the full staged diff, then verified the kernel's assumptions against the engine internals it observes (spawner scheduling in `spawn.go`, director queue/expiry in `director.go`, `Step` phase order and `boundaries()` single-pass/reverse-index semantics, exit-only despawn, contract-layer verb dedup, `RunLive` loop pairing). The engine model the kernel encodes — reverse-index boundary pass producing both same-pass exit reprocessing and internal-lane overshoot, spawner `st.tick ≥ 1`, run-wide first-seen verb dedup with `""` rejected at the contract layer, despawn meaning exit and nothing else — all checks out against the actual code. Determinism discipline is clean: every map that reaches emission is sorted first (`gone` IDs, `servedIDs`, `Totals` lane IDs, the JSON denied array), accumulation orders are fixed, no wall clock or RNG touches the kernel, and the replay-rederivation, CRC-invariance, and byte-determinism tests pin it. No NATS subject or payload changes; `ContractConfig.Observer` is Go API, and the file-sink schema is pinned with ADR coverage. (Note: `go build`/`go vet` were blocked by sandbox approval, so compile status rests on the ten prior gate rounds over this diff.)

## Findings

**Blockers** — none.

**Should-fix**

1. **ADR addendum contradicts the staged diff's content.** `docs/kb/decisions/ADR-0014-observability-metrics.md:240` ("the JSON file sink and the scenario `metrics/*.yaml` bindings land in the immediately following commit") and `:291` ("the JSON file sink … lands in the following commit") — but this staged diff *contains* `engine/metricsjson.go`, both `-metrics-out` flags, and `engine/scenario/metrics.go`. If this ships as one commit, the ADR misdescribes its own landing commit (KB-currency rule). If you intend to split the staged diff into two commits, note that the gate stamps the full staged snapshot — confirm the split mechanics actually pass, or fix the text.

**Nits**

2. **Overshoot refunds can smear across interval boundaries.** `engine/metrics.go:962-975` (`accountShares`): the overbooked distance (`splitTick` books `v.S` to a tiny internal lane even when `v.S > Length`, ~line 937) lands in one interval and the negative refund in the next, so a lane's `IntervalRecord.SumDistM`/`Q` can go negative (or inflated) for a single interval on sub-vehicle-length internal lanes. Run-aggregate is exact (tested), but the per-interval artifact is undocumented — one sentence on `IntervalRecord` or in the ADR addendum would spare a downstream consumer a confusing negative q. Defer beyond that; it's v1 semantics.

3. **EnqueueSpawn compat edge lives only in a code comment.** `engine/director.go:66-77`: the retroactive-unreplayability note (pre-change local-harness recordings with simultaneously-live duplicate RequestIDs) belongs in the commit message or KB per your own triage rule, not just the doc comment. The change itself is sound — contract-layer dedup is run-wide first-seen and rejects empty `request_id` (`natsio/contract.go:592-599`), so wire recordings can't contain the case and the error surfaces as a verb rejection, never a run failure.

4. **serve compiles the network twice and discards work.** `engine/cmd/serve/main.go:207-215`: `BuildNet` runs once for the metrics config and again inside `RunLive`; `DefaultKernelConfig(mnet, …)` is computed then immediately overwritten when `scen != nil`. Wasted work only — the kernel resolves lane IDs against the live engine's net, so there's no aliasing bug.

5. **simrun prints `metrics=<path>` before the write.** `engine/cmd/simrun/main.go:113-127`: a failed `writeMetricsFile` exits 1 after the summary line already advertised the path. Cosmetic.

**Questions**

6. **Hash-protocol transition without the promised format_version bump.** `engine/scenario/scenario.go:267-283`: metrics parts move from raw-byte to canonical-YAML hashing while `FormatVersion` stays 1; ADR-0012 said this transition would come "under a format_version bump." No in-tree scenario carries a metrics part and free-form old parts now fail parse loudly rather than silently re-hashing, so nothing durable moves — confirm intentional and let ADR-0012's note stand corrected by ADR-0014 §5, or say so explicitly.

The kernel itself is in strong shape: the pairing guard, seeded-vehicle trip semantics, single-clamp time-loss split (with a test that genuinely discriminates the formulas), gap-heuristic-plus-exact-reconciliation for invisible vehicles, and the conservative-and-loud ambiguity handling are all internally consistent and well-tested (conservation, replay equality, and byte determinism are the right invariants to pin).

REVIEW-COMPLETE
