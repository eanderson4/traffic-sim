should-fix — `engine/natsio/run.go:205`: `rec.LogStart` durably writes the initial keyframe before `reg.Start`. If the subsequent KV write fails, the store contains a non-empty recording with no `RunMeta`/spec/status; replay cannot open it, and `checkFreshRecording` rejects reusing the run ID. Previously this path produced an aborted registry entry. Add rollback or a pre-readiness metadata state, then transition to `running` only after responders and static data are ready.

question — `engine/natsio/run.go:196`: `StatusRunning` now carries a client-visible readiness guarantee. Since consumers rely on this lifecycle semantic, should ADR-0006 record it rather than only the cross-topic KB article?

REVIEW-COMPLETE
