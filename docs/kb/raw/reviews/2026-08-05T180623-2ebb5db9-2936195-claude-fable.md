All context gathered. Here is the review.

---

**Rename is coherent end-to-end** (map app `index.html`→`app.html`, splash→`index.html`): demosrv `/{$}`→splash, `/app/`→`handleApp` now serving `app.html`, static FileServer covers `/app.html`, the legacy `/?bake=` redirect in the new `index.html` covers circulated quiz/show links (including via `/index.html?bake=`), and `mksite.sh`'s RUNS list exactly matches the three manifests referenced by `viz/public/quiz.html` and the splash CTA. No engine/determinism surface is touched (the Go change is strings only), and no NATS subject/payload changes, so no ADR is strictly required.

Findings:

**should-fix**

1. `viz/src/splash.ts:21-23` — Hardcoding `BAKED_FEATURED_URL` to `location.origin + "/baked/chishow/…"` makes `baked` always true, so the entire live-demosrv fallback (registry fetch, start POST, 401/403 gating — half this file, and still promised by the header comment at `splash.ts:4-9`) is unreachable dead code on *every* deployment. On the GKE demosrv deploy that `deploy/README.md` still describes as the target, demosrv serves no `/baked/*` route and `dist/` contains no bakes, so the featured card deep-links to a manifest that 404s — the one CTA on the landing page dead-ends, and this is reachable in normal operation the moment that deploy goes live. Either probe for the bake (HEAD the manifest, fall back to live mode on failure) or record that demosrv deploys are now knowingly Pages-only for the splash and update `deploy/README.md` accordingly.

2. `viz/public/_headers:11-15` — `/baked/*.br` and `/baked/*/index.json` (and `:5` `/baked/*/network.geojson`) use non-trailing splats. Cloudflare Pages `_headers` matching is documented around trailing splats; if infix/suffix splats silently don't match, `.br` chunks ship without `Content-Encoding: br` (exactly the "replays show no vehicles" failure `mksite.sh:59-60` warns about) and `index.json` keeps `max-age=31536000, immutable` from the `/baked/*` rule — a stale pinned manifest cached forever, which the comment itself calls the unacceptable outcome. The failure is completely silent. Verify header behavior on a preview deployment (curl `-I` a `.br` chunk and an `index.json`) before any link circulates; also confirm `! Cache-Control` removal respects the rule ordering you rely on.

3. `scripts/show/mksite.sh:41-47` — RUNS is a hand-maintained duplicate of the manifests baked into `splash.ts` and `quiz.html`. The check fails loudly when a listed bake is *missing*, but not when a *new* bake reference lands in the viz without a RUNS entry — that deploys half a site that "breaks silently," the script's own stated worst outcome. Grep `dist/` for `/baked/.*/index.json` and assert the set matches RUNS (or derive RUNS from it).

**nit**

4. `scripts/show/mksite.sh:25` — default `BAKED=/home/eric/grove/traffic-sim/data/baked` is machine-specific; the env override exists, but deriving from `$ROOT` when the checkout has bakes would make the script portable.
5. `viz/index.html:597` — `location.search.indexOf("bake=") !== -1` also fires on any future param merely containing `bake=` (e.g. `?rebake=1`), and the redirect drops `location.hash`. Harmless today.
6. `viz/public/_headers:5-7` — the root `/network.geojson` rule matches nothing in the Pages bundle (`dist/` carries no top-level network.geojson); dead rule or a hint something expected isn't staged.

**question**

7. The phantomjam Cloudflare Pages project is a new public deploy target (brotli-in-place storage convention, immutable-cache contract on `/baked/*`, root-URL ownership moved from the map app to the splash). Docs are updated, but under ground rule 3 this looks like a decision of consequence — is an ADR (or an addendum to ADR-0020/0023) intended, so the Pages-vs-demosrv split (demos menu dead on Pages, featured bake dead on demosrv) is recorded somewhere more durable than script comments?

No blockers under the 2026-08-04 triage bar: nothing here corrupts simulation, replay, or the record plane, and the demosrv splash regression sits behind a deploy that is explicitly not yet applied — but finding 1 should be resolved (or explicitly recorded) before that Ingress ever goes live, and finding 2 verified before Pages links circulate.

REVIEW-COMPLETE
