- **blocker** — `viz/src/artic.ts:107`: the widened threshold assumes the entire heading sweep arrives with nonzero `dtS`, but `main.ts` derives `dtS` from `sample.tick`; during `SnapshotBuffer` interpolation that tick stays constant while heading advances, so most calls have `dtS = 0`. Legitimate tight turns can therefore cross the fixed 85° threshold and snap—e.g. ~75–80° articulation with a 0.5–0.8 s replay interval. `viz/test/artic.test.ts:183` tests a single lumped update, not the actual interpolated caller cadence, so it misses this regression.

REVIEW-COMPLETE
