## Review — stop bars + 45° approach cone (viz-only)

Verified: staged patch = staged index; all 11 `signals.test.ts` tests pass; staged `main.ts` is self-consistent (`THEME` import at main.ts:50, `LineString` in the geojson import at main.ts:26 — the working-tree theme refactor is unstaged and not part of this review); test arithmetic in the bar/cone tests checks out by hand.

**Blockers** — none.

**Should-fix** — none.

**Nits**
- `viz/src/signals.ts:113` — the shared-id feature-state trick (point + line under one id in a single GeoJSON source) is load-bearing for the whole design and only verifiable in a live map; `sigshot.mjs` counts head points but nothing headless asserts a bar ever gets colored. Fine to defer per the triage bar, but worth a line in the commit message that the bar-coloring path is screenshot-verified only.
- `viz/src/signals.ts:131` — with the per-join running-mean gate, a chained cluster (arms joined at ≤45° per step but spanning ~90° total) gets a bar perpendicular to the *mean* bearing, so outlier entries project to an over-long diagonal bar. Hypothetical geometry, self-evidently cosmetic — noting so it's a known outcome, not a surprise.

**Questions**
- `viz/src/main.ts:415` — bar "off" state renders in `THEME.noData`, the same tone as no-congestion-sample lanes drawn beneath it. Intentional per the comment, but at z≥13 an off bar over a no-data lane is invisible-by-coincidence. Acceptable?

**Determinism / contract** — clean. Clustering iterates `p.links` in wire order with a deterministic greedy `find`; float sums accumulate in fixed order; no wall clock, no RNG, no map iteration in Go (diff is TS-only). No NATS subject or payload change — `SignalHead.bar` is viz-internal derivation from existing static geometry (ADR-0006 M9 addendum unchanged), so no ADR needed. The header comment's measured head counts were updated with the new cone, which keeps the doc honest.

REVIEW-COMPLETE
