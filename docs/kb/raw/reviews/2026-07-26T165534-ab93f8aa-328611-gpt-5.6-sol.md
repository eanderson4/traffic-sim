- **blocker** `engine/natsio/driver_test.go:1167` — The claimed authoritative lag test waits until the complete response arrives before choosing each normal drain boundary. This makes lag=1 true by construction and gives each wire mode a different response-dependent schedule; it cannot detect whether batching misses a fixed production boundary. Therefore the “same boundary schedule” and M3 acceptance claims in `engine/BENCHMARKS.md:211` and `engine/BENCHMARKS.md:269` are unsupported.
- **should-fix** `engine/natsio/driver_test.go:1209` — The delayed probe drains immediately after publishing an observation without synchronization guaranteeing the driver has not responded. A fast callback can enqueue fresh intents before this drain, causing scheduler-dependent failures at `engine/natsio/driver_test.go:1215`. Use an explicitly controlled barrier rather than relying on a local NATS round trip losing the race.

REVIEW-COMPLETE
