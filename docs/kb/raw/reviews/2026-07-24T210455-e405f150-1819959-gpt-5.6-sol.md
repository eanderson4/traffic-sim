- **should-fix** `engine/cmd/demosrv/supervisor.go:341` — Readiness treats `{run}/meta status=running` as fully initialized, but `RunLive` writes that entry before recorder creation, NATS subscriptions, and initial frames. The one-time child-death check cannot close a subsequent failure race, so `/start` can return a dead/not-ready run. Probe an initialization signal established after live-plane setup.
- **should-fix** `viz/src/main.ts:333` — Every completed broadcast resets `sigReqRetries`, even while a request is incomplete. When that request times out at line 370, it retries with a replenished budget; periodic broadcasts can therefore defeat the documented three-request bound and repeatedly resend multi-megabyte tables.
- **should-fix** `contracts/asyncapi.yaml:455` — The new `stateSigReq` contract has no operation referencing it. Consequently the AsyncAPI source of truth does not describe which endpoint receives the request or sends replies, despite this being a new sacred subject and message flow.

REVIEW-COMPLETE
