- **blocker** `viz/public/_headers:8` — This marks every baked `network.geojson` as Brotli, but `engine/cmd/bake/bake.go:331` writes ordinary JSON and `scripts/show/mksite.sh:70` copies it unchanged. Small normal-operation bakes such as `mergejam` and `townbase` will be Brotli-decoded as raw JSON and fail to load. It also contradicts ADR-0023’s explicit “Content-Encoding: br applies ONLY to chunk objects” rule.
- **should-fix** `scripts/show/mksite.sh:25` — The default bake source is a developer-specific absolute path. The documented no-argument invocation fails in every other clone; derive it from the repository/environment or require `--baked`.
- **should-fix** `engine/cmd/demosrv/main.go:186` — Startup verifies only the splash `index.html`. A stale or incomplete build can pass this check while `app.html` is absent, making every live/replay launch return 404 without the intended warning.
- **nit** `viz/index.html:12` — Substring matching redirects unrelated queries such as `?notbake=x`. Use `new URLSearchParams(location.search).has("bake")`.

REVIEW-COMPLETE
