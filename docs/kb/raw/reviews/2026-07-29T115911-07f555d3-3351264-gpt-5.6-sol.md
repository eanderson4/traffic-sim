- **blocker** `engine/natsio/demand/director.go:277` — Warm-start fast-forward skips only arrivals through `StartTick`, but the cold director’s lead window has already submitted later arrivals retained in the restored `dirQueue`. The new contract starts with an empty dedup map (`engine/natsio/contract.go:332`), so those request IDs are accepted again and can double-inject, breaking demand identity and CRC continuity.
- **blocker** `engine/cmd/serve/main.go:262` — A saved-state seed mismatch only prints a note. Restore builds the continuation using the new spec seed (`engine/keyframe.go:237`), and future vehicles derive RNG streams from it (`engine/engine.go:273`); the demand director likewise uses the new seed. This silently splices history and continuation from different deterministic programs, contradicting ADR-0029’s bit-exact acceptance criterion.
- **should-fix** `engine/gridlock.go:149` — `jammedAtJunction` requires the immediate successor to be internal. `gateTarget` explicitly supports non-internal boundary stubs before controlled internal lanes (`engine/rightofway.go:138`), so vehicles held upstream through such imported stub chains never qualify for escape and can remain gridlocked indefinitely.

REVIEW-COMPLETE
