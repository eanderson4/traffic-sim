- **should-fix** — `engine/scenario/variant.go:95`: Explicit `null` overrides silently become “absent” because pointer/slice fields decode to nil (`seed: null`, `types: null`, `spawner: null`, `network: null`, etc.). This violates fail-loud semantics and ADR-0012’s “present fields replace; no removal directives.” Reject nulls in `variant.yaml`.
- **should-fix** — `engine/scenario/variant.go:185`: The materialized `Scenario` is not actually indistinguishable from a hand-authored scenario: `Dir` is the variant directory, while `Manifest.Demand/Control/Metrics` includes base-relative paths that generally do not exist beneath `Dir`. Any consumer resolving manifest references through `Scenario.Dir` will break.
- **should-fix** — `engine/scenario/variant.go:287`: `path.IsAbs` does not reject Windows drive-qualified paths such as `C:/base`; on Windows, `filepath.Join` may resolve these outside the variant directory despite the ADR’s “never absolute” rule. Validate using platform-aware volume/absolute-path checks too.

REVIEW-COMPLETE
