Review complete. All layout math in the regenerated artifacts checks out against the new generator code; findings below.

## Findings

**Should-fix**

1. `scripts/show/mkopenslide.py:229-244` — the signal legend derives cycle length, green duration, and the "main-road green" share from `sigs[0]` **alone**, then asserts it for the whole population: the new slides print "1057 fixed-time signals · 90s cycle, 82s main-road green (91%)" (`docs/show/diag/chi-loop-cbd__setup.svg`) and "486 … 80s main-road green (89%)" (kennedy). That derivation was honest for Bottleneck Town's four hand-authored signals sharing one plan; this change applies it to OSM-imported per-junction tlLogic programs (`engine/netimport/netimport.go:442-450` copies each junction's own phase list), where green splits vary per junction almost by construction. The comment "Derived, never asserted" no longer holds — the sentence asserts junction 0's split for 1,056 others. The coordination line already aggregates over all signals (`offs` set); do the same for cycle/green — e.g. assert uniformity (`{cycle_len(s) for s in sigs}`) and print a range or drop the split when plans differ. Presentation-only, no sim impact, so not a blocker under the triage bar.

**Question**

2. `docs/show/baselines.json` (chi-loop-cbd entry) vs `docs/show/quiz/chi-loop-cbd.json` — the new window note says 7000 ticks is the fidelity-gated cut because "at 12000 the saturated grid refuses 7% of demand at injection", but the quiz results shown on the same page were measured over 12000 ticks (`ticks: 12000`, 12 seeds), and the chi-loop-cbd caveat in `mkquiz.py:85-89` doesn't mention the refusal. Intentional? The paired design plausibly survives 7% refusal (it hits both arms), but the page will say the measurement horizon is one the baseline note calls fidelity-compromised. Worth a sentence in the caveat, or confirmation it's already accepted.

**Nit**

3. `scripts/show/build-quiz.sh:56-58` — chi-loop-cbd's `--peers` names `widen-secondary`, which is not one of the four quiz options (`retime-truckban`, `calm-secondary`, `transit-5`, `truck-ban`); and since both new pods are deliberately setup-only, the stated `--peers` rationale (frame identically to the option cards) applies to cards that don't exist. Harmless — peers only widen the bbox — but under `set -e` a missing arm dir on another machine kills the build for no presentation benefit.

**Verified clean**

- New two-row legend geometry is self-consistent: viewBox heights 297/280/166 match `LEGEND_PAD + LEGEND_ROW·rows` for two-row+signals, one-row+signals, and one-row/no-signals respectively; `merge-pod__setup.svg` correctly untouched; `bottleneck-town__setup.svg` diff is exactly the wording change and nothing else, so checked-in SVGs match the generator.
- Determinism discipline holds: new code is pure arithmetic; the existing sorted-sinks / PYTHONHASHSEED guard is untouched; no map-iteration or float-accumulation issues introduced.
- Consistency: bake hashes `7406456b6031`/`03c7bc162e73` agree between README and baselines.json; baselines keys match the quiz JSON filename stems mkquiz keys on; both quiz JSONs exist with non-null winners; setup-only satisfies mkquiz's all-or-none rule (0 of N is allowed); mkquiz validates bake paths at build time so a stale hash fails loudly.
- Contract: no NATS subject or payload touched; no ADR required for this diff.
- Couldn't verify from this checkout (data/ is gitignored): existence of `data/pods/chi-{loop-cbd,kennedy}/{base,arms}` and that arm dirs carry the network under the import's filename — build-quiz.sh will fail fast if not.

REVIEW-COMPLETE
