Review of the staged diff (ADR-0035: TSLB batched intent log + S2 stream compression).

**Blockers** — none. The codec is strict and shared with the v2 path, the header/payload tick cross-check closes the one hole between grouping-by-header and decoding-by-payload, all three in-repo readers handle both subjects, the contract change ships with an ADR + AsyncAPI bump + migration note, and there are no map iterations, wall-clock reads, or float accumulation in a determinism-relevant path. I verified nats.go v1.52.0's legacy `Fetch` passes `MaxBytes` through (no rejection), and `logcursor.nextMsg` only fails on `err != nil && len(msgs)==0`, so a byte-cap-terminated partial batch is consumed correctly; a zero-message max-bytes error would need a single message >16 MiB, impossible under the 768 KiB budgets.

**Should-fix**

1. `engine/natsio/tslb_migration_test.go:30-32,69,136` — the migration-equality test never exercises the split path, contrary to its own comment ("some ticks are wide enough to split") and its log line ("with splits forced"). Budget = `tslbHeader + 3*loggedIntentMax` = 1,113 B and flush needs `len(buf) > 748`, but ticks carry ≤4 records of ~62–90 B, so `buf` tops out near 380 B and no tick ever splits. Split correctness is covered only by `TestBatchedLogSplitsAWideTickAcrossMessagesInOrder` (which does verify content+order through `MaterializeRunRecord`), so this is a false-coverage claim, not a safety gap — either widen a few synthetic ticks past ~10 records or fix the comments.

**Nits**

2. `engine/natsio/recorder.go:383` — `loggedIntentMax` budgets 256 B for the controller name, but nothing enforces it: `appendLoggedIntent` writes `uint16(len(ctl))` uncapped and `Controller` is a verbatim subject token (`bus.go:348`). An oversized controller makes a record exceed the per-record reservation, so one batch can overshoot `IntentBatchMax`. Consequence-free at defaults (subject length caps the overshoot at ~4 KB against the 256 KiB headroom to NATS max_payload), and `TestLoggedIntentMaxBoundsARealRecord` only probes 200 chars — "too early to need this" is a fair triage, but a one-line `len(ctl) ≤ 256` guard or comment tightening would make the constant honest.

3. `engine/natsio/recorder.go:166-172` — StreamInfo-emptiness-then-UpdateStream is a TOCTOU: a competing writer could land a message between the check and the reconfigure. Sole-writer + local-first makes this theoretical; noting for the record.

**Questions**

4. ADR-0035 ships as PROPOSED (`docs/kb/INDEX.md:119` unchecked) while the implementing code lands in the same commit; prior ADRs (0032–0034) flip to ACCEPTED on ship. Intentional staging (accept after the milestone review), or an oversight?

REVIEW-COMPLETE
