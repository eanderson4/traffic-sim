- **should-fix** — `scripts/chicago/mkod.py:1291`: The integrated rate is an **expected demand volume**, not a realized vehicle count. Poisson sampling produces seed-dependent counts, starts each flow with an immediate arrival, and may carry sampled gaps across slice boundaries (`engine/natsio/demand/director.go:423`). Therefore `completed + stranded + active` need not equal this figure, and the claim at `scripts/chicago/mkod.py:1293` that any gap is delivery error is false.
- **should-fix** — `scripts/chicago/mkod.py:1016`: `tally()` records unrounded rates, while `emit_flow()` writes `veh_per_h` to one decimal at `scripts/chicago/mkod.py:462`. Across hundreds of flows, reported peak/volume can differ materially from the file despite repeated “as WRITTEN” claims. Tally the serialized rate or weaken the claim.
- **should-fix** — `scripts/chicago/mkod.py:494`: District accounting uses original destination weights after filtering, not the four-decimal weights written at `scripts/chicago/mkod.py:486`; it also does not renormalize after dropped weights, while the sampler normalizes implicitly. Thus “destinations by district” still does not measure the emitted file exactly.
- **nit** — `scripts/chicago/mkod.py:1101`: `rate` is now an integrated vehicle count but remains labeled `veh/h`; use `vehicles`.

REVIEW-COMPLETE
