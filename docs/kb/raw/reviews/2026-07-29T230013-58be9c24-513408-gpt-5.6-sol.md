- **should-fix** `engine/natsio/recorder.go:131` — `IntentBatchMax` validation omits the 18-byte TSLB header. A value of `loggedIntentMax` is accepted, but a maximal record produces `tslbHeader + loggedIntentMax`, exceeding the configured limit and potentially the broker payload limit.
- **should-fix** `engine/natsio/recorder.go:171` — Empty-stream adoption checks only `Msgs`. A purged or expired stream can have `Msgs == 0` with `LastSeq > 0`; the recorder retains `lastSeq == 0`, so its first `Nats-Expected-Last-Sequence: 0` publish fails. Reject nonzero `LastSeq` or initialize the OCC cursor from it.
- **should-fix** `engine/natsio/tslb.go:102` — Zero-record batches are accepted despite the contract requiring non-empty batches. Because `decodeTSLBMsg` only cross-checks ticks by iterating records (`engine/natsio/replay.go:433`), an empty batch can have mismatched payload/header ticks and can inflate `logIndex.lastTick`, allowing replay beyond the verified record. Reject `count == 0` or always expose and validate the payload tick.

REVIEW-COMPLETE
