- **blocker** `engine/natsio/bus.go:325` accepts explicit `intent_encoding: v2`, but the public contract says every value except `tsib` must be dropped (`contracts/asyncapi.yaml:105`, `contracts/asyncapi.yaml:222`, `docs/kb/decisions/ADR-0026-batched-intents.md:53`). This changes sacred demux semantics without updating the ADR, migration note, or AsyncAPI.
- **blocker** `contracts/asyncapi.yaml:683` makes `schema_version` optional for `ControlIntent`, contradicting the file-wide binary-header invariant (`contracts/asyncapi.yaml:15`) and ADR-0006’s envelope requirement. If headerless v2 is now the ratified contract, record that exception explicitly in an ADR/migration amendment rather than silently relaxing the schema.
- **should-fix** `contracts/asyncapi.yaml:231` and `contracts/asyncapi.yaml:698` describe sequence numbers as “per controller per vehicle.” The implementation has one monotonically increasing sequence per controller (`engine/natsio/contract.go:766`); vehicle ID merely sorts ahead of sequence during arbitration. Keep the outcome-neutral explanation, but describe the sequence model accurately.

REVIEW-COMPLETE
