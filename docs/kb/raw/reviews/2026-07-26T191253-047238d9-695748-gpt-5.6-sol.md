- **should-fix** — `engine/natsio/driver/driver.go:394`: `completedObsTick` advances unconditionally, although `publishIntent` and `flushBatch` silently ignore publish failures. It can therefore claim “fully published” after a partial or entirely failed response. Have the helpers return success/error and advance only after every intent publish succeeds.
- **should-fix** — `engine/natsio/driver_test.go:1228`: the 50 ms silence window is still a timing assumption, not proof that no response remains in flight. `CompletedObsTick` only follows asynchronous `Publish` calls; stable `SentIntents` plus an empty receiver buffer cannot exclude queued network/client output. The second window at `engine/natsio/driver_test.go:1287` can therefore release measurement before the sentinel arrives. Use a publisher-side flush or an ordered marker/ack barrier tied to the sentinel tick.

REVIEW-COMPLETE
