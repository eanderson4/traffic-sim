You are reviewing a traffic-simulation repo (deterministic Go engine, NATS contract, ADR-driven). Two commits just landed:

- 6724c36 — ADR-0012: scenario format (design decision)
- 33a55a1 — M11: its implementation

Do a critical design + code review. Read these first:
- docs/kb/decisions/ADR-0012-scenario-format.md (the decision, incl. the M11 addendum)
- engine/scenario/scenario.go and engine/scenario/scenario_test.go (the implementation)
- engine/cmd/scenario/main.go
- git show 33a55a1 (full diff: serve/simrun/demand-director wiring, RunSpec.Hash)
- AGENTS.md (project rules), docs/kb/decisions/ADR-0005-time-model.md (determinism discipline), docs/kb/decisions/ADR-0006-nats-message-contract.md (contract planes)

Focus on, in priority order:
1. Correctness bugs in the loader / strict-YAML fence / canonical fmt / content hash (engine/scenario/scenario.go).
2. Determinism risks: hash stability across machines and yaml.v3 versions, map ordering, float formatting in canonical YAML, path framing.
3. Design weaknesses relative to the ADR's own goals: diffability, content addressing, overlay readiness (variant.yaml + network delta patches are the next milestone — will anything in M11's shape make that harder?).
4. Contract/consistency issues: RunSpec.Hash in the run registry, the -scenario flag semantics, demand-director migration.
5. Anything under-specified in the ADR that will bite at overlay or observability-ADR time.

Be specific: cite file:line, rank findings by severity (blocker / should-fix / nit / question). Do NOT modify any files — review only. If something is actually good and load-bearing, say so briefly; don't pad.
