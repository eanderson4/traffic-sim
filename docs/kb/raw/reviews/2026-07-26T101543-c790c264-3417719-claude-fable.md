All spot-checks confirm the agents' claims (stale `next.vehs` during `boundaries()`, the flat `veh_per_h` read, the NDJSON left in the published city dir). Final report:

---

# External review — staged diff (127 files, +21.5k)

Five parallel subsystem reviews (engine core, bake pipeline, natsio/contracts, viz, scripts/docs); top findings independently re-verified against the staged code.

## Blockers

None. Specifically verified clean: TSRB/TSRL/index wire format matches `contracts/baked-replay-v1.md` field-for-field on both the Go encoder and TS decoder (offsets, LE, quantization round-trips); determinism sweep clean (no `time.Now`/new RNG in sim paths, vehicle order from `e.order`, every map feeding output is sorted or display-only, `SafetyDecel` defaults to 0 with recorded CRCs pinned); VerbRequest/loggedVerb ↔ asyncapi ↔ ADR-0021 match; brotli dep confined to `cmd/bake` with ADR-0023 justification, kernel stays stdlib-only; Python pipeline has no unseeded randomness and no set-order reaching shipped files; ADR-0024 reader is genuinely bounded.

## Should-fix

1. **`engine/engine.go:665-673`** — `CrossOverlaps` reads `next.vehs` mid-`boundaries()`, but the array isn't maintained during the pass: same-tick crossers/despawners remain with rewritten `S`, breaking the sorted invariant `sort.Search` needs, so the counter can invent or miss overlaps — exactly at the chained-junction-stub sites ADR-0025 uses it to localize. Deterministic and diagnostic-only. Filter `x.Lane == next` or count after `rebuildOccupancy`.
2. **`engine/metricsjson.go:172-183`** — `delivered_frac = injected/(injected+expired)` ignores directives still queued at horizon; a run ending with a backlog reads as fully delivered, defeating the validity-flag purpose stated at lines 66-70. Add pending-at-horizon to the denominator or as its own field.
3. **`engine/cmd/bake/bake.go:340-349`** — intermediate `network.geojson.ndjson` is written into the *published* content-keyed `city/{hash12}/` dir and never removed; anything syncing the prefix ships the full uncompressed NDJSON. Stage in a temp dir or delete after tippecanoe.
4. **`engine/cmd/bake/index.go:83-94`** — bake-config digest carries `brotliQuality` but not the brotli *library* version; a dep upgrade changes chunk bytes under an unchanged content key, violating the spec's "immutable forever" rule unless someone remembers to bump `bakeToolVersion`. Fold the module version (build info) into the digest.
5. **`engine/cmd/serve/main.go:418-423`** — `make(chan attachOutcome, *drivers+1)` runs before the `*drivers < 1` validation; `-drivers=-2` panics instead of exiting cleanly.
6. **`engine/natsio/logcursor.go:332`** — an empty nil-error Fetch returns `(nil, nil)` = end-of-record while `curSeq < lastSeq` — the silent truncation ADR-0024 §4 forbids. Dead code with nats.go v1.52 (zero-message Fetch errors), but if ever reached it presents a partial run as complete; route to the loud error path at line 329.
7. **`viz/src/baked.ts:403-404, 669-674`** — `frameCache`/`laneCache` never evict successful entries (only failed fetches); a long zoomed-in session accumulates every decoded chunk unbounded. Add a behind-the-playhead eviction window.
8. **`viz/src/baked.ts:399, 431-436` + `viz/src/main.ts:1148`** — frame 0 is always emitted empty (`gated` defaults true; `start()` runs before the first `reportViewport()`), so a deep link at zoom ≥ 13 forecloses the real tick-0 frame — violating the module's own invariant at baked.ts:26-29. Wire the initial viewport in before `start()`.
9. **`scripts/render-demand.py:94`** — reads flat `veh_per_h`, but mkod.py emits `slices`; every OD-demand origin renders at minimum size with a "0 veh/h" legend, and the zero (not-None) rate defeats the injects-only filter at line 100. Silent under-report of the artifact's one job.
10. **`docs/show/README.md:169-171`** — the Bonferroni paragraph says the CBD winner (p=0.0122) doesn't survive correction, but the shipped winner (line 84, cbd-options.md, quiz JSON) is `retime-truckban` at p=0.0028, which audit.md §6 says *does* survive. Guest-facing stats error.
11. **`docs/show/labels-chi.json:6`, `labels-cbd.json:5`, `labels-ken.json:7`** — "34% shorter, same green share" misdescribes `--retime 0.66`: it scales green phases only (mknetvariant.py:141-146), so the cycle shortens <34% and green share *falls*. On-camera claim is the opposite of what the tool does.
12. **`scripts/chicago/pod-chi-loop.sh`** — unlike pod-portal.sh, never generates a metrics part, so reruns get the 900 s-from-tick-0 default and (under `--warmup 4000`) measure only a 5-minute tail — the script doesn't reproduce the shipped chi-loop-urban report's `begin_s: 400` config.
13. **`scripts/chicago/pod-portal.sh:111`** — `--begin-s $(… $TICKS*0.1/3)` hard-codes dt and yields a non-integral begin_s for TICKS not divisible by 3, which the loader rejects; the `2>/dev/null` then hides the diagnostic. Works only for the shipped TICKS=12000.

## Nits

- `engine/keyframe.go:287-294` — v4 restore validates `LaneIdx`/`TypeIdx` but not `Destination`/`OffsetM` (EndWall/NaN smuggle path around the `EnqueueSpawn` guards).
- `engine/proj.go:47-68` — `parseUTMZone` silently assumes WGS84; reject unrecognized `+ellps`/`+datum` loudly.
- `engine/cmd/bake/bakefmt.go:88-89,146-147` — `header + per*n` with u32 `n` overflows int on 32-bit, bypassing the length check (test-oracle only today).
- `engine/cmd/bake/bake.go:379-404` — crash between staging rename and `index.json` write leaves a manifest-less "complete-looking" prefix; rerun self-heals.
- `engine/natsio/demand/director.go:314-317` — publish failure now logs and advances the sampler (old path retried next snapshot); arrival silently dropped on a dying connection. Also `Close`'s Flush comment (236-241) overstates — final-tick verb replies can still be dropped.
- `contracts/asyncapi.yaml:1118/1130` — reject-reason description text omits the EndWall-destination rejection the engine emits (director.go:140-141).
- `scripts/chicago/congestion.py:291,326,338`, `scripts/chicago/mkod.py:234`, `scripts/nulltest.py:63` — assorted ZeroDivisionErrors on empty/degenerate runs instead of graceful messages.
- `scripts/chicago/scaledemand.py:105-133` — "N veh/h removed" stderr sums deltas across all six slices (~4-6× the peak-hour figure an operator would check).
- ADR-0023 drift vs the normative spec: record-digest header list omits `schema_version` (ADR-0023:497-499 vs baked-replay-v1.md:112-114); `quant.origin` example omits the 100 m margin (ADR-0023:319).
- `scripts/osm-urban-us.typ.xml:61-68` — enumerated speeds match ADR-0022, but the file also changes `*_link`/living_street classes the ADR's table doesn't list.
- `viz/public/quiz.html:101-112` — `o.label`/`o.verdict` into `innerHTML` unescaped (build-time repo-authored data only, but `textContent` is free); line 107 renders p=1.8e-05 as "p=0.0000".
- `viz/src/baked.ts:657-661` — stall timer in `Promise.race` never cleared when waits win.

## Questions

- **`engine/cmd/serve/main.go:198`** — `spec.Params.SafetyDecel = *safetyDecel` mutates the spec *after* the scenario hash is fixed, so runs with different gate settings share a `scenarioHash` (carried into the bake key and index.json). Intentional per ADR-0025?
- **`engine/mobil.go:43-44`** — `tryRouteRecovery` consumes the lane-change slot (and under IDM skips `DecideLaneChange`), changing RNG consumption vs a route-blind run. Understood as a permanent fork for future re-record comparisons against route-carrying baselines?
- **Cross-language format testing** — the TS decoder tests decode hand-built fixtures and the Go side tests against its own oracle; nothing decodes actual Go-encoder bytes in the TS suite, so a shared spec misreading would pass both. My manual cross-check found none; a checked-in Go-produced golden chunk in the TS tests would close the gap cheaply.

---

Per the repo's triage bar (blockers only, one round): nothing here blocks the commit. The ones I'd still fix before this round closes, because they ship wrong *content* rather than hypothetical edge cases: #9-11 (demand render reads the wrong schema; guest-facing stats and label claims are factually wrong) and #3 (publishes the NDJSON). #1, #2, #4 are cheap and protect instruments/contracts the ADRs in this very diff lean on; the rest are defensible deferrals.

REVIEW-COMPLETE
