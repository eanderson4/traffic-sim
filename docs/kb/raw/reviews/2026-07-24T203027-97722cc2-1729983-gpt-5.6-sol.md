- **should-fix** `engine/geojson.go:85` — `WriteGeoJSONRange` permits a negative `end`; `(start=0,end=-1)` sets `start=-1`, then `geoJSONFeatures` indexes `nf.Lanes[-1]` at line 96. Clamp both bounds or return an error.
- **should-fix** `engine/cmd/demosrv/main.go:166` — remote IPv6 hosts produce invalid URLs such as `ws://2001:db8::1:8443`. Build host/port with `net.JoinHostPort`; also URL-encode the resulting `ws` query value at lines 358 and `engine/cmd/demosrv/replay.go:134`.
- **should-fix** `docs/kb/decisions/ADR-0018-chunked-geojson.md:46` — the ADR says part generations use the scenario content hash, but `engine/cmd/demosrv/geojson.go:91` hashes only network bytes. Align the decision/contract wording with the implementation; manifest-only scenario edits intentionally retain the same generation.

REVIEW-COMPLETE
