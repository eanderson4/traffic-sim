Review of the staged diff (viz-only: split network rendering into external/internal layer pairs, zoom-gate junction interiors at z≥13, wire the new line layer into the congestion toggle).

**Blockers:** none.

**Should-fix**

- `viz/src/main.ts:529-543` (patch lines 116-133) — layer order regressed: layers now stack casing(ext) → line(ext) → casing(int) → line(int), so `network-internal-casing` draws **above** `network-line`. At z≥13, where an internal lane meets its external approach, the internal casing's round end-cap (wider than the line, ~0.9 opacity) overpaints the external lane's congestion color at every junction mouth. Before the split, all casing sat below all lines. Reorder the four `addLayer` calls to casing, internal-casing, line, internal-line (or use `beforeId`).

**Nits**

- `viz/src/main.ts:479-483` (patch lines 41-44) — the casing comment's "Lanes without an edge group (junction interiors, stale caches) are all boundaries, so they degrade to full casing" now half-describes a different layer: interiors are filtered out of `network-casing` and live in `network-internal-casing`. Still literally true since the paint is shared, but a reader hunting for interiors in `network-casing` will be misled; reword to "junction interiors (now the internal pair) and stale caches".

**Verified clean**

- Toggle wiring and tests are consistent: both line layers under `state.congestion`, both casings never toggled; no other code references these layer ids (`viz/src/layertoggles.ts:58-59`, `viz/test/layertoggles.test.ts:64-70`).
- `internal` is a real boolean the engine already emits (`engine/network.go:35`, testdata GeoJSON), so this is not a contract change — no ADR needed. Legacy caches lacking the property degrade gracefully: `["!=", ["get","internal"], true]` treats missing as external, restoring the old all-zoom behavior.
- `minzoom: 13` matches the signal-head gate as the comment claims (other layers at `main.ts:729/751/828`).
- Congestion feature-state is set per-lane on the **source** (`main.ts:965`), so internal lanes pick up ratios with no updater change; `removeFeatureState`/`clearCongestion` unaffected.
- Shared `casingPaint`/`linePaint` objects across two `addLayer` calls are safe (MapLibre copies spec on add) and prevent drift as intended.
- No determinism surface touched (no sim/engine code, no RNG, no wall clock, no map iteration in a sampled path).

REVIEW-COMPLETE
