Review of the staged diff. The substantive content is README.md, deploy/README.md, deploy/k8s/cronjob.yaml (gated), the new ADR-0023 (580 lines), and ~60k lines of verbatim review-round archives under `docs/kb/raw/reviews/` (ungated docs). No code, no NATS subject/payload changes staged.

**Blockers** — none.

**Should-fix**

1. ADR-0023 internal contradiction on the degrade path when *zero* regions have arrived. `docs/kb/decisions/ADR-0023-baked-replay-pipeline.md` item 3 (diff.patch:308-313) says the 2 s stall "degrades to emitting without the laggard"; item 6 (diff.patch:445-448) says the degrade path emits "the partial union of arrived regions — never an empty frame" because an empty frame permanently forecloses tick T. On initial load over a slow network *all* viewport regions are laggards, the partial union is empty, and the two rules conflict — the resolution (keep stalling until ≥1 region arrives? never degrade to an empty union?) is implicit. One sentence fixes it; worth pinning before implementation since round 3 already litigated this exact corner.

**Nits**

2. ADR-0023 (diff.patch:249-254): the claim that floor quantization "never overflows the byte" isn't airtight — `normalized` a few ulps below 2π can round to `q = 256` depending on expression order (`×256` first is exact and safe; `×(256/2π)` is not). Say "clamp to 255" or pin the operation order at implementation.

3. ADR-0023 (diff.patch:638-641) cites "ADR-0006's co-migration rule"; ADR-0006 contains no such named rule — the nearest is its `schema_version` discipline. Cite what's actually there, or add the rule to ADR-0006 when writing `contracts/baked-replay-v1.md`.

4. ADR-0023 item 8 (diff.patch:552-555): `index.json` is fetched `no-cache`, but it lives inside the content-addressed `{prefix}` and the site "pins the full index URL" — so it's as immutable as the chunks, and `no-cache` contradicts the "immutable on content-keyed objects" rule two bullets later. Harmless (one revalidation per page load), but pick one story.

5. deploy/README.md (diff.patch:43): minute-resolution timestamp still collides for two builds of the same HEAD within one minute — the exact stale-image failure the comment warns about. `%Y%m%d%H%M%S` is free.

6. ADR-0023 is dated 2026-07-26 and the r1–r3 design-review archives are stamped `2026-07-26T000000` — tomorrow. Presumably a deliberate sort-after-today placeholder; if not, fix the dates.

**Questions**

7. `contracts/asyncapi.yaml` is modified in the working tree but *not* staged (contracts are sacred per AGENTS.md). Not part of this patch — confirm it's intentional leftover, not a forgotten `git add`.

**Checked and clean**: TSRB/TSRL header and record arithmetic (20 B / 14 B / 5 B), u32 quantization headroom, 2 Hz↔stride-5↔dt 0.1, 1801-frame / final-single-frame chunk math, size-budget tables, ratio×170 ↔ 1.5 clamp ↔ u8, cronjob's 360k-tick ≈ 10 wall-h claim vs the 6 h recycle, buffer formula (625 ms at 1×), ADR numbering (0022 exists, no 0023 collision), token-rotation rollout-restart note (correct — env is pod-creation-time), no secrets in the archived review patches.

REVIEW-COMPLETE
