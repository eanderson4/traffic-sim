All doc claims verified against the implementation (bakeStride, recordHasher framing, manifestRegions union, aggregate grid, TODO markers, furniture.ts, the baked.test.ts fixture). Findings:

## Findings

**Blockers:** none. The staged diff is docs + tests, and every contract claim it adds matches the committed implementation:

- Cadence rule (`contracts/baked-replay-v1.md:13-17`) matches `bake.go:53-63` exactly, including the [1,2] Hz band and rejection.
- Record-digest framing (`contracts/baked-replay-v1.md:60-65`) matches `engine/natsio/resim.go:49-75` byte-for-byte: u64be seq, length-framed subject/header-block/payload, header keys `schema_version`/`tick`/`kf_chunk`/`sig_chunk` in that fixed order (`bus.go:49-52`, `recorder.go:94`).
- Region-union claim (`contracts/baked-replay-v1.md:43-45`) matches `manifestRegions` (`bake.go:503-510`).
- Exact aggregate grid / terminal-frame-carries-no-aggregate (`contracts/baked-replay-v1.md:28-30`) matches `bake.go:264-276` (`tick%aggEvery`, and off-stride terminal ⟹ off-grid since `aggEvery = stride×laneEveryFrames`).
- MVP-deferred digests (`contracts/baked-replay-v1.md:73-75`) are TODO-marked at `bake.go:357` and `bake.go:545` as claimed.
- The dt-derived cadence is already recorded in ADR-0023 (lines 186-189), so the contract edit is a clarification, not an unadopted contract change.
- New e2e assertions (`bake_e2e_test.go:88-89, 143-157`) mirror `bake.go:232-234`'s schedule; the baked.test.ts hold-last asserts match the fixture (`AGG_TICKS = [0,10,20]`, stride 10, tickEnd 20).

**should-fix** — `engine/cmd/bake/README.md:29`: "tippecanoe ≥ 2.49" turns a pin into an open-ended floor, but ADR-0023:110-111 says the version "is pinned in the bake README", and the ADR's real requirement is behavioral (no feature dropping/coalescing that destroys lane identity) — verified only at 2.49.0 and 2.78.0, not across the range or future releases. The content key protects immutability, not correctness of the tiles. State it as "verified: 2.49.0, 2.78.0" (or amend the ADR's "pinned" language in the same commit).

**nit** — `contracts/baked-replay-v1.md:63` "where present": the implementation treats a present-but-empty header value as absent (`resim.go:62`) and uses only the first value of a multi-valued header (`Header.Get`). The digest is durable identity; one clause pinning these two edge rules would make the contract reimplementable without reading the Go.

**nit** — `viz/test/furniture.test.ts:337-353`: the "never throw" test omits `geometry: null`, which is legal GeoJSON — and committed `parseFurniture` (`viz/src/furniture.ts:38,62`) would in fact throw on it for a head/bar with an id. Producer is your own bake script, so deferring is a valid triage, but the test name claims a property the code doesn't have.

**nit** — `engine/cmd/bake/bake_e2e_test.go:148`: `tk/aggEvery > uint64(nAgg-1)` silently assumes `TickStart == 0`; it's asserted at line 107 so it holds today, but a `(tk-idx.TickStart)/aggEvery` form wouldn't rot if the start ever moves.

**question** — `contracts/baked-replay-v1.md:13` says "round(0.5/dt)" without pinning tie semantics (Go is half-away-from-zero; dt 1.0 relies on round(0.5)=1). Consumers read `bakeEveryTicks` from index.json rather than recomputing, so this is documentation-only — confirm that's the intent.

REVIEW-COMPLETE
