Diff read; verified against `bus.go` demux, `driver.go` `New`, `tsib.go`, the batch-test helpers, and the existing asyncapi message style. Findings:

**Blockers** — none.

**Should-fix** — none. The change is small, additive, and consistent with its own claims: `applyIntentEncodings` runs in `New` before any subscription binds (driver.go:222), so the unguarded `d.cfg.IntentBatchOff` write is race-free against `onObs`; the fallback is idempotent and can only tighten (batch→v2), never loosen; the asyncapi doc claims (exact-case map lookup, first-value-wins, absent⇒v2, present-empty⇒drop) all match the shipped demux at bus.go:308–345.

**Nits**
- `engine/natsio/driver/driver.go:449` — the wire discriminator `"tsib"` is duplicated as a string literal because `intentEncodingTSIB` is unexported in `natsio` (bus.go:54). The comment owns this, but the driver already imports `natsio`; exporting the const would pin the fallback check and the encoder to one definition. Same for the literal `"v2"`, which has no const anywhere.
- `engine/natsio/bus.go:62` — `intentEncodingsAdvertised` is a mutable package-level `var` reused in every reply (contract.go:441); a `func` returning a fresh slice or a comment saying "never mutate" would be marginally safer. Cosmetic.
- `engine/natsio/driver/batch_test.go:358` (fallback subtest) — the log line at driver.go:452 fires with `d.id = "drv"` here but with `d.id` already set in production; fine. Real nit: `t.Run("batch on tsib advertised")` asserts only `!IntentBatchOff` plus record count — it doesn't assert the `intent_encoding` header, but `decodeTSIBMsg` does that internally, so covered.
- `engine/natsio/tsib_test.go:485` — the `nc.Request` goroutine swallows its error; on failure the test dies 10 s later with the less-specific "hello never answered". Matches existing test idiom; fine.

**Questions**
- `contracts/asyncapi.yaml:` `IntentEncodingHeaders` declares only `intent_encoding`, while `ControlIntent` uses `SchemaHeaders`. That matches the wire (tsib.go:99 sets only the demux header; driver v2 intents are headerless per the batch-off test), so presumably intentional — confirming schema_version is deliberately not carried on TSIB messages.
- ADR-0026 amendment says "engine deploys before TSIB drivers" is no longer load-bearing — correct for the default driver, but only for controllers that implement the SHOULD-fallback. The contract text says SHOULD, not MUST; acceptable for an advisory, just noting the residual gap is documented as such.

Contract discipline is satisfied: additive `HelloReply.intent_encodings` with `omitempty`, info version bumped 2.4.0→2.5.0, ADR-0026 amended in place with the ratification note, KB index updated. No determinism exposure: no map iteration in the new paths, no wall clock feeding state, the fallback decision is a pure function of the hello reply.

REVIEW-COMPLETE
