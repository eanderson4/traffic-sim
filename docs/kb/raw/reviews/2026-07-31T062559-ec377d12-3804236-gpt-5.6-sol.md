- **blocker** — `engine/engine.go:95`, `engine/scenario/scenario.go:207`, `engine/scenario/scenario.go:410`: manifests omitting `adaptive_routing` retain their existing content hash, but now materialize `AdaptiveRouting=true` instead of `false`. Thus identical `(content-hash, seed)` produces a different trajectory, violating ADR-0012’s run-identity guarantee and potentially conflating historical and new runs. Version/migrate the default or hash the fully defaulted parameters.
- **should-fix** — `docs/show/README.md:284`: one aggregate survivable-demand bracket does not establish that every published what-if result is “conservative”; adaptive routing may change intervention effects non-monotonically. Narrow this claim to the measured bracket.

REVIEW-COMPLETE
