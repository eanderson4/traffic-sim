- **blocker** `engine/routing.go:200` — `routeTabs`/`routeEpochs` are history-dependent but omitted from keyframes. With more stale destinations than the budget, the live engine serves stale tables while a restored engine treats them as cold and performs unmetered fresh builds at `engine/routing.go:215`, diverging immediately. The restore test exercises only one destination and misses this case.
- **blocker** `engine/routing.go:266` — Replacing individual Dijkstra hops with static-table hops can combine two acyclic trees into a cyclic table. The 30-second additive margin invalidates the stated telescoping proof, and no final cycle check exists. A free-flow loop produces floored free-flow samples, so the claimed “self-poisoning” backstop need not break it.
- **blocker** `engine/engine.go:59` — The new exported `Params` fields change the NATS KV run-registry payload because `engine/natsio/registry.go:29` embeds `RunSpec` and JSON-serializes it. ADR-0036 incorrectly states “no payload-schema change” and provides no contract migration note.
- **should-fix** `engine/routing.go:215` — Cold-table builds are unmetered, so the documented ≤4 Dijkstras/tick bound is false. A restore makes every destination cold, allowing hundreds of city-scale Dijkstras in one tick.
- **should-fix** `engine/scenario/scenario.go:424` — Negative `reroute_budget_per_tick` values pass validation and permanently disable stale-table recomputation. Reject values below zero, and require a positive value when explicitly supplied.

REVIEW-COMPLETE
