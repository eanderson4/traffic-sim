- **should-fix** `scripts/chicago/mkod.py:971` accounts portal demand before profile resolution/scaling at `scripts/chicago/mkod.py:989`; unmatched flows are silently omitted, while relocated volume and printed mass-balance totals still count them. Interior diagnostics similarly ignore `pscale` at `scripts/chicago/mkod.py:1046`. Resolve profiles before accounting/relocation or fail on unmatched flows.
- **should-fix** `scripts/runreport.py:559` mixes subset corridor lane-km with whole-network lane-km when printing corridor share. The JSON repeats the mismatch at `scripts/runreport.py:725`, so text and viz derive different percentages from one report.
- **should-fix** `scripts/whatif.py:255` counts stranded trips as horizon-censored, despite stranded vehicles having already exited through the gridlock escape. Consequently `censored_frac` at `scripts/whatif.py:271` overstates the population still driving.
- **should-fix** `viz/src/flowpanel.ts:254` divides every bin by the full configured width, although `scripts/show/mkflowcurve.py:41` deliberately emits a partial final bin. Final-bin rates are understated, especially when the horizon is exactly divisible and the last bin contains only the horizon tick.
- **should-fix** `viz/src/flowpanel.ts:256` excludes strandings from the y-axis maximum but draws them at `viz/src/flowpanel.ts:276`; a mass-stranding burst is silently clipped, hiding the primary gridlock signal.
- **question** `scripts/show/mkflowcurve.py:121` introduces a versioned Python→TypeScript data contract, but ADR-0030 documents only the run-report schema. Should this flow document be added there or receive its own ADR?

REVIEW-COMPLETE
