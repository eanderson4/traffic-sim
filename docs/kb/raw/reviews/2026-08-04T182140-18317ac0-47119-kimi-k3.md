• Now let me verify the kernel/wire semantics the controller mirrors.

• Now checking the wire contract types and kernel phase math.

• A few final checks on serve integration and bracket flags.

• Review complete. Verified the controller's mirrored semantics against the kernel (`engine/sigctl.go:291` chain rule, `engine/signal.go:139` phaseAtElapsed, `engine/natsio/sigframe.go:258` wire PhaseAt), the contract types, and the bracket script's whatif.py imports. All cited line numbers are in the new files as staged.

  **Blocker**

  - `engine/natsio/sigctl/sigctl.go:289-297` + `:762` (cadence gate) — mid-walk lapse to fixed time. Walk steps command intermediate/transition phases at their natural duration (`walkStep`, `:419-425` — e.g. 10 ticks), but decisions are hard-gated to every `CadenceTicks` (20) snapshots in `onSnapshot`, and the walk keep-condition requires `holdUntil-tick >= RenewBelow` (30), which a 10-tick hold can never satisfy. So with default flags, *every* switch walk goes: 10-tick transition hold → lapses → **~10 ticks of fixed-time schedule** (`sigPhaseAt` falls back to `phaseAtElapsed`) → next walk-step verb. The fixed-time fallthrough can re-green the movement being vacated, after which the next step jumps it green→red and the target red→green with no live clearance — precisely the "free clearance / never a jump" behavior the M2 addendum claims is simulated, and the shipped +15.9 % smoke numbers were measured with it. Side effect: each lapsed step hold fires a `signal_lapse`, breaking the "≤ 1 lapse per service per program" record-volume claim. Fix direction: don't cadence-gate while `ps.target >= 0` (decide every snapshot mid-walk), or command the next step before the current step's hold ends.

  **Should-fix**

  - `sigctl.go:250-253` — `estimated()` underflows: `tick - phaseElapsed(...)` wraps when `tick < OffsetTicks%cycle` (the kernel's own wrap semantics mean the phase "began" before tick 0; `phaseAtElapsed` can return elapsed > tick). `since` becomes ~2⁶⁴, so the gap-out min-green check `tick >= since+MinGreenTicks` can never pass for the first `offset` ticks of a run. Conservative direction (fixed time persists; the contested max-green path doesn't use `since`), and the 6,000-tick warmup mostly hides it — but saturate the subtraction.

  **Nits**

  - `sigctl.go:819-833` (`onReply`) — rejections only bump a counter; the optimistic `myPhase`/`holdUntil` set in `send()` is never rolled back. A rejected verb (e.g. duplicate request id after a controller restart — ids are `sigctl-{prog}-{seq}` with seq restarting per process, colliding with the override table's dedup window, `engine/sigctl.go:209-215`) leaves the controller tracking a hold/phase that was never applied, for up to HoldTicks. Self-corrects at `holdUntil`; the live test asserts 0 rejections, so this is restart-robustness only.
  - `sigctl.go:762` — `ps.lastDecide != 0` carve-out: a decision landing exactly on tick 0 leaves `lastDecide == 0`, disabling the gate for the next snapshot (one doubled decision). Harmless.
  - `sigctl.go:656-691` (`newProgState`) — only ≥1 non-nil detector is required; a candidate movement whose green links all lack shapes never registers presence, so the controller can "gap-out" a phase that is actually serving traffic and never see its calls. Consider requiring detector coverage of candidate green links, or logging partial coverage.
  - `sigctl.go:581-583` (`onSigTable`) — a whole-table frame (`hdr == ""`) installs without resetting `acc`/`accN`, so a stray later chunk can complete a stale generation. Inert today (table bytes are static); loose invariant.

  **Questions**

  - The ADR defers the `c.progs` map-iteration verb-order nit as "sim state unaffected" — confirmed: verbs for distinct programs don't interact in the drain, and any SigLog order variance is record-plane only. Triage stands; just noting the recorded bracket run's verb *bytes* are not run-to-run reproducible even at pace 0 (applied ticks can still lag a boundary there, per your own F note).
  - `cmd/sigctl/main.go` never calls `ctl.Ready()` — fine in practice (the table `PublishRequest` flushes all pending SUBs), but the exported `Ready` contract is then only honored by the serve embed. Intended?

  Verified clean: chain bookkeeping matches the kernel exactly (equality continues, `canExtend` matches the decline point); `phaseElapsed` matches kernel/wire math; director grant correctly requested (signal_set rejects signal-grant controllers); no RNG/wall-clock in decision paths; presence counting is integer-only; grid is lookup-only, no map iteration leaks into sim-affecting order beyond the deferred nit; bracket script's `whatif` symbols all exist; `-sigctl` attach failure is fatal before tick 0 (never a degraded run).

  REVIEW-COMPLETE

