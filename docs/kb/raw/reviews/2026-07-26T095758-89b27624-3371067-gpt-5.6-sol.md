- **blocker** `engine/cmd/serve/main.go:190` — `SafetyDecel` is applied after `Scenario.RunSpec` fixes `Hash` at `engine/scenario/scenario.go:408`. Runs with different safety physics therefore share the same scenario identity, directly contradicting ADR-0025’s requirement that this parameter participate in the content hash.
- **blocker** `engine/engine.go:382` — `leaderAt` always follows `Successors[0]`, while actual crossing may follow `Route` or `HeldTurn` (`engine/engine.go:720`). Consequently `safetyGate` at `engine/engine.go:515` can guard against the wrong branch and allow a routed vehicle to cross into an occupied successor.
- **should-fix** `viz/src/baked.ts:431` — `start()` emits frame zero before the caller can invoke `setViewport`; because `gated` initially equals true (`viz/src/baked.ts:399`), every baked replay begins with a false empty frame even when initially zoomed above the vehicle gate. `main.ts:1148` reports the viewport only after subscription completes.
- **should-fix** `engine/metrics.go:1269` — despawn attribution assumes a routed vehicle necessarily reached its route destination whenever that destination is reachable. An explicit held turn overrides routing (`engine/engine.go:722`) and may send it through an exit instead, causing fabricated destination, distance, and completed-trip metrics.
- **should-fix** `engine/cmd/serve/main.go:190` — `-safety-decel` lacks finite/non-negative validation. Negative values silently disable the gate; NaN propagates through `engine/engine.go:530` and can later make run metadata serialization fail rather than producing a clear usage error.

REVIEW-COMPLETE
