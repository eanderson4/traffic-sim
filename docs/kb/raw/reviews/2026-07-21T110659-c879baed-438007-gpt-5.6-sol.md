- **should-fix** `scripts/external-review.sh:38` — Reviewers use the live working tree for context, so unrelated unstaged edits can make a staged change appear correct when the committed tree is not. The warning only detects files that are both staged and unstaged, and does not fail closed. Review against a temporary checkout of the captured index tree.
- **should-fix** `hooks/pre-commit:36` — Converting NUL-delimited paths to newline-delimited text can incorrectly exempt newline-bearing filenames. For example, `docs/foo\nbar.md` splits into two fragments that both match the ungated regex, contradicting the stated fail-safe behavior. Preserve NUL delimiters during classification.
- **nit** `hooks/pre-commit:5` — The header says `tools/` is ungated, but the regex and ADR intentionally gate tooling.

REVIEW-COMPLETE
