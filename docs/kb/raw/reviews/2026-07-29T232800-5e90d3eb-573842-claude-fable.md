Read the full diff and the surrounding `mkod.py` context (`ProfileSet.resolve`, `emit_flow`, portal/interior/resident loops, header assembly). Verified the `peak_rate` half-open interval math against the tests by hand. Findings:

**Blockers** — none.

**Should-fix**

1. `scripts/chicago/mkod.py:1101` — stale unit label. The `stranded` tuple now carries a whole-program **vehicle count** (`total_veh(...)`, mkod.py:1080-1082), and the summary line was correctly relabelled "vehicles" (mkod.py:1097), but the per-origin detail line still prints `{rate:.0f} veh/h`. Same value, two labels, in a warning the diff itself says calibration leans on. (The loop variable is also still named `rate`.)

**Nits**

2. `scripts/chicago/mkod.py:1288` — the `# REALIZED:` header block is emitted unconditionally; an empty program writes "peak 0 veh/h at t=0s" into the artifact, whereas the stderr twin is guarded by `if peak:` (mkod.py:1224). Harmless, just asymmetric.
3. `scripts/chicago/mkod.py:494` — `observe`'s integral counts every slice with `rate * f > 0`, but `emit_flow` writes `veh_per_h: {r:.1f}` (mkod.py:462), so a slice in (0, 0.05) is counted in the realized totals yet lands in the file as `0.0`. Sub-vehicle noise; noting only because the diff's stated standard is "measure the file, not the intent."

**Questions**

4. `scripts/chicago/mkod.py:1060-1063` — relocation is taken off the corridor **before** the `pf is None` check (mkod.py:1070), so a portal whose rule set resolves to no profile still deposits its `--ramp-share` into `relocate`, which the interior loop then re-emits under the interior rule. That bends the diff's own invariant ("an absent flow must not appear in a realized total") — though it's only reachable with no `default` in the library, the ordering is pre-existing, and the "relocation splits physical demand" framing arguably covers it. Intentional?

**Checked and clean**

- `peak_rate` elementary-interval max: half-open boundaries, gaps contribute zero, tie-break is earliest-interval and deterministic; all seven tests' expected values verified by hand, including the non-uniform-grid case. `total_veh` arithmetic likewise.
- The four correction claims in the ADR text match the code: `pscale` and shape fraction now enter every tally (`tally` records exactly what `emit_flow` writes, post-drop of zero slices); tallies moved after the `pf is None` continue; through share and district table are both on integrated counts; the realized-demand print sits after the resident and interior loops as the comment requires. Interior `eff` comes from the per-pick tuple (mkod.py:1125), not a stale loop variable.
- Determinism: all sampled iteration is over `sorted(...)` or insertion-ordered lists; float accumulation order is fixed; no wall clock, no RNG. The header now embeds computed floats, but they're a pure function of the inputs, so file bytes stay reproducible.
- Contract: generator-side only — no NATS subject, payload, or scenario-schema change; demand-file `format_version` untouched (header additions are comments). ADR-0028 correction and INDEX update ship in the same diff, satisfying the KB-currency rule. `profiles-am-kennedy.json` rule ordering is consistent with the first-match-wins trap the ADR documents (the corridor rule precedes the generic `kind=interior` rule), and all four rules should fire under the shipped setup.

REVIEW-COMPLETE
