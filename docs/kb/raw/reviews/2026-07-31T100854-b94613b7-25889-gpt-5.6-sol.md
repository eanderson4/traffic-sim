- **blocker** `engine/cmd/serve/main.go:239` — Explicit `-adaptive-routing` overrides scenario-owned, content-hashed `params` without changing `spec.Hash`. This permits two trajectories under identical `(content-hash, seed, engine build)`, contradicting ADR-0012 and the addendum’s run-identity rationale; warm-start hash checks also miss the mismatch. Reject this flag with `-scenario`, require it to match the manifest, or incorporate the override into identity.

REVIEW-COMPLETE
