Both cited tests exist. Review complete.

## Findings

**Blockers** — none. The diff is benchmark-harness + BENCHMARKS.md only; no engine, contract, or wire-path changes, so no determinism or contract exposure. I verified the load-bearing claims against the tree: the TSIB header tick really is at bytes 8–16 LE (`tsib.go:68`, matching the patch at `bench_intent_test.go:248`); the stats accounting is sound — `bus` is recreated on every trial of the sub-benchmark closure, so `allTicks = warmupTicks + b.N*measTicks` (`bench_intent_test.go:350`) matches what a fresh `IntentBatchStats` counted, including warmups; mutating `m.Data` after `PublishMsg` returns is safe because nats.go copies the payload into the outbound buffer synchronously, and each goroutine patches only its own `batches[k]`; `TestBatchOnOffParity` (`driver_test.go:776`) and `TestBatchSplitAtCap` (`batch_test.go:238`) exist as cited; and the BENCHMARKS.md arithmetic (2–2.5× complete path, ~85–90% drain share at 30k) checks out against its own table.

**Should-fix**

- `bench_intent_test.go:349` — the fifth `IntentBatchStats` counter (`encodingUnknown`) is discarded while the other two drop counters are asserted zero. The sibling assertion at `tsib_test.go:451` checks all five. A record-count mismatch would likely catch an unknown-encoding drop indirectly, but so would it catch the drops you *do* assert — assert `encodingUnknown == 0` for the same price and consistency.

**Nits**

- `bench_intent_test.go:248` — the header-tick offset `8` is hardcoded rather than a named constant shared with `tsib.go`. A future header-layout change would silently corrupt magic/count here; mitigated because a corrupted batch would be structurally rejected and the batch-count assertion at line 351 would fail loudly, but a `tsibTickOffset` const would make the coupling explicit (tsib_test.go patches the same way, so this is a pre-existing pattern).
- `bench_intent_test.go:216` — the failure message says "over the TSIB cap," but `NewTSIBMsg` also returns nil when any record carries a route (`tsib.go:21`). Unreachable here (intents are accel-only), so message-accuracy only.

**Questions**

- `bench_intent_test.go:289` — the "applied-lag proxy" computes `e.Tick − hTick` from the local variable, never decoding the patched header tick back off the wire, so the pin proves "drain happens at the publish tick," not that the header field round-trips. BENCHMARKS.md is honest that this pins the measurement path rather than measuring lag, and the codec round-trip is covered in `tsib_test.go` — flagging only so the committer confirms that's the intended scope. The `lag_p50/p99` metrics are then zeros by construction (a `Fatalf` fires on anything else), which is dead weight as a metric but documented as such.

Doc claims in BENCHMARKS.md accurately describe what the code asserts (batches = controllers × ticks exactly, records fully expanded, zero drops, benchmark fails otherwise). No wall-clock, map-iteration, or float-accumulation issues in any sampled/deterministic path — everything new lives in the test package.

REVIEW-COMPLETE
