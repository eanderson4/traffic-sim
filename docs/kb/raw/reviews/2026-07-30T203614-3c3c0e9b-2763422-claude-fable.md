Review of the staged diff (routing weight: lane length → free-flow time).

**Verified clean:**

- **Correctness** — reverse Dijkstra unchanged structurally; `w` computed once per popped node (`engine/routing.go:188`), lazy-deletion guarded by `done[]`, strict `<` relaxation keeps first-order choice. The diamond fixture genuinely discriminates: via-b cost ≈ 23.7 s vs via-c ≈ 15.7 s under time weights (b wins 152 m vs 452 m under length), with a wide margin — no float-tie fragility. Test mirrors the established `routeForkSpec` pattern exactly.
- **Determinism** — weight is still a pure per-lane function; division is IEEE-deterministic and the divide-then-add shape isn't FMA-fusible, so no cross-platform contraction risk. Heap tie-break (lower index) and fixed pred order untouched. The ADR claim "runs without Route intents are bit-identical" holds: `routeNextHop` (engine.go:803) short-circuits on nil dest, and the table cache is derived state outside the CRC.
- **Contract discipline** — no wire change; `asyncapi.yaml` description updated in lockstep with a staged ADR-0006 addendum that squarely addresses the durable-binding question (routed recordings under length weights now break strict replay; documented as gitignored/regenerable/superseded, BAKED artifacts unaffected). This is the right shape per the repo's own 2026-07-24 precedent.
- The `freeFlowTime` fallback claim checks out: `netfile.go:148` rejects `SpeedLimit <= 0`, so the degrade path is defensive-only.

**Findings:**

- **nit** — `engine/routing.go:150` fallback returns `Length` (implicit 1 m/s), mixing meters into a seconds-weighted graph. Unreachable today and documented as such; fine under the triage bar, but if a future constructor ever skips netfile validation, a mixed-unit graph would silently mis-route rather than fail. A panic would be louder; not worth a round.
- **nit** (adjacent, not in diff) — `engine/cmd/default-driver/main.go:5` still advertises "Dijkstra routing for -dest," but the driver only sets the Route intent; this diff removed the last reference to the retired driver-side router's weight convention, leaving that header the only stale mention. One-line comment fix whenever that file is next touched.
- **question** — `routeLatDepth` (lateral gradient) stays hop-count-weighted while next-hop is now time-weighted. That's consistent in kind (it was never length-weighted, and lane changes aren't time-costed), but worth a one-line note in the routing.go header if the asymmetry ever surprises someone.

No blockers, no should-fixes. The change is well-scoped, tested with a discriminating fixture, and the contract/replay consequences are documented where the rules require.

REVIEW-COMPLETE
