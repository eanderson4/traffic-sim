- **blocker** `docs/kb/decisions/ADR-0014-observability-metrics.md:241` — The addendum says the file sink and scenario bindings landed, but the staged patch contains neither and has no production caller of `NewKernel`; this commit ships an unused kernel while documenting `simrun`/`serve -metrics-out` as available. Stage the integration or correct the addendum.
- **should-fix** `engine/metrics.go:949` — `crossedChain` returns the first BFS match without detecting multiple valid paths. Parallel/converging internal lanes can silently attribute distance to the wrong chain, contradicting the “ambiguous-branch cases … count DroppedCrossings” guarantee at `docs/kb/decisions/ADR-0014-observability-metrics.md:263`.
- **should-fix** `engine/metrics.go:1030` — `exitResolve` treats multiple paths to one exit lane as unambiguous because `exits` is keyed only by exit pointer, then selects the first parent chain. Different-length paths silently corrupt exit distance instead of incrementing `DroppedCrossings`.
- **should-fix** `engine/metrics.go:406` — Invisible-prefix detection is silent: `maxFirstSeenID == 0` suppresses gaps before the first observed spawn, and initial vehicles seeded into `k.last` do not seed this maximum. A first vehicle that spawns/despawns within one tick can therefore violate the “loud via DroppedCrossings” claim at `docs/kb/decisions/ADR-0014-observability-metrics.md:269`.
- **question** `docs/kb/decisions/ADR-0014-observability-metrics.md:247` — The addendum claims a mid-run restore can be re-observed with pre-seeded semantics, but `NewKernel` rejects every engine with `Tick != 0` at `engine/metrics.go:279`. Is mid-run metric re-derivation intentionally unsupported?

REVIEW-COMPLETE
