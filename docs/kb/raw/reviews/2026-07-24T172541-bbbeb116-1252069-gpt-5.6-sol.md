- **blocker** — `engine/cmd/demosrv/geojson.go:151`: manifest part URLs omit the content hash despite `viz/src/netload.ts:46` relying on hash-pinned URLs. A scenario edit during sequential fetching can silently combine parts from different network generations instead of producing the expected 404/retry.
- **blocker** — `engine/cmd/demosrv/supervisor.go:286`: readiness only checks that `{run}/meta` exists. A foreign broker running the same demo/run satisfies this while the newly spawned child dies from the port conflict, so demosrv can return a false-success start. Bind readiness needs child-specific identity/nonces, not run-name existence.
- **blocker** — `engine/cmd/demosrv/supervisor.go:25`: directly imports `github.com/nats-io/nats.go`, violating the repo’s explicit ADR-0006 dependency confinement to `engine/natsio/`. Put the probe behind an `natsio` helper.
- **blocker** — `engine/cmd/demosrv/geojson.go:67`: introduces a new chunk-manifest data contract, but the staged patch contains no ADR/migration note; `viz/src/netload.ts:47` even references ADR-0018, which is absent from what ships.
- **should-fix** — `engine/cmd/demosrv/supervisor.go:60`: `wsClientURL` exposes the listen host verbatim. Common valid bind forms such as `-ws :8443` or `-ws 0.0.0.0:8443` produce invalid/unreachable browser URLs; normalize wildcard/empty hosts to a client-reachable host as `serve` already does.

REVIEW-COMPLETE
