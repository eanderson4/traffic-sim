Verification complete. The staged diff is contract documentation (plus KB index/notes) for the already-committed ADR-0026 TSIB implementation, and I cross-checked every normative claim against the committed code.

**Verified accurate against code** (no discrepancies found):

- Header key `intent_encoding`, exact-case map lookup (not `Get`), absent⇒v2, `tsib`⇒batch, anything-else-including-empty⇒drop+count `intentEncodingUnknown` — matches `engine/natsio/bus.go:290-341` exactly, including the present-but-empty-slice case.
- Wire layout: 24 B header (magic/version u16/flags u16/tick u64 @8/count u32 @16/reserved u32) + 44 B records; offsets and field order match `bus.go:80-101` (`intentFixedBytes = 44`, route flag = bit4) and `tsib.go`.
- Validation split: route_len≠0 or bit4 ⇒ whole-batch drop; NaN/Inf ⇒ per-record drop; exact length `24 + 44·count`; count ≤ 20000 (880,024 B checks out, < 1 MiB); tick informational; header flags/reserved genuinely unread on decode — all match `tsib.go:110-139`.
- Counter names in the contract (`intentBatches/intentRecords/intentBatchDropped/intentRecordDropped/intentEncodingUnknown`, `Bus.IntentBatchStats`) match `bus.go:552-558`.
- Migration math: multi-record batch as v2 always fails (`24+44n > 44+48` for n≥2); single-record 68 B passes only iff bytes 40–41 (accel low bytes) read 24 — the contract's "pathological case" description is arithmetically correct against `DecodeIntent` (`bus.go:154-168`).
- Discipline: subject unchanged, add-only message, ADR-0026 committed (`e5291d1`), migration note present, INDEX link resolves. `payload: {schemaFormat, $ref}` + `x-json-view` mirrors the file's existing ControlIntent pattern.

**Findings:**

**question** — `contracts/asyncapi.yaml` (demux paragraph, ~line 208): the "never a fall-through to v2 parsing" guarantee covers unknown *values* of the exact key only. A producer whose client library canonicalizes header keys (emitting e.g. `Intent_encoding: tsib`) lands in the ABSENT⇒v2 path and gets misparsed as v2 — silently for the 68 B case, malformed-v2 drop otherwise (same backstop as the old-engine skew story). The exact-case semantics are documented loudly, so this is arguably covered; confirm ADR-0026's risks section owns the variant-*key* case, not just the variant-*value* case.

**nit** — `contracts/asyncapi.yaml` TSIBFrame description: `DecodeTSIB` explicitly treats count = 0 as valid (`tsib.go:108-109`), and `TSIBView.count` has `minimum: 0`, but the prose never states an empty batch is accepted. One clause would close the gap.

**nit** — ControlIntentBatch declares only `IntentEncodingHeaders`; ControlIntent declares `SchemaHeaders` while real v2 producers publish headerless (the documented hot path). Pre-existing tension, just inherited here — no action needed for this commit.

No blockers, no should-fixes. The contract text is unusually tight — it documents the implemented behavior byte-for-byte.

REVIEW-COMPLETE
