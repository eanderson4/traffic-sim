- **blocker** — `contracts/asyncapi.yaml:139`: chunking changes `ts.{run}.state.sig` incompatibly while retaining the same subject, `schema_version: 2`, and TSSG v1. Existing contract-compliant readers silently install individual chunks as whole tables, usually ending with only the last chunk. An AsyncAPI minor-version bump/documentation warning is not wire migration; use a versioned subject/envelope or dual-publish.
- **should-fix** — `engine/cmd/demosrv/supervisor.go:258`: `stop()` snapshots `startGen` before acquiring `mu`. A new start can acquire the mutex between lines 258–259, receive a newer generation, and ignore the abort while holding the lock for the full 660-second readiness wait. Stamp/abort the actual in-flight generation atomically with start ownership.
- **should-fix** — `engine/cmd/demosrv/supervisor.go:171`, `engine/cmd/demosrv/main.go:351`: queued concurrent starts become broken with per-spawn run IDs. Start A can unlock, then start B kills A before A’s handler writes/navigates to its returned URL; that URL permanently subscribes to A’s dead namespace. Reject/coalesce concurrent starts or verify the returned generation remains active before responding.

REVIEW-COMPLETE
