- **blocker** `engine/keyframe.go:364` — `request_id` length is encoded as `u16`, but `engine/natsio/contract.go:777` only rejects empty IDs and the schema has no `maxLength`. A valid `signal_set` with an ID over 65,535 bytes writes a truncated length followed by the full string, producing an unreadable v7 keyframe and breaking seek/replay. Enforce the codec limit at the contract boundary.
- **should-fix** `contracts/asyncapi.yaml:1443` / `engine/natsio/verb.go:61` / `engine/natsio/contract.go:797` — `phase` is described as required for `signal_set`, but the schema only requires `verb` and `request_id`, and the Go `int` cannot distinguish omission from phase 0. An accidentally omitted phase silently commands phase 0. Use a conditional required schema and presence-aware decoding.

REVIEW-COMPLETE
