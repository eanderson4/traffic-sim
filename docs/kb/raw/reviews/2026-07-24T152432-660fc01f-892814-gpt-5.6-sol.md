- **blocker** `scripts/osm-stop-nodes.py:105` — All-way detection counts distinct OSM ways, not directed approaches. Two stop signs on two crossing bidirectional ways are classified `allway_stop` even though only two of four approaches stop. This materially changes right-of-way behavior.
- **blocker** `scripts/osm-stop-nodes.py:96` — The resolved stopped arm (`wid`) is discarded after classification; output only retypes the junction. For `priority_stop`, netconvert chooses stopped connections from edge priorities, so the OSM-signed approach is not reliably the approach receiving `row: stop`.
- **should-fix** `scripts/overpass2osm.py:49` — Mixed input with geometry before its real node creates a synthesized node, then emits the later real node too (`scripts/overpass2osm.py:61`, `scripts/overpass2osm.py:70`). This explicitly supported shape produces duplicate OSM node IDs.
- **should-fix** `scripts/import-city.sh:49` — `netimport` omits `-source`, losing the netconvert version and two-pass stop workflow from provenance, contrary to the documented auditable recipe at `contracts/network-format-v1.md:198`.
- **should-fix** `scripts/import-city.sh:3` — The script claims to implement the contract bootstrap but introduces JSON conversion and a second netconvert pass while `contracts/network-format-v1.md:173` still documents XML and a single pass. Update the contract/KB so the canonical import recipe is unambiguous.

REVIEW-COMPLETE
