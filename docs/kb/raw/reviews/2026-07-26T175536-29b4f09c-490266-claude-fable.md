Review complete. I verified the diff against the repo: bench harness accounting (fresh `Bus` per sub-benchmark invocation, so the `IntentBatchStats` totals of `warmupTicks + b.N×measTicks` are right), driver emission shape (one intent per claimed vehicle per tick — the boundary FIFO's `needed: vehicles` holds on a stable ring), ack-before-obs order (run.go:184-185 matches the tap's assumption), header names, `KeyedIntent` fields, and imports.

## Findings

**Blockers:** none. No engine/kernel code changes; the only non-test production changes are `Bus.BufferedIntents()` (read-only, correctly locked, bus.go:565), `SubjectCtlAll` (a subscription-pattern helper over existing subjects — no new published subject, no ADR needed), and the `tsibTickOff` constant refactor (byte-identical encode, tsib.go:72). No determinism risks: the one map in the new test code (`touched`, driver_test.go:1211) is membership-only, never iterated; wall-clock use is confined to tests/benchmarks.

**Should-fix — environment-sensitive hard failures in the default test path** (driver_test.go:1330, driver_test.go:1315-1317). The fast-pace leg fatals if `v2Fast.splits == 0`: that asserts a 1.5 ms boundary lands mid-delivery of a ~sub-ms 400-message burst at least once in 300 ticks. Your box measured 18–23/300, but the straddle window scales with delivery time — a faster or quieter machine can legitimately produce 0/300 and fail spuriously. Same shape at prod pace: any deadline overrun on a loaded box fatals the whole test rather than invalidating the measurement. Since both are preconditions about the environment, not properties of the code under test, `t.Skip` (with the log) when the precondition fails would keep the structural assertions (`tsib.splits != 0` fatal, which is genuinely impossible) while removing the flake surface. Defensible to defer under the "no hypothetical hardening" rule — you have measured margins — but this is the one thing I'd expect to bite CI first.

**Question** (driver_test.go:1244, 1268): the boundary test's measurement loop drains intents but never calls `e.EnqueueIntent`, unlike its own warm-up loop — the engine never applies anything during measurement, so "COMPLETE-application" is really complete-drain, and the driver spends 300 ticks controlling a fleet that ignores it. Both legs are identical so the A/B comparison stands (arguably it's *more* controlled this way), but if it's intentional the comment claiming "a drain at pre-Step tick u applies at u+1" deserves a note that application is notional here.

**Nits:**
- driver_test.go:896-897: a v2 intent reaching the tap before the tap's first obs pairs against `lastObs = 0`, producing one absurdly large v2 lag sample. At n≈400 it can't reach the p99 rank, and the error direction is lenient-toward-batch, so it's harmless today — worth knowing it exists.
- BENCHMARKS.md:176 says v2 splits "~5–9%" while the M3 section (line 88) says "18–23/300 (~6–8%)" — same data, two ranges; pick one.
- bench_intent_test.go:286-291: the lag proxy is a tautology by construction (`e.Tick` cannot change between `hTick` capture and the drain — no `Step` intervenes), so the `b.Fatalf` can never fire and the reported percentiles are constant 0. The doc comment says as much; the assertion and percentile machinery are dead weight.
- bench_intent_test.go:417: TSIB `SetBytes` counts one header for the 2-batch 30 k row (24 B off in 1.32 MB — immaterial to the MB/s figure).

The BENCHMARKS.md claims are consistent with what the tests actually pin: the per-vehicle-p99 framing, the "structural vs report-only" split at fast pace, and the "PROVEN in M2 / not re-measured" attributions all match the code.

REVIEW-COMPLETE
