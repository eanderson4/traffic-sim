- **blocker** — `viz/src/nats-client.ts:79`: the viz now publishes `ts.{run}.state.sig.req` and subscribes to a dynamic `_INBOX`, but the documented observer authorization remains read-only on `ts.>` (`docs/kb/decisions/ADR-0006-nats-message-contract.md:65`). Under the repo’s contract, both request publication and reply receipt are denied, breaking the new attach/gap recovery path. ADR-0016 must explicitly amend the auth contract and AsyncAPI permissions for scoped request/reply access.

REVIEW-COMPLETE
