**Verification run:** full `go test ./...` in the engine module passes; `go vet` clean. Keyframe gating checked both directions (v6-into-off errors at `engine/keyframe.go:313`; pre-v6-into-on warns at :321, and restore constructs via `NewEngine` so the Step lazy-init sentinel can never clobber restored `ttEMA`/`ttSnap` — invariant at `engine/engine.go:409` holds). Determinism scan: `initAdaptiveRouting` iterates the lane slice, `routeEpochs` is keyed-access only, no wall clock or RNG in state. Hash claim checked: the golden-hash tests pass, and under yaml.v3 `omitempty` a nil `*bool` and the old zero `bool` canonicalize identically while `adaptive_routing: true` emits the same bytes under both types — the ADR's "no existing scenario hash moves" argument is sound. No subject changes; keyframe v6 gating already documented in `contracts/asyncapi.yaml:922-930`.

## Findings

**Blocker** — none.

**Should-fix**
1. `engine/scenario/variant.go:121-122` — a variant's `params:` block replaces the base's wholesale. A variant over a pinned base that overrides params for any other reason (e.g. `params: {dt: 0.05}`) silently drops the base's `adaptive_routing: false` and inherits the new default ON. Before this flip that lossage was harmless (both sides static); now it's a silent regime switch on exactly the class of artifact the addendum's identity argument leans on (offline metrics pinned only by manifest). The docs/show arms are M12 variants — confirm none override `params`, and either merge the pointer field field-wise or add a loud note/validation. One-line fix candidate: in `applyVariant`, carry the base's `AdaptiveRouting` forward when the variant's is nil.

**Nit**
2. `engine/keyframe.go:322` — first use of the process-global `log` logger in the otherwise-silent kernel package; no injection point (contrast natsio's `Log` field). Accepted as ADR-designed loudness; note it sets the precedent.
3. `engine/engine.go:409` — the lazy init covers `Step` only; a struct-literal flag-on engine calling `routeNextHop` before its first `Step` still hits integer divide-by-zero at `engine/routing.go:209`. Test-only surface, but the comment could say so.
4. `engine/engine.go:63` — "validated M1 defaults" doc now also carries a 2026-07-31 flip; stale label.

**Question**
5. The addendum's pin claim ("stamped on disk alongside this commit") lives in gitignored `data/` and can't be verified from the patch — the generator-script pins (`mkscenario.sh`, `bottleneck_town.py`, `merge-pod.py`) do ship and cover regeneration; confirm the already-materialized arm directories were actually restamped.

REVIEW-COMPLETE
