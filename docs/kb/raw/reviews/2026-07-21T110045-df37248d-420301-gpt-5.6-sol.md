- **blocker** — `hooks/pre-commit:26`: gated-path detection fails open. Any `git diff`/first-`grep` error makes the final `grep` return 1, which is interpreted as “docs-only.” Additionally, BSD/macOS `grep` lacks `-z`, so the gate silently permits every commit there. This contradicts ADR-0013’s fail-closed requirement.
- **should-fix** — `scripts/external-review.sh:38`: reviewers inspect the live working tree, while only same-file unstaged edits trigger a warning. Unstaged or untracked changes elsewhere can supply context that does not ship, undermining the claim at `docs/kb/decisions/ADR-0013-external-review-gate.md:42` that review always covers what ships. Review against a staged-tree worktree or reject any dirty context.
- **should-fix** — `docs/kb/decisions/ADR-0013-external-review-gate.md:54`: the ADR describes a fail-closed gate with a loud escape hatch, but both hooks are silently bypassable with Git’s standard `--no-verify`. Record this accepted limitation explicitly or enforce the policy in a non-bypassable server/CI gate.
- **nit** — `scripts/external-review.sh:48` and `hooks/pre-commit:32`: `git rev-parse HEAD` prevents the mechanism from reviewing an initial commit on an unborn branch. Use the empty-tree object as the base or document the limitation.

REVIEW-COMPLETE
