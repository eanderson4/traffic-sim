- **blocker** `hooks/pre-commit:24` — Unconditionally bypassing the gate when `MERGE_HEAD` exists allows unreviewed conflict resolutions—or arbitrary staged edits after `git merge --no-commit`—to ship. Parent reviews do not cover the synthesized merge tree, contradicting ADR-0013’s exact-tree guarantee.
- **should-fix** `hooks/pre-commit:19` — The allowlist omits existing code/tooling paths `prototypes/` and `.github/workflows/`. Changes there exit ungated at `hooks/pre-commit:22`, contradicting “every commit touching code, contracts, or tooling” in `docs/kb/decisions/ADR-0013-external-review-gate.md:25`.
- **should-fix** `scripts/external-review.sh:98` — `grep -qx` accepts the marker anywhere, including marker-only output or a marker echoed before truncation. This does not enforce the claimed completion/fail-closed behavior; require substantive output and the marker as the actual final line.
- **should-fix** `docs/kb/decisions/ADR-0013-external-review-gate.md:1` — The new ADR is absent from the staged KB registries: `docs/kb/INDEX.md:52` and `docs/kb/articles/decisions/adrs.md:29` still stop before ADR-0013. Matching updates exist only unstaged, so they will not ship with this patch.

REVIEW-COMPLETE
