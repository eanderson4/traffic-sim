- **should-fix** `scripts/chicago/mkod.py:1293` — `total_veh()` integrates nominal rates; it is not the number emitted by `spacing: poisson`. Sampling variance, the forced first arrival, and gaps crossing slice boundaries can produce a healthy run whose `completed + stranded + active` differs, so the header’s “gap … is delivery” diagnosis is false.
- **should-fix** `scripts/chicago/mkod.py:494` — District accounting uses unrounded `kept` weights, while the engine samples the four-decimal weights serialized at `scripts/chicago/mkod.py:486`, normalized by their rounded sum. With many destinations, the printed district and through shares can differ materially from the shipped demand file. Account using the serialized weights.
- **nit** `scripts/chicago/mkod.py:1016` — The new “what emit_flow writes” timeline retains full-precision rates and times, but serialization rounds rates to 0.1 veh/h and boundaries to whole seconds at `scripts/chicago/mkod.py:462`. Peak and vehicle totals therefore are not strictly file-derived as documented.

REVIEW-COMPLETE
