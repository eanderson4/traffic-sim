- **blocker** — `engine/natsio/run.go:40`: The public signal republication cadence changes to 20 ticks, but the AsyncAPI source of truth still promises keyframe cadence/default 100 ticks (`contracts/asyncapi.yaml:61`, `contracts/asyncapi.yaml:107`, `contracts/asyncapi.yaml:114`). Update the contract and associated stale documentation/smoke assertion (`engine/natsio/server.go:87`, `viz/scripts/sigsmoke.mjs:181`).
- **should-fix** — `viz/src/signals.ts:130`: Cluster IDs use only `(program, representative linkIdx)`. Multiple geometry clusters can originate from lanes sharing one `linkIdx`, producing duplicate feature IDs and overwritten `Map` state. Include a deterministic cluster discriminator or guarantee/test that each link index can form only one cluster.
- **should-fix** — `engine/natsio/sigframe_test.go:284`: The revised test only proves a fresh table arrives after tick 60; the old 50/100-tick behavior also passes. Assert the received tick is within `signalCatchUpEvery` ticks of subscription, and cover replay cadence too.

REVIEW-COMPLETE
