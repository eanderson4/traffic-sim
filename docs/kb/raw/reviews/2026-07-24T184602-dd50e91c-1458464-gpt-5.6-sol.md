- **blocker** — `docs/kb/INDEX.md:101`, `viz/src/nats-client.ts:72`: the staged patch declares ADR-0016 implemented and adds the client request path, but contains no staged engine publisher/responder changes. The shipped engine still emits one unchunked `SignalFrame` and has no `state.sig.req` responder; the city-scale failure remains. Stage the corresponding `engine/natsio`/serve/replay/contract implementation or leave ADR-0016 pending.
- **should-fix** — `viz/src/main.ts:363`: reply retries never occur. A gap resets `replyAccum.partial` to false, while a truncated reply leaves `partial=true` but never sets `retry`; therefore `retry && replyAccum.partial` is false in both failure modes. The advertised three-attempt recovery is dead code.
- **should-fix** — `docs/kb/decisions/ADR-0018-chunked-geojson.md:39`: the accepted ADR specifies hash-only part URLs, while `engine/cmd/demosrv/geojson.go:151` ships schema+hash URLs. Update the authoritative decision and `viz/src/netload.ts:6` to match the actual contract.
- **nit** — `engine/cmd/demosrv/main.go:329`: start responses interpolate `ws` raw, while `viz/src/demos-core.ts:65` percent-encodes it despite claiming identical URL shapes. Use one URL-construction/escaping rule, especially for IPv6 zone identifiers.

REVIEW-COMPLETE
