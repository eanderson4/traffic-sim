• Review of the staged diff (favicon links + ws-scheme-follows-protocol + friendlier live-connect failure). No Go/engine changes, no NATS subjects or payloads touched, no determinism surface — contract/ADR gates not implicated.

  **Blockers** — none.

  **Should-fix** — none.

  **Nits**

  - `viz/src/main.ts:1187` — the guidance text `/quiz.html` is plain text in the status element (`main().catch`, main.ts:1407-1413), not a clickable link. The person who needs this message most is the one least likely to retype a path. Acceptable as-is, but an `<a>` would make the recovery actually usable.
  - `viz/src/config.ts:117` — `protocol === "https:"` is correct for `location.protocol` (colon included), and the `"http:"` default preserves existing callers/tests. Fine; just noting the comparison is silently case/format-sensitive if a future caller passes `"https"` or uppercase. Test at `viz/test/config.test.ts:56-58` covers the three cases that matter.

  **Questions**

  - `viz/src/main.ts:1180` — the new message only appears when `connect()` (nats-client.ts:28) *rejects*. With nats.ws default retry policy (reconnect attempts + waits), an unreachable engine may take tens of seconds before the promise rejects, during which the user stares at the loading screen. Was that wait observed/acceptable on the Pages deploy, or should connect get a short `maxReconnectAttempts`/`timeout` for this default-URL case?
  - `viz/src/config.ts:117` — on the https Pages deploy this now defaults to `wss://<pages-host>:8443`, which only works if something TLS-terminates 8443 on that host. Confirm that's the actual topology (proxy/fly.io/etc.); otherwise the default flips the failure from "mixed-content throw" to "wss handshake fail" with no functional gain. (The improved error message mitigates this either way, so not a blocker.)
  - The comment in main.ts:1184 claims "the local serve-baked does no rewrite" so `/quiz.html` must stay literal — verified consistent with `scripts/chicago/mkquiz.py:12-14` (quiz.html sits next to the app at `/app/` under demosrv) and both generator and generated artifact were updated in lockstep. Good.

  Verified: `viz/public/favicon.svg` and `favicon-32.png` both exist, matching `viz/index.html:7-8`. Generator (`mkquiz.py:332-333`) and checked-in artifact (`viz/public/quiz.html:4-5`) are in sync.

  REVIEW-COMPLETE

