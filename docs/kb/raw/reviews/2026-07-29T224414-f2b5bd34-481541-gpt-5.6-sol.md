blocker — `contracts/asyncapi.yaml:651` and `contracts/asyncapi.yaml:679`: The writer/replayer operations still reference only `logIntent`. The new default `logIntents` channel is therefore absent from the AsyncAPI operation surface, so generated tooling describes neither the default publish nor read path. Add corresponding send/receive operations.

should-fix — `engine/natsio/recorder.go:131` and `engine/natsio/recorder.go:428`: `IntentBatchMax` is not a hard payload bound. Validation omits the 18-byte TSLB header, and splitting reserves `loggedIntentMax` rather than the actual encoded record length. Supported legacy controller tokens can exceed the assumed 256 bytes, producing oversized batches and potentially broker-rejected writes. Encode each record first and split using its actual length.

should-fix — `engine/natsio/logcursor.go:44` and `engine/natsio/logcursor.go:331`: The fixed 16 MiB pull cap is lower than the unrestricted configurable `KeyframeChunkMax`/`IntentBatchMax`. A valid recording containing a larger broker-accepted message fails with “Message Size Exceeds MaxBytes.” Either constrain writer maxima to the reader cap or make the cursor accommodate the configured/broker maximum.

question — `docs/kb/decisions/ADR-0035-batched-intent-log.md:3`: Is a default contract implementation intended to ship while its governing ADR remains `PROPOSED`?

REVIEW-COMPLETE
