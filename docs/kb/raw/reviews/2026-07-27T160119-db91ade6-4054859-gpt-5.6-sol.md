- **blocker** `scripts/show/build-quiz.sh:95` — The Chicago results use ticks 4000–12000 (`docs/show/chi-loop-options.md:3`), but the map defaults to warmup 6000. `scripts/show/mkcongestionmap.py:129` also has no end-tick cutoff, so longer metrics files include observations beyond tick 12000. The canonical build therefore does not enforce the claimed same measurement window.
- **should-fix** `scripts/show/build-quiz.sh:89` — `CHI_METRICS` is validated only for readability. A metrics file from another demand, network variant, set, or horizon—the exact stale/wrong-run failure this change targets—still succeeds. The sidecar records caller-provided provenance after the fact but verifies no identity against the quiz inputs.
- **should-fix** `scripts/show/mkcongestionmap.py:67` — Pillow becomes a new default-path dependency without a pinned dependency manifest or ADR, despite repo rules treating dependency choices as consequential. The inline justification and `CHI_SKIP_MAP` escape hatch do not make regeneration reproducible across environments.

REVIEW-COMPLETE
