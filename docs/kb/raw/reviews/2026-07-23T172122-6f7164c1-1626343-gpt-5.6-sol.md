- **should-fix** `engine/cmd/replay/main.go:41` — `-http` accepts `0.0.0.0`, public IPs, etc., and `net.Listen` uses it unchecked at line 105. This exposes unauthenticated replay controls beyond loopback, contradicting ADR-0006’s explicit trust-boundary requirement (`docs/kb/decisions/ADR-0006-nats-message-contract.md:286`).
- **should-fix** `engine/natsio/player.go:334` — `paceSleep` fixes its deadline using the old speed. `/speed` reports success at line 531 but cannot shorten the current floor; at very low speeds playback may remain frozen for seconds—or vastly longer—after increasing speed. A mid-floor seek similarly resumes waiting on the obsolete deadline.
- **should-fix** `engine/natsio/player.go:502` — `/resume` is not serialized with the run goroutine. Between stepping onto `endTick` and setting `done` at line 281, it can return 200 and `paused=false`, immediately followed by the loop forcing the end hold. The caller receives a successful resume that never resumes.

REVIEW-COMPLETE
