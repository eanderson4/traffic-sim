**Verdict: no blockers.** Presentation-layer change only — generated SVGs/HTML, two doc files, and the two Python generators. No engine, NATS subject, or payload surface is touched; no ADR trigger. The new bake hashes in README/baselines.json are the documented digest-fix re-bakes, and `mkquiz.py:553-563` fails the build if a stale hash 404s, so the content-key swap is guarded. Wiring checks out end-to-end: `--net` exists (`mkopenslide.py:114`), both new shortlist JSONs exist with non-null winners, setup-only diagrams are legal under the all-or-none rule (`mkquiz.py:601-609` checks options only), and the `1-5` key hint in quiz.html is generated from `len(data)` with a dynamic handler (`quiz.html:218`), so hint and behavior can't drift.

Determinism of the generators is clean: float sets (`cycs`, `firsts`, `offs`) are only ever compared for cardinality/equality or read via `next(iter())` on singletons, rounding is applied before set-formation exactly to absorb float-order differences, `by_edge` is insertion-ordered, and no wall clock or RNG appears. Verified against the emitted artifacts: both new SVG legends show the expected "90s cycle" (shared cycle, divergent greens → green share correctly suppressed), and bottleneck-town's regenerated SVG changed only the reworded line, confirming the sub-pixel cull and legend refactor are no-ops on the authored pods.

Findings:

- **nit** — `scripts/show/mkopenslide.py:250-255`: if every program shares a cycle but none has a G/g phase, `firsts == {0.0}` passes the singleton check and the legend prints "Ns cycle, 0s main-road green (0%)"; likewise a zero-length cycle prints "0s cycle". Not reachable with any pod this repo runs — defer-by-default under the triage bar.
- **nit** — `scripts/chicago/mkquiz.py:90` says the CBD grid "refuses about 5-7% of demand" while `docs/show/baselines.json` ("refuses 7%") and the README table state 7% flat. If the range is arm-vs-baseline spread, one clause saying so would stop the numbers reading as disagreement on the same page.
- **nit** — `scripts/show/mkopenslide.py:268`: `len(sigs)` renders "1057 fixed-time signals" while the same page's context line says "1,057 signals" and the portals line uses `{total:,.0f}`. Cosmetic inconsistency; `{len(sigs):,}` would align it.
- **question** — `scripts/show/mkopenslide.py:150-153`: the cull recomputes `proj(p)` for every point and `path_d` projects them again; on the 15k-lane imports that's doubled projection work. Harmless at current runtime — flagging only in case build time matters later.

REVIEW-COMPLETE
