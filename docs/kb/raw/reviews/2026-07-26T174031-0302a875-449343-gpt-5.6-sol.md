- **blocker** — `engine/BENCHMARKS.md:211` declares the M3 applied-lag target met using the claim at `engine/BENCHMARKS.md:223` that batch completion cannot trail v2. That claim is false: v2 encodes/publishes incrementally, while batch mode performs the entire `NewTSIBMsg` encoding only after collection (`engine/natsio/driver/driver.go:378`, `engine/natsio/driver/driver.go:418`). The ingest benchmark pre-encodes payloads (`engine/natsio/bench_intent_test.go:194`), and the acceptance test uses only 400 vehicles (`engine/natsio/driver_test.go:1059`), so neither measures this tail cost at the 5k/15k/30k M3 scales. A fleet-scale real-driver ack-lag measurement is needed before marking M3 met.
- **should-fix** — `engine/natsio/driver_test.go:1319` calls p99 the per-vehicle “worst case,” then `engine/natsio/driver_test.go:1321` compares only p99. With 120,000 samples this ignores roughly the worst 1,200 observations. Either compare maxima or consistently describe/assert only the ADR-required p99 claim; the same overclaim appears at `engine/BENCHMARKS.md:257`.
- **nit** — `engine/natsio/driver_test.go:1017` says the demonstration uses 500 µs, but `engine/natsio/driver_test.go:1349` uses 1.5 ms.

REVIEW-COMPLETE
