- **blocker** `engine/metrics.go:417` — Warm starts with `-metrics-out` panic on their first observation: the guard requires tick 1, but `RunLive` attaches at restored tick N and first calls `Observe` at N+1. This breaks the primary measured warm-start workflow.
- **blocker** `engine/natsio/run.go:200` — The start gate opens without publishing an observation at the restored tick. Existing vehicles therefore resume before the newly attached driver can claim/observe/control them; hold-last contract state and acceleration are not keyframed. The first resumed ticks coast and cannot match the cold continuation.
- **blocker** `engine/engine.go:451` — Routed lookahead clears `HeldTurn` only when the current lane has multiple successors. `boundaries()` consumes it on the first crossing regardless, so a single-successor stub followed by a fork makes lookahead follow the held turn at the wrong junction, potentially braking for the wrong branch.
- **should-fix** `engine/cmd/serve/main.go:731` — The promised unconditional gridlock warning is nested under `Stats.Despawned > 0`. A run with stranded vehicles but no ordinary despawns prints no `GRIDLOCK` warning, contradicting ADR-0034’s “never silent” requirement.

REVIEW-COMPLETE
