- **blocker** `engine/cmd/serve/main.go:266` — `RunLive` starts ticking before the driver (`:281`) and demand director (`:306`) attach. A sufficiently high finite `-pace` can finish or advance substantially before either handshake completes, producing attach timeouts, missing demand verbs, or scheduler-dependent recordings. Rejecting only pace zero is insufficient; establish a startup barrier before ticking.
- **should-fix** `viz/src/snapshots.ts:53` — The fixed 60-frame cap cannot cover the default 250 ms buffer above roughly 24× pace. Sampling then clamps to the oldest retained frame, and `older === newer` forces displayed speed to zero, corrupting vehicle and congestion rendering.
- **should-fix** `viz/src/config.ts:18` — Correct speed now depends on an manually supplied `?dt=` value, with a silent 0.1 fallback. Custom-timestep runs opened through an omitted or stale link render incorrect speeds; obtain authoritative `dt` from run metadata rather than introducing an out-of-band consistency requirement.

REVIEW-COMPLETE
