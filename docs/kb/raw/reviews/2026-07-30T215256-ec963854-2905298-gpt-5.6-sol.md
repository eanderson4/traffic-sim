should-fix — `docs/kb/decisions/ADR-0034-gridlock-escape.md:431`: The ADR says the new arm calls `exitBlocked(v, lane, false)`, but that wrapper uses `turnSpent=true`; the implementation correctly calls `exitWalk(v, lane, false, false)` to honor `HeldTurn`. Make the authoritative ADR match the implemented semantic.

nit — `docs/kb/decisions/ADR-0034-gridlock-escape.md:450`: Says “Two refinements” but lists three.

No blocker-level correctness, determinism, or contract findings.

REVIEW-COMPLETE
