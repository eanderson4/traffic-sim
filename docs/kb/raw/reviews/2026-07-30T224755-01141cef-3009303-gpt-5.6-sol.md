- **blocker** `engine/routing.go:218` — Adaptive cache misses call raw `routeDijkstra`, bypassing `rerouteTable` hysteresis. After any mid-epoch restore, caches are empty, so a marginally congested destination can choose a different hop than the uninterrupted run, violating ADR-0005 replay determinism. Adaptive cache misses must use the same hysteretic construction as stale-table recomputes. Existing restore tests only exercise congestion strong enough that both paths agree.

REVIEW-COMPLETE
