- **blocker** `engine/metrics.go:979` — `findChains` is capped during depth-first traversal, then sorted afterward. It therefore neither implements the ADR’s promised BFS nor necessarily discovers the shortest/all relevant paths. A third path can be ignored after two equal paths, causing `crossedChain` to miss ambiguity; similarly, a fourth path to another exit can make `exitResolve` falsely report one unique exit. This silently books incorrect distance without incrementing `DroppedCrossings`.
- **should-fix** `engine/metrics.go:233` — director denial tracking collapses valid duplicate `RequestID` entries into one map key. `EnqueueSpawn` itself does not enforce uniqueness, and both entries can inject or expire, so `DeniedServed` can undercount and can attribute service to the wrong lane. Track queue entries independently or reject duplicates at the engine boundary.
- **should-fix** `engine/metrics_test.go:57` — ADR-0014 explicitly requires replay re-derivation equality, but this test only compares two fresh same-seed runs. Add a recording/replay metrics comparison so divergence in replayed intents or spawns is covered.
- **nit** `docs/kb/decisions/ADR-0014-observability-metrics.md:241` — the addendum says the file sink and scenario bindings “landed,” while this staged patch contains neither and later says the file sink lands in a following commit. Describe this commit’s actual scope.

REVIEW-COMPLETE
