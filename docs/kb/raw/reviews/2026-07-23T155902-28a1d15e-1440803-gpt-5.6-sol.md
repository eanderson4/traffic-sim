- **blocker** `engine/cmd/serve/main.go:247` — `-pace 0` starts the unpaced run before the default driver is launched at line 254. The engine can race through most/all ticks before the asynchronous NATS driver attaches, making the advertised “fast recording” stationary or scheduler-dependent. Refuse `-pace 0` with `-driver`, or provide a synchronously coordinated batch controller.
- **blocker** `engine/cmd/serve/main.go:184` — Reopening a durable `-store` with the default `-run demo` corrupts replay metadata: `RunLive` overwrites `demo/meta`, then `NewRecorder` fails because the per-run stream already exists, leaving the old log paired with the new aborted spec. Require a fresh run ID or explicitly implement safe read-only/adoption semantics before modifying the registry.
- **should-fix** `viz/src/config.ts:18` — `dt` defaults to `0.1` instead of coming from authoritative `RunMeta.Spec.Params.Dt`; ADR-0005 makes tick length scenario-configurable. The printed URL at `engine/cmd/serve/main.go:310` also omits `dt`, so non-default scenarios silently display incorrect speeds/congestion.
- **should-fix** `engine/cmd/serve/main.go:334` — `paceFloor` accepts `+Inf`, and extreme finite values can underflow to zero or overflow `time.Duration`; those cases silently become unpaced while reporting a nonzero multiplier. Reject non-finite values and durations outside the positive representable range.

REVIEW-COMPLETE
