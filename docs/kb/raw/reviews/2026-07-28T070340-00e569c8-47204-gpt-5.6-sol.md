- **blocker** `viz/public/quiz.html:122` — The staged page still presents and ranks the Chicago results as valid, while `docs/show/chi-loop-options.md:5` declares those exact numbers superseded due to a wrong interval window and fidelity-void uncontrolled coasting. The page caveat omits both defects and even calls parts “realistic.” Remove Chicago from the shipped quiz or regenerate its results from clean runs.
- **blocker** `scripts/whatif.py:228` — `mean_time_loss_s` now folds horizon-censored, truncated losses into completed whole-trip losses. This changes the metric’s meaning and biases it according to entry time/horizon. It contradicts the completed-only semantics in `engine/cmd/metview/main.go:332` and the explicit KB decision at `docs/kb/articles/podcast-demo-workqueue.md:126` requiring an ADR-0014 interpretation before changing v1 semantics, likely as a separate censored-inclusive bound.
- **should-fix** `scripts/whatif.py:588` — Even under the new interpretation, the human-readable supporting-metrics table prints `mean_time_loss_s` without `censored_frac`, despite the code’s stated requirement that the statistic always disclose censoring. Only the JSON carries it.

REVIEW-COMPLETE
