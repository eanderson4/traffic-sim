Review complete. I read the full staged diff and verified the load-bearing claims against the repo (metrics JSON field names and units, RNG derivation, keyframe round-trip of `Route`, netfile fixture fields, driver locking, the already-committed `PaceFloor` sweep condition at `engine/natsio/contract.go:348`). Process note: test execution required approval unavailable in this session, so this is a static review; all cross-package references, signatures, and JSON tags were verified by hand.

# Findings

## Blockers

None.

## Should-fix

None. The three prior-round blockers are correctly closed: `engine/routing.go` reverse Dijkstra is correct (weight convention matches the retired router — cost = sum of entered-lane lengths; `dist[dest]=0`, relax preds with `w = dist[u] + Lanes[u].Length`) and deterministic (fixed-order predecessor lists, heap tie-break on lane index, strict-`<` relaxation, caches are lookup-only — `routing.go` determinism note is accurate); the `exitDomain` separation is real and pinned by `TestPickExitStreamDomainSeparated`; the O(V²) router is gone and `exitCache` use is correctly under `d.mu` (`driver.go` onObs holds the lock around `pickExit`). The rule-5 closure is done right: no wire change, ADR-0006 addendum + asyncapi text match actual `contract.go:348` behavior. `scorecard.py` now reads `totals` whose keys/units I verified (`vmt` meters, `vht` seconds — the `/3600` and `*3.6` conversions are correct despite the field names).

## Nits

- `scripts/chicago/water.py:128-131` — the skip's comment ("nothing polygonal to render") is wrong in general: a `GeometryCollection` from a polygon∩polygon degeneracy *can* contain polygon members, which are now silently dropped along with the feature. Rare, and better than the old `KeyError`, but it's a silent cap; extracting polygonal parts (or logging the drop) would match your own "no silent caps" bar.
- `engine/natsio/driver/destinations_test.go:584` — `TestPickExitRouteFound` lost its independent oracle: it now checks `pickExit` against the same `reachableLanes` the function uses internally, so the reachability assertion is near-tautological. Acceptable since path correctness moved to `engine/routing_test.go`, but the test now proves less than its name says.
- `engine/engine.go:87-89` — cache growth is unbounded by design: `routeTabs` costs ~4 B × lanes per distinct destination (~224 KB each on a 56k-lane zone; tens of MB with hundreds of drawn exits) and `exitCache.reach` ~56 KB per distinct first-observed lane (orphan re-claims mid-network widen that set). Fine today; worth remembering if a metro-scale zone ever routes thousands of distinct destinations.
- `scripts/chicago/scorecard.py:32` — empty `m["intervals"]` raises `IndexError` at `most_common(1)[0]`; and "largest set" is by record count, not coverage. Offline tool, deterministic per file — fine, just brittle.
- `contracts/asyncapi.yaml:25` (route description) — embedding the repo path `engine/routing.go` in public contract text is implementation leakage; the semantics sentence stands on its own without it.

## Questions

- **Q1.** Any recording produced between the `d4673f8` merge and this commit carries Route intents and will now fail CRC replay under the route-following kernel (routed trajectories changed; only un-routed runs are bit-identical — the ADR addendum says this). The addendum implies the window is clean of durable artifacts; worth stating explicitly in the commit message that no recordings from that window are kept.

## Verdict

Clean hardening pass. Every prior-round blocker and should-fix is either fixed correctly or recorded as a deliberate deferral in the right place (ADR-0006 addendum, gaps-and-roadmap, freshness note), the new engine-side routing meets ADR-0005's determinism bar including the pure-function/no-new-keyframe-state argument, and the tests added (`TestPickSuccessorFollowsRoute` middle-successor case, CRC determinism, cache transparency, domain separation) pin exactly the properties the fixes claim. Nothing here blocks the commit.

REVIEW-COMPLETE
