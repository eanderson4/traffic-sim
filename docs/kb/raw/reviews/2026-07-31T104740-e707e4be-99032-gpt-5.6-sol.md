- **should-fix** `engine/scenario/scenario_test.go:134` — The `bool`→`*bool` migration affects the sacred canonical-hash contract, but tests only verify resolved behavior. Add assertions that absent and explicit `false` produce different hashes, and that explicit `true` retains its pre-change hash.
- **should-fix** `docs/kb/decisions/ADR-0012-scenario-format.md:410` — “Every run artifact” records resolved params is contradicted by `docs/kb/decisions/ADR-0036-congestion-adaptive-routing.md:234`, which says metrics JSON and what-if reports record neither params nor build. Narrow the identity guarantee to RunMeta-backed recordings.
- Validation note: `git diff --check --cached` passed; Go tests could not run because the review sandbox forbids creating `/tmp/go-build*`.

REVIEW-COMPLETE
