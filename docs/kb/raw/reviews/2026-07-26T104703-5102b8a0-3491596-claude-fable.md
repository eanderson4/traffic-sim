Findings:

**Blocker — the answer key still says the thing the change exists to stop saying.**
`scripts/chicago/curate.py:227-239`: the answer-key section prints raw `v["verdict"]` and only special-cases the UPGRADE-below-floor demotion; the new near-miss correction is applied only to `shown_verdict` (menu table + JSON). Result, live in the staged doc: `docs/show/cbd-options.md:9` says "not demonstrated (near-miss)" while the answer key at `docs/show/cbd-options.md:16` says "no-op (n.s.)" for the same option — exactly the table/answer-key contradiction the function's own docstring gives as its reason for existing, and the answer key is the part the host reads on air as the authoritative reveal. The docstring says calling this a no-op "is the easiest thing here to say wrongly"; the staged output still says it. Fix: apply the near-miss branch in the answer-key loop too (with its rationale, mirroring the sub-floor annotation at curate.py:228-236), and regenerate the doc.

**Should-fix — `abs()` makes "near-miss" direction-blind.**
`scripts/chicago/curate.py:196-199`: `whatif.py:378-379` assigns `no-op (n.s.)` on p alone, regardless of sign. A non-significant *negative* effect ≥ the floor (e.g. −4%, p=0.2) would be relabeled "not demonstrated (near-miss)" — but it nearly *hurt*, it didn't nearly work. Not triggered by any current dataset (all large-n.s. deltas staged are positive), so it's latent, but the branch as written encodes the wrong claim for half its input space. Gate on the signed helpful direction (delta > 0 for the current higher-is-better metrics; note the report doesn't record `higher_is_better`, so either persist it in whatif.py's JSON or infer the sign convention from an UPGRADE/WORSE row).

**Nit — same option, two verdicts within one doc (pre-existing pattern).**
`docs/show/cbd-options.md:9` vs `:26`: the "Everything tested" table intentionally shows raw verdicts, so transit-5 reads "near-miss" in the shortlist and "no-op (n.s.)" below. The UPGRADE-demotion case already had this property, so this is a known trade-off, not new — but with the answer key fixed (blocker above), the full table becomes the only place the raw string survives; a one-line footnote there would close the loop.

Checks that came back clean:
- Hand-edited artifacts match what regeneration would produce: the JSON verdict field, the quiz.html embedded blob (mkquiz.py `html.escape` is a no-op on the new string), and the md table row are all byte-consistent with rerunning curate.py + mkquiz.py. The answer-key line is also "consistent with regeneration" — which is the blocker, not a divergence.
- quiz.html JS (`cls()`, reveal gating) keys off `delta_pct`/`winner`, not verdict strings — the new string renders fine and leaks nothing pre-reveal.
- `startswith("no-op")` is safe against the raw vocabulary (`UPGRADE` / `WORSE` / `no-op (n.s.)` / `— baseline —`) and can't double-apply, since it only ever sees raw verdicts.
- No NATS subjects, payload schemas, or engine code touched — no ADR required; no determinism-relevant paths (analysis-side Python only).

REVIEW-COMPLETE
