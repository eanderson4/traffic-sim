- **blocker** `engine/signal.go:277` / `engine/sigctl.go:189` — Clearance derives phase onset from the currently selected source, not the actual override transition. On lapse, it uses the fixed schedule’s earlier onset; on replacement, overwriting `sigOv` destroys the prior commanded phase. Thus commanded amber→red transitions can receive no clearance and recapture amber-committed vehicles, contradicting ADR-0037’s “enforcement untouched” guarantee.
- **blocker** `engine/keyframe.go:259` / `engine/keyframe.go:461` — A v7 flag-off keyframe writes and restores `laneEntryTick` merely because `ver >= 6`. Adaptive-off engines do not update this clock on lane departures, so the explicitly supported restore into an adaptive-on spec imports stale, potentially run-long dwell clocks instead of seeding them at the restore tick. The first departure can poison the current lane’s EMA.
- **should-fix** `engine/sigctl.go:55` — The starvation rail is fixed at 3000 ticks, despite `Dt` being scenario-configurable. It equals the claimed 300 seconds/`StrandAfterS` horizon only at `Dt == 0.1`; supported larger ticks permit holds up to 1500 simulated seconds. Derive the tick bound from simulated seconds or document it as intentionally tick-based.
- **should-fix** `engine/sigctl.go:138` / `contracts/asyncapi.yaml:1439` — One-cycle default holds are not generally “seamless.” One cycle later the fixed schedule returns to the phase active when the command began, not necessarily the commanded phase, so lapse may jump to an unrelated phase. Correct the contract/rationale or calculate an aligned resume duration.
- **should-fix** `contracts/asyncapi.yaml:1455` / `engine/natsio/verb.go:158` — `LoggedVerbView` now requires only `tick` and `request_id`, so neither verb variant’s actual required fields are represented. The decoder also treats every unknown discriminator as spawn. Model spawn/signal records as a strict union and reject unknown record kinds to prevent malformed or future verbs from being replayed as spawns.

REVIEW-COMPLETE
