- **blocker** — `scripts/import-city.sh:60`, `engine/cmd/netimport/main.go:46`, `engine/scenario/scenario.go:217`: the canned import stamps `time.Now()` into network provenance, which is included in the scenario content hash. Identical inputs therefore produce different run identities. Omit the field or supply a deterministic value.
- **should-fix** — `scripts/osm-stop-nodes.py:164`, `scripts/osm-stop-nodes.py:199`: walked stop arms are not checked against one-way direction, while `incident_arms` excludes outbound-only arms. An against-traffic stop can alone satisfy `len(arms) >= total`, incorrectly producing `allway_stop`.
- **should-fix** — `scripts/osm-stop-nodes.py:153`, `docs/kb/decisions/ADR-0017-city-import-decisions.md:41`: absent and unrecognized `direction` values silently become `forward`, and the promised defaulted-stop count is never reported. Unknown values should be distinguished from intentionally directionless nodes.
- **should-fix** — `scripts/import-city.sh:64`, `docs/kb/decisions/ADR-0009-osm-import-strategy.md:39`: provenance records the netconvert version but not versions of `overpass2osm`, the stop resolver, or Go `netimport`; the shared recipe therefore lacks the mandated importer version.

REVIEW-COMPLETE
