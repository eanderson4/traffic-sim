No blockers found.

- **should-fix** `scripts/chicago/mkod.py:437` — `total_veh()` is presented as a realized count that should reconcile with completed + stranded + active. Poisson arrivals are seed-dependent; the director also emits an immediate first arrival and allows a gap drawn under one slice to cross later rate boundaries (`engine/natsio/demand/director.go:21`, `engine/natsio/demand/director.go:451`). The integral is nominal/expected demand, so a reconciliation gap is not necessarily delivery loss.
- **should-fix** `scripts/chicago/mkod.py:1006` — `tally()` claims to record exactly what `emit_flow` writes, but retains unrounded boundaries and rates while serialization rounds seconds to integers and rates to one decimal at `scripts/chicago/mkod.py:462`. Rates below 0.05 are counted yet serialize as `0.0`; non-integral boundaries can also change duration and overlap. Compute diagnostics from the serialized values.
- **should-fix** `scripts/chicago/mkod.py:827` — district destination reporting still weights flows by profile-scaled base rate, not by profile-integrated vehicle count. With `kennedy-am`, `freight`, and `reverse-commute` having different shapes, the reported district shares no longer describe where the full demand program sends vehicles.

REVIEW-COMPLETE
