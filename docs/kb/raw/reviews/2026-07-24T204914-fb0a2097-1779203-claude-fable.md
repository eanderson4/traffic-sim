Review of the staged diff (ADR-0016 TSSG chunking + pull resync, ADR-0018 chunked GeoJSON, spawn identity, themes/stop signs). Contract checks pass: `ts.{run}.state.sig.req`, the `sig_chunk` header, and the 4 MiB `MaxPayload` amendment are all covered by ratified ADR-0016 and already present in the committed `contracts/asyncapi.yaml`; the chunk pack is a pure function (no map iteration, no wall clock in sim paths); wall-clock uses (`runNonce`, `CreatedUnix` probe, viz timers) are edge/presentation only. The accumulator's generation rules, the dedicated reply accumulator, byte-identical full-range GeoJSON, and the manifest-last write order all look correct and are pinned by tests. No blockers found.

**Should-fix**

1. `engine/cmd/demosrv/supervisor.go:152` — `start` holds `s.mu` across the whole readiness wait, which this change grows from ~10 s to up to `readyTimeout` (60 s) + `identityTimeout` (660 s). `status()` (supervisor.go:237) and `stop()` (supervisor.go:211) take the same mutex, so during an la-lean spawn (~400 s, the exact workload this change enables) `/api/status` hangs — the menu polls every 3 s and those fetches pile up against the browser's per-host connection cap — and a bad start cannot be cancelled for up to ~12 minutes. Consider serving status from a snapshot outside the lock, or making the identity wait cancellable by `stop`.

2. `engine/cmd/demosrv/geojson.go:211` — every part request runs `netKey` (full read + SHA-256 of the network file), then calls `c.path`, which does `scenario.Load` + `netKey` **again** before its `Stat` hits the cache. That's two full reads/hashes of a ~1 GB file per part — ~10 for an la-lean page load, serialized behind `c.mu`. Warm page cache hides most of it, but a `Stat` of the requested part file (hash already validated) before invoking the generation path would drop the second read for free.

**Nits**

3. `engine/cmd/demosrv/geojson.go:82-89` — `path`'s doc comment ("path returns the cached GeoJSON file…") is fused onto `isManifest`'s doc block; `path` itself (line 114) now has no adjacent doc comment.

4. `engine/cmd/demosrv/geojson.go:97` — `isManifest` substring-sniffs `"parts"` in the first 4 KiB; a single-file doc whose early lane/edge id strings contained `parts` would false-positive. Consequence is only a spurious `no-store` header, but manifests are tiny — a full unmarshal (or matching the compact-marshal prefix) would be exact at the same cost.

5. `engine/cmd/demosrv/identity_test.go:149` — comment says "the 120 s identity timeout"; production is 660 s.

6. `viz/demos.html` (inline theme script) — the navy/paper palettes are hand-duplicated from `theme.ts` with no test tying them together; a `theme.ts` tweak will silently skew the menu. Fine for now, but a drift magnet.

7. `engine/natsio/bus.go:~300` — `noteFrameSize` runs for signal chunks even when the publish failed, but for snapshots only on success. Both failure logs already name the size, so purely an inconsistency.

**Questions**

8. `engine/cmd/demosrv/geojson.go:60` — `netCache.mu` is held across an entire multi-minute la-lean generation, so a `/net` request for any *other* (small) demo blocks behind it. The "one at a time is plenty" comment predates city-scale generation times — still intended?

Note: I could not run `go vet`/build in this session (sandbox denied); findings are from reading the patch and the working tree.

REVIEW-COMPLETE
