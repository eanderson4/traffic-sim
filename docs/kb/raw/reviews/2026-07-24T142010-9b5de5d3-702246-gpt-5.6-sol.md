- **should-fix** `engine/cmd/metview/main.go:56` — Optional metric fields are decoded as scalars. Schema v1 omits disabled groups, so valid files can display unavailable density/occupancy/stops/time-loss as real zeroes and produce false rankings. Preserve presence with pointers and suppress unavailable columns/sorts.
- **should-fix** `engine/cmd/metview/main.go:268` — Malformed intervals with `end_tick < begin_tick` contribute VMT/VHT/time-loss/stops before being “skipped,” yielding internally inconsistent rows. Reject the input or skip the entire interval before accumulation.
- **question** `engine/cmd/metview/main.go:8` — Advertising `/api/*` as a scripting interface creates an HTTP integration boundary despite ADR-0002’s NATS-only inter-service rule. Is this intentionally human-local only? If scripts are supported consumers, document the exception/contract via ADR.
- **nit** `engine/cmd/metview/main.go:453` — Lane bars always encode time loss even when sorting by density, occupancy, or stops, making the selected view visually misleading.

REVIEW-COMPLETE
