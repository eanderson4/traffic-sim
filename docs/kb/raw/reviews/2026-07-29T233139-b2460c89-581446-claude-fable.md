Review of the staged diff (ADR-0028 correction: realized-demand reporting in mkod.py, `peak_rate`/`total_veh`, the Kennedy profile library, tests, ADR/INDEX amendment).

I verified the math and the restructured portal/interior/resident loops against the full source, cross-checked the numeric claims in the ADR text (the 11% overstatement, the 1.57× Kennedy delta — both reproduce from the shapes and scales), and confirmed the new tests' expected values. No blockers.

**Blockers** — none.

**Should-fix**

1. `scripts/chicago/mkod.py:1060-1072` — the ramp-share split is booked **before** the `pf is None` check. A freeway portal whose profile resolves to nothing (library with no `default` and no matching portal rule) is refused emission, yet `relocate[corr] += moved` has already run, so `--ramp-share` of that absent flow's demand ships anyway as interior injections and enters the realized totals via `tally("interior", ...)`. This contradicts the invariant the diff itself states three times ("an absent flow must not appear in a realized total") and the comment at :1064-1066 which claims a profile-less flow "contributes nothing here." Both shipped libraries have a `default`, so it's latent, but the fix is a one-line reorder (skip the relocation block when `pf is None`) or an explicit comment saying the relocated share deliberately survives.

**Question**

2. `scripts/chicago/mkod.py:335` / ADR-0028 correction — the shadowing trap is documented as undetectable by `check_all_rules_fired`, which is true, but a runtime shadow check is cheap: in `ProfileSet.resolve`, after the first match, keep scanning and count flows where a **later** rule would also have matched; warn per (winner, shadowed) pair at exit. That converts the trap from author discipline (an `_why` comment in the JSON) into a printed fact. If the triage call is "too early," fine — but it's ~10 lines and this exact trap already cost a debugging round.

**Nits**

3. `scripts/chicago/mkod.py:408-410` vs :1008-1010 — slice-numbering prose disagrees with itself and with the shapes: the docstring says "freight at slice 4 … reverse-commute at 3", the tally comment says "reverse-commute crests at slice 3, freight at 4", while both shapes actually crest at index 3 (freight at 3–4). Harmless, but these comments exist to teach the invariant, so they shouldn't need reconciling.
4. `scripts/chicago/mkod.py:487-495` and :1080-1082 — `observe` and the stranded entry each rebuild the same `(s, e, rate·f)` integral that `tally` builds one line away; a small helper would keep the "same terms as emit" claim in one place. Also pre-existing: `kept` is not renormalized after dropping <5e-5 destinations, so `observe`'s shares under-count by the dropped mass — unchanged by this diff, just now denominated in vehicles.

**Determinism / contract** — clean. Generator-side only, as ADR-0028 claims: no NATS subject or payload touched; the demand-file change is comment-header lines plus the same `slices` schema; all new reductions (`peak_rate` sorted-bounds sweep, `total_veh`, `timeline` keyed by explicit category names, `sorted(entries)` / `sorted(relocate.items())` loops) are order-deterministic, no wall clock, no RNG. The ADR amendment and INDEX update accompany the behavior change as the repo rules require, and `profiles-am-kennedy.json` follows its own ordering rule (corridor rule before the generic `kind=interior` rule).

REVIEW-COMPLETE
