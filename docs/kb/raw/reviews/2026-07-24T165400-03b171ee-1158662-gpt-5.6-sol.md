- **blocker — `contracts/asyncapi.yaml:125`:** The staged contract promises chunked TSSG and `state.sig.req`, but the exact staged patch contains none of the corresponding engine/client implementation; those changes are only unstaged. Committing this patch makes the source-of-truth contract describe behavior the shipped runtime does not provide.

- **should-fix — `scripts/overpass-lean.py:47`:** Filtering ways before stop resolution destroys original junction incidence. A stop at a retained arterial/removed side-street junction becomes a mid-way node and can be walked to an unrelated downstream junction, silently applying false stop control.

- **should-fix — `scripts/osm-stop-nodes.py:219`:** Split continuation chooses orientation but never validates the new way’s `oneway` flow. A walk can cross a same-name split against traffic and mark an arm that `incident_arms()` does not count, potentially promoting the target to `allway_stop`.

- **should-fix — `contracts/asyncapi.yaml:441`:** No receive operation references the new `stateSigReq` channel, so AsyncAPI tooling will not expose the engine’s request handler. The additive contract also remains version `2.2.0` at `contracts/asyncapi.yaml:23`, despite prior additive changes incrementing the info version.

- **should-fix — `docs/kb/INDEX.md:101`:** The registry still calls ADR-0016 proposed/implementation-pending and mentions `sig_gen`; the ADR is accepted and explicitly uses only `sig_chunk` (`docs/kb/decisions/ADR-0016-tssg-chunking.md:47`). This violates the keep-the-KB-current rule.

- **should-fix — `engine/cmd/netimport/main.go:47`:** `-imported` is validated but not canonicalized. Equivalent RFC3339 values such as `Z` and `+00:00` produce different canonical network bytes and ADR-0012 content hashes; parse and reformat to UTC before storing.

REVIEW-COMPLETE
