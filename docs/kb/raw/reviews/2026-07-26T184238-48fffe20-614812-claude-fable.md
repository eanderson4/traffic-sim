**Review: ADR-0026 M3 stress rerun + applied-lag acceptance (bench, boundary test, BENCHMARKS.md).**

Verified against the repo: bench stats accounting is correct across `b.N` escalation (fresh bus per sub-bench invocation, warmups counted once); the driver emits exactly one intent per ego per obs (`driver.go:347-378`), so the boundary test's FIFO attribution invariant (`needed: vehicles`) holds, and desyncs fail loudly; ack-before-obs ordering the tap relies on is real (`run.go:184-185`); header names (`tick`, `applied_tick`, `intent_encoding`) and the pinned `lagTickOff=8` match the bus/tsib definitions; `nats.Msg` reuse with in-place tick patching is safe (next patch only after full delivery confirmation); the 4 MiB test-server cap matches production `sigMaxPayload` (`bus.go:374`). No kernel/determinism-relevant code is touched; no subject or payload schema changes (`SubjectCtlAll` is a wildcard helper over existing subjects; `tsibTickOff` is a byte-identical refactor). ADR-0026 M3 targets (lines 238-242) match what the tests pin.

**Blockers:** none.

**Should-fix**

1. `engine/BENCHMARKS.md` (diff lines 157-166): the two "PROVEN in M2" bullets (`TestBatchOnOffParity` replay + paired-seed agreement) are list items of the "M3 targets, point by point" list but land *after* the "Driver-side encode tail" `###` heading, so they render as stray bullets under the wrong section. Move them above the encode-tail heading.
2. `engine/natsio/driver_test.go` (diff lines 925, 1039): stale "at 3 ms" comments — "at 3 ms with sub-ms work it is zero" and "overruns REQUIRED zero as at 3 ms" — but `prodPace` is 10 ms (and BENCHMARKS.md explains why 3 ms was abandoned). These annotate the acceptance-validity condition, so the wrong number is actively misleading.

**Nits**

3. `engine/natsio/server.go:33` (diff line 1080): use `MaxPayload: sigMaxPayload` instead of a second literal `4 << 20` — same package, and it locks the test broker to the production cap it claims to mirror.
4. `engine/BENCHMARKS.md` diff line 58: stray `**` ("pinned where provable:**") — unbalanced bold from the "MET" span; garbles rendering of the whole bullet.
5. `driver_test.go` diff lines ~843-851 vs ~857-864: the Phase B quiescence comment is duplicated nearly verbatim above Phase A and again above Phase B. Keep one.
6. `bench_intent_test.go` diff line 436: `b.SetBytes` for the TSIB leg counts one header even when `⌈n/20k⌉ = 2` batches (30k row). Off by 24 B — negligible, but the label says "production shape."

**Questions**

7. `driver_test.go` diff line 1061: the fast leg hard-fails if v2 never straddles (`v2Fast.splits == 0`). Measured margin is 17-29/300, but this is a scheduler-dependent assertion in the default test path on a shared box — if CI flakes here, the fix is pace tuning, not tolerance (the pin is load-bearing). Flagging so a future flake isn't misread as a regression.
8. `TestBatchAppliedLagBoundary` adds roughly 30-60 s to the default `go test ./natsio/` path (two 5k-vehicle warm-up/quiescence legs plus 3 paced pairs). Fine if that matches the package's existing e2e budget; `-short` gating is available if it isn't.

The lag-proxy metric being structurally 0 in the ingest harness, the passive tap's ±1 tolerance, and the per-vehicle-vs-complete-response claim scoping are all honestly labeled in both code and BENCHMARKS.md — no overclaim found; the load-bearing assertions (exact p50/p99 ≤ on an undisturbed schedule, batch splits structurally 0, v2 straddle exercised) are the right ones for the ADR's stated collect-before-publish concern.

REVIEW-COMPLETE
