**No blockers.** The default flip is wired correctly end to end: `main.go:499-500` passes the flags straight through (no negation), `recorder.go:141` and `recorder.go:202` invert correctly, and the zero-value `RecorderConfig` now selects per-message + uncompressed — which resolves the prior round's blocker (library callers like `RunLive` get the measured-reproducible configuration without serve's help). All three doc drift sites from the archived 15:48 round are fixed in this diff (asyncapi.yaml `logIntent` channel, ADR-0006:389, ADR-0035 §5), the field rename leaves no stale `Unbatched…`/`Uncompressed…` references anywhere in code or live docs, the migration note is now stated by flag rather than chronology, and the adoption test correctly moves to `CompressedStore: true` to keep the differing-config fallback exercised. The `network.go:59-63` comment change is accurate against the CRC fold at `engine.go:1069-1077` (lane order = network order, flag-gated). No subject or payload changes; the default flip carries its ADR notes (ADR-0035 correction, ADR-0006 addendum, contract file) as required.

**should-fix** — `docs/architecture.md:92`: the tick-loop sequence diagram still shows the engine's record-plane publish as `log.intents (TSLB)`. After this flip the unflagged engine publishes `log.intent`; this is the exact untouched-reference drift class this commit exists to correct, in the repo's top-level architecture doc. (Docs-only, so it can ship in an ungated follow-up commit — but it should not be forgotten.)

**nit** — `contracts/asyncapi.yaml:467,504`: `compression: s2` remains the unconditional binding value on both channels; a machine reader of `x-nats` still sees s2 as invariant while the comment says default off. Carried over — third round it's been noted.

**nit** — `docs/kb/decisions/ADR-0026-batched-intents.md:138-146`: the "Superseded for the intent log (ADR-0035)" note reads as though TSLB replaced the recorder's per-intent re-expansion; unflagged, that expansion is again the shipped behavior. A one-line "opt-in, default off since 2026-08-04" would prevent this note re-drifting the same way ADR-0035 §5 did.

**question** — `engine/natsio/recorder.go:154-166`: the adopt-guard comment says the non-empty check "makes the invariant local instead of trusting every caller," but the guard only fires when `AddStream` errors on a *differing* config — a leftover non-empty stream whose config matches the (now zero-value-common) default is adopted silently, with only serve's `checkFreshRecording` upstream. Pre-existing and symmetric before/after the flip, so nothing this diff worsens — flagging only because the comment overclaims what the guard covers.

REVIEW-COMPLETE
