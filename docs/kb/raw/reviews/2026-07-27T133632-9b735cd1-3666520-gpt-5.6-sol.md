- **blocker** `scripts/demos/bottleneck_town.py:281` — The generator knowingly reverses the `edgeIndex` contract while junction construction assumes index 0 is rightmost. Right turns originate from inside lanes and protected-left paths can cross, yet signalized junctions omit foe arbitration (`scripts/demos/bottleneck_town.py:615`). The staged town reports and quiz are therefore derived from a physically invalid network.
- **should-fix** `scripts/demos/bottleneck_town.py:567` — Successors are deliberately reordered “same road first,” violating the network-format left-to-right ordering and changing `HeldTurn` semantics. “This controller does not currently issue HeldTurn” does not make the generated network contract-valid.
- **should-fix** `scripts/chicago/fwsweep.sh:73` — The sweep claims to measure fidelity limits but treats any zero-exit run with metrics as successful. It never rejects serve’s demand-delivery, uncontrolled-coasting, or blind-controller warnings, so void runs are labeled “done.”
- **should-fix** `scripts/demos/meter-sweep.sh:71` — The sweep omits `--warmup`; `whatif.py` therefore includes the empty-network fill transient. Its curve is not comparable to the shipped merge report’s 3,000-tick warmup and can select the wrong meter timing.
- **should-fix** `engine/natsio/obsframe.go:72` — `ObsEgoCapacity` subtracts only the binary observation header, but NATS enforces `max_payload` against data plus NATS headers. `TestObsFrameSizeCliff` publishes without the production tick/schema headers (`engine/natsio/intentload_test.go:242`), so it does not empirically pin the production ceiling as claimed.

REVIEW-COMPLETE
