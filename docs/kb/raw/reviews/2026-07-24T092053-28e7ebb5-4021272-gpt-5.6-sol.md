- **should-fix** `viz/src/replaypanel.ts:345` — The guard prevents poll-vs-poll overlap only. A status GET can still start before a pause/speed/seek POST, arrive afterward, and overwrite the POST’s newer status at `ReplayControl.refresh():209`. Use request generations or serialize all status-mutating requests.
- **should-fix** `viz/test/replaypanel.test.ts:89` — No test covers the new in-flight behavior or the poll/control stale-response race, despite this being a concurrency fix. Current tests and TypeScript checks pass.

REVIEW-COMPLETE
