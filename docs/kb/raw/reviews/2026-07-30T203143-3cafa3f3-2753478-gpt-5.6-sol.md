- **blocker** — `docs/kb/decisions/ADR-0006-nats-message-contract.md:436` incorrectly claims recorded runs “do not re-derive routing” and remain valid. Both replay paths restore/rebuild the engine, re-enqueue logged intents/verbs, call `Step()`, and verify the old CRC (`engine/natsio/player.go:433`, `engine/replay.go:46`). Routed recordings created under length weighting will therefore diverge after the first affected routing decision; strict replay aborts, while Player only logs and continues. The change needs a real compatibility/migration strategy or must explicitly declare those recordings incompatible rather than calling them valid.

REVIEW-COMPLETE
