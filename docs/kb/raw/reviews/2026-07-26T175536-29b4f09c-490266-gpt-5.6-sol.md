- **blocker** — `engine/natsio/driver_test.go:1324`: the acceptance assertion checks only per-vehicle **p99**, not ADR-0026’s required p50 **and** p99. Worse, it runs only at the generous 3 ms pace; the straddling run at `engine/natsio/driver_test.go:1357` is report-only even though the test acknowledges v2 vehicles can apply earlier. Thus `engine/BENCHMARKS.md:211` falsely marks per-vehicle p50/p99 “MET”; pre-encoded ingest throughput cannot prove application-lag p50 at 5k–30k.
- **should-fix** — `engine/natsio/driver_test.go:991`: the live corroboration explicitly permits `p99TSIB == p99V2+1`, so a one-tick p99 regression—the exact ADR metric—passes. The passive pairing can also undercount intents arriving after drain but before ack, making this ineffective as regression coverage.

REVIEW-COMPLETE
