**Findings**

**should-fix**

1. **Near-miss direction test hardcodes higher-is-better, inverting under `--lower-is-better` metrics** — `scripts/chicago/curate.py:204`. The new branch promotes on signed `delta >= args.min_effect`, i.e. positive-delta = helpful. But `whatif.py:267-269` supports `--lower-is-better` (and the module docstring discusses `mean_trip_s`/`mean_time_loss_s` as primaries), where a helpful non-significant effect has *negative* `delta_pct`. Under such a metric this branch produces exactly the inversion its own comment (curate.py:199-203) says it prevents: a +4% n.s. (nearly *hurt*) gets promoted to "not demonstrated (near-miss)", while a −4% n.s. (nearly *worked*) stays "no-op (n.s.)". The direction can't be recovered from the raw verdict here — "no-op (n.s.)" is direction-less — and whatif's report JSON doesn't record the direction flag, so the design note at curate.py:104-106 ("direction is read from the verdict, not re-derived") is now violated by re-derivation with an assumed sign. The same signed assumption already existed in `big_enough`'s caller context but was harmless (abs); this branch is the first place a wrong sign changes the label. Only `speed_kmh` is used today, so not a blocker — but at minimum the assumption deserves a comment/guard, or whatif should persist `higher_is_better` in the report.

**nit**

2. **Answer-key text asserts `p<alpha` for a verdict computed at fixed 0.05** — `scripts/chicago/curate.py:248` prints "does not reach p<`{args.alpha:g}`", but the raw "no-op (n.s.)" verdict it keys off is assigned at a hardcoded `p > 0.05` (`whatif.py:378`), not at curate's `--alpha`. Run curate with a non-default alpha and the read-out sentence is false (e.g. `--alpha 0.1`: an option at p=0.07 keeps raw verdict "no-op (n.s.)", gets promoted to near-miss, and the text claims it "does not reach p<0.1" when it does). The alpha split is pre-existing; the new text is what turns it into a false on-air claim. Fine at defaults.

3. **"Everything tested" preamble is unconditional** — `scripts/chicago/curate.py:256-260`. It references "the practical floor and the near-miss correction applied above" even for pods where neither correction fired (both `chi-loop-options.md` and `kennedy-options.md` in this diff have no near-miss in the shortlist), and also in the no-winner branch where the shortlist framing differs. Harmless, slightly confusing boilerplate.

**question**

4. The guest-facing quiz shows only the terse "not demonstrated (near-miss)" (`viz/public/quiz.html:78`, rendered verbatim at line 106) without the "power limit, not a null result" expansion the answer key insists is the thing to say correctly on air. Intentional that the careful wording lives only in the presenter docs, not the reveal card? (The `.note` mechanism used for the VMT caution would fit if you ever want it.)

**Checked and clean:** no consumer string-matches the verdict — `mkquiz.py:214` escapes it and `quiz.html` displays it verbatim; `cls()` colors from `delta_pct`/winner only, so the new string breaks nothing. The regenerated docs and `chi-loop-cbd.json` match what the new code emits (verified transit-5 through both branches; kennedy's `mainline-widen` at 0.67% correctly stays below the 1% floor). Floor/near-miss boundary is consistent (`abs(delta) < floor` vs `delta >= floor`). No NATS subjects or payload contracts touched — the quiz JSON is a generated doc artifact, no ADR needed. No determinism-relevant paths (Python doc generation; the one dict-order tie in the sorted-by-p table is pre-existing and cosmetic).

REVIEW-COMPLETE
