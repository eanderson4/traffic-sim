- **blocker** `engine/cmd/demosrv/main.go:160` — `-wspublic` exposes the unauthenticated, publish-capable NATS WebSocket plane and only logs a warning. Any visitor can publish controller/director intents or JetStream API requests, violating ADR-0006’s observer-read-only permissions. `docs/kb/decisions/ADR-0020-demosrv-public-deployment.md:84` acknowledges this as a go-live prerequisite, but nothing enforces it; fail closed until broker auth/permissions and viz credentials exist.
- **should-fix** `engine/cmd/demosrv/deploy_test.go:430` — the test models stop ordering as epoch-then-pending, opposite the load-bearing pending-then-epoch implementation at `engine/cmd/demosrv/supervisor.go:369`. It does not exercise the claimed race invariant and would miss an ordering regression.

REVIEW-COMPLETE
