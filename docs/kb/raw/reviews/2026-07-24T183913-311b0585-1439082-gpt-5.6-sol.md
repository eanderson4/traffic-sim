- **blocker** — `viz/src/main.ts:343`: Pull-response gaps and incomplete chunk sets are ignored. `finally` only clears `sigReqInFlight`; a dropped reply during paused replay or without a successful broadcast leaves late joiners permanently without a signal table. Retry when `res.gap` is true or `replyAccum.partial` remains true.
- **should-fix** — `engine/natsio/probe.go:54`, `engine/cmd/demosrv/supervisor.go:292`: Readiness accepts foreign same-run metadata up to 30 seconds old and does not recheck child death after a successful probe. A recently stale broker can pass while the newly spawned child loses the port race. Use an exact per-spawn identity/nonce or, minimally, remove the broad tolerance and recheck `done`.
- **should-fix** — `viz/src/netload.ts:48`, `viz/src/netload.ts:64`, `engine/cmd/demosrv/main.go:399`: Stale-part recovery refetches the stable manifest URL with default browser caching. It can receive the cached old manifest and repeat the same 404, defeating ADR-0018’s generation guarantee. Force manifest revalidation/no-cache on retry.
- **should-fix** — `viz/src/theme.ts:112`, `viz/src/main.ts:129`: `name in THEMES` accepts inherited keys such as `constructor` and `toString`, returning a non-theme object instead of falling back to navy. A crafted `?theme=constructor` breaks rendering. Use an own-property check.

REVIEW-COMPLETE
