- **blocker** `docs/kb/decisions/ADR-0023-baked-replay-pipeline.md:481` — The content-addressed identity explicitly excludes `index.json`, although that file controls `dt`, horizons, chunk mappings, and the absolute network URL. The same immutable prefix can therefore serve different replay semantics. Hash canonical manifest bytes, excluding only an unavoidable self-reference.
- **blocker** `docs/kb/decisions/ADR-0023-baked-replay-pipeline.md:503` — The shared PMTiles key hashes inputs/tool versions, not the emitted archive bytes. Tippecanoe nondeterminism or an unbumped exporter change can replace bytes at the same immutable URL. Include the completed `.pmtiles` digest in its key.
- **should-fix** `docs/kb/decisions/ADR-0023-baked-replay-pipeline.md:186` — `round(0.5/dt)` rejects valid ADR-0005 timesteps: at `dt=0.4`, it selects one tick/2.5 Hz and rejects, although two ticks/1.25 Hz is valid. Additionally, fixed 120/10-frame windows are not 60/5 seconds below 2 Hz. Derive both stride and window counts from elapsed simulation time, or require exactly 2 Hz.
- **should-fix** `docs/kb/decisions/ADR-0023-baked-replay-pipeline.md:488` — The record digest’s semantic-header block omits required `schema_version`. Schema versions define contract interpretation under ADR-0006 and must be identity-bound alongside tick/chunk headers.
- **question** `docs/kb/decisions/ADR-0023-baked-replay-pipeline.md:344` — Define whether bake rejects non-`done` recordings or sets `tickEnd` to the Player’s actual `endTick`. Using the scenario tick budget for an aborted/truncated recording would invent an unrecorded tail, contrary to ADR-0006.
- **nit** `docs/kb/decisions/ADR-0023-baked-replay-pipeline.md:70` — Brotli output is mandatory, but the pipeline names only Go, tippecanoe, and Node. Specify and pin the compressor dependency or approved library.

REVIEW-COMPLETE
