Review complete. Viz-only change (splash baked-first CTA); no NATS subjects/payloads touched, no engine code, no determinism surface. ADR-0023 exists and covers the baked-replay design the change cites. No blockers.

**Should-fix**

1. `viz/src/splash.ts:14` — the fill-in instruction documents a URL shape the app doesn't support. The comment says the baked URL is `?bake=<index.json>&center=...&zoom=...`, but `config.ts` parses no `center`/`zoom` params (viz/src/config.ts:41-66) and the map has no MapLibre `hash` option — the camera comes from the network-frame fit (viz/src/main.ts:392). Whoever publishes the bake and fills in this constant will append inert params and expect an initial camera that never applies. Fix the comment (or add the params, but that's scope).

2. `viz/src/splash.ts:93-95` + `viz/splash.html:130` — in baked mode the destination is a static, load-time-known URL, yet the card keeps `href="#"` with `preventDefault()` + `location.href`. Middle-click/ctrl-click/open-in-new-tab/copy-link all break, and a JS failure dead-ends the card. Simpler and more robust: in `main()`'s baked branch set `featured.href = BAKED_FEATURED_URL` and skip attaching the click handler — deletes the `activate()` special case rather than adding one.

**Question**

3. `viz/src/splash.ts:16` — `BAKED_FEATURED_URL` as a source constant means every bake publish is a code edit through the full two-reviewer gate, and ADR-0023's content-hash artifact paths (`baked/{run}/{hash12}/…`) guarantee the URL changes on every re-bake. Is coupling "publish a new bake" to "external review round" intended? If not, a deploy-time config (or a stable alias path in R2) decouples them. Fine to defer as too-early.

**Nit**

4. `viz/src/splash.ts:76-84` — the `BAKED_FEATURED_URL` guard is now repeated in four places (render ×2, activate, main). At this size it's fine; a single `const baked = BAKED_FEATURED_URL !== ""` would read slightly better. (The explicit `: string` annotation on line 16 is correct — it's what keeps the always-true/always-false comparisons compiling under strict literal narrowing; keep it.)

Consistency checks that passed: `unregistered`/`gated` are unreachable in baked mode so `render()`'s fallback arms are dead-but-harmless there; the baked branch correctly skips both fetches so no live status can contradict the CTA; the live-path behavior (deep-link / start POST / 401-403 soft landing) is unchanged from the previously reviewed shape.

REVIEW-COMPLETE
