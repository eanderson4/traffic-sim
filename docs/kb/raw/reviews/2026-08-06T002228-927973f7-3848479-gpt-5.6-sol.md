- **blocker** `engine/rightofway.go:101` — Internal→internal seam gating omits `RowStop`’s mandatory full-stop/`stopDone` logic. With no conflicting foe, a vehicle runs a consolidated downstream stop sign; the ordinary road path enforces this at `engine/rightofway.go:147`.
- **blocker** `engine/netimport/consolidate.go:138` — Independent parallel slivers sharing a far internal each extend it. This sums unrelated lengths and concatenates parallel shapes, corrupting stop distance, storage, geometry, and metrics. The test explicitly accepts the malformed `5 + 2 + 3` result at `engine/netimport/consolidate_test.go:355`.
- **should-fix** `docs/kb/decisions/ADR-0038-sliver-junction-consolidation.md:8` — The ADR claims `engine/netimport`-only scope, unchanged kernel, and no schema change, but the patch changes `engine/rightofway.go` and changes the v1 internal-lane cardinality contract from exactly one successor to multiple. Update the decision and migration consequences for reader compatibility/versioning.

REVIEW-COMPLETE
