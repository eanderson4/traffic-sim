- **should-fix** `contracts/asyncapi.yaml:476` — `sendSignalTableReply` references `stateSigReq`, so AsyncAPI tooling models the engine as publishing replies back onto `ts.{run}.state.sig.req`; implementation publishes to the dynamic reply inbox. Model a reply-inbox channel rather than contradicting the sacred wire contract.
- **should-fix** `viz/src/main.ts:972` — replay status replaces `currentDt` and updates the buffer/seek gate, but the legend retains constructor-time `dt` (`viz/src/legend.ts:38`). For recordings whose authoritative timestep differs from the URL/default, the new sim clock remains wrong.
- **question** `engine/natsio/bus.go:344` — the observer permission amendment scopes replies to the requester’s `_INBOX`, but the responder publishes to any supplied `Reply` subject. Is broker response-permission configuration guaranteed to enforce inbox-only replies? Otherwise an observer can make the engine publish multi-megabyte TSSG data onto arbitrary subjects.

REVIEW-COMPLETE
