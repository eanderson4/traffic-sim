- **should-fix** — `scripts/demos/bottleneck_town.py:60` and `scripts/demos/bottleneck_town.py:987` still claim parity with engine routing but `shortest_path()` weights successors by lane length at `scripts/demos/bottleneck_town.py:1015`. After this patch, `--check` can predict different routes and reports meaningless distance margins where speed limits differ. Update it to `length / speedLimit`; `docs/show/bottleneck-town.md:166` is stale for the same reason.

REVIEW-COMPLETE
