- **blocker** `engine/sigctl.go:212` — Replacing an active override discards its history before `sigInClearance` queries `onset-1` at `engine/signal.go:285`. A held amber → commanded red can therefore miss clearance—or a green → red replacement can gain false clearance—based on the unrelated fixed schedule. Preserve the superseded phase through the clearance window.
- **blocker** `engine/natsio/run.go:234` — Lapses are written only to process stderr via `log.Printf`; nothing is published to `ts.{run}.log.event`. This violates ADR-0037 and ADR-0006’s durable “logged event” contract, so recordings contain no evidence that the starvation rail fired.
- **should-fix** `engine/keyframe.go:259` — A flag-off v7 keyframe still serializes `laneEntryTick`, and `engine/keyframe.go:461` consumes it when restored into an adaptive-on spec. This contradicts the documented migration that seeds dwell clocks at the restore tick, potentially producing run-long travel-time samples despite the emitted migration notice.

REVIEW-COMPLETE
