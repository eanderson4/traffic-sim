• Now let me verify key context in the repo: `SignalProgram` fields, other `decodeLoggedVerb` callers, and references to the deleted `docs/architecture.md`.

• No dangling references to the deleted doc. Now checking a few implementation details in the staged tree.

• One more sweep: check every place that re-enqueues spawn verbs got the signal twin, and any remaining references to the deleted doc.

• Checking whether `laneEntryTick` is maintained flag-off — this matters for a v7 flag-off keyframe restored into a flag-on spec.

• Let me verify the change actually builds and the new tests pass.

• Review of the staged ADR-0037 milestone-1 diff. Verified against the staged tree; `go vet ./...` clean and the new engine + natsio tests pass (`go test -run 'Signal|Sigctl|LoggedVerb|Keyframe'`).

  **Blockers**

  None found. The core mechanics are sound: `sigOv` is lookup-only with every walk in `Net.Signals` network order (sigctl.go:216, engine.go:1104, keyframe.go:332); the CRC fold is last and conditional (engine.go:1102); writer/reader keyframe conditions are symmetric (`hasLaneSection`, keyframe.go:367/289); the unknown-kind discriminator is a hard error on all four readers (verb.go:162, logcursor.go:97, replay.go:76/229, tested); the clamp is idempotent so replay re-enqueue of the effective hold can't double-clamp; lapse/retention arithmetic is exact (lookback reaches `T−clearance−1`, deletion at `T > until+clearance`).

  **Should-fix**

  1. `engine/keyframe.go:461` + `engine/engine.go:505` — v7 flag-off keyframe into a flag-ON spec restores a *stale* `laneEntryTick`. Flag-off, `noteLaneLeave` early-returns so `laneEntryTick` stays at the injection tick forever; the v7 writer still emits it (gate is `version >= 6`, keyframe.go:259), and the reader trusts the wire word for any ver ≥ 6. The honest-seeding guard two lines up (keyframe.go:456-460: "laneEntryTick = 0 would make every vehicle's first departure a capped run-long poison sample") keys on `ver < keyframeAdaptiveVersion`, so the case your own comment says must not happen now does happen — via the flag word instead of the version. Every vehicle's first post-restore departure feeds a capped injection-to-now dwell into `ttEMA`, network-wide. Realistic trigger: scenario manifests pin `adaptive_routing: false` (documented), a signal-verb run there marshals v7 flag-off, `-state-in` on a default (flag-on) engine. Self-healing and inside an already-loud regime switch, so not a blocker — but the fix is small: when `!hasLaneSection && ver >= 7`, consume the word and seed `tick` if the target spec is flag-on.

  2. `engine/natsio/contract.go:765` + `engine/sigctl.go:166` — restart dedup gap, the ADR-0029 spawn finding's exact shape. After `-state-in`, `c.verbs` is empty and `EnqueueSignal`'s backstop scans only `sigNew` (never keyframed), so a retry of a pre-dump accepted signal verb is accepted and re-applied, replacing `since/until` — warm run diverges from cold. Spawn got partial cover from the keyframed `dirQueue` scan; signal has none even though the keyframed override *carries* `requestID` (sigctl.go:60, keyframe.go:606). Cheap fix: reject when `d.RequestID` matches the live override's. No controller ships in M1, so this can ride to milestone 2 — but record it now, since the spawn analog shipped twice.

  **Nits**

  3. `engine/sigctl.go:154` — wholesale replacement deletes the old override immediately, so `sigInClearance`'s onset−1 lookback (signal.go:285) for the *new* hold reads the fixed-time phase, not the phase actually in force at onset−1. Retention covers lapse, not replacement. Only reachable via amber-hold → red-command back-to-back; fails safe (stale-red branch; committed vehicles still cross).

  4. `engine/sigctl.go:2189` / `keyframe.go:600` — restore bounds `until` below (`until > since`) but not above; a corrupt v7 payload with huge `until` pins an override indefinitely (`ov.until+p.clearance` can also overflow). Other sections range-check; a `until ≤ tick + holdMax` reject would match. Corrupt-input hardening only.

  5. `engine/natsio/verb.go:63-65` — the "They are ignored by spawn…" sentence is stuffed onto one long line, unlike the surrounding comment block.

  **Questions**

  6. `docs/architecture.md` is deleted (243 lines) inside this feature commit. No dangling references (grep clean), but its deleted content was dated 2026-07-31 and already describes v2.7.0/ADR-0037 — i.e. it documented this milestone before this commit lands it. Intentional, and if so why here rather than a docs-only commit?

  7. The lapse is edge-logged only via `log.Printf` (run.go:233), not on `ts.{run}.log.event`. Replay/bake re-derive lapses deterministically so the record plane doesn't need them, but the ADR-0006 addendum calls lapses "a logged event" — confirm the process log, not the `log.event` subject, is the intended reading.

  REVIEW-COMPLETE

