You are reviewing an UNCOMMITTED change in a traffic-simulation repo
(deterministic Go engine, NATS contract, ADR-driven; project rules in
AGENTS.md — determinism discipline ADR-0005, message contract ADR-0006,
message contracts are sacred).

The exact diff under review is at /tmp/claude-1000/-home-eric-grove-traffic-sim/b6b10bf9-859d-4b15-a06e-43c7fde235bb/scratchpad/rev2/diff.patch (stat: /tmp/claude-1000/-home-eric-grove-traffic-sim/b6b10bf9-859d-4b15-a06e-43c7fde235bb/scratchpad/rev2/diffstat.txt).
Read the diff first, then whatever repo files you need for context.

This change implements TWO ADRs, both of which are in the working tree and
are themselves part of what you are reviewing:
  * docs/kb/decisions/ADR-0021-od-demand-buildings.md — building-anchored
    OD demand: arrival despawn at a non-exit destination, destination +
    offset_m on the NATS spawn verb, interior (mid-network) origins,
    scenario-schema flow destinations, and a lateral-depth route-recovery
    table in engine/routing.go (layered 0-1 BFS over the reversed lane
    graph, memoized per destination).
  * docs/kb/decisions/ADR-0022-us-urban-speed-typemap.md — scripts/
    osm-urban-us.typ.xml + scripts/import-city.sh: US statutory urban
    speed defaults for OSM import, paired with
    --junctions.right-before-left.speed-threshold 0.

Priorities, in order:
1. Correctness bugs in engine/routing.go (the 0-1 BFS / routeLatDepth /
   routeHopOK / tryRouteRecovery gradient descent) and engine/spawn.go
   (interior origins, arrival despawn). Can a vehicle get stuck, loop, or
   despawn at the wrong place? Is the depth table correct on multi-lane
   networks and on lanes with no path to the destination?
2. Determinism: Go map iteration in sampled paths, non-associative float
   accumulation, wall clock, RNG discipline. Replay/CRC fidelity matters —
   recordings are content-hashed (ADR-0012).
3. Contract: engine/natsio/verb.go and contracts/asyncapi.yaml add fields
   to a published subject. Is that backward compatible? Is the ADR note
   sufficient?
4. Whether the ADR-0022 claims still overreach. An earlier draft claimed a
   speed-only typemap could not change right-of-way; that was FALSE (1,218
   chi-loop junctions retyped priority -> right_before_left) and has been
   retracted in the ADR text. Check for any REMAINING unsupported claim of
   that kind, in the ADR, the typemap header comment, or import-city.sh.

Report findings cited file:line, ranked: blocker / should-fix / nit /
question. Review only — DO NOT modify files. Be terse; padding wastes the
committer's time. Finish with a final line containing exactly: REVIEW-COMPLETE
