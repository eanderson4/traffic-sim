- **blocker** — `engine/cmd/demosrv/replay.go:109`: replay start verification is outside the supervisor’s serialized `start` operation. If A starts, then B starts before A’s hash check, A checks B’s status and may kill B at line 115—or return A’s URL while B is active. Bind verification and cleanup to the exact child generation.
- **blocker** — `engine/cmd/demosrv/replay.go:153`: control run binding is TOCTOU. The active run is checked, the supervisor lock is released, then the request is forwarded to a fixed port at line 188 after only rechecking that *some* replay is active. A concurrent replacement can receive a command authorized for the previous replay.
- **should-fix** — `engine/cmd/demosrv/replay.go:135`: status requests are unbound. `viz/src/replaypanel.ts:184` accepts whichever replay is currently active and replaces its stored `replayRun`, so an old tab learns the replacement’s token and can control it normally, defeating the stated stale-tab protection. Require the page’s expected replay identity on status probes.
- **should-fix** — `engine/cmd/demosrv/registry.go:133`: recording run IDs need not be unique, while `engine/cmd/demosrv/supervisor.go:176` uses only `{run}-replay` as the control identity. Two recordings from different stores with the same run ID are indistinguishable to the binding; use the recording ID/generation or reject duplicates.
- **should-fix** — `engine/cmd/demosrv/replay.go:83`: recordings without a scenario hash deliberately skip verification, yet line 124 displays the registry-supplied scenario network. A flag-built recording can therefore replay correctly while showing an unrelated road network. Fail closed or derive/verify display geometry from recorded metadata.
- **question** — `engine/cmd/serve/main.go:148`: embedding an absolute network path fixes cross-working-directory replay but makes the durable recording dependent on the original checkout path and machine. Is store portability intentionally out of scope? If not, record portable network identity/content instead.

REVIEW-COMPLETE
