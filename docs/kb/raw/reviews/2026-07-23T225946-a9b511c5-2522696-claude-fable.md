Both test scenarios check out by hand-computation (the 60°/10° cone gates, the exact and toleranced bar coordinates, the null-bearing fallback). Determinism: cluster order follows wire link order, no map iteration in an output path, no wall clock/RNG; bar math is pure per-cluster. No NATS subject/payload change — `SignalHead` is viz-internal derived state, so no ADR needed. The unstaged `main.ts` already consumes `h.bar`, but the staged snapshot compiles without it since the field is additive and only `signalHeads` constructs the type.

Findings:

**Blockers:** none.

**Should-fix:** none.

**Nits:**

1. `viz/src/signals.ts:98-105` — The comment overstates the 45° guarantee. `sameApproach` tests against the *running mean*, so greedy chaining can merge arms up to ~85–90° apart when a bridging lane exists (A at 0° joins, B at 40° joins and drags the mean to ~20°, C at 62° now passes cos 45° against the mean). Deterministic for a given wire order, but "arms entering 45–90° apart … are distinct approaches" is not strictly true, and the split outcome can differ if link order changes across a scenario regen. Worth softening the comment; the gate itself is a fine heuristic.

2. `viz/src/signals.ts:74-84` with the tighter cone — `entryBearing` uses the first nonzero segment of the internal lane, so a coarsely-sampled left-turn connection can point 30°+ off its sibling through movement at the same stop line, splitting one true approach into two heads. Wilshire jumping 824 → 1407 likely includes some of this; a visual pass over one skewed junction would confirm the split is approach-level, not movement-level.

3. `viz/src/signals.ts:118-137` — The bar is drawn through the cluster centroid, so with entries staggered along the travel axis (test 1's in-line lanes: entries at x=0 and x=6, bar at x=3) the bar sits ahead of one entry and behind the other. Cosmetic; real approaches have laterally-arrayed entries where this vanishes.

**Questions:**

1. This commit ships `bar` with no renderer — the `main.ts`/`signalhead.ts` consumers are working-tree-only, unstaged. Intentional commit split? Fine either way; the field is just dark for one commit.

REVIEW-COMPLETE
