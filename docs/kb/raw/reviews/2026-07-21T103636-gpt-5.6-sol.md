- **blocker** `hooks/pre-commit:23`, `scripts/external-review.sh:34` — Plain `git diff --cached` does not encode staged binary contents; distinct binary revisions can produce the same “Binary files differ” patch and therefore the same stamp. Hash the staged tree/index, or use a byte-complete binary diff consistently.

- **should-fix** `hooks/pre-commit:18` — The allowlist lets code/config changes such as `viz/index.html`, `viz/package.json`, `viz/pnpm-lock.yaml`, and `viz/tsconfig.json` bypass review, contradicting “every code commit.” Cover all relevant `viz/` files or explicitly define exclusions.

- **should-fix** `scripts/external-review.sh:43`, `scripts/external-review.sh:56` — Reviewers inspect the live working tree, not an index snapshot. Partially staged or concurrently edited files can therefore provide context different from what ships while the index drift check still passes. Review from a temporary checkout of the captured index or reject unstaged changes in affected files.

- **should-fix** `scripts/external-review.sh:93` — The archive omits `diff.patch` and `diffstat.txt`, while the archived brief points to a temporary directory deleted on success. Consequently, the archived reviews cannot establish what content was reviewed. Archive the captured patch/stat and bind them by hash.

- **question** `AGENTS.md:40` — Making external proprietary CLIs/models mandatory for every code commit is a consequential, expensive-to-reverse workflow/dependency decision under the repo’s own ADR rule. Why is there no ADR documenting availability, failure policy, trust boundary, and bypass expectations?
