- **blocker** — `engine/cmd/bake/README.md:116`: This replaces ADR-0023’s pinned tool version with “any build that honors” the flags, without amending the ADR (`docs/kb/decisions/ADR-0023-baked-replay-pipeline.md:110`). Version-keying prevents cache collisions but does not prove that an unverified version preserves lane identity or minzoom semantics. Keep an explicit allowlist or update the ADR with qualification criteria.
- **should-fix** — `contracts/baked-replay-v1.md:73`: The stated 60-second chunk window is false for newly documented accepted cadences below 2 Hz. For example, `dt=1.0` yields 1 Hz, making 120 TSRB/12 TSRL frames span 120 seconds; TSRL also becomes 0.1 Hz rather than 0.2 Hz. Specify frame-count windows and their cadence-dependent duration, or derive frame counts to retain 60 seconds.
- **should-fix** — `viz/test/furniture.test.ts:101`: The test claims malformed features “never throw” while explicitly excluding legal `geometry: null`, which dereferences at `viz/src/furniture.ts:38`. It also misses malformed `link` values because `Number(null)`, `Number(false)`, and `Number("")` are accepted as link 0. Either harden the parser and test these cases or narrow the documented guarantee.

REVIEW-COMPLETE
