• I've read the full diff. Now checking repo context for the pieces the patch depends on.

• Two more context checks: all consumers of the verb log, and the same-tick lapse/supersede edge.

• Let me try running the new tests to verify (read-only checkout, but tests are executable).

• New kernel and natsio tests pass; the full engine suite is still running in the background — I'll deliver the review when it completes.

• Full engine suite green (9.2s), `go vet` clean, new kernel + natsio tests pass. Review of the staged diff:

  ## Blockers

  None.

  ## Should-fix

  - `contracts/asyncapi.yaml:1481` vs `1495-1497` — `LoggedVerbView` now contains a **duplicate YAML key `tick`** plus stray `run:`/`reason:` keys that belong to an attach-reply shape. Pre-existing (context lines in the diff), but this change rewrites that exact schema block, and strict YAML parsers/codegens reject duplicate keys. Fix while the block is open.
  - ADR-0037's "Verified" line claims "replay/**player/bake** reproduce a signal-verb run CRC-exact." The tests cover `ReplayFromStream`, `MaterializeRunRecord`, and in-memory `engine.Replay`; the player (`engine/natsio/player.go:462`) and bake (`engine/natsio/bake.go:190`) sverb loops are untested 6-line mirrors. Either test them or soften the claim — the shared `decodeLoggedVerbKind` is unit-tested, so risk is low, but the ADR overstates.

  ## Nits

  - `engine/natsio/verb.go:114` — `loggedVerb.Phase` is a plain `int` with `omitempty`, so a logged `signal_set` commanding phase 0 omits `phase` from the record. Round-trips correctly (decode defaults to 0), but it's inconsistent with the phase-0 care taken everywhere else in this same diff (`VerbRequest.Phase` is `*int` and presence-checked; `ContractEvent.Phase` is `*int` precisely so 0 serializes). The record is the durable artifact — same pointer treatment, or a schema note that phase is omitted-when-zero.
  - `engine/sigctl.go:142` / keyframe writer — the u16 request-id bound is enforced only at the NATS boundary (`handleVerb`, `maxRequestIDBytes`). Direct kernel callers (`engine.Run`, in-memory replay) can still enqueue an over-long id and `MarshalState` will silently truncate the length prefix, producing a keyframe that fails only at restore. Documented as a deliberate boundary choice; a one-line `len(d.RequestID) > 65535` reject in `EnqueueSignal`/`EnqueueSpawn` would close it at the source.

  ## Questions

  - `engine/sigctl.go:228-240,260` — a hold whose `until` lands exactly on the tick a superseding verb applies fires **no lapse event** (no truncation since `until == e.Tick`; the lapse check sees only the newest entry). Semantically defensible — the fixed-time program did not resume, so arguably nothing "lapsed" — but the contract language ("a held phase past its bound LAPSES … never silent", asyncapi:123, ADR-0006 addendum) doesn't carve this out. Worth one sentence in the ADR either way.
  - The starvation rail bounds each *command*, not the controller: a wedged controller that re-issues `signal_set` at every clamp boundary holds a phase indefinitely, one logged verb at a time. Presumably accepted by design (each verb is an auditable record-plane entry), but the ADR's "bounds a wedged controller's blast radius" framing oversells slightly.

  ## Verified clean

  - Determinism: `sigOv` is lookup-only everywhere (state derivation, lapse sweep, CRC fold, keyframe marshal all walk `Net.Signals` in network order, chronological slices); hold arithmetic is integer; no wall clock/RNG in the kernel; CRC fold is conditional on non-empty table and ordered, so no-verb runs keep the pre-ADR-0037 byte stream (pinned by `TestSignalKeyframeVersionFloor`).
  - TSKF v7 flags-word handoff (v6-implied → explicit bit) is consistent between writer and reader, and the dwell-clock seeding on flag-off→flag-on restore matches the ADR-0036 migration. Retention bound `until + clearance` exactly covers `sigInClearance`'s deepest lookback.
  - Contract process: asyncapi 2.7.0 bump, ADR-0006 addendum, omitted-when-spawn discriminator with hard-error on unknown kinds, schema_version held at 2 — by the book. All four record-plane readers (replay, player, bake, logcursor) route through `decodeLoggedVerbKind`.
  - `go build`, `go vet`, full `engine` suite, and the new `natsio` signal-verb tests all pass on the staged tree.

  REVIEW-COMPLETE

