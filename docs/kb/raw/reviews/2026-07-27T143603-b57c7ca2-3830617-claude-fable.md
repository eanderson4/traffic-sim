**Review of staged diff** (docs/show artwork + KB article + `mkcongestionmap.py` + quiz build plumbing; no engine, no NATS contract surface — no ADR required).

## Blockers

None.

## Should-fix

1. **`scripts/show/mkcongestionmap.py:115,151` — the zero-traffic case is not fail-closed, contradicting the change's own thesis.** If `--warmup-tick` ≥ the run's last interval (e.g. `build-quiz.sh`'s default `CHI_WARMUP=6000` applied to a 6,000-tick metrics file — exactly the shape of the article's own `chishow35` run), `lane_speeds` returns `{}`, every lane draws in the EMPTY colour, and the script exits 0 with only a stderr note. Downstream, `mkquiz.py` (diff at scripts/chicago/mkquiz.py, `diagram()`) worsens it: `if n and tot:` treats `lanes_with_traffic: 0` as *absent*, so the caption silently omits the lane count and the page ships an all-grey map indistinguishable from a low-demand run. This whole commit exists because "a stale PNG under the right filename looks exactly like a fresh one" — an all-empty map under the right filename is the same failure. Exit non-zero when `measured == 0` (and consider `mkquiz` refusing a sidecar with `lanes_with_traffic == 0`).

2. **Consistency claim vs `corridorspeed.py` is not true.** `mkcongestionmap.py:105-106` drops `partial` intervals and `:99` restricts to one `set_id`; `scripts/chicago/corridorspeed.py:56-64` — the source of the corridor table the map is checked against, per the KB article — includes partials and sums across all sets. The docstring ("it is what every other number in the show uses (see corridorspeed.py)", "Same convention as corridorspeed.py") claims an equivalence that only holds for single-set files with no partials. On any other file the map and the table diverge for a reason nobody will look for. Fix `corridorspeed.py` to match (it has the actual double-count/truncation exposure), or soften the docstring to state the divergence.

## Nits

3. `mkcongestionmap.py:81` — `doc["intervals"]` raises a bare `KeyError` traceback on a metrics file missing the key; everything else in the script exits with a friendly message. Use `.get("intervals", [])` (the empty case already has a good exit at `:82`).

4. KB article regeneration command (silent-fidelity-failures.md, mechanism 8) omits `--run-label`, so re-running it produces a sidecar with `run_label: ""` that does not match the checked-in `chi-congestion-fw35.png.json` (`"chishow35 (chi-show-fw35, seed 42, 6000 ticks, -drivers 8)"`). Add the flag to the quoted command since the sidecar is presented as reproducible provenance.

5. `mkquiz.py` legend renders half-open bands as closed ranges ("60%–80%" for [0.6, 0.8)); `mkcongestionmap.py`'s stderr legend says "half-open". Cosmetic.

## Questions

6. **Is the setup-map run supposed to satisfy build-quiz.sh's own stated invariant?** The comment (build-quiz.sh, new hunk) says `CHI_METRICS` "must name the -metrics-out of the run the Chicago numbers came from." But `docs/show/quiz/chi-loop-urban.json` numbers come from 6 seeds × 12,000 ticks, while the checked-in map is one hero run (base demand, seed 42, 18,000 ticks, warmup 6,000). Same demand — the fw35 divergence this fixes is genuinely fixed — but no single "run the numbers came from" exists, and nothing verifies demand match beyond the caller's honesty. The caption discloses it, which may be the intended resolution; confirm, and consider rewording the comment to "a run of the same demand the numbers came from."

**Determinism/contract**: no concerns — presentation-only Python, float accumulation follows fixed JSON file order, sidecar is `sort_keys` stable, sha256/`--run-label` provenance is sound, no wall-clock or RNG use, no NATS subject or payload touched. The KB table arithmetic (Edie totals vs interior/origin splits, 8.2–62.3% range vs the "8-62%" INDEX line) is internally consistent.

REVIEW-COMPLETE
