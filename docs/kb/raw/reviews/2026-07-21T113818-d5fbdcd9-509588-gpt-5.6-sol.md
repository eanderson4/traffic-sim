- **blocker** `engine/scenario/variant.go:93` — A `network` override never updates `eff.Network`. The scenario runs and hashes the replacement bytes (`netPath`/`netRel`) while `Manifest.Network` still names the base network, so the effective manifest is inconsistent and cannot hash identically to an equivalent hand-authored scenario. Set `eff.Network = v.Network` when present and add an equivalence assertion.
- **should-fix** `engine/scenario/variant.go:110` — Collision checks exclude the base network path. Added control/metrics parts can therefore shadow it, violating ADR-0012’s hard-error rule for added paths colliding with base parts.
- **should-fix** `engine/scenario/variant.go:142` — The replacement network path is not checked against inherited or added part paths. This permits two physically different files to occupy the same conceptual merged-directory path and creates duplicate hash frames for one relative path.
- **question** `engine/scenario/variant.go:170` — `Scenario.Dir` points at the variant, while inherited `Manifest.Demand/Control/Metrics` paths remain base-relative. Current demand is eagerly loaded, but future consumers resolving manifest paths through `Dir` will read nonexistent or wrong variant files; consider storing resolved part sources explicitly.

REVIEW-COMPLETE
