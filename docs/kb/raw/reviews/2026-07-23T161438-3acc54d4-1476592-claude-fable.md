No blockers. The Go changes are wrapper-level (pacing was already a `ContractConfig` knob with 0 = unpaced documented in `contract.go:54`), no NATS subject or payload changes, no new map iteration / wall-clock / RNG in sampled paths. The viz change is a genuine fix — the old wall-clock-span speed was wrong even at 1× whenever delivery jittered, and tick-delta × dt is also robust to any future snapshot cadence change. Findings:

**Should-fix**

1. **Pace is unrecorded run provenance — the diff's own comment declares the problem, then doesn't solve it.** `main.go` (diff line 179, `maxClientPace` comment): "pace is an unrecorded run condition — the same (scenario, seed) at different paces produces different traffic … cross-pace metric comparisons are invalid." With `-store` making recordings durable artifacts for later replay/metrics, a `-pace 10` recording is indistinguishable from a `-pace 1` one. Replay itself stays bit-exact (intents are recorded), but the metric outputs this repo compares (`-metrics-out`) inherit the invalid-comparison hazard silently. Record pace in `RunMeta` as edge metadata (ADR-0005 allows wall-clock-adjacent metadata there; a `RunMeta` field addition is a contract change per house rules → needs the ADR note). Defer-able, but do it before recordings/content-hashes bind (`/external-review` guidance).

2. **Viz `?dt=` is trust-on-URL with silent failure.** `viz/src/config.ts` (diff line 431): the registry `{run}/meta` already carries `Spec.Params.Dt` (`registry.go:27-36`); a stale bookmark or mismatched `?dt=` scales every derived speed and the congestion overlay by dt_actual/dt_url with no error. serve printing the canonical URL (diff line 140) mitigates for the demo, but nothing detects a mismatch. At minimum note it as a known gap; ideally the viz reads dt from meta when it gains a KV path. Acceptable to defer per the triage bar.

**Nit**

3. `main.go` `checkFreshRecording` (diff line 196): check-then-adopt is TOCTOU against a concurrent serve on the same `-store` dir — but the function's own doc says two brokers on one dir corrupt it anyway, so this is out of scope; fine as-is.
4. `main.go` `paceFloor` (diff line 164): with a degenerate `dt ≤ 0` the error blames `-pace` ("puts the tick floor outside the representable range"); harmless if dt is validated upstream, misleading if not.
5. `main_test.go` (diff line 362): subjects `"ts.taken.log.>"` are hardcoded rather than derived from `natsio.SubjectLogAll("taken")` — self-consistent today, drifts silently if the namespace ever changes.
6. `viz/src/snapshots.ts` `maxFrames = 60` — the comment "6 s at 10 Hz" no longer holds at high pace: at 10× (100 Hz delivery) the retained window is ~0.6 s wall, so a user-set `?buffer=` near or above that clamps to the oldest frame and renders frozen/zero-speed. Fine for the default 250 ms; worth remembering.

**Question**

7. The `-pace 0` refusal covers the in-process driver and the demand director, but a driverless unpaced run still races any *external* controller or viz client attaching mid-run — intentional ("driverless batch" implies no clients), just confirming that's the stated scope of the flag help.

Verified clean: `dt/1` is exact in float64, so `-pace 1` reproduces the old `PaceFloor` bit-for-bit (and the test pins it); the overflow guard `v >= float64(math.MaxInt64)` and the `v < 1` truncation-to-unpaced guard are both correct; `errors.Is(err, nats.ErrStreamNotFound)` works on nats.go v1.52; `checkFreshRecording` is fail-closed on other lookup errors, and the recorder's `LogStart` tick-0 keyframe guarantees any previously-started run has `Msgs > 0`, so the empty-stream pass is safe; the starved/older==newer paths in `sample()` still short-circuit to speed 0.

REVIEW-COMPLETE
