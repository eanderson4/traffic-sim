- **should-fix** `engine/cmd/demosrv/supervisor.go:341` — Readiness can pass as soon as registry metadata appears, but `RunLive` writes `"running"` before recorder, bus, contract, and initial-log setup (`engine/natsio/run.go:108`). A subsequent startup failure can race the one-time child-death check at `supervisor.go:345`, returning a successful start URL for a dying process. Publish readiness only after initialization completes, or require an additional live-plane probe.
- **should-fix** `contracts/asyncapi.yaml:574` — `SignalTableRequest` claims an empty payload but defines no `payload`/`EmptyPayload` schema, and the handler accepts any body (`engine/natsio/bus.go:331`). The sacred contract therefore neither specifies nor enforces the stated request shape. The request/reply operation at `contracts/asyncapi.yaml:464` also does not model the dynamic-inbox reply relationship.
- **should-fix** `engine/natsio/bus.go:212` — A single signal program may exceed both the 768 KiB target and the 4 MiB broker cap; construction only logs and allows the run to become ready. Every broadcast and resync then permanently fails for that table. Fail startup when any encoded chunk exceeds the broker cap, or define a splittable program encoding and update the ADR/contract.

REVIEW-COMPLETE
