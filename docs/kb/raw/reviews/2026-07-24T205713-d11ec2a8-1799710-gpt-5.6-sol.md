- **blocker** — `engine/cmd/demosrv/geojson.go:114`, `engine/cmd/demosrv/geojson.go:214`, `engine/cmd/demosrv/geojson.go:228`: even cache hits call `scenario.Load`, which reads, unmarshals, compiles, and canonicalizes the entire city network (`engine/scenario/scenario.go:192`). Every part request does this once, then `c.path` repeats it. Sequential la-lean loading therefore performs multiple full network compilations/reads and can take minutes or exhaust memory, defeating the chunk cache.
- **should-fix** — `engine/cmd/demosrv/main.go:432`: setting `Cache-Control: no-store` does not prevent `http.ServeFile` from honoring an incoming `If-Modified-Since`. When a previously cached single-file response changes to a manifest within the same timestamp second, ServeFile can return 304 and the browser uses the stale single document—the exact case the comment claims to prevent. Strip conditional headers or serve manifests without ServeFile’s conditional processing.
- **should-fix** — `viz/src/tssg.ts:212`: chunk generations have no identity beyond `i/n`. If round A delivers chunk 1 but loses its tail, then round B loses chunk 1 but delivers chunks 2..n, the accumulator merges both rounds and reports a complete generation. Static tables mask this today, but the contract explicitly discusses future mutable tables; add a round/generation identifier or document and enforce immutability before this wire contract becomes durable.
- **should-fix** — `engine/cmd/demosrv/supervisor.go:139`: the readiness ownership proof is documented as requiring cryptographic uniqueness, but entropy failure silently falls back to wall-clock nanoseconds—the exact mechanism the preceding comment says is insufficient. Propagate the randomness error and fail the spawn instead of weakening the ownership invariant.

REVIEW-COMPLETE
