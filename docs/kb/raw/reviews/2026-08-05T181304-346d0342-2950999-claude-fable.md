Third round on this staged diff (two archives from earlier today are in `docs/kb/raw/reviews/`). I read the full patch, `viz/src/splash.ts`, demosrv's mux, and ADR-0023. No NATS subject/payload changes and nothing touches sampled/deterministic engine paths — demosrv changes are static-file routing only. The prior round's blocker (raw-JSON `network.geojson` served under a `br` header) is fixed by mksite step 4, which compresses every staged copy. Findings:

**Should-fix**

1. `viz/public/_headers:8-10` + `scripts/show/mksite.sh:76-90` now contradict ADR-0023 (`docs/kb/decisions/ADR-0023-baked-replay-pipeline.md:533-535`: "The `Content-Encoding: br` rule applies ONLY to the pre-compressed chunk objects (`.tsrb.br`/`.tsrl.br`)"). The mechanism is sound, but per AGENTS.md rule 2 the KB is stale and should be fixed in this change — add an ADR-0023 addendum recording that on the Pages deploy `network.geojson` is stored brotli under its original name (25 MiB cap + truthful header). Docs are ungated, so this costs no re-review round.

2. `scripts/show/mksite.sh:87-89` — in-place compression mutates a file *inside* the content-keyed `<hash12>` directory: served bytes no longer match the key that names them, and `/baked/*` is cached immutable (`_headers:20-21`). Decoded JSON is byte-identical so nothing user-visible breaks, but any verifier that re-hashes the published tree will mismatch — the exact property ADR-0023's content-keying exists to protect. Record the deliberate exception in the same addendum.

3. `viz/src/splash.ts:21-23` — `BAKED_FEATURED_URL` is now unconditionally non-empty, so `baked` (line 23) is constant-true: on the demosrv/GKE deploy the featured CTA 404s (demosrv serves no `/baked/*`), and the entire live-fallback plane (`activate()`, registry/status fetches, `splash.ts:94-157`) is unreachable dead code plus a comment ("empty = live-demosrv fallback") describing a state that can't occur. `deploy/README.md` documents the 404 as a caveat, so this is an acknowledged deferral — noting it so the triage is explicit; the clean fix is per-deploy config or the probe-and-fall-back the README itself names.

**Question**

4. `viz/public/_headers:8,24` — the load-bearing rules use a *mid-path* splat (`/baked/*/network.geojson`, `/baked/*/index.json`) that must span two segments (`<run>/<hash>`). Cloudflare's Pages docs illustrate trailing splats; if mid-path splats don't match multi-segment, `network.geojson` arrives as undecodable brotli and `index.json` inherits `immutable` (stale pinned manifests — the failure the `!` exception exists to prevent), both silently. One `curl -I` against the deployed site settles it; if already verified on phantomjam.com, ignore.

**Nit**

5. `viz/public/_headers:5-7` — the root `/network.geojson` rule matches nothing in the bundle (no top-level network.geojson in `viz/public/` or the staged output); dead rule (also flagged last round).
6. `scripts/show/mksite.sh:83-84` — the `raw[:1] != b"{"` heuristic: JSON with a leading BOM/whitespace would ship raw under the `br` header; a brotli stream that happens to start `0x7b` would be double-compressed. Fine for tool-generated GeoJSON; worth a comment at most.
7. `scripts/show/mksite.sh:24` — default `--baked` is a personal absolute path (`/home/eric/grove/...`); intentional for worktree runs per the header comment, but it fails obscurely on any other machine even with the repo's own `data/baked` populated.
8. `viz/index.html:8-13` — the legacy-`?bake` redirect drops `location.hash` (no current links carry one) and the splash continues parsing until `replace()` lands; harmless.

Verified non-issues: mksite's `grep` does find the bake references — the splash bundle keeps `/baked/chishow/f941d8888b18/index.json` as a string literal and `dist/quiz.html:124` carries the quiz table, and missing manifests fail loudly. The heredoc keeps the inner python from eating the `find` stream, and `set -euo pipefail` propagates a python failure out of the `while` subshell. Vite's default `emptyOutDir` clears stale `splash.html` (prior-round question). `demosrv` `/{$}`→index.html, `/app/`→app.html, FileServer fallthrough are consistent with the vite three-page build, and `mkquiz.py`/`quiz.html` were updated in lockstep.

REVIEW-COMPLETE
