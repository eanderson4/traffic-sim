- **should-fix** `scripts/chicago/mkod.py:474` — destination weights are dropped and serialized at four decimals without renormalization, while `observe` at `scripts/chicago/mkod.py:494` uses the original retained weights. The engine normalizes relative weights during sampling, so district totals undercount the full flow and can misstate shares. The through counters at `scripts/chicago/mkod.py:1089` and `scripts/chicago/mkod.py:1143` similarly use the pre-rounding blend rather than the emitted distribution.
- **should-fix** `scripts/chicago/mkod.py:462` — rates are serialized at one decimal, but `tally` at `scripts/chicago/mkod.py:1019` and `observe` at `scripts/chicago/mkod.py:494` integrate full-precision values. Across hundreds of flows, the reported/header peak and vehicle count can differ from the file despite repeatedly claiming to measure exactly what was written. Compute diagnostics from the serialized rate values.

REVIEW-COMPLETE
