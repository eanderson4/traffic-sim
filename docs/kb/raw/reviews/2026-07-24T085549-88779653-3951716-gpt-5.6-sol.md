- **blocker** — `viz/src/artic.ts:69`: `MAX_HITCH_RAD + v*dtS/TRAILER_M` can exceed π, making the clamp unreachable because `wrapPi` is bounded by π. At 16× replay, `dtS≈1.6` (`engine/natsio/player.go:623`), so even at 10 m/s the limit is ~200°; at the truck’s 22.2 m/s, it is already ~212° at 8×. The normal integration path then preserves a severe jackknife. `viz/test/artic.test.ts:170` tests only 10 m/s and 0.8 s, narrowly avoiding this failure; add production-speed and 16× cases and prevent the limit from disabling detection.

REVIEW-COMPLETE
