**Review of staged diff** (metview tool + WQ-15 kernel hot-spot fixes). Context checked: `boundaries()` old/new equivalence against `pickSuccessor` (no RNG; route memo is value-order-independent), `Lane.Index` assignment at every construction site (netfile.go:160, scenario_i80.go:139, BuildNet network.go:135–175 — all position-consistent), `TotalLaneKm` callers (spawn.go:163, director.go:158 — both tick-path, single goroutine), and chained-hop test coverage (`TestKernelDespawnAfterCrossing`, metrics_test.go:904, exercises the new `past[next]` append: exit lane at index 0 precedes its feeder, cross+despawn in one pass).

**Blockers:** none.

**Should-fix**

1. `engine/engine.go:471` — correctness of the chain step now depends on the `Lane.Index == slice position` invariant, which the OLD sweep did not: the old re-scan reached lanes by position, the new code routes crossers by `next.Index`. If the invariant is ever violated (hand-built test net, future loader bug), a crosser is appended to a bucket that never gets processed (silently deferred a tick) instead of failing loudly, and old/new diverge. All current construction sites are consistent, but this is exactly the kind of silent drift a one-line debug assertion at network build/load (`Lanes[i].Index == i`) would catch. Cheap; consider it.

**Nits**

2. `engine/network.go:90` — `if n.totalLaneKm == 0` uses zero as the "unfilled" sentinel; a lane-less network recomputes every call. Harmless today (no such runs), but a `bool` filled flag is the same cost and unambiguous.
3. `engine/network.go:88` — the lazy write makes a read-named method mutating; the run-loop-only constraint is documented in the field comment, which is the right call, but any future off-goroutine caller (HTTP stats handler) is a data race the type system won't flag. Fine as-is at this stage.
4. `engine/cmd/metview/main.go:262` — the loopback guard's `strings.HasPrefix(host, "127.")` accepts hostnames like `127.evil.tld`; bind would then go through DNS. In practice binding a non-local address fails, and it's a dev tool, so cosmetic.
5. `engine/cmd/metview/main.go:417` — `float64(iv.EndTick - iv.BeginTick)` on uint64: a corrupt file with `EndTick < BeginTick` yields an astronomically large weight rather than an error. Read-only viewer, acceptable.
6. `engine/cmd/metview/main.go:510` — censored-only type buckets render zero means in the UI instead of "—"; mildly misleading but the censored count sits next to them.

**Questions**

7. `engine/cmd/metview/main.go:196` — hand-duplicating the sink schema is flagged as deliberate, but could the sink's marshal structs be exported from `engine` and reused? The stated failure mode ("a sink rename zeroes fields here") is silent, which is the worst kind for a comparison tool someone will trust. If export is awkward, a tiny "no field decoded to zero-value across ALL loaded files" sanity warning at load would catch drift cheaply. Committer's call under the triage bar.

**Determinism/contract:** clean. `past` map is never range-iterated (buckets built and drained in `e.order`/lane-slice order); the tail-append reordering of chained crossers is covered by the documented order-independence argument plus the CRC-identical 7200-tick A/B. No NATS subjects or payloads touched; metview consumes the ADR-0014 §6 file sink read-only. KB updated in-commit; the `boundaries()` comment honestly records the conditions under which bucketing stays state-identical — good future-proofing for capacity gating.

REVIEW-COMPLETE
