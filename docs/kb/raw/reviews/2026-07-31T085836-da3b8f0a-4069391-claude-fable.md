**Verification done:** restore paths (RestoreState → NewEngine, engine/keyframe.go:305; warm start via same), the v5-keyframe-into-flag-on edge (laneEntryTick seeded at restore tick, engine/keyframe.go:363-370), v6-into-flag-off refusal (engine/keyframe.go:312), RunMeta embedding the fully-resolved spec (engine/natsio/registry.go:24-29), struct-literal engines confined to tests, and `cd engine && go test ./...` — all green. The engine-side flip, the `*bool` manifest plumbing (engine/scenario/scenario.go:418-420), and the three-way scenario test are correct. One real gap found, in the pin coverage the ADR's run-identity triage rests on.

## Blocker

1. **The ADR's pin-coverage claim is false for a published docs/show arm.** The addendum (ADR-0036 addendum, patch lines 75-79) resolves the Sol run-identity blocker by asserting "every published docs/show arm now carries an explicit `adaptive_routing: false` pin … emitted by the scenario generators — mkscenario.sh, merge-pod.py, bottleneck_town.py — so regeneration keeps it." But the pod arms are generated by a fourth generator the patch misses: `scripts/chicago/mkscenario-portal.sh:37` emits a manifest with no `params:` block, and `pod-portal.sh:44` derives every arm from it via sed. Concretely, all 9 on-disk `data/pods/chi-loop-cbd/*/scenario.yaml` arms — the source of the published CBD-winner p=0.0122 in docs/show/README.md — are unpinned (verified by grep), while the bottleneck-town arms are pinned. By the ADR's own argument, offline what-if artifacts have *only* the manifest to pin regime, so re-running the CBD pod today silently measures adaptive routing against a static-measured published number. Fix: add the pin to `mkscenario-portal.sh`, stamp the on-disk chi-loop-cbd arms, or narrow the ADR sentence to what's actually covered.

## Should-fix

2. **Sweep the remaining on-disk manifests or state the boundary.** ~45 `data/scenarios/*/scenario.yaml` (t13b/t13c meter-and-diet arms, chi-show-fw*, city scenarios) and `data/pods/bottleneck-town-null/*` are unpinned. Most are non-published or A/A (the null pod flips both arms together, so its calibration conclusion survives), but the t13c/chi-show families fed earlier published analysis. A one-line KB note saying which artifact sets are intentionally left on the new default would close this cheaply.

## Nits

3. ADR addendum wording "stamped in the same commit" (patch line 77) — `data/` is gitignored, so manifest stamps cannot be in *any* commit; only the generator changes ship. Say "stamped on disk alongside this commit."
4. `engine.go` lazy-init sentinel (`routeEpochTicks == 0`, patch lines 216-224): correct today and the invariant comment is exactly right; note only that a future path flipping `Params.AdaptiveRouting` on after a flag-off NewEngine would silently re-seed the EMA mid-run through this branch. Nothing to change now.

No determinism issues: the lazy init is a pure function of the network, runs at a deterministic point, and no new map iteration in sampled paths. No subject/payload changes; asyncapi.yaml already documents v6 gating, and the bool→`*bool` YAML change is parse-compatible with existing `true`/`false` manifests.

REVIEW-COMPLETE
