**Blocker**
- `docs/kb/decisions/ADR-0023-baked-replay-pipeline.md:464` — The content key hashes inputs, but the ADR never mandates canonical ordering for region buckets, manifest regions, TSRB vehicles, or TSRL lane pairs. A natural Go implementation will iterate maps, producing different immutable bytes at the same `hash12`. Require sorted region keys, vehicle IDs, and `lane_idx` values, plus deterministic manifest serialization/compression.

**Should-Fix**
- `docs/kb/decisions/ADR-0023-baked-replay-pipeline.md:186` — “2 Hz = every fifth tick,” 120 frames = 60 seconds, and TSRL’s 0.2 Hz math only hold for `dt=0.1`, while ADR-0005 permits scenario-specific `dt`. Derive cadence from `dt` with a defined rounding policy, or reject unsupported timesteps.
- `docs/kb/decisions/ADR-0023-baked-replay-pipeline.md:211` — The region set is defined by vehicle-position occupancy, while TSRL ownership uses lane midpoints. An occupied boundary-crossing lane can assign its ratio to a home tile absent from `regions`, losing the aggregate. Define regions as the union of TSRB regions and TSRL owner regions.
- `docs/kb/decisions/ADR-0023-baked-replay-pipeline.md:398` — TSRL exists only every tenth baked frame, but the applier says to look up the frame “for the current tick.” Most ticks have no exact aggregate. Specify greatest TSRL tick ≤ current tick and hold until the next aggregate, including seek/end behavior.
- `deploy/README.md:46` — Minute-resolution timestamps are not “unique per build”; two dirty-tree rebuilds within one minute reuse the tag and can serve the stale image this change intends to prevent. Use seconds plus entropy/content identity.
- `README.md:37` — ADR-0020 requires both WebSocket authorization and `/healthz` before go-live, but this claims auth alone unlocks the live site.
- `docs/kb/INDEX.md:107` — The decision registry stops at ADR-0022; the new ADR-0023 is not registered, contrary to the repo’s KB workflow.

REVIEW-COMPLETE
