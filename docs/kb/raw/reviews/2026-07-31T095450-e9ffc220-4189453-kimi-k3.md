• Now let me examine the repo context around the changed code.

• Let me check the restore path, the tests that use `DefaultParams()` without pinning the flag, and the CLI entry points.

• Now checking the CLI entry points, RunMeta params claim, and scenario hash canonicalization.

• The code reads clean so far. Let me verify the suite actually builds and the touched test areas pass.

• Targeted tests pass. Running the full engine suite in the background while I finish the last checks.

• Full test suite is green (`go test ./...` across engine + natsio, 58s). Review complete. Findings:

  ## Blockers

  None.

  ## Should-fix

  1. **`engine/natsio/run.go:103` — the `-state-in` path drops `RestoreNotice`.** The live-run warm start (`serve -state-in` → `ContractConfig.InitialState` → `RestoreStateChecked`) never surfaces `e.RestoreNotice`. Only the recording-plane callers (replay/bake/player) were wired. But `engine/keyframe.go:317-319` explicitly names "a plain -state-in of an old static keyframe" as the case that now fires, and the ADR addendum claims "`RestoreState` now logs it loudly". The one path a user actually hits with `-state-in` is exactly the one that's silent. Wire it in `run.go` (or fix the comments/ADR claim).

  2. **`scripts/chicago/mkscenario-portal.sh:37-48` — portal generator missing the pin.** `mkscenario.sh`, `merge-pod.py`, `bottleneck_town.py` all emit `adaptive_routing: false`; the portal variant does not. `pod-portal.sh:35` builds the chi-kennedy / chi-loop-cbd what-if pods (published docs/show arms, per its own header) through `mkscenario-portal.sh`, so regenerating those pods silently rebaselines them onto adaptive routing — precisely what the pin exists to prevent, and it contradicts the addendum's "emitted by the scenario generators … so regeneration keeps it".

  ## Nits

  3. `engine/natsio/replay.go:115` and `bake.go:73` use the package-global `log.Print` while `player.go:211,503` uses the injected `lg`/`p.log`. Inconsistent, and a library writing to the default stderr logger can't be silenced by the caller.
  4. `engine/engine.go:64` — "see engine.go's Params doc" is self-referential (this *is* engine.go); presumably means the `AdaptiveRouting` field doc at engine.go:52.

  ## Questions

  5. No non-manifest opt-out: `serve -net`/`-netfile` (`cmd/serve/main.go:186`) and `simrun`'s netfile/default paths (`cmd/simrun/main.go:86,91`) use `DefaultParams()` with no `-adaptive-routing` flag, so those surfaces can't pin the static baseline — only scenario manifests can. Intended?
  6. INDEX.md:10 vs the pre-edit entry: seed-42 adaptive completions changed 831 → 786 with no note (re-run?). Trivial, but the old number is quoted in the same file's history.

  ## Verified clean

  - Determinism of the lazy init (`engine.go:417-429`): sentinel `routeEpochTicks == 0` can only fire on struct-literal engines — `NewEngine` sets it ≥ 1 always, and every restore path constructs via `NewEngine` (`keyframe.go:305`), so restored ttEMA/ttSnap can't be clobbered. The acknowledged pre-first-Step divide-by-zero (`routing.go:209`) is test-only. No map iteration, float accumulation, wall-clock, or RNG changes in the diff.
  - Contract: no subject changes. The bool→*bool hash reasoning checks out — canonical bytes come from `canonicalYAML(&Manifest)` (`scenario.go:209`), explicit `true` marshals identically under both types, and only explicit `false` (claimed nonexistent pre-commit) would move a hash. RunMeta does embed the resolved `RunSpec` incl. `Params` (`natsio/registry.go:29`). Old recordings replay bit-exactly since replay uses the recorded spec (`replay.go:75`, `natsio/replay.go:110`).
  - Tests that assert pre-v6 keyframe versions were all pinned flag-off (`destination_test.go:31`, `gridlock_test.go:578`, `adaptive_test.go:156`); no other test pins absolute CRCs (fixture CRC tests are two-run self-comparisons). `go build`, `go vet`, full `go test ./...` all green.

  REVIEW-COMPLETE

