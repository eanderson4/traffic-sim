No blockers. The mechanism is sound — stamp binds (base HEAD, index tree) captured pre-review, drift check before stamping, fail-closed marker validation, partial-commit handling via `GIT_INDEX_FILE` all check out. Findings:

**should-fix**

1. `hooks/pre-commit:5` — the header comment lists `tools/` among the ungated paths, but the regex (`hooks/pre-commit:22`), AGENTS.md, and ADR-0013 §1 all gate it — and `tools/` is a real top-level directory in this repo. The safe direction (gated), but the mechanism file misdocuments its own policy; a future editor "fixing" the regex to match the comment would silently ungate real tooling. Align the comment.

**nit**

2. `hooks/pre-commit:41-44` — message-only `git commit --amend` of a just-reviewed commit fails the gate: the stamp holds `(A, T)`, but during amend HEAD is the commit being amended (B), so the hook compares `B T` ≠ `A T`. Content is unchanged and already reviewed, yet the only path is skip-hatch or re-review. ADR-0013 §1 documents the rebase/cherry-pick gap but not this friction; worth a line in the ADR or the hook's error text.
3. `hooks/pre-commit:24-26` vs `:36` — the comment claims detection is FAIL-CLOSED, but only the `git diff` step is checked; if `grep -vE` errored (exit 2), the empty pipeline output reads as docs-only → gate waived. Theoretical (the regex is static and valid), but the comment overstates the guarantee.
4. `scripts/external-review.sh:394-395` — the marker check is byte-exact on the last nonblank line; a model emitting `REVIEW-COMPLETE ` (trailing space) or CRLF fails the round after 2×15 min of review wall time. Fail-closed, so not a correctness issue, but trimming trailing whitespace in the `awk` would cut false failures cheaply.
5. `scripts/external-review.sh:326-329` — the hooksPath check misses benign variants (`hooks/` with trailing slash, an absolute path through a symlinked root), producing a spurious "gate not enforced" warning.
6. `hooks/pre-commit:22` — only root `.gitignore`/`.gitattributes` are ungated; subdirectory ones (e.g. `viz/.gitignore`) are gated. Probably intended (they can affect what ships), but AGENTS.md's flat ".gitignore/.gitattributes" wording doesn't say so.

**question**

7. AGENTS.md is root Markdown and therefore ungated, yet it is the normative statement of the gate policy — the policy text can drift from the gated mechanism without review. ADR-0013's honest-actor boundary arguably covers this, but the ADR doesn't call it out the way it does the forgeable stamp.
8. `docs/kb/decisions/ADR-0013-external-review-gate.md:179` — "The bootstrap commit used the documented skip exactly once." This very round is reviewing the bootstrap commit; if it stamps cleanly, no skip will have occurred and the ADR ships asserting a history that didn't happen. Confirm which it is before committing.

Determinism/contract scope: no engine, contract, or NATS-touching code in this diff; ADR-0013 exists for the process change, satisfying AGENTS.md rule 3. Wall-clock use in `scripts/external-review.sh:341` is tooling-only, not sim-path — fine.

REVIEW-COMPLETE
