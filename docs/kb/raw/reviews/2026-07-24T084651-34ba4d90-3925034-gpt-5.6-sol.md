- **should-fix** `viz/src/artic.ts:103` — The clamp checks the hitch angle only *after* integration. With fast-replay deltas, an initially glitched angle can integrate below 75° and evade snapping; e.g. an 80° jump at 10 m/s with `dtS=0.8` finishes near 50°. Check the pre-integration hitch angle before any hold/substep path. The tests at `viz/test/artic.test.ts:128` cover only a small `DT`, so they miss this case.

REVIEW-COMPLETE
