All 12 tests pass against the staged code (staged == working tree for both files). The diff is viz-only — no engine, NATS subject, or payload changes, so no contract/ADR exposure, and the float math lives purely in per-client rendering, outside the determinism-disciplined sampled path.

## Findings

**Blockers:** none.

**Should-fix:** none.

**Question**
- `viz/src/artic.ts:44` — the header claims forward-driving articulation peaks ~45–60°, but the true steady-state gap is `arcsin(ωL/v)`, and a real tractor's ω/v ratio at full steer lock gives `L·tan(δ)/wheelbase`, which can exceed sin 75° in a slow tight crawl. If the engine ever emits low-v, high-heading-rate frames (creeping through a tight turn), a legitimate pose snaps with a visible pop. Is the 45–60° peak an observed engine property or an assumption? If observed, fine as-is; "too early" triage applies either way.

**Nits**
- `viz/src/artic.ts:103` — snapping to fully aligned (gap 0) is a larger visual discontinuity than clamping to the threshold; for a truck hovering near 75° this pops harder than needed. For genuine glitches (heading flips) full re-anchor is the right call, so this only matters if the question above bites.
- `viz/test/artic.test.ts:97` — `gap > 45°` is a loose bound for a converged value of 60°; `Math.abs(gap − 60°) < ε` would also catch a future regression that *overshoots* articulation. Purely optional.

Everything else checks out: the clamp is a strict `>` (exactly 75° kept), `wrapPi` correctly handles the unbounded accumulated trailer angle, the clamp sits after all three paths (spawn is a no-op, hold is the case that motivated it), and the three new tests' math is sound (test 3's circle drive converges monotonically from below, so it never trips the clamp mid-test).

REVIEW-COMPLETE
