- **should-fix** `engine/natsio/tsib.go:54` — `EncodeTSIB` silently removes every `RouteSet` intent and can return a valid empty batch. Any caller missing the separate v2 diversion loses complete vehicle updates without an error. Reject route-bearing input or explicitly return omitted records.
- **should-fix** `engine/natsio/tsib.go:89` — decoding discards the source tick at bytes 8–15; `engine/natsio/bus.go:319` then expands records without preserving it. This prevents the ADR-0026 source-tick→`applied_tick` metric or post-run correlation unless the ingest model is changed again.
- **question** `engine/natsio/bus.go:53` — this introduces a public header and payload encoding while `contracts/asyncapi.yaml:181` still specifies only v2. ADR-0026 defers schema/migration documentation to M4, but that appears to conflict with the repo rule that message-contract changes ship with an ADR and migration note.
- **nit** `engine/natsio/tsib.go:93` — the decoder accepts nonzero reserved header/record fields despite declaring them reserved-zero, reducing safe future extension options. Validate zero or document them as ignored.

REVIEW-COMPLETE
