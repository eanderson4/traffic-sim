- **blocker** `engine/keyframe.go:127` — Vehicle serialization still omits `stopDone` (`engine/vehicle.go:79`). A state saved after completing a stop but before crossing restores with `stopDone=false`, forcing a second stop and breaking ADR-0029’s bit-exact continuation guarantee.
- **blocker** `engine/natsio/run.go:179` — A warm run publishes no initial snapshot/unclaimed set before opening the start gate. Restored vehicles are only announced after the first resumed `Step` (`engine/natsio/contract.go:945`), so the default driver cannot claim or control them for that tick; the fresh contract also has no prior hold-last state. The live continuation therefore diverges even though the controller-free test passes.
- **should-fix** `engine/metrics.go:613` — The stranded branch closes the trip without accounting for the removal tick. Because `strandStuck` removes vehicles before `Observe`, that full tick contributes no occupancy, VHT, stopped time, or time loss for each stranded vehicle.

REVIEW-COMPLETE
