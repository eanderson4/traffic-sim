`go vet ./...` is clean (compiles all packages including tests — the field rename left no dangling references); gofmt check was denied but the realigned struct literals in the diff are visually consistent.

**Findings**

**No blockers.** The default flip is correct and complete end to end:

- `engine/cmd/serve/main.go:103-104` — both flags default `false`, passed straight through at `main.go:499-500` (no negation).
- `engine/natsio/recorder.go:141` (`!CompressedStore` → `NoCompression`) and `recorder.go:202` (`!BatchedIntentLog` → per-intent loop) invert correctly; the zero-value `RecorderConfig` now selects per-message + uncompressed, resolving the prior round's library-caller blocker.
- `engine/natsio/tslb_migration_test.go:243-267` — the new `TestZeroValueRecorderConfigIsTheReproducibleDefault` pins both zero-value fields *and* the actual stream compression, exactly the regression test the 15:57 round asked for. `writeLog`'s `BatchedIntentLog: !unbatched` (`tslb_migration_test.go:73`) preserves caller semantics; the adoption test's move to `CompressedStore: true` (`tslb_migration_test.go:212,234`) correctly keeps the differing-config fallback exercised.
- Every doc-drift site from both archived rounds is fixed: `architecture.md:92`, ADR-0006:389/407, ADR-0035 §4/§5/migration note (now stated by flag, not chronology), ADR-0026:145 note, the Go API rename recorded at ADR-0035:226, and — third time flagged, now actually fixed — `compression: s2` is **removed** from both `x-nats` blocks (`contracts/asyncapi.yaml:458-467,495-504`), so a machine reader no longer sees s2 as invariant. Grep confirms no stale `Unbatched…`/`Uncompressed…` or "default on" claims anywhere outside the review archives and the migration note documenting the rename.
- `engine/network.go:59-66` comment verified accurate against the CRC fold at `engine.go:1069-1077` (flag-gated, lane order = network order, flag-off keeps the pre-v6 byte stream).
- No subject or payload changes; the default flip carries its ADR notes (ADR-0035 correction, ADR-0006 addendum, contract file) as ADR-0006 requires. No determinism-relevant code paths touched beyond the defaults themselves, which is the point of the change.

**nit** — `contracts/asyncapi.yaml:490`: "stream order is the application order (ADR-0008 §4)" cites the wrong ADR — that claim lives in ADR-0006 §4 (ADR-0008 is the control plane; every other citation of this fact, e.g. ADR-0006:401, says 0006 §4). Pre-existing and untouched by this diff, but it's a wrong citation in the sacred contract file this commit is polishing — fold it into the next docs pass.

**nit** — `contracts/asyncapi.yaml:49`: "compression opt-in via -store-compress since ADR-0035" reads as if it was opt-in from ADR-0035's merge; it was default-on for six days. ADR-0035's status note carries the real history, so this is acceptable compression of the story — flagging only so nobody later "fixes" the ADR to match the contract's simplified phrasing.

REVIEW-COMPLETE
