Review of the staged diff (`/tmp/external-review-ixDot7/diff.patch` — TSSG chunking ADR-0016, chunked GeoJSON ADR-0018, spawn identity, themes/stop-signs). Verified against the repo: the `ts.{run}.state.sig.req` channel and `sig_chunk` header are **already in HEAD's** `contracts/asyncapi.yaml` (lines 139, 761), so the contract side of ADR-0016 is not a gap in this commit; ADR-0016/0018 exist and match the implementation. Chunk packing is a pure function in file order, no Go map iteration or wall clock enters any published payload, and the kernel is untouched — determinism discipline holds. All layer ids referenced by `layertoggles.ts` exist in `main.ts`; `parseChunkHeader`, `-attach-timeout`, and `validToken` (accepts the nonce hyphen) all pre-exist.

**Blockers:** none found.

**Should-fix**

1. `engine/cmd/demosrv/main.go:421` — `Cache-Control: no-store` is set for every non-part `/net/{id}.geojson` response, but the server can't tell manifest from single-file at that point. Large *unchunked* nets (sf-lean's 188 MB single doc, anything under the 256 MiB threshold) lose `http.ServeFile`'s Last-Modified/304 revalidation and re-download in full on every viz load — a regression at exactly the scale this change targets. Only the manifest needs `no-store`; set it conditionally (the generation path knows which it wrote), or at least downgrade to `no-cache` for the single-file case.

**Nits**

2. `viz/src/nats-client.ts:127-129` — doc comment says reply chunks "feed the SAME handler … converge in the one accumulator"; main.ts deliberately feeds a *dedicated* `replyAccum` (the sol-review fix). Stale comment contradicting the design it documents.
3. `engine/cmd/demosrv/main.go:350` / `viz/src/demos-core.ts:60` — the Go handler embeds `&ws=ws://…` unescaped while `buildAppURL` uses `encodeURIComponent`; the adjacent comment says the two builders "must agree". `URLSearchParams` decodes both identically so nothing breaks today, but any future string comparison of the two URLs will.
4. `engine/cmd/demosrv/geojson.go:568-585` — each part request runs `scenario.Load` + full-file SHA256 twice (once in `part()`, again inside `c.path()`). At la-lean scale that's ~10 full reads+hashes of a multi-hundred-MB file per page load, serialized against the client's sequential part fetches. A (path, mtime, size)→hash memo would remove it; acceptable to defer for a dev server.
5. `engine/cmd/demosrv/main.go:1223-1230` (`advertisedWsURL`) — an IPv6 request host (`[::1]:8080`) yields `ws://::1:8443` without brackets, which won't dial. Loopback-IPv6 dev edge only.
6. `engine/cmd/demosrv/supervisor.go` `waitIdentity` — a fresh WebSocket connection + JetStream context per 1 s probe, up to 660 s (~660 connects against a broker mid-world-build). Works; a held connection with a retried KV get would be gentler.
7. `viz/src/tssg.ts:29` exports `SIG_CHUNK_HEADER` but `main.ts`/`nats-client.ts` use the `"sig_chunk"` literal.
8. `viz/demos.html:104-135` — third copy of the palette values (theme.ts THEMES, index.html `:root` defaults, demos.html inline PALETTES). `theme.test.ts` pins only theme.ts, so the copies can drift silently.

**Questions**

9. `viz/src/main.ts` `requestSig` — under a *chronic* per-round chunk drop, every 20-tick broadcast round detects a gap and fires a full-set pull, and each completed pull re-arms `sigReqRetries`; worst case is ~2× the table bandwidth (broadcast + pull per round) aimed at the busy tab. The in-flight guard and retry cap bound each round, and this is still better than the old silent drop — confirming the sustained-2× case is accepted per ADR-0016's pull-when-ready intent.
10. `engine/natsio/player.go:404` — an *old* viz binary attaching to a *paused* new replay now gets no signal table until resume (no broadcast while paused, and it doesn't know `sig.req`). ADR-0016's coordinated-migration note covers this (in-repo viz is the only reader, migrated in this same change) — flagging only to confirm it's the intended reading.

Also verified and clean: accumulator never half-swaps a table and treats tick as non-identity (paused-replay same-tick rounds tested); a stale pull reply overwriting a fresher broadcast is harmless because table contents are tick-independent and phase derivation uses the sample tick; `NewBus`'s error path unsubscribes the sig.req responder; manifest-last write ordering plus hash+schema-pinned part 404s preserves per-fetch atomicity; the small-net single-file path is byte-identical (pinned in `TestWriteGeoJSONRange`).

REVIEW-COMPLETE
