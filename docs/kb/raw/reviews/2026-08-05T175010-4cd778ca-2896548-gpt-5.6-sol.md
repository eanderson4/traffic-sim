should-fix — `engine/natsio/run.go:182`: `rec.LogStart` durably writes the initial keyframe before `reg.Start` at line 205. A transient KV failure leaves a non-empty recording with no `RunMeta`; replay cannot discover it and freshness checks prevent retrying the run ID. Preserve a pre-ready registry state or roll back the recording on failure.

should-fix — `engine/natsio/run.go:72`: the signal table is incorrectly described as part of the “static record plane”; ADR-0006 defines `ts.{run}.state.sig` as interest-gated core NATS. Because it publishes before registry visibility, registry-driven clients necessarily miss that broadcast and must use `sig.req` or await cadence. Narrow the readiness promise to durable keyframe plus available responders.

should-fix — `docs/kb/articles/cross-topic-concerns.md:51`: changing `StatusRunning` into a readiness guarantee—and removing aborted registry entries for construction failures—is public metadata-plane lifecycle semantics. The KB records it, but the repo’s ADR rules require this consequential contract decision and migration consequence in ADR-0006.

REVIEW-COMPLETE
