- **should-fix** `engine/cmd/demosrv/main.go:159` — `advertisedWsURL` rewrites loopback listeners to the request hostname. If demosrv is exposed remotely while `-ws` remains `127.0.0.1:8443`, clients receive `ws://remote-host:8443`, but the engine only accepts loopback connections. Only wildcard listeners are safely host-substitutable; require an externally bound `-ws` for remote access.
- **should-fix** `engine/cmd/demosrv/main.go:419` — `Cache-Control: no-store` applies to both manifests and ordinary single-file networks. Every reload therefore re-downloads even a 200+ MiB unchunked network. Apply `no-store` only to manifests; single files can revalidate normally.
- **nit** `viz/src/nats-client.ts:62` — comment says pull replies and broadcasts share one handler/accumulator, but `main.ts` intentionally uses a dedicated `replyAccum`.

REVIEW-COMPLETE
