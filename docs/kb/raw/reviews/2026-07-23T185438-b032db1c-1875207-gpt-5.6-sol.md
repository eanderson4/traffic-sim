- **blocker** `engine/cmd/demosrv/replay.go:56`, `engine/cmd/demosrv/geojson.go:43` — Replay display state comes from a mutable `scenarioDir` without checking its content hash against recorded `RunMeta.Spec.Hash`. Editing/replacing that scenario can silently render a different network or timestep than the recording, violating ADR-0012’s recording binding and undermining replay trust. Validate the hash or derive display metadata from the recording.
- **should-fix** `viz/src/main.ts:552` — `onSeeked` runs only after the synchronous HTTP response, but the player publishes the landing frame before acknowledging. If that frame reaches the subscription first, this callback immediately discards it; paused seeks then display stale pre-seek vehicles until the next ~1 Hz republication. Arm/reset before the seek or coordinate with a seek generation.
- **should-fix** `viz/src/main.ts:552` — Seek cleanup does not reset `Articulator`. Trucks retaining the same ID keep trailer angles from the abandoned timeline; on a paused landing the single-frame sample reports zero speed, so articulation can remain permanently wrong. Clear articulation state on every seek.
- **should-fix** `engine/cmd/demosrv/replay.go:98` — Controls are bound only to “some replay is active.” An old browser tab can control a different recording, and a concurrent start can swap children after `replayActive()` but before forwarding to the reused port. Bind requests to the expected run/supervisor generation.
- **should-fix** `viz/src/replaypanel.ts:268` — One transient status timeout/502 permanently hides the panel and stops polling while replay continues. Require consecutive failures or keep probing so controls recover.
- **should-fix** `engine/cmd/serve/main.go:152` — Persisting an absolute `Net.Path` fixes CWD sensitivity by making durable recordings checkout-location-specific. Moving the repository/store breaks replay; use a portable recorded reference or explicitly document this artifact limitation in an ADR/KB note.

REVIEW-COMPLETE
