No blockers. Viz-only change (no NATS subjects or payloads touched — TSSG wire format unchanged, `SignalHead.bar` is client-side derivation, so no ADR needed). Clustering math, bar geometry, and tests all verified sound: `entryBearing` returns unit vectors so the `> Math.SQRT1_2` dot is a true cos-45° gate; the running-mean order dependence is acknowledged and deterministic given wire link order; sequential float accumulation is deterministic in JS; `LineString` is already imported in staged `main.ts`; shared feature-id for head+bar is valid MapLibre usage with `promoteId`, and `sigPoints` correctly stays heads-only. Test expectations recomputed by hand — all exact or within tolerance, including the negative-zero-safe `deepEqual` case.

**Findings:**

- **nit** — `viz/src/main.ts:389-391` (patch context; comment above `signals-bars`): "Off = dim lens tone" is wrong — the off arm of the match is `THEME.noData` (`#7e9dff`), not the housing's dim-lens navy (`#1d2950`, signalhead.ts). And since bars draw directly on top of lanes whose no-congestion-data tint is that same `#7e9dff`, an off-state bar (real for SUMO `O`/`o` state chars, and before the first sig frame) is indistinguishable from a no-data lane. Consider a dedicated dim tone; at minimum fix the comment.

- **nit** — `viz/src/main.ts:227-269` (pre-existing, not introduced here): feature-state entries on the `signals` source are never removed when `sigSourceKey` changes and `setData` drops ids; stale state accumulates across run switches. Harmless at current scale; noting only because head counts grew (Wilshire 824 → 1407).

- **question** — `viz/src/signals.ts:96-101`: strict `> cos 45°` per-join against a *running* mean lets a bridging lane chain two arms ~60° apart into one cluster if a ~30° lane joins first (the comment even advertises this). Is that chaining desirable, or should the gate compare against the *first* lane's bearing? Fine to defer — measured head counts say the current gate lands where you want.

REVIEW-COMPLETE
