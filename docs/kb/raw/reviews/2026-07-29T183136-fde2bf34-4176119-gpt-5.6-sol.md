- **blocker** `scripts/show/mkcongestionmap.py:165` drops measured lanes with zero vehicle-time. Consequently `scripts/show/mkcongestionmap.py:253` computes “measurement coverage” from occupied lanes, rejects legitimate quiet runs, and renders unmeasured lanes identically to measured-empty lanes—the exact absent-vs-empty fidelity failure the ADRs forbid.
- **blocker** `scripts/chicago/corridorspeed.py:119` labels the selected set’s aggregate `NETWORK (all lanes)`. ADR-0014 permits subset sets, so `--set` can silently present subset speed and VMT as whole-network results.
- **should-fix** `scripts/whatif.py:251` classifies every incomplete trip as horizon-censored. Stranded trips are incomplete but already removed, so `censored_frac` at `scripts/whatif.py:271` overstates “still in-network at horizon” on gridlocked runs.
- **should-fix** `viz/src/flowpanel.ts:256` excludes strandings from the rate chart’s y-axis maximum, although the strand series is drawn at `viz/src/flowpanel.ts:276`. A large sustained stranding burst is clamped silently, hiding the primary gridlock signal.
- **should-fix** `scripts/chicago/mkod.py:971` records realized portal totals before profile resolution/scaling at `scripts/chicago/mkod.py:989`; interior totals similarly use unscaled `each` at `scripts/chicago/mkod.py:1050`. Non-unit scales or unmatched flows make diagnostics disagree with generated demand.
- **should-fix** `scripts/show/mkflowcurve.py:121` introduces a versioned JSON model consumed by `viz/src/flowpanel.ts:60`, but no ADR defines that cross-component data contract. Repo rules require consequential data models to be recorded.

REVIEW-COMPLETE
