• Now let me read the middle portion of the diff (analysis docs, ADR-0038, and the top of consolidate.go).

• Now let me check the repo context: `engine.NetFile` structure, the kernel's successor handling, and the surrounding `netimport.go`.

• Key context found — several kernel spots lean on single-successor internals. Let me read them.

• Let me check `rightofway.go` (exitWalk/boxWalk) and `network.go:215` for single-successor assumptions.

• Nearly done — a few final checks on `pickSuccessor`, the network-format contract wording, and the Python test helper.

• Let me verify the build/tests actually pass on this tree.

• One last check on `rep.Lanes` accounting and the loader's successor validation.

• Review complete. Verified against the staged tree: `go test ./netimport` + `go vet` clean, `test_mknetvariant` 4/4 pass.

  ## Blockers

  None. The core transform is sound: slice-ordered iteration throughout (deterministic, ADR-0005 clean — the `byJunction` map iteration in `computeConflictFoes` is order-independent since junctions partition the lanes and pair loops run in file order), no wall clock/RNG, no NATS contract touch, loader never enforced single-successor internals (`engine/netfile.go:187-205` only rejects dangling/exit+successors), and the kernel probes (`exitWalk`, `gateTarget`, `leaderAtFor`, `mergeThreat`) are all `pickSuccessor`-general.

  ## Should-fix

  1. **Contract doc drift.** `contracts/network-format-v1.md:79` still documents internal lanes as "exactly one successor", and `:95` defines `foesMerge` as "shared successor". ADR-0038 addendum item 3 admits multi-successor internals now exist, but the contract document isn't updated in this commit. AGENTS.md rule 2: work that contradicts the KB/contract must fix it in the same change. The ADR's "network-format v1 is unchanged" is true of the schema but false of the documented invariant.

  2. **Merge-foe test only compares `Successors[0]`** (`engine/netimport/consolidate.go:210-211`). That was exhaustive when internals were single-successor; this very pass creates multi-successor near internals, so two same-junction internals sharing a far-internal successor at differing positions (e.g. `[f1,f2]` vs `[f2,f3]`) are not flagged merge foes, diverging from the contract's "shared successor" definition. Failure mode is bounded — in-box `mergeThreat` via `exit.Prevs` (`engine/rightofway.go:319`) serializes the funnel, so it's both-admitted-to-the-box, not overlap — but entry-time foe occupancy/approach checks silently weaken. Fix is a any-shared-successor intersection, two lines.

  3. **No fixture exercises the new topology.** All three Go fixtures rewire into single-successor internals; the multi-successor internal the ADR addendum flags as "new in practice" is only validated by the drain3 run. A fan-out sliver fixture (sliver with 2+ successors, or a predecessor internal with a second continuation) would pin both the loader acceptance and finding 2.

  ## Question

  4. **Shared-successor double extension** (`engine/netimport/consolidate.go:114-117`): if two deleted parallel slivers feed the *same* far internal (two connections converging on one via lane), that internal grows by both slivers' lengths and gets both polylines prepended — double-counting one physical gap and producing a zigzag shape that feeds `polylinesCross` (spurious foes, conservative direction). SUMO usually gives each connection its own via lane, so possibly unreachable on chi — worth a one-line report counter to confirm rather than a guard.

  ## Nits

  5. Stale comments now wrong for regenerated networks: `engine/gridlock.go:201` ("netimport networks, whose internal lanes are single-successor"), `engine/engine.go:624`, `engine/network.go:215`, and `engine/rightofway.go:144-149,245-249` ("netimport emits sub-vehicle-length exit stubs" — new imports emit none). Code stays correct for hash-keyed old networks.
  6. `engine/metrics.go:815`: boundary overshoot is carried only when `len(Successors)==1`; multi-successor internals now bump `droppedCrossings` where the old network carried the segment. Small metrics undercount on consolidated networks; pre-existing tolerated path.
  7. `rep.Connections` (`netimport.go:289,305`) still counts deleted slivers' connections while `rep.Lanes` is adjusted to the emitted count (`netimport.go:464`) — inconsistent audit units in the same report.
  8. `engine/netimport/consolidate_test.go:44`: fixture has `x="J2X,3.25"` (substitutes to a malformed `x="106.50,3.25"`); the parser ignores junction coordinates so tests pass, but it reads like a paste slip.

  Triage summary: ship after 1–2 (contract truthfulness + the two-line foe fix); 3 is cheap insurance for the exact invariant the ADR calls out; 4–8 record-and-defer.

  REVIEW-COMPLETE

