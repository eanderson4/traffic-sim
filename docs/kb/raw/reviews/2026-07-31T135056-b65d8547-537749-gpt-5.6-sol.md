- **blocker** `engine/sigctl.go:220` — New overrides are installed before lapse detection, which checks only the newest entry at `engine/sigctl.go:260`. A replacement applied exactly at the previous hold’s `until` suppresses that hold’s required `signal_lapse` event, violating ADR-0037’s “never silent” record contract.
- **blocker** `engine/signal.go:318` — `sigDisplayedOnset` merges spans by phase index rather than the lane’s displayed state. If an approach remains red across two different phases during a supersession/lapse, clearance closes early at the phase-index change; `engine/signal.go:392` may then recapture an amber-committed vehicle. Onset/merging must be link-state-aware.
- **should-fix** `engine/natsio/verb.go:115` — `signal_idx` and `phase` use `omitempty`, so valid zero values disappear from `signal_set` records. Consequently readers cannot distinguish phase 0 from a malformed record missing `phase`, despite the control contract explicitly presence-checking that field and AsyncAPI claiming logged signal verbs carry it. Encode required signal fields explicitly and validate their presence when decoding.
- Validation note: `go test ./engine/...` could not run because the review filesystem disallows Go’s `/tmp/go-build*` directory.

REVIEW-COMPLETE
