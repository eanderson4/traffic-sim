- **should-fix** `contracts/network-format-v1.md:227` — The documented manual import omits `-source-file` and `-imported`, so defaults at `engine/cmd/netimport/main.go:45` and `engine/cmd/netimport/main.go:52` inject wall-clock time and checkout-specific paths into canonical network JSON. Identical inputs therefore produce different ADR-0012 scenario hashes.
- **should-fix** `scripts/import-city.sh:60` — `$SRC` is interpolated directly into Python source. Valid paths containing `'` fail; crafted paths execute Python. Pass the path through `sys.argv` instead.
- **should-fix** `scripts/osm-stop-nodes.py:59` — Incident-arm counting recognizes only explicit `oneway` tags. OSM-implied one-way roads such as roundabouts and motorways are counted as two-way at `scripts/osm-stop-nodes.py:114`, potentially misclassifying fully stopped junctions as `priority_stop`.
- **nit** `engine/cmd/netimport/main.go:32` — Usage text omits the newly added `-source-file` option.

REVIEW-COMPLETE
