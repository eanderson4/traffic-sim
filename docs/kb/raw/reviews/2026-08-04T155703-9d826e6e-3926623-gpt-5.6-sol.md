- **should-fix** `docs/kb/decisions/ADR-0006-nats-message-contract.md:406` — Still states the stream is created with S2 compression, contradicting the new uncompressed default.
- **should-fix** `contracts/asyncapi.yaml:467` and `contracts/asyncapi.yaml:504` — Machine-readable `compression: s2` remains unconditional although actual default is `NoCompression`; comments do not fix the contract mismatch.
- **should-fix** `docs/kb/decisions/ADR-0035-batched-intent-log.md:86` and `docs/kb/decisions/ADR-0035-batched-intent-log.md:223` — Compression remains described as unconditional and applying to future blocks; both need qualification by `-store-compress`.
- **should-fix** `engine/natsio/tslb_migration_test.go:68` — No test pins the determinism-critical zero-value defaults. Assert `RecorderConfig{}` writes `log.intent` and creates an uncompressed stream.
- **question** `engine/natsio/recorder.go:53` and `engine/natsio/recorder.go:66` — Renaming exported fields breaks source compatibility for non-repo callers. Is that intentional, and should the migration note mention the Go API rename?

REVIEW-COMPLETE
