• The diff is ~4000 lines. I'll page through it fully.

• Diff fully read. Now checking repo context for the enforcement path and run-loop ordering.

• All callers of the verb decoder are updated. Now checking the run-loop ordering and event decode paths.

• The ordering invariants hold. Now I'll run the new tests to verify they actually pass.

• All tests pass. Final consistency sweep — checking for any consumer of the spawn-verb record that wasn't taught about signal verbs.

• One last determinism check — how the rolling CRC is seeded (chained vs fresh), since the override fold is conditional.

• Review of the staged ADR-0037 milestone-1 diff (signal_set verb, sigctl.go, TSKF v7). Verified against the repo, not just the patch. `go vet` clean; `go test ./engine` (full, 8.2s) and `go test ./engine/natsio -run 'SignalVerb|LoggedVerbKinds'` pass.

  ## Blockers

  None found.

  ## Should-fix

  None found.

  ## Nit

  - `engine/metrics_test.go:1642-1672` — `TestKernelReplayRederivation` hand-mirrors `Replay()`'s enqueue loop (its own comment says so) and was not extended with the `Signals` loop that `engine/replay.go:77-86` gained; the `RunLog` it builds at :1642 also omits the new `Signals` field. Harmless today (its fixture issues no signal verbs), but the first signal-verb fixture replayed through this mirror silently re-derives without the commands. Keep the mirror in sync or call `Replay`.

  ## Question

  - `engine/sigctl.go:256` (`sigNew` buffer) is not keyframed, so a marshal between `EnqueueSignal` and the next `Step` would silently drop a buffered verb on restore. Today every marshal point (`run.go:188` LogStart, :262 LogTick, :267 dumpState) runs with the buffer drained — same emergent invariant as the spawn path's `dirNew`. Fine as precedent, but unlike the spawn queue (TSKF v3) nothing in the codec makes the invariant visible; is a one-line comment at the marshal sites or an assert worth it, or accepted as the dirNew precedent?
  - `contracts/asyncapi.yaml` VerbRequestView: `required` relaxed from `[verb, request_id, origin, vtype, earliest_tick]` to `[verb, request_id]`. Server-side validation keeps the per-kind shape, and the relaxation is the additive direction — but any client validating spawn requests against the schema loses the static guarantee. Intended trade, per-kind prose accepted?

  ## What I checked (clean)

  - **Determinism (ADR-0005):** `sigOv` is lookup-only; CRC fold (`engine/engine.go:1095-1111`), marshal (`engine/keyframe.go:352-367`), and lapse/retention sweep (`engine/sigctl.go:318-345`) all walk `Net.Signals` in network order, histories chronological. The single map iteration (`sigctl.go:228`) is validation-only with an order-independent outcome. No wall clock/RNG in the new path; `signalHoldMaxTicks` is a pure `math.Round` over `Params.Dt`. CRC is chained (`u(e.crc)` first), and the conditional fold is keyed on keyframed state, so live/restore/replay agree.
  - **No-verb byte identity:** with an empty table, `sigPhaseAt`/`sigSpanOnset` reduce to `phaseAtElapsed` exactly and the merge loop is gated on `overrideBoundary` (never set without overrides); TSKF version/flags and the CRC fold are both gated on `len(e.sigOv) > 0`. Full pre-existing suite passes, corroborating the fixture CRCs.
  - **Contract (ADR-0006):** additive; asyncapi 2.7.0 + ADR-0006 addendum in the same commit; unknown `verb` discriminator is a hard decode error at every reader (replay, player, bake, logcursor — all callers switched to `decodeLoggedVerbAny`); `log.event` decode is tolerant `json.Unmarshal` with no type switch, so old readers ignore `signal_lapse`; viz/scripts don't consume either subject. Lapse exactly-once holds across live, replay, and mid-hold keyframe restore (event at `Tick == until`, re-derived from keyframed `[since, until)`).
  - **Chain/rail logic:** renewal-at-bound decline, same-boundary supersede drop, and the clearance-retention truncation in `sigctl.go:262-296` are consistent with the tests; `sigSpanOnset`'s newest-first scan is correct for gap queries and the offset-wrap guard matches `TestSignalSetLapseClearanceOffsetWrap`.

  REVIEW-COMPLETE

