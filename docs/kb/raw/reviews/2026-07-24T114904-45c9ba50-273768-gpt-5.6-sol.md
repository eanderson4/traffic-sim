- **blocker** `engine/natsio/contract.go:418` — Escape permanently disables the capacity gate until capacity fully recovers. In the motivating jam that recovery is explicitly unreachable, so excess newly spawned vehicles—having no prior intent to “hold”—coast uncontrolled for the rest of the run. Released vehicles also exhaust finite `HoldLastTicks`. This defeats the ADR-0008 failover invariant and can produce unsafe/invalid traffic; the test verifies only completion and replay CRC.
- **should-fix** `engine/natsio/replay.go:283` — `MaxAge` expires individual chunk messages. At the retention frontier, leading chunks can disappear before trailing chunks; `findKeyframe` treats that expected partial oldest group as fatal and refuses all seeks, even when later complete keyframes exist. Ignore a retention-truncated prefix or make chunk-group retention atomic.
- **question** Tests could not run because the sandbox cannot create Go’s `/tmp/go-build*` directory.

REVIEW-COMPLETE
