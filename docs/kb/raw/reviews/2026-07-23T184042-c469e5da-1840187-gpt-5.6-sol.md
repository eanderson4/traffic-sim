- **blocker** `viz/src/snapshots.ts:168` — Forward seeks of ≤240 ticks are treated as normal progression, so the buffer interpolates across unrelated states and derives bogus speeds/congestion. Forward seek size is arbitrary; stream heuristics cannot reliably identify it. Reset explicitly when `/seek` succeeds.
- **blocker** `engine/cmd/demosrv/replay.go:56` — Replay timing comes from the mutable scenario rather than authoritative recorded `RunMeta`; additionally, `viz/src/demos-core.ts:64` omits `dt` entirely for already-running replay links. Since `viz/src/main.ts:194` initializes `SnapshotBuffer` only from the URL, non-default or changed `dt` produces incorrect vehicle speeds and congestion. Read `dt` from replay `/status` and preserve it in every link.
- **should-fix** `engine/cmd/demosrv/registry.go:118` — Registry validation accepts source run IDs ending in reserved `-replay`, although ADR-0006 and `RunLive`/`NewRecorder` reject them. Such entries pass startup validation but fail only when activated, contradicting the registry’s fail-loud contract. Applies to recordings at line 139 too.
- **should-fix** `engine/cmd/demosrv/replay.go:99` — The proxy checks only that *some* replay is active, then releases the supervisor lock before forwarding. A concurrent start—or simply an old browser tab—can send pause/seek to a different replay on the reused fixed port. Bind controls to the expected recording/run or supervisor generation.
- **should-fix** `engine/cmd/serve/main.go:152` — Persisting an absolute network path fixes working-directory sensitivity by making recordings clone/machine-location-sensitive instead. Moving a durable store or repository makes replay fail; use a portable store/scenario-relative reference or embed the required network artifact.

REVIEW-COMPLETE
