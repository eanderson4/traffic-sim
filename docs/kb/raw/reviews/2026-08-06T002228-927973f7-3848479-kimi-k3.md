• Now reading the kernel right-of-way code for context.

• Now checking kernel helpers the new code depends on.

• Two more context checks: the `NetFile` struct for other lane-ID-bearing fields, and how `RowStop` duty interacts with the seam.

• Let me pin exact line numbers in the new files for citations.

• Review of the staged ADR-0038 diff. Verified: `go test ./...` in `engine/` and `python3 -m unittest test_mknetvariant` both green on the staged tree.

  **Blocker**

  1. **Stop-sign duty is dropped at consolidated seams** — `engine/rightofway.go:101-114`. The new seam gate runs `sigGate` + `foeApproachConflict` but never the `RowStop` branch that the road-approach path enforces at `rightofway.go:147-153` (hold until a full stop at the line, `stopDone`). Pre-consolidation, a vehicle approaching a stop-class far internal crossed the sliver on a *road* lane, where `gateTarget` enforced the stop; post-consolidation it crosses internal→internal and a stop-class seam degenerates to yield-only — with no conflicting foe, the movement rolls the stop line at speed. `stopDone` is also reset on every crossing into an internal (`engine/engine.go:932-934`), so the duty can't be recovered downstream. Reachable on any stop-controlled divided crossing with a sub-5 m connector — exactly the topology this commit consolidates at scale. Fix is cheap: mirror the `next.Row == RowStop && !v.stopDone` hold (with the `stopDone` set-at-line side effect) in the seam branch before the foe check, plus one seam test with `Row: "stop"` on `iB_0`.

  **Should-fix**

  2. **ADR-0038 text is stale re: kernel scope** — `docs/kb/decisions/ADR-0038-sliver-junction-consolidation.md` says "Scope: `engine/netimport` only… The engine kernel is unchanged" and validation item 1 says "engine untouched", but `engine/rightofway.go` gains a real behavior change (the seam gate, per the Sol finding), and the addendum's four clarifications don't mention it. Worse, the gate is not scoped to consolidated seams: it fires at *any* internal→controlled-internal boundary, including pre-existing unconsolidated networks (SUMO multi-part internals where only the final via-internal carries the row class/signal). Those crossings were ungated in-box before; now e.g. a fresh red can capture a vehicle mid-box. Old networks get changed physics with no importer-hash change to mark it. Suite is green so the CRC fixtures evidently lack such seams, but the ADR should record the kernel change and state whether existing fixtures/nets were explicitly re-verified against it — per AGENTS.md rule 4, the KB must not contradict what shipped.

  **Nit**

  3. `engine/netimport/consolidate.go:138-145` — the extension loop iterates `succs` (the sliver's raw successor copy) without dedupe; the predecessor-side rewire at `:116` dedupes but this side doesn't. A sliver listing the same internal twice would double-add length and double-prepend shape. Corrupt-input class, never observed — defer per the triage bar; a one-line comment would do.

  4. `analysis/chicago-throughput/*.md` cite `/tmp/chidiag/*.py` scratch scripts as method — ephemeral paths; if the diagnoses are meant to be reproducible, the scripts belong in the repo. Defer-able.

  **Question**

  5. `rightofway.go:96-100` claims the box-occupancy/exit-room half "deliberately does NOT run here", but for *signalized* seams — the common case (per the ADR, 52% of slivers connect two traffic lights) — `sigGate`'s green path calls `boxBlocked` with entry semantics (`engine/signal.go:450`), holding the vehicle inside the near box until the far box's whole exit chain fits it. So discharge across signalized seams does serialize on far-box room, contrary to the comment's rationale. Is that the intended asymmetry (faithful to road-approach semantics) or an oversight? If intended, the comment overclaims and should be narrowed to the row path.

  Determinism scan clean: `consolidateSlivers` iterates slices/file order only (maps used for membership lookups); `computeConflictFoes`'s `byJunction` map iteration (`consolidate.go:229`) is output-insensitive — junctions are disjoint and per-lane foe appends happen in fixed nested order. No wall clock, no RNG in new paths. Contract side is in order: `contracts/network-format-v1.md` documents multi-successor internals and the successor-set merge test with ADR-0038 references, and `import-city.sh` adds `consolidate.go` to the importer-identity hash.

  REVIEW-COMPLETE

