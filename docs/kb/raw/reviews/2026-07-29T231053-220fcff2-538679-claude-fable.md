Read the full diff and the surrounding `mkod.py` context (`ProfileSet.resolve`, `emit_flow`, the portal/interior/resident loops). The change does what the ADR correction says: tallies are now taken on the emitted rate (`rate × pscale × f`), the peak is a max over elementary time intervals, and the mass balance is on vehicle counts. `resolve` confirms the `(None, 0.0)` comment at mkod.py:1054. Segment lists are built in sorted-loop insertion order and reduced in that order — no map-iteration or float-order determinism risk; no NATS contract touched; the ADR-0028 correction ships in the same commit as required.

## Findings

**Should-fix**

1. `docs/kb/INDEX.md:18-20` — the changelog reorder looks accidental: the ADR-0034 line is deleted before ADR-0035 and re-added after it, leaving the dated list as 0033 (07-28), 0035 (07-29), 0034 (07-28). Nothing in the commit's subject matter explains breaking chronological order.

**Nit**

2. `scripts/chicago/mkod.py:1062,1078-1084` — the stranded warning quotes `emitted = rate * pscale`, still without the shape fraction — by this diff's own standard (error #2 in the ADR note) that's an authored rate, not one in the file; peak arrival from a stranded freight-shaped portal is 0.90× the printed figure. Also, a portal with no workplace *and* no profile is appended before the `pf is None` continue, so it shows in the WARNING as a "0 veh/h" stranded flow that is never emitted at all.
3. `scripts/chicago/mkod.py:1207` vs the header at ~1264 — the stderr realized-demand report is gated on `if peak:` but the `# REALIZED:` header line is written unconditionally; a zero-demand file gets a header claiming "peak 0 veh/h at t=0s" with no stderr counterpart. Harmless, just asymmetric.
4. `scripts/chicago/mkod.py:1015` (context, preexisting) — a rule with `scale: 0` (validation allows `>= 0`) still calls `emit_flow`, whose slices all drop, producing a `slices:` key with an empty body (YAML null). The new tally handles it correctly (contributes nothing); the file the loader gets may not. Out of this diff's scope — noting because the diff's "absent flow must not appear" reasoning almost, but not quite, covers it.
5. Header "Knobs:" line (~mkod.py:1268) prints in the no-`--profiles` path too, but omits the shape knobs that matter there (`--flat-peak`, `--drain-s`, `--slice-s`), so the reproducibility guarantee only fully holds when a profile library is in use.

**Question**

6. `scripts/chicago/profiles-am-kennedy.json` rule 2 (`kind: portal, corridor: kennedy`) fires only if the Kennedy's boundary portal lanes carry a `lane_corridor` label. If they don't, `check_all_rules_fired` hard-exits (the right failure), so this is self-checking — but confirm the shipped library was actually run against chi-loop-urban rather than only unit-tested. The 5,258/4,733 = 1.11 figures in the ADR note suggest it was.

No blockers. The `peak_rate` half-open midpoint sampling, the double-count avoidance in the through denominator (relocated volume subtracted from "freeway" before tally, so adding "interior" is now correct — the old comment's warning no longer applies), and the tests all check out.

REVIEW-COMPLETE
