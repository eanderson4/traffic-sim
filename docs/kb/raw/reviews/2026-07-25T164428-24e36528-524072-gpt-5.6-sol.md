- **blocker** — `engine/cmd/demosrv/supervisor.go:283`: unconditional random suffixing changes every live NATS namespace from configured `ts.{run}.*` to `ts.{run}-{nonce}.*`, while `/api/demos` still advertises the base run. ADR-0020 neither specifies a migration nor audits external controllers, and falsely claims local defaults are unchanged at `docs/kb/decisions/ADR-0020-demosrv-public-deployment.md:80`. This sacred contract change needs an ADR-0006 addendum/migration before shipping.
- **should-fix** — `engine/cmd/demosrv/supervisor.go:356`: `stopEpoch` is bumped before `stopPending`, leaving a race where a start acquires `mu` between those operations, sees no pending stop, snapshots the already-bumped epoch, and becomes unabortable. The stop can then wait through the full 660-second readiness phase. Mark the stop pending before publishing the new epoch or make the protocol atomic.
- **test note** — Targeted tests could not run because the review sandbox forbids Go from creating `/tmp/go-build*`.

REVIEW-COMPLETE
