**Review of staged diff** (`RouteBudgetPerTick` + hello-retry hardening + new budget test):

## Blockers

None found.

## Should-fix

1. **driver_test.go — the determinism mechanism the diff introduces (frozen first-obs lane) is never exercised.** All five egos keep `LaneIdx: originIdx` on every tick, so the test passes identically whether `wantLane` freezing works or `routeStep` re-reads the current lane. Mutate a deferred ego's `LaneIdx` between tick 1 and tick 2 and assert the draw matches the tick-1-lane pick (and/or assert `len(d.wantLane)` after tick 1). As written, the `wantLane` map could be deleted entirely and the test still passes. Fine to defer per the one-round triage bar, but record it — this is the load-bearing new invariant (driver.go:412-419).

## Nits

2. **driver_test.go:16-18** — header claims over-budget egos stay "COMPLETELY untouched (no wantRoute entry, no routed mark)", but they do get a `wantLane` entry by design. Misleading next to the driver.go comment that says the first-obs lane *is* remembered.
3. **driver.go:414-416** — the `d.wantLane == nil` lazy init exists only because the test's `Driver` literal omits the field (driver_test.go:112-124). Initializing it in the test literal (like `wantRoute`) would keep production code free of test scaffolding.
4. **driver_test.go:307-322** — `waitRouteSet` only exits early on exact equality; an overshoot (extra sends) burns the full 5 s before failing. `got >= want` with a mismatch fatal would fail fast.
5. **driver_test.go:263** — `startTestBroker` returns `ns` that no caller uses.

## Questions

6. **ADR/KB note for the timing-invariance caveat?** No subject or payload changes — the contract is untouched, so no ADR is strictly required. But the Config comment (driver.go:63-70) itself flags a bounded weakening of ADR-0008's failover-invisibility story (deferred vehicles' routes land up to `ceil(unrouted/budget)` obs later; a longer window in which a replacement replica draws from a different first-obs lane). If that caveat only lives in a struct comment it will be hard to find when the O(1) `pickExit` future-work lands; consider a line in the ADR-0008 notes or KB with the commit.
7. **Pre-existing, not introduced here:** an ego that appears in obs without ever landing in `d.fleet` (lost `ClaimReply` after an engine-side grant) accrues `routed`/`wantRoute`/`wantLane` entries that the departure sweep (driver.go:351-357, keyed on `d.fleet`) never reclaims. The diff adds one more map to that leak shape; worth a KB note someday, not this round.

## Verified clean

- **Determinism sweep:** no map iteration in decision paths (`d.fleet` sweep only deletes — order-independent; `exitCache` maps are lookup-only); `pickExit`'s float accumulation walks an ID-sorted slice; wall clock only in the startup hello path; the draw stays on the domain-labeled per-ID stream. Budget admission order follows `obs.Egos` frame order, which is engine-authored, not a Go map.
- **Hello retry (driver.go:182-199):** deadline now genuinely bounds the wait — request timeout capped at remaining, sleep capped at remaining, negative-remaining exits with the wrapped last error; the nil-`helloErr` `%w` case is unreachable since `MetaWait > 0` guarantees one request attempt. `min` builtin fine on go 1.25.
- **Budget semantics:** failover adoption (`ego.Route` echo) and explicit `Destination` bypass the budget; `pickExit` ok=false consumes budget but correctly resolves to routed-with-no-intent; over-budget egos leave no `wantRoute`/`routed` state; departure sweep now clears `wantLane`.
- **Test arithmetic** (2 / 6 / 11 cumulative RouteSet) checks out against re-send-until-echo behavior; broker setup matches the repo's established `DontListen` in-process pattern; all map access is single-goroutine or under the counter mutex.
- **Contract:** no NATS subject, schema, or codec changes; `Intent.Route` stays within the existing 48-byte cap.

REVIEW-COMPLETE
