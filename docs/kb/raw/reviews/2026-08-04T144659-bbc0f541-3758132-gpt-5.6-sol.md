- **should-fix** — `engine/sigctl.go:193`: duplicate detection checks only `sigNew`, not the keyframed live override history. After a warm restore, `Contract.verbs` is empty; retrying a previously accepted `request_id` re-applies the command and changes `since`/`until`, violating the advertised run-lifetime idempotency. The restored overrides already retain `requestID`, so reject matches against active/retained history.
- **nit** — `engine/natsio/contract.go:224`: `Since` is documented as the tick “the hold was applied,” but renewed chains emit `chainStart` (`engine/sigctl.go:305`), potentially belonging to a different request. Align the Go contract documentation with AsyncAPI’s chain-start semantics.
- **nit** — `contracts/asyncapi.yaml:1614`: `until = since + effective hold_ticks` is false for renewals because `since` is the chain start while the last logged verb’s `hold_ticks` is its requested effective duration. Describe `until` as the chain’s clamped end instead.

REVIEW-COMPLETE
