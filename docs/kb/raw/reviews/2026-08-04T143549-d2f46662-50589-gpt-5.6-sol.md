- **should-fix** `viz/src/splash.ts:72` — Baked mode still renders live-server state. An active LA run shows a “running” badge beside “watch the replay,” and a registry without LA displays a notice telling users to browse demos even though the replay CTA works. Skip live registry/status UI when `BAKED_FEATURED_URL` is configured.
- **nit** `viz/splash.html:124` — Card documentation and initial CTA still describe launching a live run, contradicting the new baked-first behavior.
- **question** `viz/src/splash.ts:16` — The URL remains empty, so this staged change has no runtime effect. Is this intentionally scaffolding for a later source edit/rebuild, rather than enabling the published replay now?

REVIEW-COMPLETE
