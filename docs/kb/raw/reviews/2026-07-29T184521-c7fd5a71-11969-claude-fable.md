Review complete. Scope reviewed: all non-archive code in the staged diff (runreport.py, mkod.py, mkzones.py, mkcongestionmap.py, mkflowcurve.py, corridorspeed.py, whatif.py, mkquiz.py, external-review.sh, build-quiz.sh, the four viz TS files, tests); the `docs/kb/raw/reviews/*` bulk is archive material and was skimmed only for placement. Verified against the engine that `metricsjson.go` writes `ticks, dt, intervals, trips, totals` in that order (the streaming reader's assumption holds) and that `totals` carries `stranded_trips` (mkflowcurve's subscript is safe).

## Findings

**Blockers** — none found. No NATS subject or payload touched; report JSON is a new versioned artifact with an ADR (0030). No wall clock, RNG, or map-iteration nondeterminism in the sampled paths; `spread()` ties, sorts, and dict orders are all pinned to input order.

**Should-fix**

1. `scripts/chicago/mkod.py:981-991` — a portal flow refused for lack of a profile still leaks demand into the run's accounting and the network. Order in the portal loop: `relocate[corr] += moved` (981) and `realized["through"] += rate * eff` (983) execute before the `pf is None → continue` check (989-991). With a `--profiles` library that has no `default` and no portal-matching rule, the cut-face flow is dropped, but its `ramp_share` portion is still emitted at the interior points (the interior loop reads `relocate`), and the printed through-traffic/mass-balance line counts a flow that never made it into the file. Resolve the profile before the relocation/realized accounting, or skip relocation when `pf` is None. Today's `profiles-am.json` has a default so the path is dormant, but the file's own philosophy is "refuse rather than let a silent path exist."

2. `docs/show/img/chi-congestion-fw35.png.json:134004` (patch) — the shipped sidecar's `run_label` regressed from `"chishow35 (chi-show-fw35, seed 42, 6000 ticks, -drivers 8)"` to `""`. The sidecar's stated purpose (and mkcongestionmap's `--run-label` help text) is that the metrics document carries no run identity, so the label is the only human handle besides the digest. The regeneration that added the new three-state fields was evidently run without `--run-label`. Re-render with the label, or the tracked asset now identifies its run by SHA alone.

3. `scripts/chicago/mkod.py:917-921` — the ramp-pick `mid()` returns `s[len(s) // 2]`, the middle *shape vertex*, while the comment above it says "the first shape point". For a two-point lane that is the far end — the exact endpoint-vs-arclength-midpoint defect this same commit fixes in `mkzones.midpoint()` and `runreport.lane_centre()`, and the ADR-0030 postmortem calls out by name. Here it only skews k-center spreading of injection sites on curved corridors, so it is not the same severity, but using the arclength midpoint (or at least correcting the comment) keeps one definition of "midpoint" in the repo.

**Nits**

4. `viz/src/statspanel.ts:614-621` (`groupColumns`) — two columns share `key: "k"` ("veh/km/ln" and "%crit"), so the sorted-column indicator highlights both at once, and clicking the second flips direction instead of starting a fresh descending sort. Semantically the sort is right (%crit *is* k rescaled); only the affordance is off.
5. `scripts/runreport.py` (`main`) — `head["dt"]` / `head["ticks"]` raise a bare KeyError on a streamed file whose scalars fall outside the 1 MB head scan. Safe for engine-written files (field order verified), but every other malformed-input path in this file exits with an explanation.
6. `scripts/chicago/mkod.py` horizon-refusal message — "raise the tick count to `prof_end / 0.1`" hardcodes dt, and the "--slice-s" advice is dead when `--profiles` is in use (the flag combination is refused). Message-only.
7. `scripts/chicago/mkzones.py:159` — `p["name"]` on a feature missing `name` is a bare KeyError; everything else in the file refuses by name.
8. `scripts/chicago/corridorspeed.py:122` — "MEASURED SET (1 lanes)" (the test pins the plural).
9. `scripts/chicago/districts.geojson` — no trailing newline.

**Questions**

10. `docs/kb/decisions/ADR-0030` / report JSON — `schema_version` stays 1 while `coverage`, `totals_scope`, `critical_k`, and `band_order` were added and the four shipped sample reports regenerated. The panel treats them all as optional, so additive-under-v1 is coherent — confirm that is the intended versioning rule for this contract (additive = same version, breaking = bump), since ADR-0030 calls the JSON a UI contract but doesn't state the rule.

Notably solid: the coverage/subset reconciliation in runreport and its narrowed group denominators are correct and thoroughly tested; the 0-1 lateral reachability matches the kernel relation it claims to mirror; the external-review.sh input-isolation reasoning (reviewers never handed each other's output files) closes a real integrity hole.

REVIEW-COMPLETE
