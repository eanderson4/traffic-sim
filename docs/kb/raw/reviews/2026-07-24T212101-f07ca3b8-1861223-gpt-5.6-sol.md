- **should-fix** `engine/natsio/bus.go:213` — Oversized-program validation compares only `len(c)` with `MaxPayload`, but nats.go enforces the limit against payload **plus serialized headers**. A chunk just below 4 MiB passes startup validation yet every publish fails permanently once `tick`, `schema_version`, and `sig_chunk` headers are added. Validate the complete message size or reserve header headroom.
- **should-fix** `contracts/asyncapi.yaml:784` — The machine-readable `sig_chunk` pattern accepts contract-invalid coordinates such as `0/1` and `1/0`, while `parseSigChunkHeader` rejects them. Tighten the schema to positive integers; ideally document/enforce `i <= n` where tooling permits.

REVIEW-COMPLETE
