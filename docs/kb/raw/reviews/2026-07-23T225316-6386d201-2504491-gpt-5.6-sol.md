- **blocker** — `viz/src/signals.ts:40` computes stop bars, but staged `viz/src/main.ts:230` still emits only point features and defines no bar layer. The advertised stop bars never render; the necessary renderer changes are unstaged.
- **should-fix** — `viz/test/signals.test.ts:215` places lane entries along the approach bearing, so their perpendicular projections coincide. The test verifies only the minimum-width extension, not that a bar spans laterally separated lanes.

REVIEW-COMPLETE
