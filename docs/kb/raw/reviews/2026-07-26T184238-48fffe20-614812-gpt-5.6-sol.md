- **should-fix** `engine/natsio/driver_test.go:1197` — `LastObsTick` is stored at the start of `onObs`, before computation/publication finishes. The quiescence guard can therefore accept after 50 ms while a slow 5k response is still being computed, letting warm-up intents contaminate measurement or causing attribution failure. Quiescence needs a completion/publish watermark, not `LastObsTick`.
- **should-fix** `engine/natsio/server.go:113` — The comment promises cross-channel “broker order,” but NATS ordering guarantees are per publisher/connection, not causal across the engine and driver publishers. This contradicts the limitation correctly acknowledged at `engine/natsio/driver_test.go:859` and risks future misuse of this exported helper.
- **nit** `engine/BENCHMARKS.md:215` — “batch never worse than v2” overstates the evidence and conflicts with `engine/BENCHMARKS.md:270`, which correctly says neither per-vehicle distribution dominates; only measured p50/p99 are claimed.
- **nit** `engine/BENCHMARKS.md:217` — Stray `:**` breaks the paragraph’s Markdown formatting.

REVIEW-COMPLETE
