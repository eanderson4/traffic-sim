- **blocker** `engine/natsio/driver_test.go:1180` — `sentMark` is captured while earlier warm-up observations may still be processing. Their post-mark intents can satisfy the `vehicles` delta at `engine/natsio/driver_test.go:1228`; once the sentinel merely starts (`LastObsTick`), the watermark can pass before its response publishes. A slow sentinel can then survive the 50 ms “quiet” window and contaminate measurement. Quiesce before publishing/marking the sentinel, or track completion by observation tick.

REVIEW-COMPLETE
