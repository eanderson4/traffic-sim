- **blocker** — `viz/src/signals.ts:39` adds and documents stop-bar geometry, but the staged renderer at `viz/src/main.ts:230` still emits only `Point` features and never consumes `SignalHead.bar`. Thus no stop bars render; the relevant `main.ts` implementation is unstaged and will not ship with this patch. The new tests validate geometry generation only, so they miss the broken integration.

REVIEW-COMPLETE
