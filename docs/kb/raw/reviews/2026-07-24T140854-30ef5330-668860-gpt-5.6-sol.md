- **blocker** `engine/cmd/metview/index.html:50` — Implements a substantial browser client in plain JavaScript, contradicting ADR-0001’s explicit “web/browser clients: TypeScript” invariant. Move source to TypeScript or add an ADR superseding that decision.
- **should-fix** `engine/cmd/metview/main.go:29` — Valid schema-v1 optional metric fields are decoded as scalar zeroes. Sets disabling a metric group therefore display and rank fabricated zeros instead of “not computed”; preserve pointer presence or reject unsupported files.
- **should-fix** `engine/cmd/metview/index.html:93` — `data.sets` is `null` when a valid file contains no intervals (for example, all measurement windows begin after the horizon), causing `.length` to throw and breaking the lane view.
- **should-fix** `engine/cmd/metview/main.go:95` — `strings.HasPrefix(host, "127.")` accepts DNS names such as `127.example.com`; resolution can defeat the promised loopback-only binding. Parse and validate an IP with `IsLoopback`, or resolve every address and require loopback.
- No NATS subject or payload change is present; the kernel map is lookup-only and does not introduce map-iteration nondeterminism.

REVIEW-COMPLETE
