- **blocker** `viz/src/main.ts:355` / `viz/src/main.ts:379` — `MsgHdrs.get("sig_chunk")` returns `""` when absent in installed `nats.ws`, but `parseSigChunkHeader("")` throws (`viz/src/tssg.ts:151`). Every unchunked TSSG frame is rejected, so signals never render on current small networks. Check `headers.has(...)` or normalize `""` to absent.
- **should-fix** `viz/src/main.ts:332` / `viz/src/main.ts:346` — three failed pull attempts permanently exhaust `sigReqRetries`; later broadcast gaps cannot request recovery unless a complete generation somehow arrives. Re-arm after a cooldown or new broadcast round.
- **should-fix** `viz/src/demos-core.ts:69` — running-replay deep links omit `dt`; the replay status later corrects simulation math, but the new `Legend` retains its constructor `dt` (`viz/src/legend.ts:38`). Non-0.1 recordings show the wrong clock when opened from the running card.

REVIEW-COMPLETE
