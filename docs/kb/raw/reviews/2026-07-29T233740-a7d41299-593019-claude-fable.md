All 69 tests pass. Findings below.

## Findings

**Blockers** — none. The four reporting defects the ADR correction names (missing `pscale`, missing shape fraction, sum-of-peaks, share-on-rates) are each genuinely fixed in the code, the tally is taken on exactly what `emit_flow` writes (verified: `offset` is `offset_m`, spatial, so unshifted slices are the correct timeline basis), and the arithmetic in the ADR/JSON prose checks out (5,258×0.90≈4,733; kennedy-am 7.875 veh-units vs freight 5.0 = 1.575 ≈ "1.57x").

**Should-fix**

1. `scripts/chicago/mkod.py:1063` + `mkod.py:1131` — if the *portal* rule resolves but the *interior* rule resolves to `(None, 0.0)` (library with rules but no `default`), the relocation still fires: `rate -= moved` ships the portal at (1−ramp_share) while the interior loop `continue`s, so the moved volume silently leaves the file. The new guard covers the portal-pf-None case only. It is *semi*-loud (the ramp-share print would show "peak 0 veh/h, 0 vehicles" EMITTED plus an `interior no profile` counter), but this is exactly the "corridor at a fraction of its own demand that reads as a plausible number" failure the diff's own comments condemn. Pre-existing shape, but the diff added the adjacent guard and rationale — one `sys.exit`/WARNING when `relocate` is nonempty and `ramp_stats["interior no profile"] > 0` would close it. Defensible to defer under the triage bar since both signals do print; flagged because the diff's own stated invariant ("an absent flow appears in NO realized total, including via the detour through relocate") is not actually upheld on this path.

**Nits**

2. `mkod.py:1071` vs `mkod.py:1076` — `portal no reachable exit` is counted *before* the pf-None `continue`, `portal no reachable workplace` *after*. A profile-less flow lands in one diagnostic but not the other; the stranded-list rationale only argues for moving the latter's `stranded.append`, not the counter asymmetry.

3. `mkod.py:1023` — `cats()` on a `defaultdict` means a typo'd category (`"residents"`) silently yields an empty list and a 0 total — the exact silent-nothing failure mode this script keeps exiting on elsewhere. A plain dict lookup or an assert against a fixed category set would be self-checking.

4. `mkod.py:1300-1308` — the "Knobs" header line reproduces the demand-level knobs but not `--truck`, `--dest-lanes`, `--origin-lanes` (counts appear as outcomes, not inputs), so "reproduce the invocation from the header" is still not fully closed. Fine to defer.

**Questions**

5. `profiles-am-kennedy.json` — control-arm claim verified: everything not matching a Kennedy rule resolves identically to `profiles-am.json` (residents→reverse-commute, other interiors→freight, all portals→default commute-am in both), so the A/B delta is Kennedy-only. One assumption worth confirming before the run: at least one *portal* lane maps to corridor `kennedy` in corridors.json — if not, `check_all_rules_fired` will fatal on `assign[1]`, which is the intended loud outcome, so no action needed either way.

Determinism: no wall clock, no RNG, no map-order dependence in the new code (`bounds` sorted, categories accessed by literal, rules kept in authored order deliberately). Contract: generator-side only, no NATS subject or payload touched; the demand-file header is comments-only so the scenario loader contract is unchanged; ADR-0028 correction and INDEX entry ship in the same diff, satisfying the ADR-currency rule.

REVIEW-COMPLETE
