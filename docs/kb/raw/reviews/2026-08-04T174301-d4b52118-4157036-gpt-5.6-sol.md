- **blocker** `engine/natsio/sigctl/sigctl.go:565` — Any phase containing one green is treated as commandable, including mixed amber/green clearance phases (e.g. `data/scenarios/chi-loop-urban-half-base/chi-loop-urban.json:2169310`). The controller can hold amber for 100 ticks; switches at `engine/natsio/sigctl/sigctl.go:358` also jump directly between movement phases, bypassing required yellow/all-red transitions.
- **blocker** `engine/natsio/sigctl/sigctl.go:670` — Self-tracking assumes every verb applies at snapshot tick + 1. The contract is asynchronous, and the bracket explicitly runs unpaced at `scripts/sigctl-bracket.py:81`; the engine may advance arbitrarily before processing the verb. Predicted phase, hold, and chain bounds therefore diverge from reality, making results scheduler/load-dependent. Random map iteration at `engine/natsio/sigctl/sigctl.go:652` further destabilizes publication timing.
- **blocker** `engine/natsio/sigctl/sigctl.go:684` — Every same-phase renewal resets `phaseSince`. With default `HoldTicks == MinGreenTicks == 100`, a gap arising after renewal cannot satisfy minimum green before the hold expires, so the command lapses to fixed time instead of performing the intended gap-out switch.
- **blocker** `engine/natsio/sigctl/sigctl.go:516` — The TSSG accumulator does not enforce the ADR-0016 `1..n` sequence, preserve the old table until a complete generation, or request resynchronization. Receiving a valid chunked stream starting mid-generation silently installs a partial table at chunk `n`; attach also never requests the table, so it can miss the tick-0 publication despite reporting ready.

REVIEW-COMPLETE
