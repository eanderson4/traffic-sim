- **should-fix** `engine/cmd/serve/main.go:56` — `-pace` changes controller/director latency in ticks and therefore traffic and metrics, but is absent from recording/KV metadata. Runs with identical ADR-0012 `(scenario hash, seed)` identity can silently differ. Record this condition—with the required ADR/contract note—or prohibit cross-pace identity equivalence.
- **question** `viz/src/config.ts:18` — `dt` is authoritative in run metadata but manually copied through `?dt=`. Omitting it for a non-0.1-second scenario silently corrupts displayed speeds and congestion classification. Why not read it from registry metadata?
- **question** `engine/cmd/serve/main.go:348` — SIGINT now preserves a durable store while leaving its run metadata permanently `running`. Should durable interrupted recordings be finalized as `aborted` before broker shutdown?
- **nit** `viz/src/snapshots.ts:124` — skipped ticks measure a straight-line chord, increasingly underestimating speed on curves. The pace-invariance test covers only straight motion; document this as an estimate.

REVIEW-COMPLETE
