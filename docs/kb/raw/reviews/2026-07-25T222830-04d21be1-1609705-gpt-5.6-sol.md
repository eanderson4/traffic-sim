- **blocker** `contracts/baked-replay-v1.md:117` — Declaring output-manifest and PMTiles archive digests “MVP-deferred” contradicts ADR-0023 §8’s content-addressing requirement. The same immutable URL can name different Brotli/furniture/tile bytes, and `bake-furniture.mjs` mutates the keyed prefix afterward. Implement the digests or amend ADR-0023 explicitly; a contract-only exception is insufficient.
- **should-fix** `engine/cmd/bake/README.md:27` — Documentation calls tippecanoe pinned while permitting `>=2.49`; implementation still names 2.78.0 as pinned and enforces no minimum (`engine/cmd/bake/wgs84.go:28`, `engine/cmd/bake/wgs84.go:184`). Align the ADR, documentation, and validation policy.
- **should-fix** `engine/cmd/bake/README.md:19` — Still promises “2 Hz decimation,” contradicting the newly documented/tested derived 1–2 Hz cadence.
- **should-fix** `engine/cmd/bake/bake_e2e_test.go:226` — The claimed aggregate-grid verification decodes only the first lane chunk from the first matching region, so it cannot catch a terminal or later-chunk off-grid frame. The browser addition also uses `tickEnd=20`, exactly on its aggregate grid (`viz/test/baked.test.ts:398`), leaving the newly specified off-stride-terminal behavior untested.

REVIEW-COMPLETE
