- **blocker** `engine/natsio/sigctl/sigctl.go:293` — Transition walks do not preserve natural durations. With cadence 20, a 30-tick yellow advances after 20 ticks because the remaining 11 ticks are below `RenewBelow`; transitions shorter than cadence lapse into fixed time before the stale walk continues. This can shorten clearance or jump from an untracked phase, invalidating the ADR claim and bracket results.
- **blocker** `engine/natsio/sigctl/sigctl.go:537` — `Attach` requests the signal table but returns before a complete generation is installed. `Ready` only flushes outbound traffic, so `serve` can open the start gate with zero programs. In the bracket’s `-pace 0` mode, the run may outrun table/snapshot callbacks, making actuation scheduler-dependent or a silent no-op.
- **blocker** `engine/natsio/sigctl/sigctl.go:806` — Request IDs restart at sequence 1 for every controller process, but verb idempotency is run-global. Restarting/replacing `sigctl` silently replays old accepted replies without applying commands until each program’s sequence exceeds the previous process’s count; optimistic state tracking then diverges from the engine.
- **blocker** `engine/natsio/sigctl/sigctl.go:761` — Programs are sampled from a Go map and verbs are published in randomized order. `ProcessControl` may drain only a prefix at a tick boundary, so map order determines which signals apply this tick versus the next, affecting simulation state and recorded ordering.
- **should-fix** `scripts/sigctl-bracket.py:87` — A successful process plus metrics file is accepted as an actuated run without verifying any signal verbs were applied. Geometry/table/timing failures can therefore be reported as valid actuated measurements.

REVIEW-COMPLETE
