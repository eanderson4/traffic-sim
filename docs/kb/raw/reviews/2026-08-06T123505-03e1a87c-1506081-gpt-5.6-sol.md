- **blocker** — `scripts/show/build-quiz.sh:146`, `viz/public/quiz.html:124`: the generator now includes `chi-loop-cbd` and `chi-kennedy`, but the tracked generated page is unstaged and still contains only the three old scenarios. The shipped quiz therefore exposes neither new scenario nor their diagrams.
- **should-fix** — `docs/show/baselines.json:35`, `docs/show/quiz/chi-loop-cbd.json:6`, `docs/show/cbd-options.md:3`: the linked CBD baseline ends at tick 7,000, while results are scored through tick 12,000 after a 4,000-tick warmup. The note admits the omitted period reaches 7% rejected demand, so “watch the baseline” hides much of the actual measurement window.
- **should-fix** — `scripts/show/mkopenslide.py:229`: the legend describes hundreds of signals using only `sigs[0]`’s cycle and first green phase. No uniformity check establishes that all 1,057/486 imported programs share those values or that their first green is consistently “main-road green”; mixed programs produce a false aggregate claim.
- **should-fix** — `scripts/show/mkopenslide.py:157`, `docs/show/diag/chi-kennedy__setup.svg:1`: the Kennedy legend reports 486 signals, but only 304 signal-head circles are emitted; `junction_xy()` failures are silently skipped. The setup graphic omits 182 signals while presenting itself as the baseline signal map.

REVIEW-COMPLETE
