No blockers. The cadence change is a republish-frequency tweak on an existing subject (no subject/payload change), and the ADR-0006 note ships in the same diff — contract discipline satisfied. Determinism is untouched: no sim-state changes, cadence is tick-derived, `signalHeads` iterates arrays and insertion-ordered Maps.

**Should-fix**

1. `viz/src/signals.ts:84` (grouping key in `signalHeads`) — the merge key is the state column alone, with no approach identity. Opposing through movements at a symmetric fixed-time junction (NS-north and NS-south both running `G/y/r…`) have identical columns, so they merge into ONE head whose centroid is the midpoint of two *opposite* stop lines ≈ the junction center, and whose mean bearings cancel (`bl ≤ 1e-6` → `back = 0`). That head floats unoffset in the middle of the intersection — precisely the artifact the header comment says this change removes. Real SUMO programs often break the tie via `G` vs `g`, but plain symmetric programs won't. Consider keying on `(column, approach edge)` or splitting groups whose entry bearings diverge beyond ~90°.

**Nit**

2. `viz/src/signals.ts:41` — comment says absent chars "join as NUL"; code uses `" "` (which also equals the join separator). Unambiguous in practice (fixed phase count per program), but the comment is wrong.
3. `docs/kb/decisions/ADR-0006-nats-message-contract.md:175` — "shortened 2026-07-24" is tomorrow's date (today is 2026-07-23).
4. `engine/natsio/player_test.go:400` / `:433` — `TestPlayerZeroCadenceRecordFails` and `TestPlayerSingleKeyframeFallback` names are now stale: "cadence 0" is no longer the failure concept, and there is no fallback anymore. Rename when convenient.
5. `engine/natsio/sigframe_test.go:270` — "(missed tick 0/50)" comment is stale; with the 20-tick cadence the late joiner missed 0/20/40/60.

**Question**

6. `engine/natsio/run.go:40` — `SignalCatchUpEvery` is exported but only referenced inside `natsio`. Intentional public API (e.g. for demosrv/clients to size timeouts), or should it be lowercased to keep the surface minimal?

Checked and clean: `player.go` no longer needs the derived cadence (seek uses `findKeyframe` against the stream, not the cadence); the retained duplicate-keyframe guard catches the dirty-store case exactly as before; the late-join test's `≥ 60` assertion is race-safe (core NATS, subscribe-after-tick-60 means any received table is a fresh publish); decoupling `PublishSignals` from `KeyframeEvery` leaves the registry `UpdateState` rhythm unchanged.

REVIEW-COMPLETE
