- **blocker** `scripts/show/build-quiz.sh:64` — With `CHI_METRICS` unset, the normal build deliberately reuses any existing Chicago PNG and regenerates the page. Quiz results can change while the map remains stale or comes from another demand—the exact silent divergence this change claims to prevent. Fail closed or omit the artwork.
- **should-fix** `scripts/show/mkcongestionmap.py:98` — Aggregation includes `partial: true` horizon intervals. ADR-0014 explicitly requires comparison/derived tooling to drop partial intervals; these can bias lane colors and make maps horizon-dependent.
- **should-fix** `scripts/show/mkcongestionmap.py:197` — Provenance records only absolute paths and file size, not a metrics digest, scenario content hash, seed, or horizon. It therefore cannot prove the map matches the ADR-0014 showcase run. The staged sidecar already points to a disposable scratch path at `docs/show/diag/chi-loop-urban__setup.png.json:51`.
- **should-fix** `scripts/chicago/mkquiz.py:397` — Missing or malformed PNG provenance silently degrades to an uncaptioned image. That permits a map with unknown bands/window/run to ship; PNG artwork should require a valid sidecar and fail the build otherwise.

REVIEW-COMPLETE
