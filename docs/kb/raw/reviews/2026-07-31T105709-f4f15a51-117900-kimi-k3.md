• I'll read the key context files to verify the diff's claims.

• Now let me verify the scenario RunSpec defaults and run the affected tests.

• Tests pass. Let me check the demosrv hash binding, since the pin changes manifest content hashes.

• Build and vet clean. One more test run for the natsio package, then I'll write up findings.

• Review of the staged diff (ADR-0036 default flip). All tests green: `go test ./ ./scenario/ ./natsio/`, `go build ./...`, `go vet ./...` clean.

  **Blockers**

  None.

  **Should-fix**

  1. `docs/kb/decisions/ADR-0012-scenario-format.md:394-410` vs `engine/cmd/demosrv/replay.go:98` — the addendum rejects hash-folding because it "would start rejecting every pre-flip recording" via `checkRecordingHash`, but the chosen path does the same thing to the docs/show arms: stamping `adaptive_routing: false` into those manifests shifts their content hashes (your own `TestAdaptiveRoutingPinMovesHash` proves absent ≠ false), so any existing recording whose RunMeta.Spec.Hash was taken from an unpinned manifest now 409s against its scenarioDir. The ADR-0036 addendum acknowledges "their content hashes shift" but neither addendum addresses the display-binding fallout. If recordings bind to those arms, they need re-recording/re-binding, or a note here that none do. `data/` is gitignored so I can't verify on disk — confirm before commit.

  **Nits**

  2. `docs/kb/decisions/ADR-0036-congestion-adaptive-routing.md:118` — generator list omits `mkscenario-portal.sh`, which the diff also pins (`scripts/chicago/mkscenario-portal.sh:52`). Either add it or say why the portal scenario is excluded.
  3. `engine/natsio/bake.go:72`, `replay.go:114`, `run.go:108` — use the global `log` while `player.go:211,503` uses the configured logger; cosmetic inconsistency in how the same notice is surfaced.

  **Questions**

  4. `engine/engine.go:418-428` — the lazy `initAdaptiveRouting` in `Step` relies on "every restore path constructs via NewEngine" (verified, `keyframe.go:305`). Struct-literal engines calling `routeNextHop` before first `Step` still divide by zero (`routing.go:209`); the comment owns this as test-only. Acceptable, but worth a `TestAdaptiveStructLiteral` pin so a future test harness doesn't discover it as a panic?
  5. `engine/cmd/serve/main.go:175` — flag default is `true`, so `-netfile` runs with `-state-in` of a pre-v6 state silently flip regime (loudly logged via `RestoreNotice`, good). Is a one-line stderr notice sufficient for a bit-exactness break, or should `-state-in` of a pre-v6 state without an explicit `-adaptive-routing` refuse/confirm? Leaning fine-as-is since the notice names the remedy.

  **Verified clean**

  - Determinism: no new map iteration (`routeEpochs` is key-access only), fixed lane-order float loops, no wall clock/RNG changes; `RestoreNotice` never serialized or CRC'd.
  - Contract: no subject changes; TSKF v6 already existed; RunMeta/TSKF notes updated in both ADRs; golden hash canary for the bool→*bool canonicalization passes.
  - Flag plumbing: `-scenario` rejection via `flag.Visit` correct in both `serve` and `simrun`; explicit-only application happens before engine construction and `-state-in` restore.
  - Restore gates: v6-into-flag-off refused, pre-v6-into-flag-on noticed; v6 lane section reads into NewEngine-allocated `ttSnap`.

  REVIEW-COMPLETE

