**Verification done:** full engine-module suite green (`go test ./...`), `go vet` clean. Replay/restore paths audited: `Replay` takes params from the recorded `log.Spec`, `RunMeta` embeds the full spec and `engine.Params` marshals without `omitempty`, so pre-flip recordings (`AdaptiveRouting:false`) replay correctly under the new default. The lazy-init invariant at engine.go:409 holds — `restoreState` (keyframe.go:305) always passes through `NewEngine`, so the sentinel can't clobber restored `ttEMA`/`ttSnap`. The `*bool` change doesn't disturb the ADR-0012 content hash (hash is over canonical manifest bytes, not the struct), and variant.go:354's empty-override guard still fires correctly with the pointer field. Struct-literal test engines (persistence_test.go:109) are covered by the Step lazy init; routing is only reachable from inside `Step` (engine.go:937), so no pre-init division/nil-slice path exists.

**Blockers:** none.

**Should-fix**
- docs/show/README.md:280, ADR-0036 addendum — the sanctioned reproduction path ("set `adaptive_routing: false` in the manifest") changes the ADR-0012 content hash, so a reproduction run gets a different (hash, seed) run key than the published artifacts it reproduces. The addendum's run-identity defense leans on the hash staying constant for omitted fields, but the opt-out it prescribes breaks the registry linkage. The shipped what-if manifests also weren't actually pinned in this commit. Either pin them now (accepting the new hashes, with a migration note) or add one sentence to the README/addendum acknowledging the hash change on reproduction.

**Question**
- engine/keyframe.go:312 — the mismatch check is asymmetric: v6-into-flag-off errors loudly, but a v5 (static-era) keyframe restored under a flag-ON spec is silently accepted with free-flow-seeded EMA. Before the flip, flag-on specs were the rarity; after it, `DefaultParams()` + old keyframe is the *likely* accidental mismatch, and it silently yields a trajectory the original run never had. If warm start intends this (fresh EMA when crossing runs is the only sane seed), a one-line comment there would close it; if not, consider the converse check.

**Nits**
- docs/kb/INDEX.md:121 — ADR-0036 flips to ACCEPTED in the text but the checkbox stays `[ ]`; every other ACCEPTED ADR in the list is `[x]`.
- engine/scenario/variant.go:121 — variant `params:` override is wholesale replacement, so a variant expressing only `adaptive_routing: false` (exactly the docs/show reproduce move) silently resets a base `dt` override to the engine default. Pre-existing semantics, but this commit creates the first realistic reason to override params in a variant.

REVIEW-COMPLETE
