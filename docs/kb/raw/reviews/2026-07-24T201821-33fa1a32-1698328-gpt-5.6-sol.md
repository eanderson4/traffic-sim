- **should-fix** `engine/cmd/demosrv/geojson.go:85` — The cache key hashes one `scenario.Load`, but the network is reread at line 103. An edit between those reads can cache new network bytes under the old hash, defeating ADR-0018’s generation pinning and edit/revert guarantee. Export the exact bytes used to compute the hash or revalidate before publishing.
- **should-fix** `engine/cmd/demosrv/main.go:58` — `/api/demos` exposes verbatim `wsClientURL()`, while start responses use request-aware `advertisedWsURL()` at line 158. Running-card links therefore receive undialable addresses such as `ws://0.0.0.0:8443`, despite the initial start URL working. Compute the registry response’s `ws` per request using the same helper.

REVIEW-COMPLETE
