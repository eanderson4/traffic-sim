- **should-fix** `engine/natsio/run.go:34` — Fixed `20` ticks does not guarantee the documented ~2-second catch-up. ADR-0005 permits `dt` through 1 second, making the wait up to 20 seconds at 1×. Either derive cadence from `dt` or remove the wall-time guarantee from `contracts/asyncapi.yaml:108` and `docs/kb/decisions/ADR-0006-nats-message-contract.md:174`.
- **should-fix** `engine/natsio/sigframe_test.go:284` — The cadence test measures from `sawTick`, before connection/subscription setup, and permits `3*signalCatchUpEvery`. It can fail under slow setup and does not pin the promised ≤20-tick convergence; a substantially slower cadence could pass.

REVIEW-COMPLETE
