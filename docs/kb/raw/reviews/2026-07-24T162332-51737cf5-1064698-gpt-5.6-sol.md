should-fix — `contracts/network-format-v1.md:227`: The documented stop-overlay flow writes `region.final.net.xml`, but `netimport` still reads `region.net.xml`. Following the manual recipe discards all stop overrides and contradicts ADR-0017.

should-fix — `scripts/import-city.sh:65`: Provenance uses repository `HEAD`, which changes after unrelated commits and does not represent staged/dirty importer code. Because `provenance.source` participates in the ADR-0012 hash, identical extracts and netconvert output can receive different scenario hashes—or misleading importer provenance. Use a stable importer version/content hash or explicitly include dirty state.

should-fix — `scripts/import-city.sh:20`: The netconvert path hardcodes `lib/python3.12`, while the documented `python3 -m venv` may create another Python-version directory. Use the stable `tools/sumo-venv/bin/netconvert` entry point.

REVIEW-COMPLETE
