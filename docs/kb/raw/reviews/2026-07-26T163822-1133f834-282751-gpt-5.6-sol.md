- **blocker** `engine/natsio/driver_test.go:896` — The tap pairs an ack with the newest intent observed **before the ack**, not the intent actually consumed at that tick’s drain. An intent can miss the drain, arrive while `Step()` runs, and still precede the ack publication; lines 902–908 then attribute that future intent to the current `applied_tick`, undercounting lag by one. This bias is mode-dependent because batching publishes later, and the `p99 + 1` allowance at line 981 means a real systematic batch regression from 1 to 2 ticks can pass if these undercounts pull its median back to 1. Consequently the M3 acceptance claim at `engine/BENCHMARKS.md:211` is unsupported. Measure at the authoritative drain/application boundary or add a correlation identifier that the ack echoes.

REVIEW-COMPLETE
